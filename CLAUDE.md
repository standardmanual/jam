# JAM! 프로젝트 Claude 운영 규칙

## 절대 규칙 (모든 작업에서 예외 없이 적용)

### 1. 항상 한국어로 출력할 것
대화 응답, 커밋 메시지, 작업 진행 상황 설명, 빌드/배포 상태 요약, 코드 주석 등
사용자에게 보여지는 모든 출력물에 예외 없이 적용한다.
영어 문구를 그대로 노출하지 말고 반드시 한국어로 번역/서술한다.
(예외: 코드 식별자·라이브러리명·API명 등 고유명사는 원문 유지)

### 2. 개선·기능 변경 시 문서 체계 업데이트
새로운 기능 추가·로직 변경·컨텐츠 변경 등 서비스 변경이 발생하면
아래 "문서 체계 개요"에 따라 해당 카테고리 문서를 업데이트한다.

### 3. 주요 개발 완료 후 즉시 commit + push
작업이 완료되면 사용자가 별도로 요청하지 않아도 자동으로:
`git add` → `git commit` (한국어 메시지) → `git push origin main`

### 4. 로컬과 git을 항상 동일하게 유지
- 작업 후 `git status`를 확인하여 untracked·modified 파일이 없도록 처리
- .gitignore에 새 항목 추가 금지 (보안 예외: 토큰/시크릿 파일은 gitignore 허용)

### 5. 글로벌 스킬과 프로젝트 스킬을 항상 동일하게 유지
`~/.claude/skills/`(글로벌)와 `.claude/skills/`(프로젝트) 스킬 목록을 항상 동일하게 유지한다.
- 새 스킬 설치 시 양쪽 모두 복사 후 commit + push

---

## 문서 체계 개요

`Service Plan/`은 4가지 문서 카테고리로 구성된다. 신규 개선사항이 들어오면
**어느 카테고리에 해당하는지 먼저 판단**하고, 하나의 개선사항이 여러 카테고리에
걸치면 각 카테고리 문서를 모두 업데이트한다.

| # | 카테고리 | 내용 | 저장 위치 |
|---|---|---|---|
| ① | **PRD** | 신규 기능/개선사항의 기능·스펙·로직 정의 | `Service Plan/Specs/PRD/` |
| ② | **티켓** | ①을 실행하기 위한 계획, 개선/개발 작업 이력 | `Service Plan/History/Migration/Ticket/` |
| ③ | **컨텐츠 관리** | 액티비티배지·아이템북·아이템배지·세계관·장소(POI) | `Service Plan/Specs/Content/` |
| ④ | **배지 드랍 로직** | 배지 발급·드랍 엔진의 판정 로직 | `Service Plan/Specs/BadgeEngine/` |

**신규 개선사항 접수 시 항상 이 순서로 진행한다:**
1. 기존 티켓(②) 검토 — 유사 작업 여부, 미채택 대안, 잔여 이슈 확인
2. 관련 PRD(①)·컨텐츠(③)·엔진 로직(④) 문서 확인
3. 구현
4. 완료 후 해당 카테고리 문서 갱신 + 티켓 CLOSED 처리

> `Service Plan/Specs/` 루트의 `UX_WRITING_GUIDELINE.md`처럼 4개 카테고리에 속하지
> 않는 횡단 참조 문서는 루트에 유지한다.

---

## ① PRD 문서 규칙

**대상 변경 범위:**
- 신규 서비스 기능 페이지 추가 (`jam-web/src/app/(main)/` 등)
- `jam-web/src/app/api/` API 라우트 추가/변경/삭제 (기능 단위 변경)
- 기존 기능의 정책·플로우 변경

**실행 방법:**
1. `Service Plan/Specs/PRD/` 하위에서 관련 문서 확인 (`01_PRD.md`, `02_DATA_MODEL.md`, `03_PHASES.md`, `04_PROJECT_SPEC.md` 또는 주제별 하위 폴더 — 예: 어드민 관련은 `PRD/AdminUI/`)
2. 새로운 기능 영역이면 `PRD/{주제}/` 하위 폴더를 신설한다 (예: `PRD/AdminUI/`, `PRD/PointSystem/`). 파일명에 날짜·Phase 접두어를 붙이지 않고 폴더로 구분한다.
3. 기존 파일을 직접 수정한다 (SERVICE_OPERATIONS처럼 신규 파일로 쌓지 않음 — PRD는 "현재 기준 최신 스펙"이 원칙).
4. 문서 내 상호 링크(`[01_PRD.md](...)`, `@Specs/PRD/...`)를 경로 변경 시 함께 갱신한다.

---

## ② 티켓 문서화 규칙

개선사항·기능 추가·의사결정이 발생할 때마다 티켓을 생성하고, 완료 후 문서화한다.

### 저장 위치
`Service Plan/History/Migration/Ticket/YYYYMMDD_NNN_[카테고리]_[제목].md`

- `NNN`: 당일 3자리 일련번호 (001~). 기존 파일명에서 당일 최대 번호 확인 후 +1. 날짜가 다르면 001부터 재시작.
- `제목`: 한국어 또는 영어, 공백 대신 `-` 사용

### 카테고리

| 카테고리 | 해당 범위 |
|---|---|
| Admin | 어드민 패널 기능·UI |
| Service | 배지엔진·드랍엔진·Strava 동기화 등 핵심 서비스 로직 |
| Feature | 사용자 대면 기능 단위 |
| Content | 배지·아이템·텍스트 컨텐츠 |
| BadgeEngine | 배지 엔진 전용 |
| Infra | DB 마이그레이션, 환경변수, 도메인, 배포 설정 |
| UI | UI/UX 개선 |
| API | API 라우트 변경·추가·삭제 |

### 신규 개선사항 접수 시 — 기존 티켓 먼저 검토

새 개선사항이 들어오면 구현에 앞서 반드시 아래를 먼저 실행한다:

1. `Service Plan/History/Migration/Ticket/` 폴더의 기존 티켓 목록을 확인
2. 유사하거나 관련된 티켓이 있으면 내용을 읽고 다음을 파악:
   - 이미 완료된 작업인지 (재구현 방지)
   - 채택하지 않은 대안과 그 이유 (동일한 검토 반복 방지)
   - 잔여 이슈로 남긴 사항인지 (이어서 처리 가능)
3. 기존 티켓에서 충분한 컨텍스트를 확보한 후 구현 시작
4. 티켓이 PRD·Specs 문서와 함께 "첫 번째 정보 소스" — 코드 탐색보다 우선

> 목적: 반복 검토와 토큰 낭비를 줄이고, 이미 내려진 의사결정에 빠르게 접근.

### 언제 티켓을 생성하는가

1. **개선·기능 요청 접수 시** → `status: OPEN`으로 초안 생성
   - 뭉뚱그려진 요구사항은 카테고리·범위별로 분리해 별도 티켓 또는 하위 섹션으로 구분
   - 서비스/코드, UI/UX, 컨텐츠 관점으로 나눠 기술
2. **"기존 유지" 결정도 포함** — 검토 후 채택하지 않은 경우도 의사결정 기록으로 티켓 작성
3. **작업 완료 시** → `status: CLOSED`로 변경 + 완료 기록 섹션 기입

### 완료 기록 필수 항목 (CLOSED 시)

- 구현 내용 요약
- 변경된 파일 목록
- 테스트 결과
- 배포 정보 (날짜, 환경, 커밋 해시)
- 주요 의사결정 / 핵심 대화 내용 요약 (검토한 대안과 미채택 이유 포함)
- 잔여 이슈

### 템플릿
`Service Plan/History/Migration/Ticket/_TEMPLATE.md`

> **폐기된 규칙**: 과거 `SERVICE_OPERATIONS_YYYYMMDD_HHMM.md` 타임스탬프 파일 생성 방식은
> 티켓 시스템으로 완전히 대체되었다. 더 이상 생성하지 않는다. 기존 파일은
> `Service Plan/History/Operations/`에 아카이브로만 보관.

---

## ③ 컨텐츠 관리 문서 규칙

**대상 범위:** 액티비티배지 · 아이템북 · 아이템배지 · 세계관(faction) · 장소(POI)

**대상 변경 범위:**
- 배지/아이템 추가·제거·조건값 변경 (migration)
- 아이템북 구성·세계관 소속 변경
- POI 카테고리·수집 소스 변경
- 조합 레시피 추가·변경

**저장 위치 및 문서:**

| 문서 | 대상 |
|---|---|
| `Content/ACTIVITY_BADGES.md` | 액티비티배지 전체 목록·조건·설명 |
| `Content/ITEMBOOKS.xlsx` | 아이템북 + 아이템배지 전체 목록, 세계관 인접 그래프 |
| `Content/COMBINE_RECIPES.md` | 조합 레시피 |
| `Content/FACTIONS.md` | 세계관(faction) — 현재 스텁, 채우기 필요 |
| `Content/POI.md` | 장소(POI) — 현재 스텁, 채우기 필요 |

**실행 방법:** 기존 파일을 직접 수정한다 (컨텐츠는 "현재 상태"가 원칙 — 버전 이력은 티켓에 남긴다).

---

## ④ 배지 드랍 로직 문서 규칙

**대상 변경 범위:**
- `jam-web/src/lib/badge-engine/`, `jam-web/src/lib/drop-engine/` 로직 변경
- 새 조건 타입 구현/미구현 상태 변경
- 정책 변경 (홍수방지·성장티어·첫싱크게이트·pity 등)
- 드랍 확률·파라미터(모멘텀 비율, pity 임계 등) 변경

**저장 위치:** `Specs/BadgeEngine/BADGE_ENGINE_UNIFIED.md` (단일 문서, 자립 문서로 유지)

**실행 방법:** 기존 파일을 직접 수정한다. 이 문서는 발급·드랍 로직 전체를 다루는
단일 진실 원천(source of truth)이며, 컨텐츠 목록(③)과는 분리해서 관리한다.

---

## 문서 자동 업데이트 크론 — 폐기

과거 매시간 SERVICE_OPERATIONS 파일을 자동 생성하던 크론 규칙은 폐기되었다.
티켓 시스템(②)이 그 역할을 대체하므로 별도 크론을 등록하지 않는다.
