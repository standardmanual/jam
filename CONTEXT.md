# CONTEXT.md

JAM! 프로젝트의 도메인 용어집. 구현 세부사항이 아니라 개념 정의와 용어만 담는다.

## 진행 중인 인터뷰: 서비스 텍스트 최종 정비 (2026-09-15)

### 배경
반복적인 개선 작업 중 안내 메시지·보이스(어조)가 서로 일관성을 잃은 상태. 목표는 두 가지:
1. anti-slop(AI가 쓴 티가 나는 표현 제거)
2. 서비스 전반 언어의 톤앤매너 일관성 확보

### 용어

**딕셔너리(dictionary)**: `jam-web/src/lib/i18n/ko.ts`에 정의된 키-값 텍스트 저장소. 23개 네임스페이스(`common/nav/profile/tabs/feed/today/badges/...`)로 구성되며, 컴포넌트는 `d.<namespace>.<key>` 형태로 참조한다. 사용자가 이 작업에서 "locale 파일"이라 부른 대상과 동일하다 — 이 프로젝트에서는 코드베이스 기존 용어인 **딕셔너리**로 통일해서 부른다. (`locale 파일`이라는 용어는 이후 사용하지 않는다.)

**전수검사**: `/im-not-ai` 스킬(플러그인: `humanize-korean`)로 텍스트의 AI 티(anti-slop 대상 표현)를 찾아내는 작업. 이번 프로젝트에서는 딕셔너리 전체 + 미편입 하드코딩 텍스트 + `UX_WRITING_GUIDELINE.md` 문서 자체까지 대상으로 한다 (Q3 답변: c).

**톤앤매너 일관성**: 같은 성격의 메시지(예: 안내/축하/에러)가 화면이나 작성 시점에 관계없이 동일한 어조·문장 구조를 유지하는 상태. `UX_WRITING_GUIDELINE.md`의 "상황별 톤 매트릭스"가 기준 문서 역할을 한다.

### 결정 사항 (인터뷰 라운드 1)

- **완료 기준**: 가이드 보완 + 전수검사 + 딕셔너리 통합 3단계를 모두 마치고, 모든 사용자 노출 화면이 딕셔너리에 편입되고 전수검사를 통과해야 프로젝트 종료로 본다. 단계별 독립 완료가 아니다.
- **전수검사 범위**: 딕셔너리(`ko.ts`) 전체 + 미편입 하드코딩 텍스트(46개 사용자 노출 화면) + `UX_WRITING_GUIDELINE.md` 문서 자체.
- **딕셔너리 통합 범위**: 46개 사용자 노출 화면 전부. 단, 최초 조사(정규식 기반 grep)가 놓쳤을 수 있는 텍스트가 있는지 추가로 확인한다.
- **동적 생성 텍스트 처리**(`badgeConditionText.ts`, `badgeProgressText.ts`): 완전 재작성이 아니라, 기존 `t(template, vars)` 체계가 제대로 적용됐는지 확인하고, 이 체계로 노출되지 않는 부분(즉 템플릿화되지 않은 하드코딩 조각)을 찾아 개선하는 감사(audit) 성격의 작업.
- **DB 소스 텍스트(배지 이름·설명)**: 이번 범위에서 제외. 코드상 텍스트만 대상으로 한다.
- **어드민 화면**: `UX_WRITING_GUIDELINE.md`가 이미 원칙 적용 대상에서 제외하고 있음 — 이번 작업도 이 경계를 따른다(인터뷰 라운드 2에서 재확인 중).

### 결정 사항 (인터뷰 라운드 2)

- **어드민 경계**: `UX_WRITING_GUIDELINE.md`가 이미 정의한 어드민 제외 원칙을 예외 없이 그대로 유지한다. 어드민 코드 내부에 있는 문구라도 이번 범위에 포함하지 않는다.
- **실행 순서**: ① 가이드 확정(선행) → ② 전수검사(흩어진 상태 그대로 문제 문구 식별) → ③ 문제 수정과 딕셔너리 편입을 화면 단위로 동시에 진행. 검사와 편입은 사실상 하나의 파이프라인이다.
- **가이드 갱신 시점**: 전수검사 중 가이드 자체의 문제(AI 티, 누락 규정)가 발견되면 그 즉시 가이드를 갱신한다. 후속 과제로 미루지 않는다 — `Specs/`는 "현재 상태를 담는 덮어쓰기 문서" 원칙을 따른다.
- **서버 API 에러 메시지 포함**: 조사 결과 `app/api/**` 내 약 35개 비관리자용 라우트에 한글 에러 메시지가 하드코딩돼 있고, 클라이언트 처리 방식도 파일마다 다름(무시/그대로 노출 혼재) — 이게 Q2에서 밝힌 "톤 일관성 상실"의 구체적 원인 중 하나로 확인됨. 이번 프로젝트 범위에 포함한다.
- **서버-클라이언트 에러 메시지 규칙**: "서버가 단일 진실 소스" 방식을 채택 — API route도 `d.*` 딕셔너리를 직접 import해 에러 메시지를 만들고, 클라이언트는 서버가 보낸 메시지를 그대로 신뢰해 노출한다. 중복 정의 금지. (`lib/i18n`는 `'use client'` 없는 순수 모듈이라 서버에서도 바로 import 가능함을 확인함.) 근거는 [[0001-server-error-message-single-source]] 참고.

### 다른 텍스트 채널 조사 결과 (사실 확인, 결정 불필요)
- 푸시 알림(web push/FCM/APNs) 발송 코드 없음 — `lib/notifications/`는 인앱 소식함(피드)만 담당, 모든 문구가 `d.notifications.*` 경유
- 이메일 발송 템플릿 없음
- zod 검증 스키마에 한글 하드코딩 없음
- 동적 OG 이미지·manifest.json 없음
- SEO 메타데이터는 `app/layout.tsx`의 `description`이 하드코딩(개별 페이지는 대체로 딕셔너리 경유) — 이번 딕셔너리 편입 대상에 포함

### 프로젝트 범위 확정
인터뷰 종료. 아래를 모두 만족해야 완료로 본다:
1. `UX_WRITING_GUIDELINE.md` 보완 완료
2. 딕셔너리(`ko.ts`) 전체 + 미편입 사용자 노출 화면(46개, 추가 확인분 포함) + 서버 API 에러 메시지(~35개 라우트) + 가이드 문서 자체가 `/im-not-ai` 전수검사 통과
3. 모든 사용자 노출 텍스트(화면·서버 에러 메시지)가 딕셔너리로 편입되고, 서버-클라이언트 단일 진실 소스 규칙이 적용됨
4. DB 소스 텍스트(배지 이름·설명)와 어드민 화면은 범위 밖
