---
Title: JAM! 서비스 플랜
modified: 2026-08-21
supersedes: "02 JAM! 서비스 플랜 v4.0 (2026-08-20).md"
---

- **버전:** 4.1

- **업데이트 일자:** 2026.08.21

- **서비스 성격:** 게이미피케이션 기반 Tracker-less 아웃도어 소셜 피지털(Phygital) 플랫폼

## 0. 이 문서에 대하여

v2.2(2026-06-11 작성) 이후 실제 개발이 상당히 진행되면서, 원 문서에 있던 구상 중 일부는 그대로 구현됐고, 일부는 다른 방식으로 구현됐으며, 일부는 아직 손대지 못했습니다. 이번 개정은 **2026-08-10 시점 코드베이스·프로덕션 DB 대조 기준**으로 전체 내용을 다시 쓰고, 모든 기능·시나리오에 아래 태그를 붙여 "지금 실제로 되는 것"과 "앞으로의 계획"을 명확히 구분합니다.

> **v4.1 변경점 (2026-08-21)**: 유저가 실제로 만지는 화면 두 곳이 바뀌었고, 그 화면들을 지탱하는 **디자인 시스템(MODULAR)이 처음으로 서비스 코드에 실제로 연결**됐습니다. ① **지도(드랍) POI 탐색 방식 전환** — 이전에는 POI 마커를 하나 누르면 바텀시트가 열리고, 옆 POI를 보려면 시트를 닫고 다른 마커를 다시 눌러야 했습니다. 이제 마커를 누르면 반경 내 POI 전체가 **좌우로 넘기는 캐러셀 카드**로 열려, 지도를 건드리지 않고 주변 POI를 연속으로 훑을 수 있습니다. 드랍 데이터는 **중앙에 온 카드만** 불러와 불필요한 호출을 없앴습니다(4.2 참고). ② **프로필 화면 상단 개편** — 헤더에서 이메일 노출을 없애고(불필요한 개인정보 노출 제거) 카드 박스를 걷어낸 뒤 아바타·아이디·잼포인트·편집/팔로우 버튼을 재배치했으며, 본인 프로필에서는 빈 상단 바(TopNav)를 제거했습니다. ③ **MODULAR ↔ 서비스 실연결** — 그동안 MODULAR는 Storybook 카탈로그로만 존재했고 서비스는 같은 값을 손으로 맞춰 써왔습니다. 이날 토큰(색상·반경·모션·스페이싱·타이포)을 `@import`로 전량 연결하고, 컴포넌트 24종 중 10종을 서비스가 실제로 가져다 쓰도록 전환했습니다. 나머지는 "전환하지 않기로 한 이유"까지 기록해 남겼습니다(8.2 참고). 이 외 여백·볼드 등 화면 다듬기와 POI 캐러셀 후속 버그 수정이 이어졌습니다. (티켓 20260820_001~026 참고)
>
> **v4.0 변경점 (2026-08-20)**: v3.9에서 "기술 스택 미확정"으로 남겨뒀던 **배지 배경 쉐이더가 완전히 다른 형태로 해소**되면서, 배경 테마가 단순 배경색을 넘어 **어드민 배경 제너레이터**라는 하나의 저작 도구로 완성됐습니다. 핵심은 **"유저 기기에서 쉐이더를 실시간 연산하지 않는다"**는 결정입니다 — 어드민에서 패턴·애니메이션·필터를 조합해 저작한 뒤 그 결과를 **정지 이미지(PNG) 또는 반복 영상(MP4)으로 구워(bake) Storage에 저장**하고, 유저단은 구워진 결과물만 재생합니다. 모바일 전용 서비스의 배터리·성능 부담을 원천 제거하면서 표현력은 그대로 얻는 구조입니다. 이와 함께 ① 세계관·컬렉션(아이템북) 배경 **일괄 적용이 프로덕션에 배포**됐고(세계관은 직속 배지 → 소속 컬렉션 → 컬렉션 아이템배지 **3단 캐스케이드**, DB 함수로 all-or-nothing 보장), ② 컬렉션 상세화면에도 배경 테마 렌더링이 확장됐으며, ③ 배지 상세 배경이 **단일 레이어**로 통합돼 어드민 미리보기가 실제 화면과 같은 컴포넌트를 쓰게 됐습니다. 상세는 4.1.4 참고. (티켓 20260819_001~017 참고)
>
> **v3.9 변경점 (2026-08-19)**: 배지에 **개별 비주얼 아이덴티티**를 부여하는 두 축이 새로 들어왔습니다. ① **배경 테마 커스터마이징** — 어드민에서 배지별 배경색을 지정하면(배지 이미지 업로드 시 평균 컬러를 자동 추출해 기본값으로 제안) 배지 상세화면 전체가 그 색으로 물듭니다. 세계관·컬렉션(아이템북) 단위로 하위 배지에 **일괄 적용**하는 버튼도 개발됐습니다(현재 staging 반영, 프로덕션 승격 전). 배경 쉐이더는 어드민에 값 저장 자리만 마련했고 렌더링은 미연결(기술 스택 미확정). 배지 이미지 등록 방식도 외부 URL 입력에서 **Supabase Storage 업로드**로 전환됐습니다. ② **배지 이미지 대량 생산 파이프라인** — 900종 아이템배지 이미지를 Recraft V3로 생성하기 위한 워크플로우·문서 체계(세계관 10 × 컬렉션 10 = 컨텐츠 브리프 100종)가 확정·생성됐습니다. 상세는 4.1.4 참고. (티켓 20260818_001~004 참고)
>
> **v3.8 변경점 (2026-08-16)**: 서비스 용어 '인벤(INVENTORY)' → '인벤토리'로 전면 교체. UX 라이팅 가이드라인 고정 용어 정의 갱신(금칙어 역전: 인벤→금칙, 인벤토리→공식), i18n/ko.ts 및 관련 소스코드·문서 일괄 반영. TabBar 레이블, API 에러 메시지, PRD·서비스플랜·티켓 문서 동기화 포함. (티켓 20260816_002 참고)
>
> **v3.7 변경점 (2026-08-16)**: 서비스 용어 '겟(GET)' → '획득'으로 전면 교체. UX 라이팅 가이드라인 고정 용어 정의 갱신, i18n/ko.ts 및 관련 소스코드 일괄 반영, PRD·서비스플랜 문서 동기화. '미겟' → '미획득', '겟 조건' → '획득 조건' 등 파생 표현 포함. (티켓 20260816_001 참고)
>
> **v3.5 변경점 (2026-08-13)**: 종목별 대표 배지 5종(동네 산책러/첫 숨결/언덕의 도전자/첫 고도/야생의 주자)의 Rare 이상 획득 조건이 기존 크로스게이트(같은 종목 다른 속성 배지 보유)에서 **미션 완료 기반 게이팅**으로 전환됐습니다. 이를 위해 미션 엔진이 확장되어 연속일수·단일세션 지속시간·등반고도 조건을 판정할 수 있게 됐고, 종료일 없는 "상시 미션"과 다른 참가자를 노출하지 않는 "개인형 미션 진행상황" 표시가 신규 지원됩니다. 상세는 4.1.1, 4.6 참고.
>
> **v3.4 변경점 (2026-08-13)**: 신규 기능 추가는 없으며, 핵심 루프(동기화→배지→드랍→포인트) 신뢰성을 강화하는 버그 수정과 정책 표준화가 반영되었습니다 — 신규 유저 인벤토리 미생성으로 아이템배지가 발급되지 않던 버그 근본 수정, POI 반경 오탐(지하철역이 500m로 잘못 생성되던 문제)의 코드 레벨 근본 수정과 카테고리별 정확 매칭 반경 정책 표준화(3.3 참고), 다건 동기화 시 드랍 상태가 뒤바뀌던 배치 처리 순서 버그 수정(4.1.2 참고). 상세 이력은 7장 2026-08-13 항목 참고.

| 태그 | 의미 |
|---|---|
| 🟢 **현재 구현** | 프로덕션에 실제로 배포되어 동작 중 |
| 🟡 **부분 구현** | 데이터/로직은 있으나 유저 대면 플로우가 불완전하거나 미확정 |
| 🔵 **향후 업그레이드** | 컨셉·정책은 확정됐거나 유효하나 아직 코드로 구현되지 않음 |

**한눈에 보는 구현 상태** (상세는 각 장 참고)

| 영역 | 상태 |
|---|---|
| Strava 동기화 + 배지 자동 발급 | 🟢 |
| 액티비티 배지 (조건 달성형) | 🟢 |
| 아이템 배지 드랍/픽업 (디지털) | 🟢 |
| 세계관(faction) 10종 + 아이템북 | 🟢 |
| 아이템 믹스 시스템 | 🟢 |
| 미션 시스템 | 🟢 |
| JAM 포인트 (디지털 재화) | 🟢 |
| POI 사후 매칭 인증 | 🟢 |
| 어뷰징 방지(섀도우밴·GPS 조작 감지) | 🟢 |
| 어드민 패널 | 🟢 |
| 지도 POI 캐러셀 탐색(드랍 화면) | 🟢 마커 클릭 → 반경 내 POI 좌우 스와이프 |
| MODULAR 디자인 시스템 ↔ 서비스 실연결 | 🟡 토큰 전량 연결, 컴포넌트 24종 중 10종 전환 |
| 배지 배경 테마(배경색) 커스터마이징 | 🟢 어드민 지정 + 상세화면 렌더링 |
| 배경 제너레이터(패턴·애니메이션·필터 저작 후 굽기) | 🟢 어드민 저작 + PNG/MP4 굽기 + 유저단 재생 |
| 세계관·컬렉션 배경 테마 일괄 적용 | 🟢 컬렉션 1단 / 세계관 3단 캐스케이드, 프로덕션 배포 완료 |
| 컬렉션(아이템북) 상세화면 배경 테마 | 🟢 배지 상세와 동일 렌더링 원칙 적용 |
| 배지 배경 쉐이더(유저 기기 실시간 렌더링) | 🟢 **폐기 확정** — 굽기(bake) 방식으로 대체 해소 (아젠다 아님) |
| 배지 이미지 대량 생산 파이프라인(Recraft) | 🟡 워크플로우·브리프 100종 확정, 실제 양산 전 |
| **D2C 실물 패치 구매** | 🔵 배지 조건 필드만 존재, 결제 플로우 없음 |
| **B2B 브랜드 스폰서십** | 🔵 컨셉만, 미착수 |
| **B2G 지자체 챌린지** | 🔵 컨셉만, 미착수 (미션 시스템으로 유사 구현 가능) |
| **P2P 플리마켓 실거래** | 🔵 "coming soon" 화면만 존재 |
| SNS 자동 연동(스트라바 피드 침투 등) | 🔵 미구현 |
| 인스타 공유 포토카드 | 🔵 한때 구현됐다 제거됨(2026-08-07) |
| 푸시 알림 | 🔵 미구현 |
| 신화 아이템 "떠돌이" 속성 | 🟡 DB·크론은 있으나 유저 UI 노출 확인 필요 |

---

## 1. 브랜드 아이덴티티 및 서비스 비전

### 1.1. 브랜드 네임 및 슬로건

- **서비스명:** JAM! (Join And Move!)

- **의미:** 방 안에 머물지 말고 지금 바로 커뮤니티에 합류하여 밖으로 나가 움직이자는 강력한 행동 촉구 메시지.

    - **합류(Join):** 유저 간의 아이템 드랍·픽업(🟢), 세계관·아이템북 컬렉션(🟢), 팔로우·프로필 공개(🟢), 미션 참가(🟢)를 통한 사회적 연결망. P2P 거래·클랜 활동·오프라인 팝업스토어는 아직 계획 단계(🔵).

    - **움직이자(Move):** 걷기, 달리기, 등산, 자전거, 트레일러닝 등 인간이 자신의 에너지를 소모하여 현실 공간을 이동하는 모든 역동적인 아웃도어 활동을 총망라함. (v2.2 대비 **트레일러닝**이 5번째 정식 종목으로 추가됨 🟢)

- **공식 슬로건:** > **"망설이지 말고 Join And Move, JAM! 지금 바로 JAM 해!"**

- **브랜드 무드 및 포지셔닝:** 기록에 집착하는 하드코어한 스포츠 트래킹 브랜드와 달리, **경험과 수집**에 집중합니다. 포켓몬고나 닌텐도 동물의 숲처럼 캐주얼하고 유쾌하며 수집욕을 강하게 자극하는 팝(Pop)한 톤앤매너를 지향합니다. 가상 세계의 성취감을 현실 세계의 물리적 소유로 완벽하게 연결하는 **피지털(Phygital) 라이프스타일 브랜드**를 지향하나, **2026-08-10 기준 "피지털"의 물리적 축(실물 커머스)은 아직 미구현**이며 디지털 수집 경험만 서비스 중입니다(🔵 아래 3.1 참고).

### 1.2. 서비스 철학: "Tracker-less 플랫폼" (Executive Summary) — 🟢 현재 구현

JAM!은 유저의 운동 궤적을 직접 측정하고 기록하는 트래커 앱이 아닙니다. 앱 메인 화면에 `[운동 시작]`이나 `[종료]` 버튼, 혹은 실시간으로 이동 경로를 보여주는 무거운 GPS 지도가 전면에 배치되어 있지 않습니다. 이 원칙은 이후 개발 과정에서도 **한 번도 흔들리지 않은 절대 규칙**으로 유지되고 있으며, 프로젝트 스펙 문서의 "절대 하지 마" 1순위 항목입니다.

대신 사용자가 이미 습관적으로 사용 중인 Strava의 데이터를 백그라운드에서 동기화하여, 그 위에 게임적 상상력과 실질적 보상을 덧입히는 **라이프스타일 게이미피케이션 플랫폼**입니다.

```
[ Strava ] ──(OAuth 연동 + 백그라운드 동기화)──► [ JAM! 게이미피케이션 엔진 ] ──► [ 오프라인 피지털 커머스 & O2O ]
                                              (배지 발급, 아이템 드랍, 믹스,        (실물 자수 패치, 매장 방문 리워드)
                                               세계관 컬렉션, 미션, 포인트)              🔵 향후 업그레이드
```

> **v2.2 대비 변경점**: 원안은 Garmin·Apple Health까지 다중 소스 연동을 상정했으나, 실제로는 **Strava 단일 소스**로 확정되어 운영 중입니다(🟢). Garmin 연동은 미착수, Apple Health는 "웹 앱에서 직접 접근 불가"라는 구조적 이유로 별도 네이티브 앱이 필요해 보류됐습니다. 이 결정 덕분에 v2.2 §8에서 우려했던 "이기종 헬스 데이터 중복 인식" 문제는 현재로선 발생하지 않습니다 — 자세한 내용은 8장 참고.

#### Tracker-less 아키텍처의 3 대 핵심 이점

1. **유저 피로도 제로** — 🟢 구현됨. 온보딩 → Strava 동기화 완료까지 마찰 없는 플로우로 구현되어 있으며, 로그인 시점 동기화 + 일 1회 자동 동기화(Cron)로 "밤에 앱을 켜면 보상이 쌓여있는" 경험이 실제로 동작합니다.

2. **개발 및 서버 비용의 혁신적 절감** — 🟢 유지 중인 설계 원칙. 실시간 GPS 연산을 전면 위임하는 대신, POI 반경 매칭·드랍/픽업 지도 등 필요한 최소한의 위치 기능만 구현했습니다(지도는 네이버 지도 NCP Maps.js 기반, 2026-07-22 Google Maps에서 전환).

3. **네이티브 B2B O2O 마케팅 채널화** — 🔵 향후 업그레이드. 이동 데이터를 활용한 광고주 대상 마케팅 채널 자체는 아직 사업화되지 않았습니다(3.2, 3.4 참고).

## 2. 타겟 퍼소나 및 심층 행동 시나리오

JAM!은 별도의 앱을 켜고 기록할 필요가 없어 진입 장벽이 극단적으로 낮습니다. 각 시나리오는 **2026-08-10 기준 실제로 가능한 것과 아직 계획 단계인 것을 구분**하여 재작성했습니다.

### 2.1. 🚲 자전거 - 구매력 1 위 핵심 타겟 및 굿즈 소비의 주축

|분류|대상 및 특성|불편 사항 및 JAM! 솔루션 (구현 상태 반영)|
|---|---|---|
|**초급**|**공유자전거 자출족** - 2030 직장인 - 따릉이/일레클 등으로 왕복 3~10km 출퇴근 반복|매일 똑같은 풍경의 지루한 출퇴근길이 **랜덤 박스 파밍 시간**으로 변모. 출퇴근만 완료해도 밤에 동기화 시 아이템배지(디지털)가 확률 드랍됨🟢. 실물 간식 쿠폰이나 아메리카노 추첨권 같은 **실물 보상 연계는 아직 없음**🔵.|
|**중급**|**주말 크루 라이더** - 3040 세대 - 수백만 원대 카본 로드 자전거 소유 - 한강/남산 위주 주행 (Strava 헤비 유저)|남산, 북악 등 유명 업힐 POI 통과 시 극악의 확률로 발급되는 **한정판 시리얼 넘버 아이템배지(디지털)**에 열광🟢. 시리얼 번호는 발급 순서를 그대로 노출하지 않도록 1~999,999 난수로 부여됨🟢. 실물 메탈/러버 패치 제작·발송은 아직 없음🔵. 남는 중복 아이템은 자주 쉬는 POI에 실제로 드랍해 크루원들이 픽업할 수 있음🟢(30일 후 만료 소각).|
|**상급**|**국토종주/그란폰도 헌터** - 40 대 이상 아날로그 취향 포함 - 4 대강 자전거길, 초장거리 극한 투어|누적 거리 조건의 액티비티 배지로 "장거리 항속" 계열 배지를 실제 획득 가능🟢. 아날로그 수첩 도장 찍기 대신 **POI 반경 매칭 기반 사후 인증**이 산·지하철역 등 1,798개 POI 배지로 실제 구현되어 있음🟢(전국 4대강 자전거길 전용 인증센터 POI는 아직 별도 등록 안 됨, 산/지하철역 위주🟡).|

### 2.2. 🚶 걷기 - 플랫폼 트래픽의 거대한 기반

- **초급 ("일상형 건강 챙김러") — 🟢 대부분 구현**

    - **현재 실제 플로우:** Strava에 기록된 걷기 활동이 동기화되면, 조건 충족 시 액티비티 배지가 자동 발급되고 아이템 배지가 확률 드랍됩니다. 아이템북(예: 세계관별 컬렉션)을 채우는 픽업 루프도 실제 동작합니다. 다만 원안의 `[분식은 항상 옳아]`류 개별 아이템북명은 실제로는 "낭만 미식가" 등 10개 세계관 체계로 재편되어 구현됐습니다(4.1 참고). **잉여 아이템을 플리마켓에 팔아 포인트를 챙기는 것은 아직 불가**🔵 — 대신 믹스(MIX)로 소각하고 확률 보상을 받는 경로는 있음🟢.

- **상급 ("워킹 챌린저") — 🟡 일부 구현**

    - **현재 실제 플로우:** 둘레길 등 특정 코스 완주를 자동 인증하는 전용 로직은 아직 없고, 산/지하철역 POI 통과 기반 배지만 구현되어 있습니다🟡. 실물 자수 패치 구매·부착 시나리오는 D2C 결제 플로우가 없어 아직 불가능합니다🔵. 다만 드랍/픽업 자체는 실제로 동작하므로, "내가 걸어온 길에 아이템을 미리 숨겨두는" 플레이는 현재도 가능합니다🟢.

### 2.3. 🏃 달리기 - 소셜 파워와 인플루언서 마케팅의 중심

- **중급 ("꾸준한 하프 러너") — 🟡 일부 구현**

    - **현재 실제 플로우:** 팔로우·유저 검색·프로필 공개 등 소셜 기반은 구현되어 있어 크루 내 "누가 뭘 모았는지" 확인이 가능합니다🟢. 다만 누적 거리 달성 시 스토어 구매 권한이 열리는 D2C 연동은 미구현🔵이며, 크루 전용 비밀 드랍 상자 같은 "그룹 드랍" 기능도 아직 없습니다(현재 드랍은 개인 단위)🔵.

- **상급 ("기록 단축 마라토너") — 🟢 배지 시스템은 구현, 🔵 실물 전시는 미구현**

    - **현재 실제 플로우:** 신화(mythic) 등급 배지 발급 조건과 시리얼 넘버 부여는 실제로 동작합니다🟢. 실물 메탈 플레이트 제작·전시는 D2C 미구현으로 아직 불가능합니다🔵.

### 2.4. 🏔️ 등산 - 공간 맵핑 및 가방 꾸미기 트렌드

- **초급 ("인증샷 하이커") — 🟡 디지털 컬렉션만 구현**

    - **현재 실제 플로우:** 배지 컬렉션 화면에서 획득한 배지를 확인하고 인스타그램 등 SNS에 캡처해 공유하는 것은 가능하지만, JAM! 자체의 "공유용 포토카드 생성 기능"은 한때 구현됐다가 2026-08-07에 제거되었습니다🔵(5장 참고). 실물 벨크로 패치를 가방에 붙이는 "가방 꾸미기"는 D2C 미구현으로 아직 불가능합니다🔵.

- **상급 ("도전형 알파인") — 🟡 일부 구현**

    - **현재 실제 플로우:** 고도 조건 기반 액티비티 배지(예: 고도 상승 누적)와 산 POI 배지(전국 853개 산 POI 1:1 매핑)는 실제로 동작합니다🟢. 실물 프리미엄 패치 제작은 미구현🔵.

---

## 3. 핵심 비즈니스 모델: Phygital 커머스 & O2O 마케팅 융합

JAM!의 궁극적 지향점은 온라인 트래픽을 오프라인 매출로 전환하는 것이지만, **2026-08-10 현재는 디지털 게이미피케이션 엔진만 완성된 상태**이며, 아래 4개 비즈니스 모델은 모두 사업화 착수 전이거나 부분 구현 상태입니다.

### 3.1. D2C 이커머스: "Earned, Not Given" 실물 패치 비즈니스 — 🔵 향후 업그레이드 (자체 캐시카우 예정)

- **현재 상태:** 배지 상세 화면에 "실물 패치 구매 가능" 조건 필드(`patch_available`)는 존재하지만, 실제 결제·주문·배송 플로우는 구현되지 않았습니다.
- **2026-08-07 확정된 의사결정:** D2C 쇼핑몰은 **외부 URL 연결이 아닌 앱 내부 구현**으로 최종 결정됐습니다. 착수 시점은 미정입니다.
- 구매 조건 게임화("돈이 있어도 아무나 살 수 없음")와 등급별 가격 전략(스탠다드 5천~7천원 / 레어 1.5만~2만원 / 신화 5만원 이상), 캔버스 굿즈 라인업(안장 가방·러닝 반다나·등산 버킷햇·배지북 등) 컨셉 자체는 v2.2 그대로 유효하며 폐기되지 않았습니다.

### 3.2. B2B 브랜드 스폰서십 (Brand Item Book) — 🔵 향후 업그레이드

- **현재 상태:** 미착수. 브랜드 전용 아이템북 캠페인, 선착순 실물 교환권 발급 등 컨셉 전체가 아직 코드화되지 않았습니다.
- 다만 어드민 패널에서 아이템북·세계관을 CRUD로 자유롭게 생성할 수 있는 인프라는 이미 구현되어 있어🟢, 착수 시 콘텐츠 제작 자체는 빠르게 가능합니다.

### 3.3. 방대한 POI (Point of Interest) 지오태깅 - 사후 매칭 로직 — 🟢 현재 구현 (핵심 인프라 완성)

앱을 켠 채로 이동하는 방식은 아웃도어 활동 중 위험하며 배터리를 방전시킵니다. JAM!은 이를 **'사후 매칭'** 기술로 해결하며, 이 부분은 v2.2 구상보다 오히려 **더 크게 확장되어 실제 구현**됐습니다.

- **확보된 인프라 DB (2026-08-13 기준 실측치):**

    | 카테고리 | POI 수 | 정확 매칭 반경 |
    |---|---|---|
    | 지하철·기차역 (transit) | 964개 | 50m (전량, 2026-08-13 확정) |
    | 산/봉우리 (mountain) | 853개 | 150m (전량, 2026-08-13 확정) |
    | 관공서·병원·약국·관광지·편의점 등 T2(네이버 지역검색 자동 수집) | 다수, 카테고리별 12~91개 | 카테고리별 정책 미대상(픽업 반경 500m 별도 적용) |
    | **POI 배지로 1:1 연결 완료된 수** | **1,798개** (주로 산·지하철역) | — |

    > v2.2에서 언급한 "4대강 자전거길 인증센터 100% 완비", "133개 걷기 코스"는 별도 전용 데이터셋으로는 아직 구축되지 않았습니다🔵 — 현재는 산·지하철역 중심으로 대량 등록되어 있습니다.

- **작동 프로세스 (실제 구현됨):**

```
[유저 활동 완료] ──► [Strava 동기화] ──► [서버 백그라운드 GPS 경로 ↔ POI DB 매칭] ──► [반경 내 통과 확인 시 배지 자동 발급]
```

  POI 반경 매칭 로직은 바운딩박스 사전 필터, 선분(segment) 기반 최근접 거리 계산 등으로 수차례 개선을 거쳐 안정화됐습니다.

- **정확 매칭 반경 정책 표준화 및 오탐 근본 수정 (2026-08-13 신규)** 🟢: 산·지하철역처럼 "단일 좌표 기준으로 정확히 통과해야만 인정"되는 카테고리의 반경을 `radius-policy.ts`로 단일화했습니다(지하철역 50m, 산 150m). 기존에는 신규 POI 자동수집 파이프라인이 카테고리와 무관하게 전부 500m 반경으로 생성하고 있어, 실제로 방문하지 않아도 반경 안을 지나가기만 하면 배지가 발급되는 오탐이 있었습니다(전수 재검증 결과 신고 접수분 외 1개 계정 7건 과다발급 추가 확인·회수). 이제 ①자동수집 ②어드민 수동 생성/수정 ③배지-POI 연결 3개 지점 모두에서 정책값이 최종 강제 적용되어, 사람이 값을 잘못 입력해도 정책이 덮어쓰는 구조로 재발을 구조적으로 차단합니다. 산 카테고리는 GPS drift로 인한 미발급 사례(50m 반경이 실제 등반 루트를 놓치는 문제)를 해소하기 위해서도 150m로 함께 상향되었습니다.

### 3.4. B2B / B2G 지자체 이벤트 스핀오프 — 🔵 향후 업그레이드

- **현재 상태:** 미착수. 다만 **미션 시스템(🟢 4.6 참고)**이 구조적으로 유사한 역할을 일부 대체할 수 있습니다 — 어드민이 기간 한정 미션을 생성하고 완료 시 배지+포인트를 보상하는 기능은 이미 구현되어 있어, 지자체 챌린지형 이벤트를 미션 프레임 위에서 구현하는 것이 향후 가장 빠른 경로가 될 것으로 보입니다.

---

## 4. 초고도화 게이미피케이션 및 경제 생태계 (2026-08-10 기준 전면 개정)

v2.2 작성 시점에는 존재하지 않았던 **세계관(faction), 아이템북, 믹스, 미션, 포인트** 시스템이 실제로 구축되어 JAM! 게임 경제의 중심축이 되었습니다. 이 장은 실제 구현을 기준으로 다시 작성합니다.

### 4.1. 두 가지 핵심 뱃지 체계 — 🟢 현재 구현

```
                        ┌──► 액티비티 배지 (조건 달성형 / 100% 발급 / 영구 귀속)  — 166개
[ JAM!의 모든 수집 요소 ]
                        └──► 아이템 배지 (정산 후 확률 드랍형 / 소모·믹스 가능)  — 3,600개
                        └──► POI 배지 (장소 체크인형 / 반복 획득 가능)          — 1,798개
```

> v2.2는 뱃지를 두 갈래로만 나눴으나, 실제로는 **POI 배지**가 세 번째 독립 유형으로 추가 구현됐습니다🟢. 장소 체크인 자체를 조건으로 하는 배지로, 같은 장소를 여러 번 체크인해도 매번 새로 발급되는 반복 획득형입니다.

#### 4.1.1. 액티비티 배지 (기록 및 조건 달성형) — 🟢

- 5개 종목(걷기·러닝·사이클링·등산·트레일러닝) × 다양한 속성(거리·속도·고도·연속일·날씨·시간대 등) 조합, 등급별(common/rare/legend/mythic) 총 162개(미션보상배지 15종 포함, 아래 참고).
- **성장 티어 정책** 🟢: 같은 이름 그룹 내에서 common → rare → legend → mythic 순으로만 인정되며, 상위 달성 시 하위는 건너뛰고 최상위 1개만 발급됩니다.
- **첫 싱크 게이트** 🟢: 첫 Strava 동기화 시 과거 수백km 이력이 있어도 Rare 이상은 즉시 발급되지 않고 Common만 발급됩니다 (배지 폭발 방지).
- **선행 배지 게이트** 🟢: Rare 이상 등급은 동일 종목의 다른 속성 배지를 먼저 보유해야 발급되는 조건이 걸려있습니다 (다양한 경험 유도). 5개 종목 대표 배지 트리 1개씩(총 5개)은 예외로, 아래 **미션 게이팅**(신규)이 이 크로스게이트를 대신합니다.
- **종목별 대표 배지 미션 게이팅(2026-08-13 신규)** 🟢: 걷기 `동네 산책러`·러닝 `첫 숨결`·사이클 `언덕의 도전자`·등산 `첫 고도`·트레일러닝 `야생의 주자` 5개 트리는 Rare 이상에서 "다른 속성 배지 보유" 대신 **전용 미션을 완료해야만** 다음 등급을 획득할 수 있습니다. 미션 완료 시 지급되는 미션보상배지(15종, `condition_json`이 비어 있어 활동 동기화로는 절대 자동 발급되지 않음)를 선행조건으로 요구하는 구조입니다. 나머지 142개 배지는 기존 크로스게이트를 그대로 유지합니다. 상세는 4.6 및 [BadgeEngine/BADGE_ENGINE_UNIFIED.md §2.11](../../Specs/BadgeEngine/BADGE_ENGINE_UNIFIED.md) 참고.
- **홍수 방지(flood cap)** 🟢: 30일 롤링 윈도우 내 종목당 최대 3개로 발급 제한.
- **판정 기준 시간대 — 활동 현지 시각으로 전환(2026-08-10 신규)** 🟢: 요일(주말)·월·시즌·주간 키·연속(스트릭) 계산이 기존 UTC 기준에서 Strava가 제공하는 **활동 현지 시각(`start_date_local`)** 기준으로 변경됐습니다. 기존에는 해외 시간대 유저(예: 한국 유저가 일요일 밤 운동 시 UTC로는 월요일 처리)에게 요일·월 조건이 잘못 적용되는 문제가 있었습니다. GPS 기반으로 활동 발생 지점의 현지 시각이 자동 계산되므로, 유저별 시간대를 별도로 저장하지 않고도 정확한 판정이 가능해 글로벌 확장에도 그대로 대응할 수 있는 구조입니다(GPS 없는 실내 활동은 Strava 계정 시간대로 자동 폴백, `start_date`(UTC)는 정렬·커서 등 절대 시각이 필요한 구간에 계속 사용).

#### 4.1.2. 아이템 배지 (정산 후 확률 드랍형) — 🟢 v2 드랍엔진으로 재설계 완료

- 10개 세계관 × 90개 아이템명 × 4등급 = 3,600개.
- v2.2의 "확률 기반 드랍(꽝 있음)"에서 **활동당 최소 1개 확정 드랍**으로 정책이 근본적으로 바뀌었습니다🟢 — "이번엔 꽝인가?"라는 실망을 제거하고, 불확실성은 "무엇이 나오는지"에만 집중시키는 설계입니다.
- 세계관 선택은 마르코프 가중 추첨(직전 드랍과 같은 세계관 50%, 인접 세계관 25%, 탐험 15%, 맥락 오버라이드 10%)으로 "서사가 이어지는" 체감을 만듭니다.
- 유저 행동과 무관하게 시스템이 상시 POI에 아이템을 자동 배치해두는 **앰비언트 드랍**도 별도로 구현되어 있습니다🟢(만료 없음, 유저 드랍은 30일 만료).
- **배치 동기화 시 드랍 상태 처리 순서 버그 수정(2026-08-13 신규)** 🟢: 신규 가입자 백필처럼 한 번의 동기화에서 여러 활동을 한꺼번에 처리할 때, 내부적으로 최신 활동부터 순서대로 드랍을 시도하다 보니 최종적으로 DB에 저장되는 드랍 상태(복귀 보너스 판정 기준 시각, 일일 하향 카운터, 세계관 모멘텀 가중치)가 실제로는 배치 중 **가장 오래된** 활동 기준으로 뒤바뀌는 버그가 있었습니다. 웹훅 기반 실시간 동기화(1건씩 처리)에서는 드러나지 않고, 신규 가입 백필·재시도로 여러 건이 한 번에 몰릴 때만 나타나는 문제였습니다. 처리 순서를 시간순(오래된→최신)으로 뒤집어 상태가 항상 실제 최신 활동을 반영하도록 수정했고, 상태 불일치를 자동 감지하는 경고 로그와 회귀 테스트를 추가해 재발 시 즉시 감지할 수 있도록 보강했습니다.

#### 4.1.3. 아이템북 & 세계관(faction) 통합 믹스 모델 — 🟢 v2.2에 없던 신규 시스템

v2.2의 `[전설의 겨울 등반러]` 같은 개별 아이템북 컨셉은, 실제로는 **10개 세계관 세계관 서사 체계**로 발전되어 구현됐습니다:

| # | 세계관 | 캐치프레이즈 |
|---|---|---|
| 1 | 낭만 미식가 | "우리가 달리는 이유는, 더 맛있게 먹기 위함이다." |
| 2 | 아스팔트 레인저 | "태풍 경보? 훌륭한 인터벌 트레이닝 날씨군." |
| 3 | 숲속의 갱단 | "네 핫도그 소시지, 우리가 털어간다." |
| 4 | 셔터 마피아 | "내 두 다리는 오직 완벽한 구도와 빛을 위해 움직인다." |
| 5 | 장비병 환자들 | "실력은 돈으로 살 수 없지만, 스피드는 돈으로 살 수 있다." |
| 6 | 비트 마에스트로 | "음악이 멈추는 순간, 나의 질주도 멈춘다." |
| 7 | 포션 연금술사 | "피와 땀? 아니, 내 몸엔 카페인과 타우린이 흐른다." |
| 8 | 아날로그 수집가 | "모든 낡은 물건에는 누군가의 기억과 행운이 깃들어 있다." |
| 9 | 작심삼일 클럽 | "나와의 싸움에서, 나는 항상 나에게 져준다." |
| 10 | 미스터리 헌터 | "숨이 턱 끝까지 차오르는 순간, 세상의 뒷면이 열렸다." |

세계관마다 전용 아이템북 1개(90개 아이템배지로 구성)가 매칭되며, 세계관 간 **인접 그래프**(서사적 개연성 기반)가 드랍 확률과 믹스 결과에 영향을 줍니다. 상세 설정은 [Content/FACTIONS.md](../Specs/Content/FACTIONS.md) 참고.

#### 4.1.4. 배지 비주얼 아이덴티티 — 배경 테마 & 이미지 생산 파이프라인 (2026-08-18 신규, 2026-08-19 전면 확장)

배지가 5,500여 종으로 늘어나면서, "모든 배지가 같은 화면 위에 놓인 다른 그림"이라는 한계가 드러났습니다. 배지 하나하나가 자기 세계의 분위기를 갖도록 **배경 테마 저작 도구**와 **일관된 이미지 생산 체계**를 도입했습니다.

**① 배경 테마 — 단색에서 "배경 제너레이터"로 (🟢 프로덕션 배포 완료)**

배경 테마는 2026-08-18에 **단색 배경색**으로 출발해, 2026-08-19 하루에 걸친 17개 티켓을 통해 **어드민 배경 제너레이터**라는 저작 도구로 완성됐습니다.

**핵심 설계 결정 — "유저 기기에서 쉐이더를 실시간으로 돌리지 않는다"**

v3.9까지 미결로 남아 있던 "쉐이더 기술 스택(CSS 의사 쉐이더 vs WebGL)" 문제는 **선택지를 고르는 대신 문제 자체를 없애는 방식**으로 해소됐습니다. 쉐이더 연산은 **어드민 저작 시점에만** 일어나고, 그 결과를 **정지 이미지(PNG) 또는 반복 영상(MP4)으로 구워(bake) Supabase Storage에 저장**합니다. 유저 기기는 구워진 이미지·영상을 보여주기만 하므로, 모바일 전용 서비스의 배터리·성능 부담이 원천적으로 사라지면서 표현력은 그대로 얻습니다. 대신 **저장되는 것은 완성된 결과물 1장이지 재편집 가능한 설정값이 아니며**, 배경을 고치려면 어드민에서 다시 저작해야 합니다(원시 파라미터 JSON은 저장하지 않음 — 의도된 트레이드오프).

```
[ 어드민 (저작) ]                                                  [ 유저 (재생) ]
이미지 업로드 ─┬─► 패턴 모드 ────┐                                 구워진 PNG를 배경으로 세로 반복
               └─► 애니메이션 모드 ┴─► Paper 필터 1종 ─► 굽기 ─►    또는 MP4를 자동재생·루프
                    (배타 선택)          (5종 중 택1)   PNG/MP4
```

**저작 도구 구성** 🟢

- **패턴 모드**: 업로드한 이미지를 타일로 반복 배치합니다. 대칭 반복·이미지 크기·행/열 간격(stagger)·회전을 조절하며, 크기는 **실제 서비스 폭(앱 컬럼 430px) 기준 절대 px 값**으로 다뤄 어드민에서 본 크기가 실제 화면 크기와 일치합니다(기본값 86px = 430÷5, 가로 5개). 초기에는 "그리드 수"와 "이미지 크기" 두 손잡이가 같은 결과값을 두고 충돌하는 구조였는데, 그리드 수를 아예 제거해 크기 결정 요소를 하나로 정리했습니다.
- **애니메이션 모드**: 오픈소스 collidingScopes(MIT)의 kaleidoscope 로직을 발췌·이식한 Canvas2D 제너레이티브 애니메이션입니다. 패턴 모드와 **배타 선택**입니다.
- **Paper 필터**: 위 결과 위에 전역 필터 1종을 얹습니다 — fluted glass / image dithering / halftone dots / halftone cmyk / lens distortion (`@paper-design/shaders-react`).
- **이미지 소스 선택**: 새 이미지를 올리거나, 이미 등록된 **배지 본체 이미지를 그대로 재사용**할 수 있습니다(배지 폼 한정).
- **단색 vs 제너레이터는 상호 배타**: 라디오로 하나만 선택하며, 저장 시 반대쪽 값은 비웁니다(렌더링 우선순위 혼란 방지).

**반복 영상 굽기** 🟢

애니메이션 모드는 정지 프레임뿐 아니라 **끊김 없이 반복되는 MP4**로도 구울 수 있습니다.

- 인코딩은 브라우저 `MediaRecorder`가 아니라 **WebCodecs `VideoEncoder` + `mp4-muxer`(H.264/MP4)** 를 씁니다 — `MediaRecorder`의 WebM 출력은 iOS Safari에서 재생되지 않고 프레임레이트·용량·키프레임 위치를 통제할 수 없기 때문입니다. 브라우저 지원 범위가 좁은 점은 **어드민 전용 저작 도구라 수용**하고, 미지원 브라우저에는 안내를 띄웁니다.
- **루프 이음매**는 캡처한 16프레임을 되감아 붙이는 **왕복(ping-pong) 방식**으로 해결했습니다. 애니메이션 한 주기가 60fps 기준 약 89초라 2~4초 루프를 정현파 주기에 맞추는 것이 불가능했고, 주기를 강제로 압축하면 저작자가 지정한 애니메이션 속도가 결과물에 반영되지 않기 때문입니다. 왕복 방식은 파라미터와 무관하게 항상 위치 점프가 없습니다.
- 실측 결과물 최대 용량은 473KB로, Storage 버킷 상한(5MB) 안에서 충분히 처리됩니다. `images` 버킷에 `video/mp4` MIME을 추가하고, API 업로드 상한을 버킷 상한과 일치시켰습니다(이전에는 API가 20MB까지 통과시킨 뒤 Storage가 거부하는 모순이 있었음).
- 유저단은 `autoplay muted loop playsinline` + poster로 재생하며, **`prefers-reduced-motion` 설정 시 영상을 숨기고 정지 이미지로 대체**합니다.

**렌더링 구조 — 단일 배경 레이어** 🟢

배경은 앱 컬럼(430px) 폭으로 고정된 **레이어 한 곳에서만** 그립니다. 상단 내비게이션·히어로 카드·본문에 각각 배경을 주입하던 초기 구조를 걷어내고, 배경이 있으면 그 요소들이 투명해지는 방식으로 통일해 이음매를 없앴습니다. 배경 크기는 `cover`(2.17배 확대·좌우 잘림) / `100% 100%`(세로로 늘어나 원이 타원이 됨) / `100% auto` + 반복 3안을 실제로 렌더링해 비교한 뒤 **`100% auto` + 세로 반복**으로 확정했습니다 — 제너레이터 자체가 타일 반복 패턴을 만드는 도구라 반복이 본래 의도와도 맞습니다.

영상 배경도 같은 원칙을 따릅니다. `<video>`는 CSS `background-repeat`가 불가능하므로, 같은 영상을 **세로로 필요한 만큼 쌓는 전용 컴포넌트**를 두고 스크롤 컨테이너의 실제 높이를 측정해 타일 수를 동적으로 계산합니다(상한 10장, 초과분은 poster 정지 이미지 폴백). 이는 페이지가 길 때 배경 영상이 화면 중간에서 끊기던 버그(2026-08-19)를 해결한 구조입니다.

**어드민 미리보기 = 실제 화면** 🟢

어드민 배경 저작 화면은 흉내 낸 전용 마크업이 아니라 **실제 서비스와 같은 컴포넌트**(TopNav / 히어로 섹션 / 조건 카드 / Footer)를 430×932 프레임에 그대로 렌더링합니다. 저작 중 옵션을 바꾸면 이 미리보기가 실시간으로 갱신되며, 저장 시에는 **화면에 보이는 그 노드를 그대로 캡처해** 굽습니다 — 별도 오프스크린 렌더러를 두지 않아 "미리보기와 결과가 다른" 문제가 구조적으로 발생하지 않습니다.

**세계관·컬렉션 일괄 적용** 🟢 (프로덕션 배포 완료)

같은 저작 도구가 세계관(`factions`)·컬렉션(아이템북, `item_books`) 폼에도 붙어 있고, **"하위에 일괄 적용"** 버튼으로 배경값 4종(배경색·쉐이더ID·이미지URL·영상URL)을 한 번에 복사합니다.

| 기준 | 적용 범위 |
|---|---|
| **컬렉션** | 해당 컬렉션에 속한 아이템배지 전체 (1단) |
| **세계관** | ① 세계관 직속 배지 → ② 소속 컬렉션 → ③ 그 컬렉션들의 아이템배지 (**3단 캐스케이드**) |

- 세계관 3단 캐스케이드는 DB 함수(`apply_faction_background_cascade`)로 묶여 **all-or-nothing**으로 실행됩니다 — 3개 UPDATE를 개별 호출하던 초기 구현은 중간에 실패하면 일부만 적용된 상태로 남을 수 있었습니다.
- 실행 전 **대상 건수를 미리 보여주는 확인 절차**를 거칩니다. 건수는 전용 카운트 함수(`count_faction_background_cascade`)를 호출하는 가벼운 조회 경로로 계산합니다(초기에는 전체 배지·컬렉션 목록을 클라이언트로 내려받아 필터링했으나 교체).
- **자동 상속(fallback)이 아니라 버튼을 누른 시점의 1회성 복사**입니다. 이후 세계관 값을 바꿔도 이미 적용된 하위에는 반영되지 않으며(다시 눌러야 함), 개별 배지에서 지정한 값도 함께 덮어써집니다. 배경값이 비어 있으면 버튼 자체가 비활성화됩니다.

**컬렉션 상세화면 배경 테마** 🟢: 배경 렌더링이 배지 상세(`/badges/[id]`)에 이어 **컬렉션 상세(`/itembooks/[id]`)** 에도 같은 원칙으로 적용됐습니다. 세계관 상세화면 전용 배경 렌더링은 별도로 두지 않았습니다 — 세계관 배경값은 캐스케이드로 하위 컬렉션·배지에 복사되어 그 화면들에서 표현되는 구조이기 때문입니다.

**적용 범위 확장 예정** 🔵: 같은 배경값을 향후 개발할 "배지 획득 알림 모달"에서도 재사용할 수 있도록, 배경 스타일 계산을 단일 함수(`getBadgeBackgroundStyle`)로 모아둔 구조입니다.

**데이터 모델**: `badges`·`factions`·`item_books` 세 테이블이 `background_color`·`background_shader_id`·`background_image_url`·`background_video_url`(전부 text, nullable)을 공통으로 갖습니다. 렌더링 우선순위는 **영상 > 이미지 > 색**이며, 값이 하나도 없으면 기존 기본 배경이 그대로 유지되므로 미지정 배지에는 아무 영향이 없습니다. `background_shader_id`는 실시간 쉐이더 렌더링 경로가 채택되지 않아 **사실상 레거시 컬럼**입니다(값이 있어도 무시됨). 관련 마이그레이션: 086~092.

**배지 이미지 저장 방식 전환** 🟢: 어드민 배지 등록·수정이 외부 이미지 URL 텍스트 입력에서 **Supabase Storage 파일 업로드**로 바뀌었습니다(평균 컬러 추출의 전제이기도 함). 기존에 외부 URL로 등록된 배지는 소급 이전 대상이 아니며 그대로 동작합니다.

**기본값 자동 제안** 🟢: 배지 이미지를 업로드하면 서버(`sharp`)가 이미지의 평균 컬러를 계산해 배경색 입력값을 미리 채웁니다. 관리자는 색상 피커·hex 입력으로 언제든 덮어쓸 수 있고, 추출 실패 시에도 저장은 막히지 않습니다(수동 입력으로 대체).

> **운영 교훈 — `sharp` 프로덕션 로딩 장애 (2026-08-19)**: 평균 컬러 추출에 쓰는 `sharp`가 프로덕션 서버리스 번들에서 네이티브 바이너리(libvips)를 찾지 못해 실패하는 장애가 발생했습니다(`ERR_DLOPEN_FAILED`). `serverExternalPackages: ['sharp']`(Next.js 공식 권장)로 해결했으나, 2차 대응 과정에서 빌드 경고를 없애려고 `outputFileTracingRoot`를 수동 지정했다가 **Vercel의 배포 패키징 단계 자체가 깨지는** 더 큰 장애를 유발해 즉시 되돌렸습니다. 교훈은 **"플랫폼이 자동 추론하는 값을 경고를 없애려고 수동으로 덮어쓰지 않는다"** — 해당 경고는 무해했고, 맞추려는 시도가 오히려 배포를 깨뜨렸습니다.

**② 배지 이미지 대량 생산 파이프라인 — 🟡 부분 구현 (양산 전)**

아이템배지 900종(세계관 10 × 컬렉션 10 × 아이템 9)의 이미지를 세계관별로 일관된 톤앤매너로 확보하기 위한 생산 체계를 확정했습니다.

- **툴·모델 확정**: Recraft 단독 사용, **Recraft V3 / V3 Vector**. 어그리게이터 경유 대비 단가 차이가 사실상 없고, 이 프로젝트의 핵심인 커스텀 스타일 고정(`style_id`)·금지 요소 지정·텍스트 억제가 V3 계열 전용 기능이라 최신 V4.1 대신 V3를 선택했습니다(스타일 일관성 최우선).
- **스타일 통제 4종 세트**: 세계관마다 ⑴ 레퍼런스 이미지 3~5장으로 등록한 커스텀 스타일 ID, ⑵ 직접 지정한 팔레트, ⑶ 금지 요소 목록, ⑷ 텍스트 억제 스위치를 세트로 관리합니다.
- **문서 체계** 🟢: `Specs/Content/IMAGE_PRODUCTION/` 아래에 워크플로우 확정 문서, 스타일그룹 정의서·컨텐츠 브리프 양식, 세계관 10개 폴더가 만들어졌고, **컨텐츠 브리프 100종(세계관 10 × 컬렉션 10)** 은 FACTIONS.md·ITEMBOOKS·운영 DB의 아이템 UUID를 기준으로 전량 생성 완료됐습니다.
- **남은 작업** 🔵: 세계관별 스타일그룹 정의서(팔레트·렌더링 규칙)와 레퍼런스 이미지는 **디자이너 입력 대기** 상태입니다. 이후 커스텀 스타일 등록 → 검증 배치(컬렉션 1개, 9장) → 양산(아이템당 6~8장 과잉 생성) → 후처리 → **수동 QC** 순으로 진행합니다. 900장 기준 예상 비용 약 $200~300으로, 비용은 제약 조건이 아니며 품질 기준으로만 판단합니다.

### 4.2. 공간 상호작용: 드랍 및 픽업 시스템 — 🟢 현재 구현 (전면 개방 상태)

```
       [ 드랍 (Drop) ]                            [ 픽업 (Pick-up) ]
유저가 보유한 아이템을 현재 위치한                  지도(네이버 지도)를 탐색하여 POI 반경
POI에 가상 배치 (인벤토리에서 차감)                  500m 이내 도달 시, 다른 유저가 드랍해 둔
                                                또는 시스템이 숨겨둔 아이템 픽업
```

> **v2.2 대비 가장 큰 정책 변경**: v2.2는 픽업 기능을 "서울 DAU 3만 명 돌파 시" 잠금 해제하는 **단계적 활성화 정책**을 제시했습니다. 실제로는 **처음부터 드랍/픽업을 전면 개방한 상태로 서비스 중**입니다🟢 — 서비스가 아직 초기 단계라 이 단계적 개방 정책 자체가 적용되지 않았습니다. 향후 실제 유저 밀도가 낮은 지역에서 "텅 빈 지도" 문제가 확인되면, v2.2가 제안한 단계적 잠금 해제 정책을 다시 검토할 수 있습니다🔵.
>
> 픽업 반경도 v2.2의 "30m"에서 **500m**로 실제 구현됐습니다(T1/T2 POI 동일 적용) — 초기 유저 밀도가 낮은 상황에서 실제로 아이템을 찾을 수 있도록 반경을 넓게 잡은 것으로 보입니다.
>
> **일일 드랍 카운터 리셋 기준도 활동 현지 시각으로 전환(2026-08-10)** 🟢: 기존에는 UTC 자정(= 한국 유저 기준 오전 9시) 기준으로 하루 드랍 한도가 리셋되어, 오전 9시 이전에는 전날 드랍 한도가 그대로 적용되는 문제가 있었습니다. 배지 판정과 동일하게 활동 현지 시각(`start_date_local`) 기준으로 전환되어 해소됐습니다.

**POI 탐색 UI: 바텀시트 → 캐러셀 (2026-08-20)** 🟢

지도에서 POI 마커를 누르면 그 POI 하나만 담긴 바텀시트가 열리던 구조를, **반경 내 POI 전체를 좌우로 넘겨보는 캐러셀**로 바꿨습니다. 캐러셀 카드 자체가 곧 모달이며, 패널은 하단 탭바 바로 위에 고정되고 내용이 길어지면 위쪽으로만 늘어납니다. 양옆 카드는 화면 가장자리에 살짝 걸쳐 보이게(peek) 배치해 "넘길 수 있다"는 것을 눈으로 알 수 있게 했습니다.

- **성능 원칙 — 중앙 카드만 불러온다**: 아이템 드랍 목록은 캐러셀 **중앙(활성) 카드**에 대해서만 조회합니다. 초기 구현은 보이는 카드 전부를 미리 불러와 로딩이 길어졌고, 이를 활성 카드 단일 조회로 좁혀 해소했습니다.
- **tier2(자동수집) POI 동일 지원**: 네이버 지역검색으로 자동수집된 tier2 POI도 정식 `poi` 레코드로 저장되어 드랍/픽업이 정상 동작합니다 — 한때 "tier2는 DB에 없어 드랍과 연결될 수 없다"는 잘못된 전제로 들어간 회귀가 있었으나 DB 확인을 거쳐 되돌렸습니다.

### 4.3. 한정판 고유 번호 부여 — 🟢 현재 구현 (형식은 변경)

- 획득한 아이템에 고유 시리얼 번호가 부여되는 것은 v2.2 그대로 구현됐으나, 형식은 **"발견 순서 그대로 노출"이 아닌 난수 기반**으로 바뀌었습니다 — 발급 순서·전체 발급량이 노출/역산되지 않도록 1~999,999 범위 난수 + 중복 시 재시도 방식입니다🟢. 형식은 `접두사(4자리 대문자) + 6자리 번호`(예: `ABCD000042`).
- **피지털 실물 연계**(실물 패치 뒷면 레이저 각인)는 D2C 자체가 미구현이라 아직 불가능합니다🔵.

### 4.4. 인벤토리 제한 시스템 — 🟢 현재 구현

- 기본 가방 50슬롯 제한은 v2.2 그대로 구현됐습니다. 단, 아이템북 슬롯에 장착된 아이템은 인벤토리 칸을 차지하지 않도록 개선됐습니다🟢(v2.2에는 없던 세부 규칙).
- 슬롯 확장을 위한 포인트 결제 경로는 현재 명시적으로 확인되지 않았습니다🟡.
- **신규 가입자 인벤토리 자동 생성 버그 수정(2026-08-13)** 🟢: 운영 DB의 신규가입 트리거 함수가 과거 컬럼명 리네이밍 작업 중 마이그레이션 파일 없이 직접 수정되면서 인벤토리 생성 구문이 누락돼 있었고, 그 결과 2026-08-04 이후 가입한 일부 계정이 인벤토리 자체가 없어 아이템 배지가 전혀 드랍되지 않는 상태로 방치되어 있었습니다. 트리거 함수를 복원하고 피해 계정을 백필·정산 발급했으며, 향후 이런 드리프트가 반복되지 않도록 함수·트리거도 마이그레이션 파일 없이 운영 DB에서 직접 수정하는 것을 금지하는 개발 규칙을 명문화했습니다.

### 4.5. 비밀 믹스법과 아이템 믹싱 — 🟢 현재 구현 (v2로 재설계 완료)

- 재료 2~10개를 소각해 새 아이템을 만드는 믹스 시스템은 실제 구현되어 있으며, v2.2 이후 두 갈래 경로로 재설계됐습니다🟢:
    - **정석 레시피 경로**: 재료가 정확히 일치하면 Legend/Mythic 확정 획득 (33개 레시피 등록).
    - **비매칭 임의 믹스 경로**: 세계관 다양성(재료가 여러 세계관에 걸칠수록 유리)에 따른 확률 보상 + 연속 실패 시 확률이 미세 상승하는 피티(pity) 시스템.
- "비밀 믹스법을 커뮤니티에 바이럴로 전파"하는 컨셉(실루엣·힌트만 제공)은 정책적으로 유효하나, 실제 UI 노출 방식은 확인이 더 필요합니다🟡.

### 4.6. 다이나믹 미션 시스템 — 🟢 현재 구현 (v2.2와 다른 방식)

v2.2는 "라이브 운영 시스템"으로 미션을 구상했으나, 실제로는 **어드민이 등록하고 유저가 참가 신청 후 완료하는 상시 미션 시스템**으로 구현됐습니다🟢:

- 참가 필수(참가자만 완료 보상 획득), 참가 취소 불가, 진행도 실시간 추적.
- 완료 시 배지 + 포인트 자동 보상.
- 정렬(기본/종료임박/신규순)·필터(활동종류/미션유형) UI 제공.
- v2.2의 "하드코어 퍼포먼스 미션", "지역 탐험 미션" 같은 구체적 시나리오는 어드민이 콘텐츠로 직접 등록하는 방식이라, 시스템은 준비되어 있으나 실제 콘텐츠 기획은 운영 역량에 달려 있습니다.
- **배지 게이팅 연동 + 엔진 확장(2026-08-13 신규)** 🟢: 미션 완료가 특정 배지의 선행조건으로 연결되는 첫 사례로, 5개 종목 대표 배지 트리(4.1.1 참고)의 Rare 이상이 미션 완료 기반으로 전환됐습니다. 이를 위해 미션 조건 타입이 3종(연속일수 `streak_days`·단일세션 지속시간 `duration_minutes`·등반고도 `elevation_gain_m`) 추가됐고, 판정 로직은 새로 만들지 않고 배지 엔진의 조건 평가 함수를 그대로 재사용해 배지 판정과 미션 판정의 불일치 위험을 차단했습니다. 종료일을 지정하지 않는 **상시 미션**(`ends_at = NULL`)과, 다른 참가자 정보 없이 본인 진행상황만 보여주는 **개인형 미션 진행상황**(`status_display_type = 'individual'`) 표시 방식도 함께 도입되어 기존 활성 미션에도 어드민에서 전환 적용할 수 있습니다.

### 4.7. JAM 포인트 시스템 — 🟢 v2.2에 없던 신규 시스템 (디지털 재화)

v2.2는 "잼 포인트"를 플리마켓 거래 기축 통화로만 언급했으나, 실제로는 **포인트 지갑 + append-only 원장** 구조로 먼저 구축됐습니다🟢:

- 배지 획득·미션 완료·믹스 실패 시 피티 보상 등으로 적립.
- 잔액은 반드시 `award_points()` 함수를 경유해 변경되며, 원장과의 정합성을 어드민 화면에서 상시 검증합니다.
- **포인트를 실물/디지털 상품으로 소비하는 경로(플리마켓, D2C 결제)는 아직 없어**, 현재는 "쌓이기만 하는" 상태입니다🔵 — 향후 플리마켓·D2C가 구현되면 포인트의 소비처가 열립니다.
- **지급 실패 로깅 중앙화(2026-08-13)** 🟢: 기존에는 미션·믹스·어드민 지급 등 일부 호출부에서 포인트 지급 실패가 조용히 무시되어 어드민에서 감지가 불가능했습니다. `award_points()` 실패 시 항상 엔진 결정 로그에 기록되도록 중앙화해, 호출부가 개별적으로 챙기지 않아도 모든 지급 실패가 자동으로 감지되도록 개선했습니다.

### 4.8. 절대 희귀도: 신화 아이템 — 🟡 부분 구현

- 신화(mythic) 등급 자체는 아이템 배지 체계에 포함되어 실제로 드랍됩니다🟢 (전체 3,600개 중 900개가 mythic 등급).
- v2.2가 제시한 "무제한 속성 결합으로 발급 조건을 극도로 까다롭게" 만드는 것보다는, 현재는 세계관별 고정 900개 풀 내 확률/피티 구조로 관리됩니다.
- **떠돌이 속성**(72시간 후 인벤토리 이탈 → 다른 POI로 이동): DB 스키마(`wandering_mythic_state`)와 매일 자동 재배치 크론은 구현되어 있으나🟢, 유저에게 실시간 위치를 보여주거나 커뮤니티 제보를 받는 **유저 대면 UI가 실제로 노출되는지는 추가 확인이 필요**합니다🟡.

---

## 5. 폭발적 바이럴을 위한 기생적 SNS 연동 전략 — 🔵 대부분 향후 업그레이드

v2.2가 그린 3대 채널(인스타그램·스트라바·카카오톡) 전략은 방향성은 여전히 유효하지만, **2026-08-10 기준 실제 구현된 것은 없습니다.** 오히려 한때 구현했던 기능을 제거한 사례가 있어, 이 장은 v2.2 대비 가장 큰 폭으로 후퇴한 영역입니다.

### 5.1. 인스타그램 (한정판 디지털 포토카드 전시) — 🔵 구현했다 제거됨

- `@vercel/og`를 이용한 공유 카드 이미지 생성 기능이 한때 구현되어 있었으나, **2026-08-07에 기능 자체와 관련 의존성이 완전히 제거**되었습니다. 재도입 여부·시점은 미정입니다.

### 5.2. 스트라바 피드 강제 침투 전략 — 🔵 미구현

- 활동 사진첩에 JAM! 성취 이미지를 자동 업로드하거나 설명란에 문구를 자동 삽입하는 기능은 착수되지 않았습니다.

### 5.3. 카카오톡 조르기 및 거래 딥링크 — 🔵 미구현 (선행 조건 자체가 없음)

- 이 기능은 P2P 플리마켓 거래를 전제로 하는데, **플리마켓 자체가 "coming soon" 상태**라 딥링크를 보낼 거래 이벤트 자체가 아직 존재하지 않습니다. 플리마켓 착수가 이 기능의 선행 조건입니다.

---

## 6. JAM! 페어플레이 선언 및 유쾌한 어뷰징 정책 — 🟢 대부분 현재 구현

v2.2가 제시한 정책 철학(완벽한 통제보다 유쾌한 수용)과 투트랙 구조(Soft/Hard-Abusing)는 실제로 거의 그대로 구현되어 프로덕션에서 동작 중입니다. 실측 파라미터를 반영해 갱신합니다.

### 6.1. JAM! 페어플레이 선언문 (온보딩 심리적 방어선)

> _"스마트폰을 흔들어서 만보기 숫자를 올리거나 자전거를 타고 달리기로 기록을 속이셨나요? 괜찮습니다! 그렇게 얻은 소소한 포인트로 즐기세요. 단, 모두를 위해 준비된 '진짜 오프라인 브랜드 협찬 쿠폰'과 '실물 한정판 굿즈 구매 권한' 앞에서는 꼼수를 거두고 정정당당하게 땀 흘린 진짜 유저분들에게 양보해 주시기를 바랍니다. 우리는 여러분의 페어플레이를 믿습니다. Join And Move!"_

> 이 선언문은 현재 온보딩 화면에 실제로 노출되는지 별도 확인이 필요합니다🟡. 문안 자체는 유효합니다.

### 6.2. 투트랙 섀도우밴 데이터 정책 — 🟢 현재 구현

```
                  ┌──► Soft-Abusing (폰 흔들기 등): 잡템/소량 포인트 정상 드랍 처리
[ 어뷰징 대응 정책 ]
                  └──► Hard-Abusing (GPS 조작 등): 섀도우밴 적용, 고가치 아이템 드랍률 0%
```

- **Soft-Abusing**: common/rare 등급 드랍률은 유지(100%)하되 legend/mythic은 완전 차단하는 정책이 실제 파라미터로 운영됩니다.
- **Hard-Abusing**: rare 이상 등급 드랍이 전면 차단됩니다.
- **GPS 조작 감지 (실측 파라미터, 2026-08-10 기준)**:
    - 직전 위치 대비 이동 속도가 시속 300km를 초과하면 조작으로 판정🟢.
    - **2026-08-09 신규 추가**: 요청 간 시간 간격을 두고 좌표를 옮겨 순간 속도 체크를 우회하는 "느린 텔레포트"를 막기 위해, **일일 누적 이동거리 3,000km 상한**을 추가로 도입했습니다🟢.
    - 조작 감지 시 해당 POI 영역 픽업/드랍을 72시간 차단(v2.2와 동일한 시간)🟢.
    - 차량/대중교통 탑승 어뷰징 방지: 시속 60km 이상 지속 구간은 라이딩 데이터로 인정하지 않는 속도 필터가 실제 구현되어 있습니다🟢(v2.2 §8-5 미결 아젠다였던 항목이 해소됨).
- **최종 방어선(실물 SMS 인증·배송지 교차 검증)**: 실물 결제 자체가 미구현이라 아직 적용할 대상이 없습니다🔵.

---

## 7. 실제 개발 이력 요약 (2026-07-09 ~ 2026-08-20)

> v2.2의 "Phase 1~5 로드맵"은 실제 개발 순서와 크게 달라져 폐기되었습니다(전체 원문은 [History/PHASES_ROADMAP_ARCHIVE.md](../History/PHASES_ROADMAP_ARCHIVE.md)에 이력 자료로 보관). 아래는 실제 티켓 이력(`History/Migration/Ticket/`, 100건 이상) 기준으로 재구성한 시간순 요약입니다. 최신 작업 이력은 항상 해당 폴더를 직접 참고하세요.

- **2026-07-09~10 (초기 구축)**: 온보딩(구글 로그인·프로필)·Strava 동기화·배지 자동발급·POI 사후인증·인벤토리·확률 드랍·어드민 패널·유저 드랍/픽업 기본 골격 완성.
- **2026-07-15~20 (배지 엔진 안정화)**: POI 매칭 성능 개선, 배지 이름당 최상위 등급만 노출하는 "성장 티어" 정책, 첫 싱크 게이트 도입.
- **2026-07-16~21 (세계관·아이템북 도입)**: factions·item_books 마이그레이션, 900개 아이템배지 시드, 아이템북 슬롯팅 UI, 배지 발급 엔진 통합.
- **2026-07-21~27 (드랍엔진 v2 + 믹스 v2)**: "활동당 최소 1개 확정 드랍" 정책 전환, 세계관 모멘텀 로직, 믹스 시스템 v2(레시피/다양성 티어), pity 로직.
- **2026-07-22 (지도·POI 소스 전환)**: Google Maps → 네이버 지도, OSM → 네이버 지역검색 API. 전국 산 847개·지하철역 929개 대량 등록 착수.
- **2026-07-24~26 (홈 개편)**: "투데이" CMS(콘텐츠 카드 7종 템플릿) 도입, 미션 시스템 신규 구축.
- **2026-07-28~08-01 (경제 밸런스)**: rare+ pity 로직, 일련번호 무작위화, JAM 포인트 시스템 신규 구축(1a단계), 배지·드랍 엔진 신뢰성 개선.
- **2026-08-02~06 (미션·POI 고도화 + 문서 재정리)**: 미션 참가 검증 강화, POI 진행률 표시, 지도 클러스터링, POI 배지 타입 신규 추가(전국 산·지하철역 1,820개 1:1 매핑), 도메인 전환(`jam-rose.app` → `j-a-m.app`), 문서 체계 4카테고리 재정리, `@vercel/og` 공유카드 기능 제거.
- **2026-08-06~09 (배지 v3 + 보안 강화)**: 배지 시스템 v3(첫 싱크 게이트 + 크로스-어트리뷰트 선행 조건), 믹스 로직 레이스 컨디션 수정, 어드민 디버그 라우트 인증 추가, GPS 어뷰징 일일 누적 이동거리 상한 도입, 어드민 UI 개선(사이드바 접기/펼치기, 테이블 반응형).
- **2026-08-10 (동기화 안정화 + 글로벌 시간대 대응)**: Strava 수동입력(`manual=true`) 필터 도입 후 정상 활동까지 걸러지는 버그 발견·롤백. reconcile 크론 제거 + 동기화 실패 시 커서 롤백 도입(2026-08-10 신규 유저 배지 미발급 인시던트 근본 수정). 배지·드랍 판정 기준을 UTC에서 활동 현지 시각(`start_date_local`)으로 전환해 해외 시간대 유저 오판정·드랍 카운터 리셋 시차 문제 해소(4.1.1, 4.2 참고). 과거 활동 백필 시 피드 이벤트 날짜가 처리 시각으로 잘못 기록되던 버그 수정. 서비스 플랜 문서 체계 신설(자동 업데이트 크론 포함).
- **2026-08-13 (핵심 루프 신뢰성 강화)**: 신규 유저 인벤토리 미생성으로 아이템배지가 발급되지 않던 버그 근본 수정 — 마이그레이션 파일 없이 운영 DB에서 직접 수정돼 있던 신규가입 트리거 함수를 복원하고 피해 계정 백필·정산 발급(4.4 참고). POI 반경 오탐(지하철역이 500m로 잘못 생성되던 근본 원인)을 코드 레벨에서 수정하고 카테고리별 정확 매칭 반경 정책을 표준화(지하철 50m·산 150m, 3.3 참고). 신규 가입자 다건 백필 시 드랍 상태가 뒤바뀌던 배치 처리 순서 버그 수정(4.1.2 참고). 포인트 지급 실패 로깅 중앙화(4.7 참고). 이 외 DB 타입 자동생성 체계 도입, Supabase 타입 안전성 전수 강화(우회 캐스팅 65개 파일 제거, 미등록 테이블 6개 타입 보강), 개발 프로세스 가드레일 문서화, 개발자·리뷰어 역할을 분리해 구현과 검증을 다른 컨텍스트에서 진행하는 `/jam-work` 파이프라인 도입 등 개발 프로세스 신뢰성 인프라를 대폭 보강.
- **2026-08-13 (종목별 대표 배지 미션 게이팅 + 배지 등급명 정정)**: 5개 종목 대표 배지 트리의 Rare 이상 획득 조건을 미션 완료 기반으로 전환하기 위해 미션 엔진을 확장(연속일수·단일세션 지속시간·등반고도 판정, 상시 미션, 개인형 미션 진행상황 — 4.1.1·4.6 참고)하고, 미션보상배지 15종·미션 15종을 생성. GPS 조작 감지 오탐 방지 보강(최소 경과시간 가드 추가, 어긋나 있던 정책값 정정, 소프트밴 무기한 잔존 문제 해소). 배지 희귀도 등급명 `Legendary`를 `Legend`로 DB·코드·문서 전 레이어 일관 변경(발음 간소화, 6.2·8장 등 본 문서 전반의 등급 표기에도 반영됨).
- **2026-08-16 (서비스 용어 정비)**: 고정 용어 '겟(GET)' → '획득', '인벤(INVENTORY)' → '인벤토리'로 각각 전면 교체. UX 라이팅 가이드라인·i18n·소스코드·문서를 일괄 동기화(v3.7·v3.8 변경점 참고).
- **2026-08-18 (배지 비주얼 아이덴티티 착수)**: 배지별 배경 테마 기능을 기획 검토(등급 태그 쉐입 커스터마이징은 "등급을 색·모양으로 즉시 인지"하는 학습된 패턴을 깨뜨린다는 이유로 폐기 결정) → 선행 구조 개편(시각 결과 무변경) → 배경색 어드민·상세화면 렌더링 → 세계관·컬렉션 일괄 적용 순으로 4개 티켓에 걸쳐 진행. 배지 이미지 업로드를 Supabase Storage로 전환하고 `sharp` 기반 평균 컬러 자동 추출을 도입, `badges`·`factions`·`item_books`에 배경 테마 컬럼 추가(마이그레이션 086·087). 배경 레이어가 링크 클릭을 가로채던 회귀를 수정했고, 데스크톱 폭에서 고정 배경 레이어가 앱 컬럼(최대 430px) 밖까지 새어나가던 버그도 함께 수정했습니다(세계관·컬렉션 일괄 적용과 함께 2026-08-19 프로덕션 배포 완료, 4.1.4 참고). 같은 날 배지 이미지 대량 생성 파이프라인(Recraft V3) 워크플로우를 확정하고 세계관 10 × 컬렉션 10 = 컨텐츠 브리프 100종을 전량 생성.
- **2026-08-19 (배경 제너레이터 완성 — 하루 17개 티켓)**: 배경 테마가 단색에서 **어드민 저작 도구**로 확장된 날입니다. ① 로그인 없이 접근 가능한 독립 스파이크 라우트(`/spike/background-generator`)에서 파이프라인(이미지 업로드 → 패턴/애니메이션 배타 선택 → Paper 필터 1종 → 실시간 미리보기)의 기술적 실현 가능성을 먼저 검증하고, 패턴 모드의 조작 모델을 6개 티켓에 걸쳐 다듬었습니다(오프셋을 행/열 간격으로 재정의 → 간격이 이미지 크기를 바꾸던 버그 근본 수정 → 그리드 수 제거·실제 서비스 폭 430px 기준 재구성). ② 검증된 저작 UI를 어드민 배지 폼에 이식하고, 저장 시 결과를 PNG로 구워 Storage에 올리는 실제 저장 경로를 연결했습니다. ③ 배경 렌더링을 **단일 레이어**로 통합하고 어드민 미리보기가 실제 화면과 동일한 컴포넌트를 쓰도록 재구성했습니다. ④ WebCodecs + `mp4-muxer`로 **반복 영상(MP4) 굽기**를 구현하고 유저단 재생·`prefers-reduced-motion` 대응을 붙였습니다. ⑤ 컬렉션·세계관으로 저작 UI를 공용화하고 **일괄 적용**을 컬렉션 1단 / 세계관 3단 캐스케이드로 완성했으며, 캐스케이드를 DB 함수로 묶어 원자성을 확보하고 건수 미리보기를 서버 카운트 함수로 교체했습니다. ⑥ 페이지가 길 때 배경 영상이 하단까지 이어지지 않던 버그를 영상 타일 스택 방식으로 수정했습니다. 이 과정에서 `sharp` 네이티브 모듈이 프로덕션에서 로딩되지 않는 장애가 두 차례 발생해 긴급 대응했습니다(`serverExternalPackages` 적용, `outputFileTracingRoot` 수동 지정 시도는 배포 패키징을 깨뜨려 되돌림). 마이그레이션 088~092 실행 완료. (티켓 20260819_001~017)

- **2026-08-20 (MODULAR 실연결 + 화면 다듬기 — 하루 26개 티켓)**: 디자인 시스템이 "카탈로그"에서 "실제로 쓰이는 코드"가 된 날입니다. ① **토큰 연결**: `globals.css`가 `design-system/tokens/*.css`를 실제 `@import`하도록 바꿨습니다(그전까지 68개 토큰을 양쪽에서 손으로 맞춰 쓰던 상태). 이 과정에서 DS v2에서 제거된 `--radius-md`를 서비스가 계속 참조해 모서리가 각지게 렌더링되던 버그도 발견·수정했습니다. ② **컴포넌트 연결**: 어디에도 없던 ProgressBar를 MODULAR에 신설해 파편화돼 있던 진행도 바 6곳을 통일한 것을 파일럿으로, 24종을 리스크별 A~D 그룹으로 전수 분류(티켓 007)한 뒤 A그룹(RarityBadge·WanderingEyesLoader·CollectionGridCard)·B그룹(Card·TopNav·BadgeGridCard·ListRowCard)·D그룹 일부(EmptyState 22곳·IconButton)를 순차 전환해 총 10종을 실연결했습니다. EmptyState 전환으로 빈 화면이 텍스트 한 줄에서 아이콘+제목+설명 구조로 확대됐고, 흰 배경 시트 4곳은 DS를 고치는 대신 화면 자체를 다크 표면으로 전환해 해소했습니다. C그룹(TabBar·BottomSheet·SlidingTabs·Toast·Button)과 Skeleton은 "서비스 구현이 이미 더 성숙하다"는 조사 결과에 따라 의도적으로 보류했습니다. ③ **드랍 POI 캐러셀**: 마커 클릭 시 반경 내 POI를 좌우로 넘겨보는 캐러셀 모달을 도입하고(4.2 참고), 프로덕션 피드백을 받아 옆 카드 노출·패널 위치·여백·무한 로딩·tier2 드랍 미노출 회귀를 연달아 수정한 뒤 활성 카드 단일 조회로 성능을 정리했습니다. ④ **프로필·여백 정비**: 프로필 헤더에서 이메일 노출과 카드 박스를 제거하고 아바타·아이디·잼포인트를 재배치, 본인 프로필의 빈 TopNav 제거, TopNav 아래 컨텐츠 상단 여백을 전 화면에서 0으로 통일, 통계탭 이중 박싱 제거·숫자 볼드·포인트 잔액 강조. 프로필 컬렉션 목록에 등급 태그가 빠져 있던 데이터 소스 버그도 수정했습니다. (티켓 20260820_001~026)


---

## 8. 향후 업그레이드 아젠다

v2.2의 "미결정 아젠다"를 재점검한 결과입니다 — 일부는 실제로 해소됐고, 일부는 여전히 미결 상태입니다.

### 8.1. 해소됨 (더 이상 아젠다 아님)

- ~~이기종 헬스 데이터 중복 인식 방지~~ — **해소**: Strava 단일 소스로 서비스 방향이 확정되어(Garmin·Apple Health 연동은 하지 않기로 결정), 다중 소스 중복 인식 문제 자체가 발생하지 않습니다.
- ~~차량/대중교통 어뷰징 필터~~ — **구현 완료**: 시속 60km 이상 구간 무효화 필터가 실제 적용 중입니다(6.2 참고).
- ~~드랍 아이템 만료일 규정~~ — **구현 완료, 단 환전 정책은 없음**: 유저 드랍은 30일 후 자동 소각되나, v2.2가 검토를 요청했던 "소량 잼 포인트로 반환(환전)"하는 정책은 채택되지 않았고 단순 소각으로 확정 운영 중입니다.
- ~~글로벌 시간대 대응~~ — **해소(2026-08-10)**: 배지·드랍 판정이 UTC 고정 기준이라 해외 시간대 유저에게 요일/월/시즌 조건이 잘못 적용되는 구조적 문제가 있었으나, Strava가 제공하는 활동 현지 시각(`start_date_local`)을 판정 기준으로 전환해 유저별 시간대를 별도 저장하지 않고도 전 세계 어디서든 정확히 동작하도록 해소했습니다(4.1.1 참고).
- ~~POI 반경 오탐(지하철역 500m 하드코딩)~~ — **해소(2026-08-13)**: 자동수집 파이프라인이 카테고리와 무관하게 500m로 POI를 생성하던 근본 원인을 코드에서 제거하고, 카테고리별 정확 매칭 반경 정책을 3개 지점(자동수집·수동생성·배지연결)에 강제 적용해 재발을 구조적으로 차단했습니다(3.3 참고).
- ~~배지 배경 쉐이더 기술 스택 확정~~ — **해소(2026-08-19)**: "CSS 의사 쉐이더 vs WebGL" 중 하나를 고르는 대신, **유저 기기에서 쉐이더를 실시간으로 돌리지 않는다**는 결정으로 문제 자체를 없앴습니다. 어드민 저작 시점에만 연산하고 결과를 정지 이미지(PNG)·반복 영상(MP4)으로 구워 저장하는 방식이 확정·구현·배포됐습니다. 실시간 쉐이더 렌더링 경로는 채택하지 않기로 확정됐고, `background_shader_id` 컬럼은 사실상 레거시로 남습니다(4.1.4 참고).

### 8.2. 여전히 미결 — 향후 업그레이드 필요

1. **실물 굿즈의 재고 및 물류 정책** — D2C 실물 패치 자체가 미구현이라 여전히 미결. 개별 주문제작 vs 사전 대량 제작 선택은 D2C 착수 시점에 재논의 필요.
2. **플리마켓 거래 봇 및 작업장 어뷰징 방어** — 플리마켓 자체가 미구현이라 여전히 미결. 플리마켓 착수 시 기기 고유 ID 체크, 거래 쿨타임, 일일 거래 제한 등을 함께 설계해야 합니다.
3. **D2C 실물 패치 결제 플로우 착수 일정** — 2026-08-07에 "앱 내 구현"으로 방향은 확정됐으나, 착수 시점은 미정입니다.
4. **P2P 플리마켓 착수 일정** — 미정. `/inventory/flea-market`은 "coming soon" 상태 유지 중입니다.
5. **푸시 알림 도입** — PWA manifest·Service Worker 자체가 미구현이라 착수 전. 도입 시점 미정입니다.
6. **신화 아이템 "떠돌이" 속성의 유저 대면 UI 완성도** — DB·크론은 있으나 실시간 위치 노출·커뮤니티 제보 UI가 실제로 얼마나 구현됐는지 추가 확인이 필요합니다.
7. **커뮤니티 피드 공개 범위 정책** — 통합 활동 피드(`user_activity_feed`)는 이미 동작 중이나, 공개/비공개/팔로우 공개/전체공개 체계가 아직 확정되지 않았습니다.
8. **SNS 연동 재도입 여부** (5장) — 공유 포토카드 기능이 제거된 상태이며, 재도입 여부·방식·시점 모두 미정입니다.
9. **밝은 배경 위에서의 텍스트 대비** — 배경색이 파스텔톤이거나 구워진 배경 이미지·영상이 밝을 때 보조 설명 텍스트(회색)의 대비가 낮을 수 있습니다. 텍스트 그림자로 최소 보정만 적용한 상태이며, 배경 밝기에 따라 텍스트 색을 흑/백으로 동적 전환하는 설계는 후속 검토 대상입니다. 제너레이터로 임의의 이미지·영상을 배경으로 쓸 수 있게 되면서 이 이슈의 적용 범위가 넓어졌습니다(4.1.4 참고).
10. **배지 이미지 양산 착수** — 생산 워크플로우와 컨텐츠 브리프 100종은 확정됐으나, 세계관별 스타일그룹 정의서·레퍼런스 이미지가 디자이너 입력 대기 상태라 실제 900장 생성은 시작 전입니다(4.1.4 참고).
11. **배지 엔진 문서-코드 정합성 점검** — `BADGE_ENGINE_UNIFIED.md` §2.6에 남아있는 "30일 롤링 윈도우 홍수 방지 캡" 서술이 실제로는 과거 티켓에서 이미 폐기된 정책이라 코드와 불일치합니다(2026-08-13 조사에서 발견). 문서 정정이 필요합니다.
12. **배경 저작 설정값 재편집 불가** — 배경 제너레이터는 완성된 결과물(PNG/MP4) 1장만 저장하고 원시 파라미터(이미지·패턴/애니메이션 값·필터 종류)는 저장하지 않습니다. 배경을 조금만 고치고 싶어도 처음부터 다시 저작해야 하므로, 운영 규모가 커지면 설정값 JSON 저장·재편집 경로가 필요해질 수 있습니다(4.1.4 참고).
13. **배경 제너레이터 스파이크 코드 정리** — 검증용으로 만든 `/spike/background-generator` 라우트(로그인 없이 접근 가능)가 어드민 정식 기능과 별개로 남아 있습니다. 정식 기능이 프로덕션에 배포된 만큼 폐기 또는 어드민 인증 적용 여부를 정리해야 합니다.
14. **배경 제너레이터 운영 검증** — 어드민 화면이 구글 로그인 필수라 배포 후 육안 검증이 생략됐습니다. 세계관 저장 → 3단 일괄 적용 → 컬렉션·배지 상세 반영까지의 흐름을 어드민 계정으로 한 번 확인하는 것이 권장됩니다.
15. **MODULAR C그룹 컴포넌트 재설계** — TabBar·BottomSheet·SlidingTabs·Toast·Button 5종은 서비스 구현이 이미 독자적으로 진화해(라우팅 결합, context 큐 방식, 자체 variant 체계) 단순 교체가 불가능합니다. 전환하려면 DS 컴포넌트 자체를 재설계하는 별도 설계 티켓이 필요하며, Skeleton도 같은 이유(서비스의 cross-fade reveal이 DS의 단일 shimmer보다 정교함)로 보류 상태입니다.
16. **디자인 시스템 드리프트 자동 감지** — 토큰이 연결됐어도 "DS에서 삭제된 토큰을 서비스가 계속 참조"하는 사고(`--radius-md`)는 사람이 발견해야 했습니다. pre-commit 훅에서 삭제된 토큰 참조를 차단하고, 손으로 유지되는 `_ds_manifest.json`을 소스 스캔 자동 생성으로 교체하는 안이 설계까지 나와 있으나 미구현입니다.
17. **CI 파이프라인 부재** — GitHub Actions 등 CI가 전혀 없어 lint/test가 강제되지 않고, 로컬 훅과 빌드가 유일한 게이트입니다. 디자인 시스템·서비스 동기화의 최종 형태를 생각하면 필요합니다.
