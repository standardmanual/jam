---
id: 20260815_003
category: UI
status: CLOSED
created: 2026-08-15
closed: 2026-08-15
---

# [UI] 미션 카드 썸네일 이미지 + DB 마이그레이션

## 배경 / 문제 정의

현재 미션 목록·상세 화면에 썸네일 이미지가 없다. Figma 와이어프레임에서는 미션마다 대표 이미지가 카드 상단에 표시된다. 현재 DB `missions` 테이블에 `image_url` 컬럼이 없어 구조적으로 불가능한 상태.

**발견 경위:** 2026-08-15 Figma vs 스테이징 분석 중 Jordan 항목으로 식별.

## 상세 요구사항

### 서비스/코드베이스 관점
- `missions` 테이블에 `image_url TEXT` 컬럼 추가 (DB 마이그레이션)
- 미션 썸네일 에셋 업로드 파이프라인 구축 (Supabase Storage 또는 외부 CDN)
- `MissionsListClient.tsx` 의 `MissionRow` 타입에 `image_url?: string` 추가
- 미션 목록·상세 컴포넌트에 썸네일 이미지 렌더링 추가
- 어드민 패널에서 미션별 이미지 업로드 기능 추가 필요

### UI/UX 관점
- 이미지 없는 미션의 fallback 처리 (플레이스홀더 또는 이모지/아이콘)
- 미션 목록 카드: 이미지 비율, 그리드 or 리스트 레이아웃 확정 (리뉴얼 시 결정)
- Next.js `<Image>` 컴포넌트 사용

### 컨텐츠 관점
- 기존 미션 데이터에 이미지 에셋 소급 필요 (미션별 대표 이미지 제작 또는 선정)

## 구현 계획
> 디자인 리뉴얼 시 DB 스키마에 반영하기로 확정 (2026-08-15 의사결정). 리뉴얼 단계에서 일괄 처리.

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
- DB 마이그레이션 파일 작성 (실행은 오케스트레이터가 처리)
- MissionRow 타입에 `image_url: string | null` 필드 추가
- 어드민 미션 폼에 `ImageUploadField` 연결 (`folder: mission-images`)
- 미션 목록 카드 상단에 16:9 썸네일 full-bleed 렌더링 (image_url 있을 때만)
- 미션 상세 화면 제목 위에 16:9 썸네일 렌더링 (image_url 있을 때만)
- fallback: image_url null인 미션은 `--color-bg-tint` 그레이 플레이스홀더 표시 (이미지 영역 항상 렌더링)
- Supabase Storage `mission-images` 버킷 생성 완료 (public, 5MB 제한, jpeg/png/webp/gif 허용)
- Supabase Storage 도메인(`ceehnkzdbecxwzxrhhns.supabase.co`)은 이미 next.config에 등록되어 있어 수정 불필요

### 변경된 파일
```
jam-web/supabase/migrations/085_missions_image_url.sql  (신규)
jam-web/src/types/database.ts
jam-web/src/app/admin/missions/MissionList.tsx
jam-web/src/app/(main)/missions/MissionsListClient.tsx
jam-web/src/app/(main)/missions/[id]/MissionDetailClient.tsx
```

### 테스트 결과
- [ ] DB 마이그레이션 적용 확인 (오케스트레이터가 실행 후 확인)
- [ ] 썸네일 이미지 렌더링 확인
- [ ] fallback 처리 확인 (image_url 없는 미션)

### UX Writing 검증
- 해당 없음 (신규 텍스트 없음)

### 배포 정보
- 배포일: 2026-08-15
- 환경: staging
- 커밋: 72e74f9 (구현), 3f610fb (fallback 수정) → staging 머지 2c0a2fd
- SQL 마이그레이션: 2026-08-15 Supabase MCP로 직접 실행 완료

### 주요 의사결정 / 핵심 메모
- 어드민 API(`POST /api/admin/missions`, `PATCH /api/admin/missions/[id]`)는 body를 그대로 전달하는 방식이라 image_url 별도 처리 불필요 — DB 마이그레이션만 적용되면 자동 동작
- 목록 카드 이미지: Card 내부 full-bleed(`-mx-6 -mt-6 w-[calc(100%+3rem)]`) — `--spacing-24`=24px=Tailwind 6 기준
- 상세 화면 이미지: Card 밖 별도 div로 독립 배치, `priority` 속성으로 LCP 최적화

### 잔여 이슈
- 기존 미션 데이터의 소급 이미지 에셋은 별도 컨텐츠 작업 필요 (Content 카테고리 별도 티켓 분리 권장)
- `mission-images` 버킷 RLS 쓰기 정책 확인 권장 (서비스 롤 키 경유 업로드이므로 당장 문제는 없으나 추후 보안 강화 시 검토)
