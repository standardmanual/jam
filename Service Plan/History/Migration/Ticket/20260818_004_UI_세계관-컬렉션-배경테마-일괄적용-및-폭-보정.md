---
id: 20260818_004
category: UI
status: OPEN
created: 2026-08-18
---

# [UI] 세계관·컬렉션 배경테마 일괄 적용 + 배경 레이어 폭 보정

## 배경 / 문제 정의
두 가지를 함께 처리한다.

1. **[20260818_001]** 원 요청의 2번 항목("어드민의 세계관 또는 컬렉션 PRD에서 이 기능을 하위
   컨텐츠에 일괄 적용할 수 있도록 선택값을 노출") — Phase 2로 미뤄뒀던 부분을 이번에 개발한다.
2. **버그**: [20260818_003]에서 만든 배지 상세 고정 배경 레이어(`position: fixed; inset: 0`)가
   브라우저 전체 폭을 덮는다. 앱은 `(main)/layout.tsx`에서 `max-w-[430px] mx-auto`로 폭이
   제한되는데, 데스크톱처럼 뷰포트가 430px보다 넓은 환경에서는 배경색이 앱 컬럼 바깥
   (원래 검은색이어야 하는 여백)까지 덮어버린다. **배경색은 TopNav와 같은 폭(앱 서비스 폭,
   최대 430px)에만 적용하고, 그 바깥의 기존 검은 배경은 그대로 유지해야 한다.**

## 확정된 결정 사항
- 세계관(factions)·컬렉션(item_books) 어드민에도 배지와 동일한 배경색(피커+hex+자동추출 없음,
  수동 입력만)·쉐이더(임시 placeholder) 필드를 추가
- 하위 배지 반영 방식은 **일괄 적용 버튼(1회성 복사)** — 자동 fallback 아님. 버튼을 누르는
  순간 값을 하위 배지들의 `background_color`/`background_shader_id`에 실제로 복사해 넣는다.
  이후 개별 배지에서 자유롭게 수정 가능하지만, 세계관/컬렉션 값을 나중에 바꿔도 자동 반영은
  안 됨(다시 눌러야 함) — 명확히 안내 문구로 고지
- 배경 레이어는 **TopNav와 같은 폭(앱 컬럼, 최대 430px, 중앙 정렬)**으로 제한. 그 바깥 여백은
  기존 검은 배경 유지

## 상세 요구사항

### 서비스/코드베이스 관점

1. **배경 레이어 폭 보정** (버그 수정)
   - 대상: `jam-web/src/app/(main)/badges/[id]/page.tsx`의 `badgeBackgroundLayer`
   - 현재: `style={{ position: 'fixed', inset: 0, zIndex: 0, pointerEvents: 'none', ... }}`
   - 변경: `inset: 0` 대신 `top: 0, bottom: 0, left: '50%', transform: 'translateX(-50%)',
     width: '100%', maxWidth: '430px'`로 — `(main)/layout.tsx`의 앱 컬럼(`max-w-[430px]
     mx-auto`)과 동일한 폭·중앙정렬을 유지하면서 fixed 특성(스크롤에 안 끌려감)은 유지
   - 검증: 뷰포트를 430px보다 넓게(예: 1280px 데스크톱) 리사이즈했을 때 배경색이 앱 컬럼
     밖으로 새어나가지 않는지 Playwright로 확인

2. **DB 스키마**
   - `factions` 테이블에 `background_color` (text, nullable), `background_shader_id`
     (text, nullable) 컬럼 추가
   - `item_books` 테이블에 동일하게 `background_color`, `background_shader_id` 컬럼 추가
   - 마이그레이션 파일 작성: `jam-web/supabase/migrations/087_add_faction_itembook_background_columns.sql`
   - **SQL 파일 작성까지만 — 실행은 오케스트레이터가 사용자 승인 후 별도 처리**

3. **어드민 세계관 폼** (`jam-web/src/app/admin/factions/FactionForm.tsx`)
   - 배경색 필드 추가: 색상 피커 + hex 입력 + 스와치 + 지우기 (자동 평균컬러 추출 없음 —
     세계관은 특정 이미지 1장에 종속되지 않으므로 수동 입력만)
   - 쉐이더 임시 드롭다운 추가: [20260818_003]의 `BADGE_BACKGROUND_SHADER_OPTIONS`를 그대로 재사용
   - **"하위 배지에 일괄 적용" 버튼** 추가 (세계관 수정 화면, 신규 등록 시에는 비활성 —
     저장된 세계관에만 하위 배지가 존재)
     - 클릭 시 확인 다이얼로그: "이 세계관에 속한 배지 N개의 배경색이 지금 이 값으로 즉시
       덮어써집니다. 개별 배지에서 따로 지정한 색상도 모두 이 값으로 바뀝니다. 계속할까요?"
       (N은 사전 조회한 실제 하위 배지 수)
     - 확인 시 `badges.faction_id = 이 세계관 id`인 모든 배지의 `background_color`/
       `background_shader_id`를 세계관 폼의 현재 값으로 UPDATE
     - 완료 후 "N개 배지에 적용했어요" 토스트/안내 표시
     - 안내 문구: "이후 세계관 색상을 바꿔도 이미 적용된 배지에는 자동 반영되지 않아요.
       다시 이 버튼을 눌러야 해요."

4. **어드민 컬렉션(아이템북) 폼** (`jam-web/src/app/admin/itembooks/ItemBookForm.tsx`)
   - 세계관 폼과 동일한 배경색/쉐이더 필드 + "하위 배지에 일괄 적용" 버튼
   - 하위 배지 기준: `badges.item_book_id = 이 아이템북 id`

5. **API**
   - `POST /api/admin/factions/[id]/apply-background` — body 불필요(폼이 이미 저장된 상태의
     현재 값을 사용하거나, 저장 직후 값을 함께 전달), 응답에 적용된 배지 수 포함
   - `POST /api/admin/itembooks/[id]/apply-background` — 동일 패턴
   - 두 라우트 모두 적용 전 최신 폼 값이 먼저 저장돼 있어야 함 (저장 안 된 상태에서 일괄
     적용 시도 시 막거나, 저장까지 함께 처리 — 구현 시 UX 흐름 판단은 jam-developer에게 위임)

6. **재사용**
   - 배경색 피커+hex+스와치+지우기 UI가 이제 3곳(BadgeForm, FactionForm, ItemBookForm)에
     반복되므로, 공통 컴포넌트로 추출 권장 (예: `src/components/admin/BackgroundColorField.tsx`)
     — 어드민 전용이라 MODULAR 대상 아님, 서비스(어드민) 내부 재사용 컴포넌트로 추출

### UI/UX 관점
- 일괄 적용은 **파괴적 작업**(기존 개별 배지 커스터마이징을 덮어씀)이므로 확인 다이얼로그 필수,
  실행 취소 기능은 없음(개별 배지에서 다시 수정 가능하다는 점만 안내)
- 신규 노출 텍스트(확인 다이얼로그, 완료 토스트, 안내 문구)는 UX Writing 가이드 준수
  (해요체, [현상]→[원인]→[해결책] 불필요한 경우 생략 가능하나 파괴적 작업이므로 결과 명확히 고지)

### DS 재사용 판정 (오케스트레이터, 1.5단계)
- 전부 admin 정책상 MODULAR 탐색 대상 아님
- 배경색 필드 공통 컴포넌트 추출은 서비스(어드민) 내부 재사용이지 MODULAR 승격 대상 아님

## 구현 계획
1. 배지 상세 고정 배경 레이어 폭 보정 (버그 수정, 최우선)
2. `087_add_faction_itembook_background_columns.sql` 마이그레이션 작성
3. 배경색 피커 공통 컴포넌트 추출 (`BackgroundColorField.tsx`)
4. FactionForm·ItemBookForm에 배경색/쉐이더 필드 + 일괄 적용 버튼 추가
5. `/api/admin/factions/[id]/apply-background`, `/api/admin/itembooks/[id]/apply-background` 구현
6. Playwright로 (a) 배경 레이어 폭 보정 확인 (b) 일괄 적용 동작 및 확인 다이얼로그 (c) 기존
   배지 개별 편집 회귀 없는지 검증

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
1. **버그 수정(최우선)**: `badges/[id]/page.tsx`의 `badgeBackgroundLayer`를 `inset:0` →
   `top/bottom:0, left:50%, transform:translateX(-50%), width:100%, maxWidth:430px`로 변경.
   `(main)/layout.tsx`의 `max-w-[430px] mx-auto` 앱 컬럼과 동일한 폭·중앙정렬로 제한하면서
   `position: fixed`(스크롤 미추종) 특성은 유지. 정적 HTML 리프로덕션 + Playwright로 1280px
   뷰포트에서 배경 레이어가 정확히 앱 컬럼 폭(430px, x:425~855)에만 그려지고 그 바깥은
   검게 유지됨을 확인, 375px(좁은 뷰포트)에서는 기존과 동일하게 풀폭으로 채워짐을 확인.
2. **DB 마이그레이션 작성**(미실행): `087_add_faction_itembook_background_columns.sql` —
   `factions`, `item_books`에 `background_color`, `background_shader_id` 컬럼 추가.
3. **공통 컴포넌트 추출**: `BackgroundColorField.tsx` — BadgeForm에 있던 색상 피커+hex+스와치+
   지우기 UI를 그대로 컴포넌트화. `HEX_COLOR_PATTERN`도 함께 export해 3곳(BadgeForm,
   FactionForm, ItemBookForm)에서 검증 로직을 공유.
4. **FactionForm·ItemBookForm**: 배경색 필드(자동 추출 없음, 수동 입력만) + 쉐이더 임시
   드롭다운(`BADGE_BACKGROUND_SHADER_OPTIONS` 재사용) + "하위 배지에 일괄 적용" 버튼 추가.
   버튼은 신규 등록 화면(`!isEdit`)에서 비활성. 클릭 시 (1) 폼의 현재 값을 먼저 저장 →
   (2) `/api/admin/badges` 목록을 조회해 실제 하위 배지 수(소프트 삭제 제외)를 계산 →
   (3) 확인 다이얼로그 노출(개수 포함) → (4) 확인 시 apply-background API 호출 → 적용된
   배지 수 안내 문구 표시. "저장 안 된 상태" 문제는 **저장까지 함께 처리**하는 방식으로
   해결(막지 않고, 버튼 클릭 자체가 저장 트리거를 겸함).
5. **API**: `POST /api/admin/factions/[id]/apply-background`,
   `POST /api/admin/itembooks/[id]/apply-background` — body 없이 DB에 이미 저장된
   faction/item_book의 배경값을 읽어 해당 `faction_id`/`item_book_id`를 가진(소프트 삭제
   제외) 모든 배지에 UPDATE, 응답으로 `appliedCount` 반환.
6. 기존 factions/itembooks POST·PUT 라우트에 `background_color`/`background_shader_id`
   필드 저장 로직 추가. `types/database.ts`의 `FactionRow`/`ItemBookRow`에 두 컬럼 타입 추가.

### 변경된 파일
```
jam-web/src/app/(main)/badges/[id]/page.tsx                              (버그 수정)
jam-web/supabase/migrations/087_add_faction_itembook_background_columns.sql (신규, 미실행)
jam-web/src/types/database.ts                                            (FactionRow/ItemBookRow 컬럼 추가)
jam-web/src/components/admin/BackgroundColorField.tsx                    (신규, 공통 컴포넌트)
jam-web/src/app/admin/badges/BadgeForm.tsx                               (공통 컴포넌트로 교체)
jam-web/src/app/admin/factions/FactionForm.tsx                           (배경 테마 + 일괄 적용)
jam-web/src/app/admin/itembooks/ItemBookForm.tsx                         (배경 테마 + 일괄 적용)
jam-web/src/app/api/admin/factions/route.ts                              (POST에 배경 필드 추가)
jam-web/src/app/api/admin/factions/[id]/route.ts                         (PUT에 배경 필드 추가)
jam-web/src/app/api/admin/factions/[id]/apply-background/route.ts        (신규)
jam-web/src/app/api/admin/itembooks/route.ts                             (POST에 배경 필드 추가)
jam-web/src/app/api/admin/itembooks/[id]/route.ts                        (PUT에 배경 필드 추가)
jam-web/src/app/api/admin/itembooks/[id]/apply-background/route.ts       (신규)
```

### 테스트 결과
- [x] `npx tsc --noEmit` — 변경 파일 범위에 타입 에러 없음(기존 `__tests__` 무관 에러 제외)
- [x] `npx eslint` — 변경 파일 범위에 신규 lint 에러/경고 없음(BadgeForm.tsx의 기존
      `set-state-in-effect` 에러·`badges/[id]/page.tsx`의 기존 `Link` 미사용 경고는 staging
      기준 이 티켓 이전부터 있던 것으로 확인, 변경 범위 밖)
- [x] 배경 레이어 폭 보정: `(main)/layout.tsx`의 `max-w-[430px] mx-auto` 구조를 정적 HTML로
      재현해 Playwright로 1280px 뷰포트에서 배경 레이어 바운딩 박스가 정확히 x:425~855
      (폭 430px, 중앙정렬)에만 그려지고 그 바깥은 검게 유지됨을 확인. 375px에서는 풀폭으로
      기존과 동일하게 렌더링됨을 확인.
- [ ] 실제 로그인 세션에서의 배지 상세화면 육안 확인 — 로그인(Google) 필요로 이번 세션에서는
      생략(정적 재현 검증으로 대체)
- [ ] 어드민 FactionForm/ItemBookForm 실제 브라우저 조작(일괄 적용 버튼 클릭, 확인
      다이얼로그, DB 반영) — 마이그레이션 미실행 상태라 실제 DB 검증은 불가, 코드 리뷰로 대체

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

- [x] 용어 일관성: "배지", "배경색" 등 기존 배지 화면 용어 그대로 사용, 신조어 없음
- [x] 톤앤매너: 파괴적 작업(일괄 적용)이므로 확인 다이얼로그는 단호·명확 톤 유지, 완료
      안내는 담담한 안내 톤
- [x] 에러 메시지: 배경색 형식 오류는 기존 BadgeForm 문구를 그대로 재사용(이미 검증된 문구)
- [x] 문장 규칙: 해요체, 확인 다이얼로그·안내 문구는 티켓에 명시된 원문을 그대로 사용
- [x] 표기 규칙: 적용 개수는 "N개 배지에 적용했어요" 형태로 직관적 표기

### 배포 정보
- 배포일:
- 환경:
- 커밋:

### 주요 의사결정 / 핵심 메모
- "저장 안 된 상태에서 일괄 적용" 이슈(#6)는 **저장까지 함께 처리**로 결정. 일괄 적용 버튼
  클릭 시 항상 폼의 현재 값을 먼저 PUT으로 저장한 뒤 하위 배지 수를 조회·확인 다이얼로그를
  띄운다. 이렇게 하면 "화면에 보이는 값"과 "실제 적용될 값"이 항상 일치해 사용자 혼란을
  방지할 수 있다(값을 바꾸고 저장을 깜빡한 채 일괄 적용해 예전 값이 적용되는 사고 방지).
- 하위 배지 수 사전 조회는 별도 count 전용 API를 새로 만들지 않고, BadgeForm의
  드랍가중치 미리보기에서 이미 쓰던 `GET /api/admin/badges`(최대 5000건) 응답을
  클라이언트에서 필터링하는 기존 패턴을 재사용했다.
- `apply-background` API는 body를 받지 않고 DB에 이미 저장된 faction/item_book 행의 값을
  그대로 읽어 UPDATE한다. 폼이 먼저 저장을 마쳤다는 전제(위 결정)가 있어야 성립하므로,
  두 로직은 항상 세트로 호출된다.
- 소프트 삭제된 배지(`deleted_at IS NOT NULL`)는 일괄 적용 대상·개수 미리보기 모두에서
  제외했다(BadgeForm의 sibling 가중치 계산 로직과 동일 기준).

### 잔여 이슈
- 마이그레이션 `087_add_faction_itembook_background_columns.sql`은 작성만 했고 실행하지
  않았다. 실행 후에는 factions/item_books 테이블에 실제로 컬럼이 생겼는지, 기존 행이
  NULL로 채워지는지 확인이 필요하다.
- 어드민 화면(FactionForm/ItemBookForm의 배경 테마 섹션, 확인 다이얼로그, 일괄 적용
  버튼)의 실제 브라우저 동작은 로그인 세션 + 마이그레이션 실행 후에만 완전히 검증 가능하다.
- 세계관/컬렉션 배경값 자체는 세계관/컬렉션 화면 어디에도 렌더링되지 않는다(요구사항대로
  "하위 배지에만" 적용되는 원본 값 저장소 역할). 추후 "이 세계관 배경 미리보기"류 요구가
  생기면 별도 티켓으로 다뤄야 한다.

### 개선 리뷰 수정 (2026-08-18, 2차)
**지적 사항**: FactionForm.tsx/ItemBookForm.tsx의 "하위 배지에 일괄 적용" 버튼이
`background_color`/`background_shader_id`가 둘 다 비어있는 상태에서도 눌려서, 하위 배지들의
기존 커스터마이징을 전부 빈 값으로 덮어써버릴 수 있는 문제.

**수정 내용**: 양쪽 폼에 `hasBackgroundValue = backgroundColor.trim() !== '' || backgroundShaderId !== ''`
파생값을 추가.
- `handleBulkApplyClick`의 가드 조건에 `!hasBackgroundValue` 추가(신규 등록 화면 가드와 동일한
  방식으로 조기 반환)
- "하위 배지에 일괄 적용" 버튼의 `disabled`에 `!hasBackgroundValue` 추가
- 버튼 하단 안내 문구를 3단 분기로 변경: (1) 미등록 상태 → 기존 "먼저 등록해야..." 문구,
  (2) 값 없음 → "배경색 또는 쉐이더를 먼저 지정해야 일괄 적용할 수 있어요.", (3) 정상 →
  기존 저장/복사 안내 문구. 값이 하나라도 있으면 기존 동작·문구 그대로 유지.
- 다른 로직(저장, 확인 다이얼로그, API 호출)은 변경하지 않음.

**변경 파일**:
```
jam-web/src/app/admin/factions/FactionForm.tsx
jam-web/src/app/admin/itembooks/ItemBookForm.tsx
```

**테스트 결과**:
- [x] `npx tsc --noEmit` — 두 파일 관련 타입 에러 없음(기존 `__tests__` 무관 에러만 존재,
      이번 변경과 무관)
- [x] `npx eslint src/app/admin/factions/FactionForm.tsx src/app/admin/itembooks/ItemBookForm.tsx`
      — 경고/에러 없음
- [ ] 실제 브라우저 조작(빈 값 상태에서 버튼 비활성 확인, 안내 문구 노출) — 로그인 세션 +
      마이그레이션 실행 후에만 검증 가능, 이번 세션에서는 코드 리뷰로 대체

**UX Writing 검증**: 신규 안내 문구 "배경색 또는 쉐이더를 먼저 지정해야 일괄 적용할 수
있어요."는 해요체, [현상]→[해결책] 구조(파괴적 작업을 막는 이유가 자명해 [원인] 단계 생략),
기존 화면 용어("배경색", "쉐이더", "일괄 적용") 재사용.
