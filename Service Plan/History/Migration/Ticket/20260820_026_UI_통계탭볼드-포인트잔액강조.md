---
id: 20260820_026
category: UI
status: CLOSED
created: 2026-08-20
closed: 2026-08-20
---

# [UI] 프로필 통계탭 숫자 볼드 + 포인트 잔액 강조컬러/볼드

## 배경 / 문제 정의
사용자 요청 2건:
1. 프로필 페이지 통계탭(배지·컬렉션·팔로워·팔로잉 숫자) 볼드 처리
2. `/points` 포인트 내역 페이지의 잔액 숫자를 메인 강조컬러 + 볼드로 처리

## 상세 요구사항
- [ProfileClient.tsx](../../../../jam-web/src/app/(main)/profile/ProfileClient.tsx)의
  `statTabs` 숫자 텍스트에 `font-bold` 추가
- [points/page.tsx](../../../../jam-web/src/app/(main)/points/page.tsx)의 잔액
  텍스트에 `font-bold` + `text-[color:var(--color-primary)]` 추가(프로필 헤더 포인트
  버튼과 동일한 스타일 재사용)

## 구현 계획
1. `ProfileClient.tsx` L414 클래스에 `font-bold` 추가
2. `points/page.tsx` L86 클래스에 `font-bold text-[color:var(--color-primary)]` 추가
   (자릿수 애니메이션 span은 부모 `<p>`에서 색상/굵기를 상속하므로 별도 수정 불필요)
3. tsc/eslint/build 검증 후 소스코드 → 스테이징 → 프로덕션까지 반영

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
- `ProfileClient.tsx`의 통계탭 숫자 span에 `font-bold` 추가
- `points/page.tsx`의 잔액 표시 `<p>`에 `font-bold text-[color:var(--color-primary)]` 추가.
  숫자 자체는 `useDigitPopIn` 훅이 명령형으로 생성하는 `.t-digit-group`/`.t-digit` span인데,
  해당 CSS 클래스에 color/font-weight 선언이 없어 부모 `<p>`에서 정상 상속됨을 CSS 확인.

### 변경된 파일
```
jam-web/src/app/(main)/profile/ProfileClient.tsx
jam-web/src/app/(main)/points/page.tsx
```

### 테스트 결과
- [x] `npx tsc --noEmit` — 변경분 관련 오류 없음
- [x] `npx eslint` (변경 2개 파일) — ProfileClient.tsx의 기존 무관 오류(576번째 줄 strava
      `<a>` 태그) 외 신규 오류 없음
- [x] `npx next build` — 정상 통과
- [ ] 실기기 시각 확인 — staging 배포 후 확인 권장(경미 수정, 오케스트레이터 직접 처리)

### UX Writing 검증
- [x] 해당 없음 (스타일 조정, 카피 변경 없음)

### 배포 정보
- 배포일: 2026-08-20
- 환경: staging (main 승격 진행)
- 커밋: c826c1c17ab90dbba998bf0af588378ec490324a (staging 머지 커밋)

### 주요 의사결정 / 핵심 메모
- 포인트 잔액 강조컬러는 프로필 헤더 포인트 버튼(`--color-primary`)과 동일 토큰을 재사용해
  두 화면 간 시각적 일관성을 유지함.

### 잔여 이슈
-
