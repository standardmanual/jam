---
id: 20260821_004
category: UI
status: CLOSED
created: 2026-08-21
closed: 2026-08-21
---

# [UI] 배지 공유 — 실제 이미지 생성 + 스트라바 연동 + OS 공유시트

## 배경 / 문제 정의

티켓 `20260821_003`에서 UI 셸(공유 아이콘 버튼 + 전체화면 바텀시트 + 더미 미리보기)까지
구현했다. 이번 티켓은 그 안을 실제 기능으로 채운다: 피그마 템플릿대로 투명 PNG를 실제 생성하고,
스트라바 데이터(페이스·시간)를 연동하고, OS 공유시트(또는 다운로드)로 내보낸다.

PRD: `Service Plan/Specs/PRD/BadgeShare/PRD.md` 참조 — 이번 티켓의 상세 스펙(템플릿 좌표,
데이터 소스, 예외 처리)은 전부 이 PRD를 단일 진실 원천으로 따른다.

## 상세 요구사항

### 서비스/코드베이스 관점

1. **이미지 생성**: `BadgeShareButton.tsx`(티켓 003에서 신설)의 더미 미리보기를 실제 생성
   파이프라인으로 교체.
   - DOM에 PRD 6장의 템플릿 레이아웃(1080×1920, 배지 483×483 contain, 텍스트 3세트 또는
     로고만)을 오프스크린(화면 밖, `position: absolute; left: -9999px` 등)으로 렌더링한 뒤
     `jam-web/src/app/admin/badges/bakePreviewToBlob.ts` 패턴을 확장해 `canvas.toBlob()`으로
     PNG Blob 생성.
   - 폰트: `Inter Bold`, 폴백 `Arial Bold`. 웹폰트 로딩 타이밍 이슈(canvas 렌더링 시 폰트 미로드로
     기본 폰트가 찍히는 문제)에 주의 — `document.fonts.ready` 대기 등 필요.
2. **스트라바 데이터 연동**:
   - 액티비티/POI 배지: `triggered_by_strava_id`로 스트라바 액티비티를 재조회해 페이스·시간을
     계산. 클라이언트가 스트라바 access token을 직접 다루지 않으므로 신규 API 라우트
     (`jam-web/src/app/api/badges/[id]/share-data` 등, 라우트명은 구현 시 기존 API 네이밍
     관례에 맞춰 결정)에서 `lib/strava/api.ts`의 `getActivityById`를 호출해 필요한 값만
     클라이언트로 반환한다.
   - `triggered_by_distance_km`는 이미 DB에 있으므로 재조회 불필요, 그대로 사용.
   - **신규 API 라우트가 추가되므로 PRD 갱신 트리거에 해당** — 이미 위 PRD 문서에 반영해뒀으니
     구현 중 API 계약(요청/응답 스키마)이 PRD와 달라지면 PRD도 함께 갱신할 것.
3. **아이템 배지**: 스트라바 조회 없이 배지 이미지 + 로고만으로 즉시 생성 가능.
4. **예외 처리 (2026-08-21 사용자 확정 — 재작업 반영)**:
   - **공유는 본인이 보유한 배지에서만 가능하다.** 타인의 배지 상세 페이지(`isOwnBadge=false`,
     기존 `TopNav backHref` 분기와 동일한 조건)에서는 공유 버튼 자체를 **노출하지 않는다**
     (비활성화가 아니라 미노출).
   - 본인 배지 상세 페이지에서, 아래 두 케이스는 공유 버튼을 **사전에 비활성화**한다
     (클릭 전에 판별 가능해야 함 — API 호출 후에야 아는 방식 금지):
     a. **미획득 배지**(`hasEarned=false`)
     b. **스트라바 미연동/토큰 만료**(activity/poi 타입만 해당. item 타입은 스트라바 데이터가
        필요 없으므로 이 조건의 영향을 받지 않는다) — `page.tsx`(서버 컴포넌트)에서
        `strava_connections` 테이블에 유효한 연동 레코드가 있는지 미리 조회해 prop으로 내려준다.
        (참고: `lib/strava/sync.ts`가 토큰 조회·갱신에 쓰는 것과 같은 테이블. 정확한 "토큰이
        아직 유효한가"까지는 사전 판별이 어려울 수 있으니, 최소 "연동 레코드 존재 여부"로
        1차 판별하고, 갱신 실패 등 세부 만료는 필요시 판별 로직을 확장할 것.)
   - **비활성화된 버튼을 클릭하면** 작은 팝오버(툴팁) 형태로 안내 메시지를 보여준다.
     케이스별로 다른 메시지: 미획득 케이스는 기존 `notEarnedTitle`/`notEarnedBody` 계열 문구,
     스트라바 미연동 케이스는 `shareErrorStravaDisconnected*` 계열 문구를 재사용한다(신규 문구
     추가는 최소화).
   - 위 세 케이스(비공개·미획득·미연동) 모두 사전 차단되므로, **바텀시트는 열렸다면 항상
     이미지 생성이 가능한 상태에서 열린다.** 시트 내부의 "미획득 안내" 상태 분기는 이제
     불필요 — 제거한다. 단, 시트가 열린 뒤 실제 API 호출이 실패하는 런타임 에러(네트워크 오류·
     스트라바 레이트리밋·5xx 등, 사전 판별 대상이 아닌 것)는 여전히 시트 내부에서 에러 상태로
     처리한다(기존 `shareErrorFetchFailedTitle`/`shareErrorUnknownTitle` 유지).
5. **공유/다운로드**:
   - `navigator.share` 지원 시 생성된 PNG Blob을 `navigator.canShare`로 확인 후 공유시트 호출.
   - 미지원(주로 데스크톱) 시 PNG 다운로드 링크(`<a download>` 또는 `URL.createObjectURL` +
     프로그래매틱 클릭)로 대체.

### UI/UX 관점

- 미리보기 바텀시트(티켓 003 결과물)에 실제 생성 이미지가 뜨는 동안의 로딩 상태 필요
  (스트라바 재조회 + canvas 렌더링에 시간이 걸릴 수 있음) — `WanderingEyesLoader`(MODULAR)
  재사용 검토.
- 공유/다운로드 액션 버튼을 바텀시트 안에 추가해야 함(티켓 003은 미리보기만 있고 액션 CTA가
  없는 상태였음 — 개발자가 alert로 명시한 잔여 이슈).
- 티켓 003에서 이미 처리: 바텀시트 상단 타이틀 텍스트 제거됨(경미 수정 반영 완료) — 이번
  티켓에서 새로 추가하는 로딩/액션 UI도 타이틀 없이 구성할 것.

## MODULAR 재사용 판정 (오케스트레이터 사전 확인)

- 로딩 표시: `WanderingEyesLoader`(이미 MODULAR 등록·서비스 연결 완료) 재사용, 신규 컴포넌트 불필요.
- 공유/다운로드 액션 버튼: `Button`(MODULAR) 재사용 검토. 신규 UI 필요 없을 가능성 높음 —
  구현 시 `/jam-design`으로 한 번 더 확인.
- 이번 티켓은 새 MODULAR 컴포넌트 신설 소지가 낮아 `ds`가 아닌 `ui` 유형으로 분류함.

## 구현 계획

1. 신규 API 라우트: 배지 ID로 스트라바 페이스/시간 조회 (토큰 만료 시 명시적 에러 코드 반환)
2. `BadgeShareButton.tsx`: 오프스크린 템플릿 렌더링 컴포넌트 신설 → `bakePreviewToBlob` 확장
   유틸로 PNG Blob 생성
3. 로딩 상태 연결 (`WanderingEyesLoader`)
4. 공유/다운로드 액션 버튼 추가, `navigator.share`/다운로드 분기
5. 스트라바 토큰 만료·API 실패 케이스 버튼 비활성화 + 안내 문구
6. activity/poi/item 3개 타입 각각 실제 동작 확인 (poi는 `user_poi_badge_earns` 최신 획득
   기준으로 트리거 활동 선택)

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
- 신규 API 라우트 `GET /api/badges/{id}/share-data`: activity/poi 배지의 `triggered_by_strava_id`로
  `lib/strava/api.ts`의 `getActivityById`를 재조회해 페이스(`moving_time`)·시간(`elapsed_time`)을
  계산해 반환. 토큰 만료 시 `lib/strava/sync.ts`와 동일한 패턴(1분 여유 갱신 → 실패 시 연동 해제로
  간주)으로 처리. item 타입은 이 API를 호출하지 않는다(클라이언트에서 바로 생성).
- `buildBadgeShareBlob.ts` 신규: 피그마 템플릿(1080×1920, 배지 이미지 483×483 contain, activity/poi는
  DISTANCE·PACE·TIME 텍스트 3세트 + 로고, item은 이미지+로고만)을 오프스크린 `<canvas>`에 Canvas 2D
  API(`drawImage`/`fillText`)로 직접 합성 — `bakePreviewToBlob.ts`의 `canvasToBlob()`을 export해
  재사용. `document.fonts.ready` 대기로 웹폰트 미로드 이슈 방지.
- `BadgeShareButton.tsx` 전면 교체: 시트를 열면 (미획득 시 API 호출 없이 미획득 안내) → (획득 시
  activity/poi는 share-data API 호출 후, item은 바로) 이미지 생성 → 로딩 중엔 `WanderingEyesLoader`,
  완료 시 미리보기 + 하단 고정 액션 버튼(`Button`, `BottomSheet`의 `footer` 슬롯 재사용). 액션 버튼은
  `navigator.share`(+`canShare`) 지원 시 OS 공유시트, 미지원 시 `<a download>` 다운로드로 분기.
- `page.tsx` 3개 배지 타입 분기 모두에서 `BadgeShareButton`에 `badgeId`/`badgeType`/`hasEarned`/
  `subjectUsername` prop 추가 전달.
- PRD(`BadgeShare/PRD.md`)에 8-1 API 계약 섹션 추가.

### 변경된 파일
```
jam-web/src/app/api/badges/[id]/share-data/route.ts (신규)
jam-web/src/app/(main)/badges/[id]/buildBadgeShareBlob.ts (신규)
jam-web/src/app/(main)/badges/[id]/BadgeShareButton.tsx
jam-web/src/app/(main)/badges/[id]/page.tsx
jam-web/src/app/admin/badges/bakePreviewToBlob.ts (canvasToBlob export 추가)
jam-web/src/lib/i18n/ko.ts (공유 관련 문구 추가)
Service Plan/Specs/PRD/BadgeShare/PRD.md (8-1 API 계약 섹션 추가)
```

### 테스트 결과
- [x] `npx tsc --noEmit` — 변경 파일 관련 타입 오류 없음 확인(기존에 있던 무관 오류 다수는 그대로 남음 — 누락된 테스트 타입 정의, `mp4-muxer`/`@paper-design/shaders-react` 미설치 등 이번 작업과 무관)
- [x] `npx eslint`(변경 파일 대상) — 신규 오류 없음. `BadgeShareButton.tsx`의 `react-hooks/set-state-in-effect` 1건은 기존 `src/components/ui/BottomSheet.tsx`에도 동일하게 존재하는 코드베이스 전반의 논블로킹 경고로 확인(pre-commit이 강제하지 않음)
- [x] 실기기/브라우저 육안 확인: 생략(구글 로그인 필수 화면이라 배포 후 육안 확인 불가 — 항상
  생략하는 정책). 코드 대조·타입 검사·게이트 리뷰 재검증으로 대체.

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

- [x] 용어 일관성: 신규 문구에 고정 용어 외 신조어 도입 없음("Strava 동기화" 용어 재사용)
- [x] 톤앤매너: 스트라바 동기화 오류는 가이드 4장 "전문·직접" 톤 적용, 로딩/미획득 안내는 기존 담담한 안내 톤 유지
- [x] 에러 메시지: `shareErrorStravaDisconnected*`는 [현상: 동기화가 끊김] → [원인/해결책: 다시 동기화 필요] 2단계로 축약(가이드 예시의 CTA 버튼 없이 안내 문구만 두라는 티켓 지시에 따름)
- [x] 문장 규칙: 해요체, 제목/버튼 마침표 없음, 본문 단문은 기존 `notEarnedBody` 등과 동일하게 마침표 생략
- [ ] 표기 규칙: 날짜/시간/금액/기간 — 해당 없음(공유 이미지 내부 텍스트는 PRD가 지정한 고정 포맷이라 이 가이드 대상 외로 판단)

### 배포 정보
- 배포일: 2026-08-21
- 환경: staging (프로덕션 반영은 `/jam-ship`으로 별도 진행 예정, 사용자 명시 승인 필요)
- 커밋: 7fd770d2 (머지 커밋), 8931df68 (재작업: 사전 비활성화·팝오버·본인 배지 전용)

### 주요 의사결정 / 핵심 메모
- **DOM 캡처 대신 Canvas 2D 직접 합성**: 티켓은 "오프스크린 DOM 렌더링 후 `canvas.toBlob()`"을
  지시했지만, 이 프로젝트엔 DOM 래스터라이즈 라이브러리(html2canvas 등)가 없고 `bakePreviewToBlob.ts`의
  기존 구현은 "이미 렌더링된 단일 `<canvas>`/`<img>` 노드를 그대로 캡처"하는 방식이라 배지 이미지 +
  텍스트 3세트 + 로고의 합성에는 맞지 않는다. 대신 `<canvas>` 하나에 `drawImage`/`fillText`로 직접
  그리는 방식을 택하고, 마지막 PNG 변환 단계(`canvasToBlob`)만 기존 유틸을 그대로 재사용해 "패턴 확장"
  취지를 지켰다.
- **PACE는 `moving_time`, TIME은 `elapsed_time` 기준**: PRD가 "moving_time/elapsed_time에서 페이스·
  시간 계산"이라고만 명시하고 어느 필드를 어디에 쓸지 명시하지 않아, 페이스는 정지 시간을 뺀
  `moving_time` 기준(표준적인 러닝 페이스 계산 방식)으로, TIME은 정지 시간을 포함한 `elapsed_time`
  기준(활동 시작~종료까지 전체 소요 시간)으로 계산하도록 결정했다.
- **미획득 배지는 API 호출 없이 바로 미획득 안내**: 티켓의 예외 처리 목록엔 스트라바 토큰
  만료/연동 해제만 명시돼 있었지만, 활동/POI 배지는 획득 전에는 거리·페이스·시간 데이터 자체가
  없어 공유 이미지를 만들 수 없다. `hasEarned=false`면 API를 아예 호출하지 않고 기존 페이지의
  "아직 획득하지 못한 배지예요" 문구를 그대로 재사용해 안내한다(신규 문구 추가 없이 일관성 유지).
- **템플릿 좌표는 재설계**: 피그마 노드(`1:55`/`1:60`)의 정확한 좌표를 조회할 수단이 없어(이 서브
  에이전트에는 Figma MCP가 연결돼 있지 않음), PRD가 명시한 캔버스 크기(1080×1920)·배지 영역
  (483×483 contain)·로고 크기(174×72)·라벨/값 폰트 크기(~20px/~51px)는 그대로 따르되 상하 여백은
  균형 잡힌 값으로 새로 설계했다. 실제 피그마 시안과 픽셀 단위로 다를 수 있어 시각 QA가 필요하다.
- **Web Share 지원 여부 판정 기준**: `navigator.canShare({ files: [file] })`는 실제 파일이 있어야
  정확히 판정되므로, 액션 버튼 라벨(공유하기/저장하기)은 시트가 열린 시점에 `navigator.share`/
  `navigator.canShare` 함수 존재 여부만으로 미리 결정하고, 실제 공유 가능 여부(`canShare(files)`)는
  버튼 클릭 시점에 다시 확인한다.

### 잔여 이슈
- 템플릿 상하 여백·폰트 크기가 피그마 시안과 픽셀 단위로 정확히 일치하는지 실제 Figma 파일과
  대조하는 시각 QA가 필요하다(위 "주요 의사결정" 참고).
- `no_strava_trigger`(획득했으나 트리거 활동 스냅샷이 없는 레거시/어드민 발급 케이스)는 실제
  운영 데이터에 이런 배지가 얼마나 있는지 확인되지 않았다 — 사용자가 이 안내를 실제로 보게 될
  빈도는 미검증.

---
### 재작업 기록 (게이트 리뷰 WARN 대응, `20260821_004` 재작업)

게이트 리뷰 WARN: "토큰 만료 시 버튼 비활성화" 스펙 문언과 달리, 버튼은 항상 활성이고 시트를
연 뒤 API 응답을 받고서야 미획득/미연동 상태를 보여줬다. 사용자가 예외 처리 흐름을 재확정해
아래처럼 다시 구현했다.

**변경 내용**
- **본인 배지 전용**: `page.tsx`의 세 배지 타입 분기 모두에서 `rightSlot`을
  `isOwnBadge && <BadgeShareButton .../>`로 감싸 타인 배지에서는 공유 버튼 자체를 렌더링하지
  않는다(미노출, 비활성화 아님).
- **사전 비활성화**: `BadgeShareButton`에 `disabledReason` 계산 로직 추가 —
  `!hasEarned`면 `'not-earned'`, `badgeType !== 'item' && !stravaConnected`면
  `'strava-disconnected'`. 두 경우 모두 시트를 열기 전(클릭 시점)에 판별된다.
  `stravaConnected`는 신규 prop으로, `page.tsx`가 `strava_connections` 테이블에서 배지
  소유자(`subjectId`) 레코드 존재 여부를 미리 조회해 내려준다(레코드 존재 = 1차 연동 판정;
  세부 토큰 만료는 여전히 런타임 API 실패로 잡힘).
- **팝오버 안내**: `IconButton`(MODULAR)에 `disabled` prop 신설 — 네이티브 HTML `disabled`와
  달리 "소프트 비활성화"로 설계했다(시각적으로만 흐리게 처리 + `aria-disabled`, `onClick`은
  계속 동작). 네이티브 `disabled`는 클릭·포커스 이벤트 자체를 막아 "비활성 버튼 클릭 시 팝오버"
  패턴이 불가능하기 때문. `BadgeShareButton` 내부에 서비스 전용 최소 팝오버(작은 절대 위치
  말풍선, 바깥 클릭 시 닫힘)를 새로 구현 — `_ds_manifest.json` 확인 결과 `Toast`/`ModalToast`
  모두 이 좁은 용례(버튼 바로 아래 앵커링된 소형 안내)에 맞지 않아 MODULAR 승격 없이 서비스
  전용으로 처리(오케스트레이터 판정에 따름).
- **바텀시트 상태 분기 정리**: `ShareState`에서 `'not-earned'` variant와 그 JSX 블록을 제거.
  이제 시트는 위 두 케이스가 모두 사전 차단된 뒤에만 열리므로 죽은 코드였다. 런타임 에러
  상태(`'error'`)는 그대로 유지.
- **서버사이드 소유자 검증 추가**: 게이트 리뷰가 지적한 사이드 파인딩(타인 배지 페이지에서
  `share-data` API로 다른 유저의 스트라바 페이스/시간을 조회할 수 있는 문제) 대응 —
  `share-data` 라우트에서 `?u=` 파라미터가 로그인 유저 본인이 아닌 다른 유저를 가리키면
  `403 { error: 'forbidden' }`으로 즉시 거부한다. 클라이언트 버튼 숨김은 UI 편의일 뿐이고,
  실제 접근 제어는 서버에서 강제한다.
- PRD(`BadgeShare/PRD.md`) 8장·8-1장 갱신: 새 예외 처리 흐름, `forbidden` 에러 코드 추가.

**변경된 파일 (재작업분)**
```
jam-web/design-system/components/buttons/IconButton.jsx (disabled prop 추가)
jam-web/design-system/components/buttons/IconButton.d.ts
jam-web/design-system/components/buttons/IconButton.prompt.md
jam-web/design-system/components/buttons/IconButton.stories.tsx (Disabled 스토리 추가)
jam-web/src/app/(main)/badges/[id]/BadgeShareButton.tsx (예외 처리 흐름 전면 재작성)
jam-web/src/app/(main)/badges/[id]/page.tsx (strava_connections 사전 조회, isOwnBadge 분기)
jam-web/src/app/api/badges/[id]/share-data/route.ts (서버사이드 소유자 검증 추가)
Service Plan/Specs/PRD/BadgeShare/PRD.md (8장·8-1장 갱신)
```

**테스트 결과**
- [x] `npx tsc --noEmit` — 변경 파일 관련 타입 오류 없음(기존 무관 오류는 그대로 남음)
- [x] `npx eslint`(변경 파일 대상) — 신규 오류 없음. `BadgeShareButton.tsx`의
  `react-hooks/set-state-in-effect` 1건은 재작업 전부터 동일 위치에 있던 기존 이슈(줄 번호만
  이동), `page.tsx`의 미사용 `Link` import 경고도 이번 변경과 무관한 기존 이슈로 확인.
- [x] 실기기 육안 확인: 생략(구글 로그인 필수 화면이라 배포 후 육안 확인 불가 — 항상 생략).
  게이트 리뷰가 코드 대조로 사전 비활성화 3케이스·팝오버 분기를 재검증함.

**미해결/주의**
- `stravaConnected` 판정은 "연동 레코드 존재 여부"만 확인한다. 레코드가 있지만 refresh_token
  자체가 만료된 극단적 케이스는 여전히 사전 판별되지 않고 시트를 연 뒤 런타임 에러로 처리된다
  (티켓 본문이 이 한계를 이미 명시적으로 허용함).
- 팝오버는 바깥 클릭으로만 닫힌다. 스크롤/리사이즈 시 재배치 로직은 없음(단순 절대 위치
  앵커링) — 좁은 용례라 이번 범위에서는 충분하다고 판단했으나, 추후 다른 화면에서 재사용
  요구가 생기면 이 부분부터 보강 필요.
