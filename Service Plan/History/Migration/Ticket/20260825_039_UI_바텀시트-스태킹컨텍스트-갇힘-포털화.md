---
id: 20260825_039
category: UI
status: CLOSED
created: 2026-08-25
closed: 2026-08-25
---

# [UI] 바텀시트가 `relative z-10` 스태킹 컨텍스트에 갇혀 탭바·페이지 콘텐츠에 가려지는 문제

## 배경 / 문제 정의

POI 배지 상세 화면에서 과거 획득 이력(2번째 이후 항목)을 눌러 바텀시트를 열면 아래 4가지가
동시에 발생한다 (사용자 제보 영상·스크린샷 기준).

1. 바텀시트 위로 **플로팅 탭바가 그려져** 시트 내용을 가린다
2. 시트가 **투명해 보이고** 뒤 페이지 텍스트(배지 설명·Footer 문구)가 겹쳐 보인다
3. 백드롭 블러가 **화면 일부에만** 적용되어, 배경 스크롤에 따라 블러/비블러 영역이 갈려 보인다

### 근본 원인 (단일)

세 증상은 서로 다른 버그가 아니라 **하나의 원인**에서 파생된다.

`BottomSheet`의 루트는 `fixed inset-0 z-50`이지만, 호출부인 `PoiEarnHistory`가
`badges/[id]/page.tsx`의 `<div className="relative z-10 …">`(info-section) 안에 있다.
`position: relative` + `z-index: 10`은 **스태킹 컨텍스트를 만든다.**

- 그 안의 `z-50`은 컨텍스트 **내부** 순서일 뿐이고, 컨텍스트 전체는 루트에서 `z-index: 10`으로 취급된다
- 플로팅 탭바(`TabBar.tsx`, `z-40`)는 스태킹 컨텍스트를 만들지 않는 `relative` 조상 아래라
  **루트 컨텍스트에서 z-40**으로 참여한다 → `10 < 40`이므로 **탭바가 시트 위에 그려진다** (증상 1)
- 같은 이유로 시트보다 **DOM 뒤에 오는 `relative z-10` 섹션**(action-section, 배지 설명, Footer)이
  시트 위에 페인트된다 → 시트가 투명해 보이는 착시 (증상 2)
- 백드롭의 `backdrop-filter: blur()`는 **자기보다 뒤에 페인트된 것만** 블러 처리한다. 위에 얹히는
  섹션들은 블러 대상이 아니므로 화면이 블러/비블러로 갈린다 (증상 3)

> 참고: `position: fixed` 자체는 `relative` 조상에 포획되지 않으므로 시트의 **위치**는 정상이다.
> 깨지는 것은 **페인트 순서(z-order)** 뿐이다.

### 영향 범위

`BottomSheet`를 쓰는 모든 호출부가 잠재적으로 같은 문제를 갖는다. `badges/[id]/page.tsx`는
변형 3종 모두 `relative z-10` 섹션을 쓰므로 `PoiEarnHistory`·`ItemEarnHistory` 둘 다 해당된다.

## 상세 요구사항

### 서비스/코드베이스 관점
- `src/components/ui/BottomSheet.tsx`가 **`createPortal`로 `document.body`에 렌더**되도록 수정한다.
  포털은 조상 스태킹 컨텍스트를 벗어나므로 `z-50`이 루트 기준으로 평가되어 탭바(z-40) 위에 온다.
- SSR 안전성: `document`는 클라이언트에서만 존재하므로 마운트 이후에만 포털을 생성한다.
- 기존 동작 회귀 금지 — 드래그-투-클로즈, `detent`/`topGapPx`/`footer`/`footerBottomInset`,
  `contentScrollable`, panel reveal 트랜지션, 배경(`main`) 스크롤 잠금이 그대로 동작해야 한다.
- 시트 루트의 `maxWidth: 430, margin: '0 auto'`(앱 셸 폭)는 포털 이후에도 유지한다.

#### 추가 요구사항 — 토스트 레이어 (1차 게이트 리뷰 FAIL 반영)

포털화의 부작용으로 **토스트가 시트 뒤로 숨는 회귀**가 확인됐다. 함께 해결한다.

- 원인: 토스트 컨테이너(`src/components/ui/Toast.tsx:111`)는 `fixed z-50`이고 `ToastProvider`가
  `{children}` **다음에** 렌더해 앱 셸과 형제(= `body` 직속)다. `createPortal`은 시트를 `body`
  **맨 뒤**에 붙이므로 **동일 z-50에서는 DOM 순서가 뒤인 시트가 이긴다.** 시트 본체는 불투명이고
  토스트 위치(`bottom: safe-area + 88px`)는 compact(75dvh)·full(92dvh) 어느 디텐트에서도 시트 영역 안이다.
- 실제 깨지는 경로: 지도 → POI 캐러셀 → 드랍 선택 → 픽업 실패.
  `PoiCarouselModal.tsx:231~239`는 실패 시 토스트만 띄우고 시트를 닫지 않아(`return`)
  **실패 사유가 화면에 전혀 표시되지 않는다.**
- **해결**: 토스트 컨테이너의 `z-50`을 시트보다 위 단계(`z-[60]`)로 올린다. 토스트는 모달·시트보다
  위에 뜨는 최상위 피드백 레이어라는 통상 UX 기준에 맞춘다. 컨테이너에 이미 `pointer-events-none`이
  있어 시트 조작을 막지 않는다.
- 이 변경은 토스트를 쓰는 **모든 화면**에 영향을 주므로, 토스트가 다른 오버레이(`alert-dialog`,
  `dialog`, `sheet`, `select` 등 z-50 사용처)를 부적절하게 가리지 않는지 함께 확인한다.
  단 `NavigationLoader`(`z-[9999]`)보다는 아래여야 한다.

### UI/UX 관점
- 통상적인 모달 UX 기준: 시트가 열려 있는 동안 **탭바는 시트에 가려지거나 숨겨져야 하며**,
  뒤 페이지 콘텐츠가 시트 위로 새어 나오면 안 된다.
- 백드롭 블러는 시트 뒤 영역 **전체**에 균일하게 적용되어야 한다.
- 토스트는 시트가 열려 있는 동안에도 항상 보여야 한다 (특히 픽업 실패 등 오류 피드백).

## 구현 계획

1. `BottomSheet.tsx`에 mounted 게이트(`useEffect`로 `setMounted(true)`)를 추가
2. 기존 반환 JSX를 그대로 두고 `createPortal(<루트 …>, document.body)`로 감싼다
3. 배지 상세 화면(POI·아이템·일반) 3종에서 시트 열기/닫기·드래그·스크롤 회귀 확인
4. 다른 호출부(`drops/BadgeDetailSheet`, `inventory/[itemId]/InventoryItemHistorySheet`,
   `badges/[id]/BadgeShareButton`, `PoiCarouselModal`) 회귀 확인

### 검토했으나 택하지 않은 대안
- **호출부의 `relative z-10` 제거**: 그 z-10은 배지 배경 레이어보다 섹션을 앞세우려는 의도적
  장치라(page.tsx:303 주석) 제거하면 배경 레이어 회귀가 난다. 또 호출부마다 반복될 문제라
  근본 해결이 아니다.
- **탭바 z-index 낮추기**: 시트 외 다른 오버레이와의 관계가 깨지고, 같은 문제가 다른
  스태킹 컨텍스트에서 재발한다.
- **`pushTabBarHidden`으로 탭바 숨기기**: 증상 1만 없애고 증상 2·3(페이지 콘텐츠가 시트 위로
  올라오는 문제)은 그대로 남는다. 근본 원인을 덮는 대증요법.

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

`src/components/ui/BottomSheet.tsx`의 반환 JSX를 `createPortal(…, document.body)`로 감쌌다.
포털된 노드는 조상 스태킹 컨텍스트(`relative z-10` info-section)를 벗어나므로 루트의 `z-50`이
루트 스태킹 컨텍스트 기준으로 평가되고, 탭바(z-40)와 뒤 페이지 섹션들(z-10) 위에 페인트된다.
세 증상(탭바가 시트를 가림 / 시트가 투명해 보임 / 블러가 화면 일부에만 적용)이 한 번에 해소된다.

SSR 안전성은 `useSyncExternalStore(subscribeNoop, () => true, () => false)` 마운트 게이트로 처리했다.
서버 렌더·하이드레이션 렌더에서는 `false`를 돌려줘 아무것도 렌더하지 않고, 하이드레이션 이후에만
포털을 만든다. 계획서에는 `useEffect`+`setMounted(true)`로 적혀 있었으나, 이 저장소의 eslint
설정(`react-hooks/set-state-in-effect`)이 effect 안의 동기 setState를 error로 잡아 새 린트 오류가
생겼다. 동작이 동일하고 저장소에 이미 같은 관용구가 있는(`src/lib/uiOverlay.ts`)
`useSyncExternalStore`로 대체했다.

그 외 로직(드래그-투-클로즈, detent/topGapPx/footer/footerBottomInset/contentScrollable,
panel reveal 트랜지션, `main` 스크롤 잠금, `maxWidth: 430 + margin: 0 auto`)은 손대지 않았다.

#### 2차 — 토스트 레이어 (1차 게이트 리뷰 FAIL 반영)

`src/components/ui/Toast.tsx`의 토스트 컨테이너 z-index를 `z-50` → `z-[60]`으로 올렸다.
포털화 이후 시트가 `body` 맨 뒤에 붙으면서, 같은 `z-50`인 토스트 컨테이너가 DOM 순서에서 밀려
불투명한 시트 뒤로 숨는 회귀가 있었다. 토스트를 모달·시트보다 위 단계로 분리해 해소한다.

레이어 관계 확인:
- 토스트 컨테이너는 `(main)/layout.tsx`에서 앱 셸 `<div class="… relative">`의 형제로 렌더된다.
  이 앱 셸은 `relative`지만 `z-index: auto`라 스태킹 컨텍스트를 만들지 않으므로, 토스트의 `z-[60]`은
  루트 스태킹 컨텍스트에서 그대로 평가된다.
- `NavigationLoader`(`z-[9999]`, root `layout.tsx`)는 여전히 토스트보다 위다.
- 나머지 `z-50` 오버레이(`BottomSheet`, `alert-dialog`, `dialog`, `sheet`, `select`,
  `FeedSection` 패널, `BadgeShareButton` 팝오버, 어드민 폼)는 모두 토스트 아래가 된다 —
  토스트가 최상위 피드백 레이어라는 통상 UX 기준에 부합한다.
- 컨테이너의 `pointer-events-none`은 그대로라 z를 올려도 아래 오버레이 조작을 막지 않는다.
  (토스트 행 자체만 `pointer-events-auto` — 탭하면 즉시 닫힘, 3초 후 자동 소멸)
- 화면 하단 고정 액션 영역은 서비스 화면 전체에서 `FeedSection`의 바텀 패널 하나뿐이고
  (`grep "fixed bottom-0 | fixed inset-x-0 bottom-0 | sticky bottom-0"`), 토스트가 그 위에 잠시
  뜨는 것은 의도된 동작이다.

#### 3차 — 노출되기 시작한 서버 원문 차단 (개선 리뷰 반영)

1·2차 수정으로 **그동안 시트에 가려져 있던 픽업 실패 토스트가 실제로 보이게 되면서**,
서버가 돌려주던 개발자용 축약 문구가 처음으로 사용자에게 노출되는 상태가 됐다.
`PoiCarouselModal.tsx`의 `msg[err.error] ?? err.error ?? …` 폴백이 원문을 그대로 띄우고 있었다.

노출되던 값: `'드랍 없음'` · `'POI 없음'` · `'인벤토리 없음'` · `'인증 필요'` ·
`'user_lat, user_lng 필요'` · `'POI 반경 50m 밖'` · 매핑 안 된 snake_case 코드
(`cannot_pickup_own_drop` · `inventory_not_found`)

조치:
- `?? err.error` 폴백 **제거** — 매핑되지 않은 코드는 전부 일반 실패 문구로 흘린다
- 서버가 사용자 도달 가능한 실패에 **안정적인 코드**를 반환하도록 변경
  (`'이미 픽업된 아이템'` → `already_picked_up`, `'POI 반경 50m 밖'` → `out_of_range`).
  특히 `already_picked_up`은 클라이언트에 매핑이 **이미 있었는데** 서버가 한국어를 돌려주는 바람에
  매핑이 한 번도 발화하지 않던 상태였다.
- 미매핑이던 `cannot_pickup_own_drop` · `inventory_not_found` 문구 추가
- 기존 문구를 UX Writing 가이드의 [현상]→[원인/해결책] 구조로 재작성

**⚠️ 부수 발견 — 반경 상수 불일치**: 서버 에러 문구와 주석이 `'POI 반경 50m 밖'`/`'50m 검증'`으로
적혀 있었으나 실제 `DROP_RADIUS_METERS`는 **500**이다. 문구에 숫자를 직접 박아두면 상수가 바뀔 때
조용히 어긋나므로, 새 문구는 `t()` 치환으로 상수를 주입한다(`{m}m`). 주석도 상수명 참조로 고쳤다.

### 변경된 파일
```
- jam-web/src/components/ui/BottomSheet.tsx        (1차: createPortal 포털화)
- jam-web/src/components/ui/Toast.tsx              (2차: 토스트 컨테이너 z-50 → z-[60])
- jam-web/src/lib/i18n/ko.ts                       (3차: 픽업 실패 문구 재작성 + 2건 추가)
- jam-web/src/app/api/drops/[dropId]/pickup/route.ts (3차: 사용자 도달 실패를 코드로 반환)
- jam-web/src/components/PoiCarouselModal.tsx      (3차: 원문 폴백 제거 + 코드 매핑)
```

### 테스트 결과
- [x] `npx tsc --noEmit` 통과
- [x] `npm run build` 성공
- [x] `npx eslint src/components/ui/BottomSheet.tsx` — 신규 오류 없음
      (기존 오류 2건 `set-state-in-effect`, `refs` 는 이번 변경 이전부터 존재)
- [x] `npx eslint src/components/ui/Toast.tsx` — 오류 없음
- [ ] 실기기 회귀 확인 — staging 병합 후 필요:
      배지 상세 3종(POI·아이템·일반) 시트 열기/닫기·드래그·스크롤,
      `drops/BadgeDetailSheet`, `inventory/[itemId]/InventoryItemHistorySheet`,
      `badges/[id]/BadgeShareButton`(topGapPx + pushTabBarHidden 조합)
- [ ] 토스트 회귀 확인 — staging 병합 후 필요:
      지도 → POI 캐러셀 → 드랍 선택 → 픽업 실패 시 실패 사유 토스트가 시트 위에 보이는지,
      `combine`·`missions`·`drops`·`SyncButton`·`StravaConnectReveal`의 토스트 정상 노출

### 모듈러-서비스 연결 범위 *(design-system을 건드렸을 경우 필수)*
- 해당 부류: design-system 미수정 (서비스 전용 수정)
- MODULAR의 `design-system/components/navigation/BottomSheet.jsx`는 Storybook 전용 병존 구현이며,
  Storybook 환경에는 이 문제를 유발하는 스태킹 컨텍스트 조상이 없어 이번 범위에서 제외한다
  (오케스트레이터 UI 재사용 판정).
- 영향받는 호출부: `src/components/ui/BottomSheet` 사용처 5곳 (drops/BadgeDetailSheet,
  inventory/[itemId]/InventoryItemHistorySheet, badges/[id]/ItemEarnHistory,
  badges/[id]/PoiEarnHistory, badges/[id]/BadgeShareButton)
- 토스트 z 변경의 영향 범위: `useToast` 사용처 5곳 (combine/CombineClient,
  missions/MissionsListClient, missions/[id]/MissionDetailClient, drops/DropsClient,
  PoiCarouselModal) + SyncButton·StravaConnectReveal. 어드민(`src/app/admin/`)은 토스트 미사용.
- `src/components/ui/`의 대응 파일 동반 수정 여부: 해당 없음

### UX Writing 검증
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조 (3차 변경분 대상)

- [x] 용어 일관성: 픽업·드랍·인벤토리 등 고정 용어만 사용
- [x] 톤앤매너: 오류 상황이라 전문적·차분한 톤 유지
- [x] 에러 메시지 [현상]→[원인/해결책] 구조 — 토스트는 1줄이라 CTA 버튼 대신 해결책을 문장에 포함
  - `pickupFailed`: `'픽업 실패'`(명사형) → `'지금은 픽업할 수 없어요. 잠시 후 다시 시도해 주세요'`
  - `pickupInventoryFull`: 해결책 추가 (`'아이템을 정리하고 다시 시도해 주세요'`)
  - `pickupAlreadyDone`: 해결책 추가 (`'다른 아이템을 찾아보세요'`)
  - `pickupOutOfRange`(신규): 가이드 4절 "체크인 실패" 예시 구조 적용
  - `pickupOwnDrop`(신규): 금지 사유 + 대안 제시
- [x] 문장 규칙: 전부 해요체, 토스트 1줄 길이 유지
- [x] 표기 규칙: 거리 단위는 상수 주입(`{m}m`)으로 하드코딩 회피

### 배포 정보
- 배포일:
- 환경: production
- 커밋:

### 주요 의사결정 / 핵심 메모
- 포털 이후에도 루트는 `fixed inset-0` + `maxWidth: 430` + `margin: 0 auto`라 앱 셸(430px 중앙 정렬)과
  동일한 폭·위치를 유지한다. body는 `min-h-full flex flex-col`이지만 포털 루트가 `position: fixed`라
  플로우에서 빠져 flex 레이아웃에 영향을 주지 않는다.
- 디자인 토큰(`--color-surface` 등)은 모두 `:root`에 선언되어 있어 body 직속으로 옮겨도 스타일이
  그대로 상속된다.
- React 포털은 DOM 위치만 바뀌고 React 트리상의 이벤트 버블링은 유지되므로, 호출부의 이벤트
  핸들러 동작은 변하지 않는다.

### 잔여 이슈
- 이 파일에 이전부터 있던 eslint 오류 2건(`react-hooks/set-state-in-effect` L122,
  `react-hooks/refs` L244)은 이번 티켓 범위 밖이라 그대로 두었다.
