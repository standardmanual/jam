---
id: 20260826_001
category: UI
status: OPEN
created: 2026-08-26
closed:
---

# [UI] 토스트 컨테이너 포털화 + footer 시트와의 겹침 방어

## 배경 / 문제 정의

티켓 20260825_039에서 `BottomSheet`를 `createPortal`로 `document.body`에 렌더하도록 바꾸고,
그 부작용으로 토스트가 시트 뒤에 숨는 회귀가 생겨 토스트 컨테이너를 `z-50` → `z-[60]`으로 올렸다.
인터랙션 리뷰에서 후속 과제 2건이 확인됐다.

### 과제 1 — `z-[60]`이 주석에만 존재하는 불변식에 의존한다

현재 토스트의 `z-[60]` 정합성은 **"앱 셸 `div`가 `z-index: auto`라 스태킹 컨텍스트를 만들지
않는다"**는 불변식에 기대고 있고, 그 불변식은 `Toast.tsx`의 주석에만 있다.
`src/app/(main)/layout.tsx:47`의 셸에 누구든 `transform`·`filter`·`isolation`·`z-*`를 하나
붙이면 **20260825_039와 완전히 같은 종류의 버그가 조용히 재발한다.**

### 과제 2 — footer 있는 시트와 토스트가 기하학적으로 겹친다 (잠복)

`Toast.tsx:46`의 `ToastRow`에는 `pointer-events-auto`와 `onClick={onDismiss}`가 있다.
컨테이너만 `pointer-events-none`이고 **토스트 사각형 자체는 포인터를 먹는다** — 그 영역을 탭하면
토스트만 닫히고 아래 버튼은 눌리지 않는다.

`footerBottomInset='tabbar'`(기본값) 기준 실측:

| | 화면 하단 기준 위치 |
|---|---|
| 토스트 (`bottom` + `py-3` + body-sm ≈ 44px) | `safe + 88px` ~ `safe + 132px` |
| 시트 footer 버튼 (safe + 16 + 64 + 12) | `safe + 92px` ~ `safe + 140px` |

거의 완전히 포개진다.

> **단, 2026-08-26 기준 실제로 터지지는 않는다.** `footer`를 쓰는 유일한 호출부인
> `badges/[id]/BadgeShareButton.tsx`가 `footerBottomInset="safe-area"`(버튼 `safe+16~70px`)를
> 쓰고, 그 컴포넌트는 토스트를 아예 사용하지 않는다.
> **기본값 조합을 쓰는 시트가 새로 생기면 터지는 잠복 위험**이다.

## 상세 요구사항

### 서비스/코드베이스 관점
- `src/components/ui/Toast.tsx`의 토스트 컨테이너를 `createPortal(document.body)`로 렌더한다.
  `BottomSheet.tsx`에 이미 도입한 `useSyncExternalStore` 마운트 게이트 패턴을 그대로 쓴다
  (이 저장소 eslint의 `react-hooks/set-state-in-effect`가 effect 내 동기 setState를 error로 잡으므로
  `useEffect`+`setMounted` 방식은 쓰지 말 것).
- 포털은 `document.body` **맨 뒤**에 붙는다 — `BottomSheet`도 같은 곳에 포털되므로
  **두 포털의 DOM 순서에 의존하지 말고 z 값으로 서열이 확실히 정해지는지** 검증한다.
- `ToastProvider`의 컨텍스트 제공(`{children}`)은 그대로 두고 **컨테이너만** 포털한다.

### UI/UX 관점
- 겹침 방어책을 하나 고르거나 조합한다:
  1. 오버레이가 떠 있는 동안 토스트를 **상단 앵커로 전환**(TopNav 아래). 현재
     `translateY(16px) → 0` 진입 모션이 "화면 아래 가장자리에서 올라온다"는 의미인데
     실제로는 불투명 시트 한복판에서 솟아나는 문제도 함께 해소된다.
  2. `--toast-bottom` CSS 변수를 두고 footer 있는 시트가 열릴 때 값을 올린다
     (`src/lib/uiOverlay.ts`의 카운터 스토어 패턴 참고).
  3. `ToastRow`의 `pointer-events-auto`(탭-투-디스미스) 재검토 — 포기하면 겹쳐도 버튼은 산다.
- 부수 검토: 토스트 위치 `bottom: safe-area + 88px`의 88px은 **플로팅 탭바 높이에 맞춘 값**이라
  `pushTabBarHidden`으로 탭바를 숨기는 화면(`BadgeShareButton`, `BadgeRevealOverlay`)에서는
  근거 없는 여백이 된다. `uiOverlay.ts`의 탭바 가시성 상태를 구독해 `safe-area + 16px`로
  내리는 방안을 함께 고려한다.

### 회귀 금지
- 토스트 표시·자동 소멸(3초)·탭 투 디스미스·진입/퇴장 트랜지션
- `NavigationLoader`(`z-[9999]`)는 여전히 토스트보다 위
- `useToast` 사용처 전부: `combine/CombineClient`, `missions/MissionsListClient`,
  `missions/[id]/MissionDetailClient`, `drops/DropsClient`, `PoiCarouselModal`,
  `SyncButton`, `StravaConnectReveal`

## 구현 계획

1. `Toast.tsx`에 마운트 게이트 + `createPortal` 적용
2. 겹침 방어책 선택·구현
3. 시트 열림 + 토스트 조합 검증 (compact·full 디텐트, footer 유무)

### 참고 — MODULAR 병존 구현
`design-system/components/feedback/Toast.jsx`는 **위치 지정이 없는 표현용 row 컴포넌트**로
(`bottom-anchored transient message` 주석만 있고 `position`/`zIndex` 없음), 컨테이너·레이어는
서비스 구현에만 있다. 따라서 이번 변경의 DS 대응 파일은 없다.

`design-system/components/navigation/BottomSheet.jsx:57~59`는 여전히
`position: fixed; zIndex: 50`에 포털이 없다. Storybook 전용 병존 구현이라 실피해는 없지만
서비스 구현과의 격차가 벌어졌으므로, 최소한 "서비스 구현은 포털 기반"이라는 주석을 남길지 검토한다.

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

**과제 1 — 포털화**
`ToastProvider`의 토스트 컨테이너를 `createPortal(document.body)`로 렌더한다. 마운트 게이트는
`BottomSheet.tsx`가 쓰는 `useSyncExternalStore(subscribeNoop, () => true, () => false)` 패턴을
그대로 재사용했다(`useEffect`+`setMounted`는 `react-hooks/set-state-in-effect`에 걸림).
`ToastContext.Provider`와 `{children}`은 그대로 두고 컨테이너만 포털한다.

이제 토스트와 `BottomSheet`가 **둘 다 `document.body` 직속 = 같은 루트 스태킹 컨텍스트**에 있으므로
DOM 순서와 무관하게 z 값만으로 서열이 확정된다(토스트 60 > 시트 50). `NavigationLoader`도
`src/app/layout.tsx:48`에서 `<body>` 직속으로 렌더되므로 `z-[9999]`가 그대로 최상위를 유지한다.
"앱 셸 `div`가 `z-index: auto`"라는 주석-only 불변식 의존이 제거됐다.

**과제 2 — 겹침 방어: 1안(상단 앵커 전환) 채택**

- 하단 오버레이 열림 여부를 알리는 카운터를 `src/lib/uiOverlay.ts`에 추가
  (`pushBottomOverlay()` / `isBottomOverlayOpen()`). `BottomSheet`가 `lingering`(= 열림 +
  닫힘 트랜지션 잔류) 동안 등록한다.
- `toast()`가 호출되는 **순간** 앵커를 확정해 `ToastItem.anchor`에 담는다.
  하단 오버레이가 떠 있으면 `'top'`, 아니면 `'bottom'`.
- 상·하단 두 개의 앵커 컨테이너를 렌더하고 각자 자기 앵커의 토스트만 그린다.
  상단 앵커 위치는 `calc(env(safe-area-inset-top) + 56px + 8px)` (TopNav = safe-top + 56px 바로 아래).
  상단 컨테이너에는 `--toast-distance: -16px`를 인라인으로 덮어써 진입 모션이
  "위에서 내려온다"가 되게 했다(하단 앵커의 "아래에서 올라온다"와 대칭).

### 변경된 파일
```
jam-web/src/components/ui/Toast.tsx
jam-web/src/components/ui/BottomSheet.tsx
jam-web/src/lib/uiOverlay.ts
```

### 테스트 결과
- [x] `npx tsc --noEmit` 통과
- [x] `npx eslint` — Toast.tsx / uiOverlay.ts 클린. BottomSheet.tsx의 error 2건
      (`set-state-in-effect` 121행, `refs` 255행)은 **이번 변경 이전부터 있던 것**으로
      `git stash` 상태에서도 동일하게 2건 재현됨(신규 유입 없음)
- [x] `npm run build` 통과
- [ ] 실기기/스테이징 육안 확인 — staging 병합 후 필요
      (검증 시나리오: 지도 → POI 캐러셀 → 배지 상세 시트 → 픽업 실패 토스트가
      TopNav 아래 상단에 뜨고 픽업/취소 버튼이 가려지지 않는지 / 픽업 성공 시
      토스트가 뜬 채 시트가 닫혀도 위치가 튀지 않는지 / compact·full 디텐트 모두)

### 모듈러-서비스 연결 범위
- 해당 부류: **서비스 전용** (레이어·위치 로직)
- 영향받는 호출부: `useToast` 사용처 전부 — `combine/CombineClient`, `missions/MissionsListClient`,
  `missions/[id]/MissionDetailClient`, `drops/DropsClient`, `PoiCarouselModal`, `SyncButton`,
  `StravaConnectReveal`. 이 중 시트가 열린 상태에서 토스트가 뜨는 유일한 경로는
  `PoiCarouselModal`의 드랍/픽업(→ `BadgeDetailSheet`)이다.
- `design-system/`의 대응 파일 동반 수정 여부: **없음**.
  `design-system/components/feedback/Toast.jsx`는 위치 지정(`position`/`zIndex`)이 없는 표현용 row
  컴포넌트라 이번 변경(컨테이너·레이어·앵커)의 DS 대응 파일이 존재하지 않는다. DS는 건드리지 않았다.

### UX Writing 검증
- 사용자 노출 텍스트 변경 여부: **없음** (문구 변경 없이 레이어·위치만 조정)

### 배포 정보
- 배포일:
- 환경: production
- 커밋:

### 주요 의사결정 / 핵심 메모

**왜 1안(상단 앵커)인가**
- **3안(`pointer-events-auto` 제거)은 배제** — 탭-투-디스미스는 이 티켓의 회귀 금지 목록에 명시돼 있다.
- **2안(`--toast-bottom` 변수)은 배제** — footer 높이(`safe+16+64+12` vs `safe+16`, 버튼 48px, pt 16px)를
  `Toast.tsx`가 다시 계산해야 해서 `BottomSheet`와 매직 넘버가 이중화된다. `footerBottomInset` 값이나
  버튼 높이가 바뀌면 조용히 어긋난다.
- **1안은 매직 넘버 의존이 없다.** footer 유무·`footerBottomInset` 종류·compact/full 디텐트와 무관하게
  "하단을 덮는 오버레이가 있으면 반대편으로 피한다"는 한 가지 규칙으로 전부 커버된다. 티켓이 지적한
  "불투명 시트 한복판에서 토스트가 솟아나는" 모션 의미 붕괴도 함께 해소된다.

**왜 구독이 아니라 "뜨는 순간 스냅샷"인가**
`isBottomOverlayOpen()`은 훅이 아닌 명령형 getter이고 리스너가 없다. 살아 있는 토스트의 앵커를
실시간으로 따라가게 만들면 `PoiCarouselModal.executePickup`의 흔한 흐름
(`toast(픽업 성공)` → `setSelectedDrop(null)`로 시트 닫기)에서 이미 화면에 떠 있는 토스트가
상단→하단으로 순간이동한다. 앵커를 토스트 생성 시점에 확정하고 소멸까지 유지하면 이 문제가 없고,
전역 스토어에 리스너가 아예 없어 렌더 루프 위험도 없다.

**`open`이 아니라 `lingering` 기준으로 등록하는 이유**
시트 닫힘 트랜지션 350ms 동안에도 시트는 여전히 화면 하단을 덮고 있다.

**부수 검토(`88px` 여백 → 탭바 숨김 시 `safe+16px`)는 이번 범위에서 보류**
현재 `pushTabBarHidden`을 쓰는 두 화면(`BadgeShareButton`, `BadgeRevealOverlay`) 중
토스트를 띄우는 곳이 하나도 없어 실제 효과가 0이다. 또한 `BadgeShareButton`은 탭바를 숨기면서
동시에 `BottomSheet`를 열기 때문에, 그 안에서 토스트가 생기더라도 위 1안에 의해 이미 상단 앵커로
간다. 반대로 탭바 가시성을 구독해 하단 offset을 실시간으로 바꾸면 위와 같은 순간이동 문제가
재발한다. 실사용처가 생기는 시점에 "뜨는 순간 스냅샷" 방식으로 함께 처리하는 편이 안전하다.

### 잔여 이슈
- **`BadgeDetailSheet`의 겹침은 잠복이 아니라 실제 발생 중이었다.** 티켓 본문은 `footer` prop
  사용처만 조사해 "실제로 터지지는 않는다"로 판단했으나, `drops/BadgeDetailSheet.tsx:47`은
  `footer` prop을 쓰지 않고 스크롤 영역 자체에
  `paddingBottom: calc(env(safe-area-inset-bottom) + 16px + 64px + 12px)`를 줘서 픽업/취소 버튼을
  **`footerBottomInset='tabbar'`와 완전히 동일한 위치(safe+92~140px)**에 놓는다. 픽업 실패 토스트
  (safe+88~132px)와 거의 완전히 포개져, 토스트가 떠 있는 3초 동안 그 영역 탭이 버튼 대신 토스트
  디스미스로 먹혔다. 이번 변경으로 해소된다.
- `PoiCarouselModal`(`z-30`) 자체는 이 스토어에 등록하지 않았다 — 티켓 범위 밖.
  캐러셀 패널 하단도 `safe+92px`라 캐러셀만 열린 상태(예: `loadDropsFailed`)에서 뜨는 토스트는
  여전히 카드 하단과 겹친다. 필요하면 `pushBottomOverlay()`를 `PoiCarouselModal`에도 한 줄 등록하면 된다.
- `design-system/components/navigation/BottomSheet.jsx:57~59`는 여전히 포털 없는 `zIndex: 50`
  병존 구현이다. 이번 티켓에서는 DS를 건드리지 않았다(별도 티켓 20260826_003의 z-index 레이어
  체계 문서화와 함께 처리하는 편이 자연스럽다).
