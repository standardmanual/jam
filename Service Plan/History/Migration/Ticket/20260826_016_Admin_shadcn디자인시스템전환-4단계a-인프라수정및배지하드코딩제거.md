---
id: 20260826_016
category: Admin
status: OPEN
created: 2026-08-26
---

# [Admin] shadcn 디자인시스템 전환 — 4단계a: 인프라 수정 + 배지 도메인 하드코딩 제거

## 배경 / 문제 정의

전체 배경은 `Service Plan/Specs/PRD/AdminUI/REDESIGN.md`(1절 "완전 분리" 원칙) 참조.
실행 순서·화면별 분배·완료 기준은 `Service Plan/Specs/PRD/AdminUI/PHASES.md`의
"Phase 4 — 하드코딩 제거" 절 참조 — **시작 전 반드시 먼저 읽을 것.**

1~3단계(폴더 분리·테마·Sidebar·Data Table)로 shadcn 프레임워크 자체는 다 갖춰졌지만,
기존 폼·모달 코드는 여전히 `text-[#111111]`/`border-[#e5e7eb]`/`bg-[#f8f9fa]` 같은
하드코딩 hex 클래스를 쓴다(전체 39개 파일 824건 실측). 이 티켓(4단계a)은 그중 가장 큰
덩어리인 배지 도메인 + 이후 모든 하위 티켓의 전제조건인 인프라 버그 2건을 처리한다.

## 상세 요구사항

### 1. 선행 인프라 수정 (이후 4b/4c/4d의 전제조건, 먼저 처리할 것)

**1-1. `--color-border` 스코프 누락**

`jam-web/src/app/globals.css`의 `@theme` 블록에 있는 `--color-border: var(--color-border)`는
DS v2(서비스 본체)가 `design-system/tokens/colors.css`의 `:root` 선언을 `border-border`
Tailwind 유틸리티로 노출하는 기존(20260820_003) 매핑이다. 1단계가 `[data-admin-theme]`
스코프 안에 shadcn 프리셋 원시 변수(`--border` 등)는 넣었지만, 이 **유틸리티 매핑 이름**
(`--color-border`)을 스코프 안에서 재정의하지 않아서, 어드민 스코프 안에서
`border-border` 클래스를 써도 여전히 서비스 다크 테두리 색으로 풀린다.

`[data-admin-theme]` 블록 안에 `--color-border: var(--border)`를 추가로 오버라이드할 것.
같은 이름 충돌 패턴이 다른 시맨틱 클래스(`--color-background`, `--color-foreground`,
`--color-primary` 등)에도 있는지 `grep -n "color-background\|color-foreground\|color-primary\|
color-secondary\|color-muted\|color-accent" src/app/globals.css`로 함께 점검하고, 있으면
같이 고칠 것. 수정 후 브라우저에서 `border-border`/`bg-background` 등 클래스가 실제
stone 테마 색으로 렌더링되는지 반드시 확인(1단계가 `--background`/`--foreground`에 썼던
`getComputedStyle` 검증 방식과 동일하게).

**1-2. Radix Portal 스코프 (dialog/alert-dialog/select)**

`sheet.tsx`(2단계, `src/components/admin/ui/sheet.tsx`)가 이미 가진 패턴 —
`container?: React.ComponentProps<typeof SheetPortal>["container"]`을 prop으로 받아
Radix Portal에 전달 — 을 `src/components/admin/ui/dialog.tsx`,
`src/components/admin/ui/alert-dialog.tsx`, `src/components/admin/ui/select.tsx`
세 파일에도 동일하게 추가한다. 이 prop 자체는 이번 티켓에서 당장 쓰이지 않아도 된다
(인터페이스만 갖춰두면 4b/4c/4d에서 이 컴포넌트들을 시맨틱 클래스로 바꿀 때 바로 쓸 수
있음) — 단, 이 컴포넌트들을 호출하는 곳(주로 `[data-admin-theme]` 하위)에서 실제로
`container={document.querySelector('[data-admin-theme]')}` 같은 형태로 넘겨야 스코프
안에서 렌더링되니, 최소 1곳(배지 관련 다이얼로그/셀렉트 중 하나)에서 실제로 연결해
동작을 확인할 것.

### 2. 배지 도메인 하드코딩 제거

대상 파일(실측 occurrence 수):
- `src/app/admin/badges/BadgeForm.tsx` (160건 — 가장 큼)
- `src/app/admin/badges/BackgroundGeneratorPreview.tsx` (28건)
- `src/components/admin/BadgeMultiSearchSelect.tsx` (13건)
- `src/components/admin/BadgeSearchSelect.tsx` (9건)

`text-[#111111]` → `text-foreground`, `border-[#e5e7eb]` → `border-border`,
`bg-[#f8f9fa]` → `bg-muted` 식으로 shadcn 시맨틱 토큰에 매핑한다. 정확한 매핑은 색상값을
보고 가장 가까운 시맨틱 의미로 판단할 것(예: 어두운 텍스트는 `text-foreground`, 옅은
회색 텍스트는 `text-muted-foreground`, 테두리는 `border-border`, 옅은 배경은 `bg-muted`
또는 `bg-accent`). 애매하면 `npx shadcn@latest docs` 관련 컴포넌트 문서나 이미 3단계에서
변환된 Data Table 컴포넌트들의 클래스 사용례를 참고할 것.

**주의**: `BackgroundGeneratorPreview.tsx`는 배지 배경 테마(WebGL 셰이더 미리보기)를
다루는데, 이 컴포넌트가 렌더링하는 **미리보기 결과물 자체**(배지 카드처럼 보이는 부분)는
유저가 실제로 보게 될 서비스 화면을 시뮬레이션하는 거라 MODULAR 색상을 쓰는 게 맞을
수 있다 — 이 컴포넌트를 열어보고 "어드민 편집 UI 껍데기"와 "미리보기 결과물" 부분을
구분해서, 껍데기만 shadcn으로 바꾸고 미리보기 결과물 부분은 그대로 둘 것(애매하면 현재
상태를 유지하고 완료 기록에 판단 근거를 남길 것).

## 절대 건드리면 안 되는 것

- 미리보기 프레임(`BadgeDetailPreviewFrame.tsx`, `ItemBookDetailPreviewFrame.tsx`) — MODULAR
  유지 대상, 이 티켓과 무관
- 4b/4c/4d 대상 파일(POI·아이템북·세계관·운영도구·기타) — 이 티켓에서 손대지 않음
- 배지 CRUD 로직·API — 색상 클래스만 교체
- 1~3단계 산출물(shadcn 폴더 구조, `[data-admin-theme]` 스코프 자체, Sidebar/Accordion,
  Data Table 공용 컴포넌트) — 재사용만 하고 재설계하지 않는다

## 구현 계획

1. `--color-border` 스코프 수정(1-1), 다른 이름 충돌 여부 점검
2. `dialog.tsx`/`alert-dialog.tsx`/`select.tsx`에 `container` prop 추가(1-2), 최소 1곳에서
   실제 연결 확인
3. `BadgeForm.tsx` 하드코딩 제거(가장 크니 먼저) → `BackgroundGeneratorPreview.tsx` →
   `BadgeMultiSearchSelect.tsx` → `BadgeSearchSelect.tsx`
4. 로컬 dev 서버 + 임시 `ADMIN_EMAILS` 오버라이드로 실제 확인 — **1440px 데스크탑에서
   배지 목록·상세·수정 폼·배지 선택 모달을 전부 실제 브라우저로 확인할 것**(색상·테두리가
   시각적으로 기존과 크게 다르지 않은지, `border-border`/`bg-background` 등이 실제로
   stone 테마 색을 내는지)
5. `npx tsc --noEmit` / `npm test` / `npx next build` 통과 확인
6. `grep -rn "text-\[#\|border-\[#\|bg-\[#" src/app/admin/badges src/components/admin/
   BadgeMultiSearchSelect.tsx src/components/admin/BadgeSearchSelect.tsx`로 잔존 0건 확인
   (BackgroundGeneratorPreview의 의도적 미리보기 부분 제외)
7. `PHASES.md`에 4단계a 완료 상태 기록

## 구현 기록 (2026-08-26, 리뷰 대기)

**1-1. `--color-border` 스코프 — 문서 초안과 실제 원인이 달랐다.** `globals.css` 자체의
`@theme inline` 매핑(`--color-background`/`-foreground`/`-muted`/`-accent`/`-primary`/
`-secondary`/`-border`)은 이미 전부 `var(--shadcn원시이름)`을 올바르게 참조하고 있었다
(자기참조는 `--color-border` 한 곳뿐이라던 문서 추정과 달리, globals.css만 봐서는 문제가
안 보였다). 실제 브라우저 `getComputedStyle`로 `[data-admin-theme]` 스코프 안 `--color-primary`
값을 찍어보니 `#e8461f`(DS v2 브랜드 레드)가 나와 — `design-system/tokens/colors.css`의
`:root`(레이어 밖, unlayered)가 `--color-primary`/`--color-secondary`/`--color-border`를
직접 선언하고 있고, Tailwind `@theme inline` 출력은 `@layer theme` 안에 들어가 동일
특정성에서 항상 레이어 밖 규칙에 진다(소스 순서 무관)는 게 진짜 원인이었다. `grep -n
"^\s*--color-" design-system/tokens/*.css`로 5개 토큰 파일 전체를 shadcn 시맨틱 이름
집합과 대조해 겹치는 이름이 border/primary/secondary 3개뿐임을 확인하고, `[data-admin-theme]`
블록에 이 3개를 `var(--border)`/`var(--primary)`/`var(--secondary)`로 재매핑했다. 이
발견은 코드만 보고는 안 잡히고 실브라우저 검증에서만 드러났다 — `PHASES.md`에 4b~4d
착수 전 재점검 가이드로 남겨뒀다.

**1-2. Portal `container` prop.** `dialog.tsx`/`alert-dialog.tsx`/`select.tsx` 3개에
`sheet.tsx` 패턴 그대로 추가. `BadgeForm.tsx`의 "타입" Select 1곳에 실제로
`container={themeContainer}`를 연결해(sidebar.tsx와 동일한 `document.querySelector
('[data-admin-theme]')` 패턴) 동작을 검증했다. 이 세 컴포넌트 자체의 하드코딩 뉴트럴
팔레트(`bg-white`/`text-neutral-900` 등)는 이번 티켓 범위가 아니라 그대로 뒀다(3단계
노트에서 예고한 대로 Portal 리다이렉션 이후 단계).

**2. 배지 도메인 하드코딩 제거.** 대상 4개 파일 + `BadgeDetailPreviewFrame.tsx`(제외
대상, 안 건드림) 외에 같은 디렉터리의 `src/app/admin/badges/new/page.tsx`에서 2건을
추가로 발견해 함께 정리했다(대상 파일 목록에는 없었지만 완료 기준 grep이 `src/app/admin/
badges` 디렉터리 전체를 스캔하므로 포함시켰다 — occurrence 합산(210건)에는 안 잡혀 있던
소형 누락으로 판단).

매핑 규칙: `#111111`(가장 어두운 텍스트/버튼 배경) → `text-foreground`/`bg-primary`,
`#374151`(라벨 텍스트) → `text-foreground`, `#6b7280`/`#898989`/`#9ca3af`(설명·플레이스홀더
텍스트) → `text-muted-foreground`/`placeholder:text-muted-foreground`, `#e5e7eb`/`#f3f4f6`
(테두리) → `border-border`/`divide-border`, `#f5f5f5`/`#f3f4f6`/`#f8f9fa`(옅은 배경) →
`bg-muted`, `#242424`(버튼 hover) → `bg-primary/90`, `accent-[#111111]` → `accent-primary`.
`BadgeMultiSearchSelect.tsx`의 선택 칩(`bg-[#111111]/10 text-[#111111] border-[#111111]/30`)은
단일 색조 배지라 `bg-primary/10 text-primary border-primary/30`으로 매핑(다른 곳과 달리
`text-primary`를 썼다 — 칩 4개 속성이 전부 같은 원본 hex의 opacity 변형이라 한 시맨틱
토큰으로 통일하는 게 맞다고 판단).

`BackgroundGeneratorPreview.tsx`의 보라색 강조 섹션("배경 테마")은 지시대로 "어드민 편집
UI 껍데기"와 "미리보기 결과물"을 구분했다 — 이 컴포넌트는 미리보기 결과물을 직접 그리지
않고 `renderPreview` 렌더 prop으로 전부 호출부(`BadgeDetailPreviewFrame`, MODULAR)에
위임하므로, 파일 안의 모든 색상은 "껍데기"에 해당한다. 다만 원본 `#9333ea`/`#fdf4ff`는
stone 테마에 대응하는 shadcn 시맨틱 슬롯이 없어(브랜드 보라색을 대체할 8종 토큰 없음),
시각적 회귀를 피하기 위해 정확히 일치하는 Tailwind 기본 팔레트(`purple-600`/`fuchsia-50`)로
치환했다 — hex 대괄호 표기만 제거하고 렌더링 색은 완전히 동일하게 유지했다.

**검증**: `npx tsc --noEmit`(0 에러) / `npm test`(60파일 562테스트 전부 통과) / `npx next
build`(성공, 전체 라우트 정상) 모두 통과. `grep -rn "text-\[#\|border-\[#\|bg-\[#"
src/app/admin/badges src/components/admin/BadgeMultiSearchSelect.tsx
src/components/admin/BadgeSearchSelect.tsx` 결과 `BadgeDetailPreviewFrame.tsx`(제외
대상) 1건만 남고 0건. 로컬 `next dev` + 임시 `ADMIN_EMAILS=dev-tester@jam.local` 셸
환경변수(`.env.local` 파일 자체는 수정하지 않음) + `/api/dev-login`으로 실제 세션 발급 후
Playwright로 1440px 데스크탑 실렌더링 확인:
- `getComputedStyle`로 `--border`/`--color-border`, `--primary`/`--color-primary`가
  `[data-admin-theme]` 안에서 완전히 동일한 값(lab 색공간)으로 나옴을 확인(스코프 수정
  검증)
- 배지 목록·배지 등록 폼(`BadgeForm.tsx`, 상단·하단 스크롤 전체)·배지 상세·배지 수정
  폼·삭제 확인 모달(커스텀 구현) 전부 스크린샷으로 기존과 시각적으로 크게 다르지 않음을
  확인(배경 테마 보라 강조 박스 포함)
- "타입" Select 드롭다운을 열어 `[role=listbox]`가 실제로 `[data-admin-theme]` 하위
  DOM에 포털됨을 프로그램적으로 확인(container prop 연결 검증)
- `ItemBookForm.tsx`(4b 대상, 안 건드림)에서 `BadgeSearchSelect`/`BadgeMultiSearchSelect`를
  간접 소비하는 화면도 열어 콘솔 에러 0건, 검색·선택 정상 동작 확인(공용 컴포넌트라 여러
  화면에 영향을 줄 수 있어 사이드 이펙트 점검 차원)

**변경 파일**: `src/app/globals.css`(인프라), `src/components/admin/ui/dialog.tsx`·
`alert-dialog.tsx`·`select.tsx`(인프라), `src/app/admin/badges/BadgeForm.tsx`·
`BackgroundGeneratorPreview.tsx`·`new/page.tsx`, `src/components/admin/
BadgeMultiSearchSelect.tsx`·`BadgeSearchSelect.tsx`.

`PHASES.md`의 "Phase 4 — 하드코딩 제거" 절에 4단계a 완료(리뷰 대기) 상태와 위 구현 노트
요약을 함께 기록했다.
