---
id: 20260827_002
category: Admin
status: OPEN
created: 2026-08-27
---

# [Admin] shadcn 디자인시스템 전환 — 4단계d: 잔여 화면 + 공용 컴포넌트 하드코딩 제거 (마지막)

## 배경 / 문제 정의

4단계a(`20260826_017`)·4단계b(`20260826_018`)·4단계c(`20260827_001`) 모두 CLOSED. 이
티켓(4단계d)이 **하드코딩 제거의 마지막 티켓**이다 — 완료되면 `PHASES.md`의 4단계 전체
완료 기준을 충족하고, 4단계 UI 디자인시스템 전환 4단계(기반→레이아웃→Data Table→하드코딩
제거) 전부가 끝난다.

시작 전 `Service Plan/Specs/PRD/AdminUI/PHASES.md`의 "Phase 4" 절과 4a/4b/4c 티켓의
색상 매핑 규칙을 반드시 먼저 읽고 동일하게 따를 것.

## 상세 요구사항

### 1. 잔여 화면 하드코딩 제거 (실측 occurrence 수, 2026-08-27 재확인)

- `src/app/admin/points/UserGrantForm.tsx` (34건 — 가장 큼)
- `src/app/admin/missions/MissionList.tsx` (34건)
- `src/app/admin/today/TodayCardList.tsx` (20건)
- `src/app/admin/theme/ThemeManager.tsx` (19건)
- `src/app/admin/recipes/RecipeList.tsx` (15건)
- `src/app/admin/points/AdminUserSearch.tsx` (14건)
- `src/components/admin/ImageUploadField.tsx` (13건)
- `src/app/admin/error.tsx` (12건 — 4b 게이트 리뷰에서 스캔 누락 발견돼 이월됨)
- `src/components/admin/BackgroundColorField.tsx` (9건)
- `src/app/admin/users/ResetUserButton.tsx` (9건)
- `src/app/admin/points/page.tsx` (8건)
- `src/app/admin/page.tsx` (8건, 대시보드)
- `src/app/admin/users/[id]/page.tsx` (4건)
- 소형 파일 1건씩(누락 방지): `src/app/admin/users/page.tsx`, `src/app/admin/today/page.tsx`,
  `src/app/admin/theme/page.tsx`, `src/app/admin/recipes/page.tsx`,
  `src/app/admin/missions/page.tsx`

### 2. shadcn 프리미티브 자체의 하드코딩 뉴트럴 팔레트 (4a/4b/4c가 범위 밖으로 미뤄둔 것)

- `src/components/admin/ui/alert-dialog.tsx` (7건)
- `src/components/admin/ui/switch.tsx` (2건)

이 두 파일은 개별 화면이 아니라 shadcn 프리미티브 자체다 — `bg-white`/`text-[#111111]`
같은 하드코딩 뉴트럴 팔레트를 시맨틱 토큰으로 바꾼다. **이미 4a에서 `alert-dialog.tsx`에
Portal `container` prop 인터페이스는 추가돼 있다** — 이번엔 팔레트만 바꾸면 된다. 이
두 파일은 어드민 전역에서 재사용되니, 바꾼 뒤 여러 화면(예: 배지 삭제 확인 다이얼로그,
아이템북 활성화 스위치 등)에서 실제로 확인할 것 — 한 곳만 보고 끝내지 말 것.

### 3. 최종 완료 기준 검증 (이 티켓이 4단계 전체의 마지막)

이 티켓 완료 후:
- `grep -rn "text-\[#\|border-\[#\|bg-\[#" src/app/admin src/components/admin` 결과가
  미리보기 프레임(`BadgeDetailPreviewFrame.tsx`, `ItemBookDetailPreviewFrame.tsx`) 관련
  파일을 제외하고 **0건**이어야 한다 — `PHASES.md` Phase 4 전체 완료 기준.
- `PHASES.md`에 **"Phase 4 전체 완료"** 를 명시적으로 기록하고, 4단계(1~4) 전체가
  완료됐음을 문서 최상단 Phase 개요 표에도 반영할 것.

## 절대 건드리면 안 되는 것

- 미리보기 프레임(`BadgeDetailPreviewFrame.tsx`, `ItemBookDetailPreviewFrame.tsx`) — MODULAR
  유지 대상
- 4a/4b/4c 산출물(배지·POI·아이템북·세계관·운영도구 도메인) — 이 티켓과 무관
- 각 화면의 CRUD·유저 관리·포인트 지급 로직·API — 색상 클래스만 교체
- `switch.tsx`/`alert-dialog.tsx`의 동작 로직(체크 상태, 열기/닫기) — 팔레트만 교체

## 구현 계획

1. `UserGrantForm.tsx`·`MissionList.tsx`(가장 크니 먼저) → `TodayCardList.tsx` →
   `ThemeManager.tsx` → `RecipeList.tsx` → `AdminUserSearch.tsx` → `ImageUploadField.tsx` →
   `error.tsx` → `BackgroundColorField.tsx` → `ResetUserButton.tsx` → 나머지 소형 파일들
2. `alert-dialog.tsx`·`switch.tsx` 팔레트 전환, 여러 화면에서 실제 사용처 확인
3. 로컬 dev 서버 + 임시 `ADMIN_EMAILS` 오버라이드로 실제 확인 — **1440px 데스크탑에서
   미션·투데이·레시피·포인트 지급·테마·유저 상세 화면 전부 실제 브라우저로 확인할 것**
4. `npx tsc --noEmit` / `npm test` / `npx next build` 통과 확인
5. `grep -rn "text-\[#\|border-\[#\|bg-\[#" src/app/admin src/components/admin` 최종 실행
   (미리보기 프레임 제외) — 0건이어야 완료
6. `PHASES.md`에 Phase 4 전체 완료 + Phase 개요 표 갱신 기록
