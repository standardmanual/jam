---
id: 20260827_014
category: API
status: OPEN
created: 2026-08-27
closed:
---

# [API] 알림 소식 전면 개편 — 활동 결산·대상 묶음·문구 규칙 R1~R15 적용

## 배경 / 문제 정의

알림함이 **한 사건을 여러 줄로 쪼개서 보여준다.** 유저 관점에서는 로직상 옳은 분리도 홍수로
읽힌다.

### 현상 1 — 결산 카테고리 (사용자 스크린샷)

```
활동 중에 아이템 배지 1개가 떨어졌어요        1시간 전
활동 중에 아이템 배지 1개가 떨어졌어요        1시간 전
```

버그가 아니다. `item_badge_earned`의 묶음 키가 `item_badge_earned:sync:{strava_activity_id}`라
**동기화 1회가 아니라 액티비티 1건**이 단위이고, 문구가 활동을 구분해주지 않아 두 행이 글자
하나까지 같아진다.

### 현상 2 — 컬렉션 (프로덕션 DB 실측, 2026-08-25 09:00:58)

한 유저가 배치 1회에서 **0.09초 안에 `collection_slottable` 5행**을 받았다.

```
새벽 물안개 몽환에 넣을 수 있는 아이템 배지가 1개 있어요        09:00:58.187
비밀의 카페 탐험에 넣을 수 있는 아이템 배지가 1개 있어요        09:00:58.189
티타늄 맹신에 넣을 수 있는 아이템 배지가 2개 있어요             09:00:58.213
180 BPM의 심장에 넣을 수 있는 아이템 배지가 2개 있어요          09:00:58.278
노을 헌터의 기록에 넣을 수 있는 아이템 배지가 1개 있어요        09:00:58.280
```

전체 알림 37건 중 `collection_slottable`이 10건(27%)으로 1위이고, 유저 10명 중 3명이 다중 행을
받았다.

### 근본 원인

**묶음의 1차 축이 「소식 종류」와 「대상(활동·컬렉션·미션·POI)」이라 대상 수만큼 행이 생긴다.**
유저의 멘탈모델은 반대다 — "오늘 아침에 달린 그거, 뭐 받았지?"

### 검토 산출물

문장을 먼저 쓰고 문장에서 정책을 역산하는 방식으로 두 라운드를 돌았다. 이 티켓의 스펙은
아래 두 문서가 **단일 진실**이다.

- [RECAP_CASEBOOK.md](../../../Specs/PRD/Notification/RECAP_CASEBOOK.md) — ① 보상 획득 6종 (v3 확정)
- [REST_CASEBOOK.md](../../../Specs/PRD/Notification/REST_CASEBOOK.md) — 그 외 17종 (v3 확정)

---

## 상세 요구사항

### 문구 규칙 R1~R15 (두 케이스북에서 확정)

| # | 규칙 |
|---|---|
| R1 | 「이번 활동으로」 접두를 쓰지 않는다 |
| R2 | 고유명사에 작은따옴표를 쓰지 않는다 (볼드가 슬롯을 표시한다) |
| R3 | 화폐 단위는 「포인트」다. 「JAM 포인트」·「P」를 쓰지 않는다 |
| R4 | 미션 소식은 「완료했다」만 말한다. 미션 자체의 보상은 문구에 넣지 않는다 (진행 정보는 남긴다) |
| R5 | 결산에서 2종 이상이면 나열하지 않고 총량으로 말한다 |
| R6 | 새 결산 화면을 만들지 않는다. 착지는 기존 화면 |
| R7 | 미션은 「완료」다 (「달성」 아님) |
| R8 | 대표는 희귀도 최상위로 고른다 |
| R9 | **R11에 흡수됨** — 활동은 「대상」의 한 종류다 |
| R10 | 착지가 다르면 행을 나눈다 |
| R11 | 같은 종류의 소식이 **대상 2건 이상이면 한 행으로 묶고 착지를 목록 화면으로 올린다** (전면 채택, 카테고리 예외 없음) |
| R12 | 부족한 것을 이름으로 부른다 — **컬렉션 #10에만 적용** (아래 결정 사항 참고) |
| R13 | 컬렉션의 목표는 「완성」이다. 「추가」·「장착」을 쓰지 않는다 |
| R14 | 본인 닉네임을 문장에서 부르지 않는다 |
| R15 | 팔로잉 소식은 사람 단위로 묶고, 하루 상한도 사람 수로 센다 |

### 서비스/코드베이스 관점

#### A. ① 보상 획득 — 활동 결산으로 재편

**새 객체를 만들지 않는다.** `strava_activities`가 이미 활동을 영속 저장하고 모든 보상이
`activity_id`를 payload에 싣고 있다. 필요한 것은 소식 종류 하나와 문구·묶음 규칙이다.

- `notification_type` ENUM에 **결산 타입 추가** (마이그레이션 필요)
- 묶음 단위 = **활동 1건**. 활동이 2건 이상이면 R11로 한 단계 올라간다
- 사다리 — 케이스북 A~F 표가 명세다

| 칸 | 상황 | 문구 | 착지 |
|---|---|---|---|
| A | 1종·1개 | 개체를 이름으로 | 그 개체 상세 |
| B | 1종·N개 | 대표 + 외 N | 카테고리 목록 |
| C·D | 2종 이상 | 총량 | `/badges` 또는 프로필 |
| E | 강한 신호 혼재 | 강한 신호를 헤드라인으로 | 그 신호의 상세 |
| F2 | 활동 2건 이상 | 활동 수 + 총량 | 프로필 |

- **포인트는 종류로 세지 않는다.** 문장 꼬리에 붙는 부속으로 다룬다
- **컬렉션 소식은 결산에 넣지 않는다** — 결과가 아니라 초대다. 별행 유지
- **미션 완료도 결산과 별행** — 착지가 다르다(R10). 케이스북 E2 참고

#### B. R11 — 대상 2건 이상 묶음 (신규 묶음 문구 9개)

| 소식 | 묶음 문구 | 착지 |
|---|---|---|
| #9 컬렉션 장착 가능 | 컬렉션에 넣을 수 있는 **아이템 배지 N개**가 있어요 | **인벤토리** |
| #10 완성 임박 | 한 칸만 남은 컬렉션이 **N개** 있어요 | `/collections` |
| #11 완성 가능 | 다 모은 컬렉션 **N개**를 완성해보세요 | `/collections` |
| #18 드랍 지점 활성 | 드랍한 **N곳**에 **M명**이 다녀갔어요 | `/drops` |
| #20 마일스톤 | 미션 **N개**가 목표에 가까워졌어요 | `/missions` |
| #21 마감 임박 | 미션 **N개**가 **이틀** 뒤 끝나요 | `/missions` |
| #23 순위 상승 | 미션 **N개**에서 순위가 올랐어요 | `/missions` |
| #24 종료 (전원 완료) | 축하해요! 미션 **N개**를 끝냈어요. 결과를 확인해보세요 | `/missions` |
| #24 종료 (미완료 포함) | 미션 **N개**가 끝났어요. 결과를 확인해보세요 | `/missions` |

> #9 묶음은 **컬렉션이 아니라 배지를 센다.** 착지도 배지가 있는 곳(인벤토리)이다.

#### C. 컬렉션 #9·#10·#11 우선순위 — 한 컬렉션에 하나만

세 조건이 동시에 참일 수 있어(잔여 1칸 + 그 배지를 미장착 보유) 한 컬렉션이 3행을 만든다.

| 상태 | 보낼 소식 |
|---|---|
| 완성 가능 (전부 보유·미장착) | **#11만** |
| 잔여 1칸 도달 (아직 그 배지 없음) | **#10만** |
| 그 외 넣을 게 있음 | **#9만** |

**순서** — 컬렉션마다 우선순위로 종류를 정한 뒤, 같은 종류가 2건 이상이면 R11로 묶는다.

**이 우선순위는 R12의 전제 조건이다.** 없으면 이미 보유한 배지를 "찾아"라고 말하게 된다.

#### D. 개별 문구 변경

| 소식 | 변경 |
|---|---|
| #10 완성 임박 | `'{bookName}', 한 칸만 남았어요` → **`{badgeName}`을 찾아 `{bookName}`을 완성해보세요** (R12) |
| #11 완성 가능 | `컬렉션에 추가해보세요` → **`컬렉션을 완성해보세요`** (R13) |
| #13 픽업됨 (묶음) | **착지 제거** — 링크 없음. 픽업된 아이템은 소프트 삭제 상태라 갈 곳이 없다 |
| #18 드랍 지점 | `{me}님이 드랍한 자리에` → **`드랍한 곳에`** (R14, 「자리」→「곳」) |
| #24 종료 | **완료/미완료 분기** — 완료자만 「축하해요!」 |
| #26 팔로우 | `{actor}님이 {me}님을 팔로우해요` → **`{actor}님이 팔로우해요`** (R14) |
| #41 동기화 지연 | `Strava 동기화가 끊겼을 수 있어요` → **`Strava에 활동이 기록됐는지 확인해보세요`** |
| #44 포인트 | `{points} JAM 포인트` → **`{points} 포인트`** (R3) |
| 따옴표 제거 | 컬렉션명·미션명·배지명을 쓰는 **문구 11개** (R2) |

#### E. #27 맞팔 소식 제거 + 공백 메우기

맞팔은 유저가 방금 자기 손으로 성립시킨 결과다 — PRD §2-2 「자기 행동의 메아리」.

**그냥 지우면 소식이 통째로 사라지는 경우가 생긴다.** 현재 로직은 맞팔 성립 시
`mutual_follow` 하나만 만들고 `followed`를 만들지 않는다.

| 상황 | 지금 | 변경 후 |
|---|---|---|
| 내가 먼저 팔로우 → **상대가 되팔로우** | `mutual_follow` | **`followed` 생성** ("**예린**님이 팔로우해요") |
| 상대가 먼저 팔로우 → **내가 되팔로우** | `mutual_follow` | **소식 없음** (내 행동) |

ENUM 값 `mutual_follow`는 **DB에서 지우지 않는다** — Postgres는 ENUM 값 제거가 안전하지 않다.
TS 타입에서만 제외하고 「예약됐으나 사용하지 않음」으로 표시한다
(`following_nearby_drop`·`nearby_drops` 선례).

#### F. #40·#41 중복 제거

끊긴 채로 3일이 지나면 확정(#40) 뒤에 추측(#41 "끊겼을 수 있어요")이 따라붙어, 정보가 더 적은
문장이 뒤에 온다.

- **#40이 미해소 상태면 #41을 만들지 않는다**
- #41은 **토큰은 멀쩡한데 새 활동이 안 들어오는 경우** 전용으로 좁히고 문구를 교체한다

#### G. ⑥ 팔로잉 — 사람 단위 묶음 (R15)

- 한 사람의 소식이 하루 2건 이상이면 대표 하나 + "소식이 **N건** 더 있어요"
- 대표 선정은 기존 배치 우선순위 재사용 (mythic > legend > 컬렉션 완성 > 미션 완료, 동순위 최근 순)
- 묶음 행의 착지 = **`/{username}` 프로필**
- **하루 상한을 「2건」에서 「사람 2명」으로** 변경

### DB 관점 (⚠️ 실행은 오케스트레이터가, 개발자는 파일 작성까지만)

1. **마이그레이션** — `notification_type` ENUM에 결산 타입 추가
   → `jam-web/supabase/migrations/0XX_notification_recap_type.sql`
2. **기존 알림 전량 삭제 + 재생성** (배포 시점 1회성)
   → `jam-web/supabase/seed_*.sql` 또는 운영 절차로 기록
   - `DELETE FROM notifications` (전량)
   - **`users.notifications_seen_at`은 건드리지 않는다** — 재생성된 소식이 「새 소식」으로
     뜨고 dot이 켜지도록 한다 (사용자 확정)
   - 삭제 후 `/api/cron/notifications` 배치를 1회 수동 실행해 상태 기반 소식을 되살린다
   - **결산 소식(①)은 백필하지 않는다** — 다음 동기화부터 새 형식으로 쌓인다 (사용자 확정)

> **Supabase는 staging·프로덕션 공용 단일 DB다.** DELETE는 즉시 프로덕션에 반영된다.
> 실행 전 반드시 사용자 승인을 받는다.

---

## 구현 계획

### 변경 대상

| 파일 | 내용 |
|---|---|
| `supabase/migrations/0XX_*.sql` | `notification_type` ENUM에 결산 타입 추가 |
| `src/types/database.ts` | `NotificationType`에 결산 타입 추가, `mutual_follow` 제외 |
| `src/lib/i18n/ko.ts` | 따옴표 제거 11문구, `slotPoints`·`msgPointsIn`·`msgPointsOut`, 신규 묶음 문구 9개, 결산 문구, #10·#11·#18·#24·#26·#41 교체 |
| `src/lib/notifications/message.ts` | 결산 렌더, R11 묶음 분기, 총량 계산(R5), 대표 선정(R8), ⑥ 사람 묶음(R15) |
| `src/lib/notifications/href.ts` | 결산 착지(R6), 묶음 행의 목록 착지, #13 묶음 링크 제거 |
| `src/lib/notifications/groupKey.ts` | R11 묶음 키 (대상 단위 → 목록 단위 승격) |
| `src/lib/notifications/batch/collections.ts` | #9·#10·#11 우선순위, 배지 `name` 조회 추가(R12), R11 묶음 |
| `src/lib/notifications/batch/missions.ts` | #24 완료/미완료 분기, R11 묶음 |
| `src/lib/notifications/batch/dropSpot.ts` | R11 묶음 |
| `src/lib/notifications/batch/following.ts` | 상한 「사람 2명」, 사람 단위 묶음(R15) |
| `src/lib/notifications/batch/account.ts` | #41 조건 축소·문구 교체 |
| `src/lib/strava/sync.ts` · `drop-engine/index.ts` · `points/index.ts` | 결산 생성 지점 통합 |
| `src/app/api/follows/` | `mutual_follow` 생성 제거 → `followed` 생성 |
| `src/lib/notifications/__tests__/` | 기대값 갱신 + 신규 케이스 |

### 구현 순서

1. 마이그레이션 SQL 작성 (실행 안 함) + 타입 갱신
2. 문구(`ko.ts`) — R2·R3 일괄 적용 + 신규 문구
3. 렌더·착지(`message.ts`·`href.ts`) — 결산 + R11 묶음
4. 배치 5개 — 우선순위·묶음·분기
5. 결산 생성 지점 통합 (`sync.ts`·`drop-engine`·`points`)
6. `api/follows` 맞팔 처리
7. 테스트 갱신

### 결산 조립 시점 — 판단 필요

현재는 발급 지점마다 `createNotification`을 흩어서 호출하고 `appendKeys`로 점진 병합한다.
종류별로 쪼개져 있으면 sync 중간 실패가 티 안 나지만, **하나로 합치면 반쪽 결산**(배지만 있고
포인트가 빠진 채 확정된 것처럼 보이는 행)이 남을 수 있다.

원칙적으로는 **활동 처리가 끝난 뒤 한 번 조립해 넣는 것**이 맞다. 다만 호출이 세 파일에
흩어져 있어 구조 변경 비용이 있다. 점진 병합을 유지하는 절충도 가능하다 — **구현자가 판단하고
근거를 완료 기록에 남긴다.**

### 범위 밖

- **프로필 페이지 개선** — R6이 결산 일부를 프로필로 보내는데 `user_activity_feed`에 활동
  참조가 없어(`recordFeedEvent`가 activity id를 받지 않는다) 도착하면 같은 활동의 이벤트가
  평면으로 나열된다. 알림함의 홍수가 프로필에서 재현되지만 **별도 후속 과제**로 분리한다.
- **「JAM 포인트」 도메인명** — R3는 유저 노출 문구에만 적용한다. PRD·데이터모델·서비스플랜의
  「JAM 포인트 시스템」이라는 시스템 명칭은 이번 범위가 아니다.
- **서비스플랜 과거 판본** 60여 건의 「JAM 포인트」 표기 — 아카이브라 손대지 않는다.

---

## 주요 의사결정 (착수 전 확정)

| 사안 | 결정 | 근거 |
|---|---|---|
| **#20 마일스톤 잔여량** | **넣지 않는다** | 티켓 [20260825_005](20260825_005_bug_미션-마일스톤-알림에-진행수치-노출.md)(CLOSED, 2026-08-25)가 "UX Writing 가이드상 불필요한 정보"로 판정해 제거한 결정을 존중한다. 케이스북 v3의 "48km 남았어요" 안은 **철회**한다. R12는 컬렉션 #10에만 적용하는 부분 규칙이 된다 |
| **결산 소식 백필** | **하지 않는다** | 전량 삭제 후 배치 1회 실행으로 상태 기반 소식만 되살리고, 결산은 다음 동기화부터 새 형식으로 쌓는다. 과거 보상 히스토리는 프로필 피드에 그대로 남아 있어 완전 소실이 아니다 |
| **삭제 후 「새 소식」 표시** | **노출한다** | `users.notifications_seen_at`을 건드리지 않아 재생성된 소식이 새 소식으로 뜨고 dot이 켜진다 |
| **R9** | **R11에 흡수** | "활동 2건 이상"은 R11("대상 2건 이상")의 특수형이다 |
| **R11 전면 채택의 대가** | **수용** | 미션 다중 마감(#21)은 묶으면 잔여량(12km·3일·2곳)이 사라지고 단위가 달라 합칠 수도 없다. **규칙을 하나로 유지하는 값**으로 받아들인다 |

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

**① 보상 획득 6종 → 활동 결산(`activity_recap`) 1종으로 재편**

- `notification_type` ENUM에 `activity_recap` 추가 (마이그레이션 105, **실행 안 함**)
- payload는 조각의 합이다 — `activity_ids` / `activity_badges` / `checkin_badges` /
  `item_badges` / `first_badge_id` / `points`
- 렌더러가 사다리를 판정한다 (RECAP_CASEBOOK A~F 표 그대로):
  첫 배지 → 활동 2건 이상(F2) → 희귀 승격(A3·E1) → 1종(A·B) → 2종 이상 총량(C·D)
- **포인트는 종류로 세지 않는다.** 문구를 `[머리] + [꼬리]` 두 조각으로 조립해
  꼬리 하나만 갈아 끼운다(`recapTail` / `recapTailPoints`) — 머리 10개 × 포인트 유무로
  문구가 두 배가 되는 것을 막는다

**결산 조립 시점 — 점진 병합 유지 (티켓 「판단 필요」에 대한 답)**

한 곳 조립이 아니라 **묶음 단위를 KST 하루로 잡는 것**으로 「반쪽 결산」을 해소했다.

- 보상 발급 지점 4곳 중 `points`·`drop-engine`은 **동기화 밖에서도 호출된다**(믹스·시뮬레이터).
  한 곳 조립으로 바꾸면 그 경로의 보상이 결산에서 빠지거나 수집기를 네 파일에 실어 나르는
  배관이 생긴다
- 하루 단위라 결산은 "확정된 행"이 아니라 **계속 자라는 행**이다. sync가 중간에 실패해도
  남는 것은 「덜 자란 결산」이고 다음 처리가 이어서 채운다
- 활동 2건 이상이면 자동으로 F2가 된다 — R11이 요구하는 묶음을 group_key가 그대로 구현한다
- `bumps_badge=false`라 병합이 dot을 다시 켜지 않는다. 중간 상태가 유저를 불러내지 않는다
- 같은 활동 재처리(F5)는 `appendKeys`의 중복 제거가 흡수한다

**R11 — 대상 2건 이상 묶음 (신규 묶음 문구 9개)**

- `foldTargets()` 하나로 접는다. 판정은 대상 단위로 두고 **마지막에 한 번만** 접는다
- 묶음 키는 **대상 집합의 지문**(`groupedTargetsKey` = FNV-1a 64비트)이다. 날짜 키로 바꾸면
  `once`가 지키던 "컬렉션당 평생 1회"가 사라져 같은 상태를 매일 다시 알린다. 집합이 그대로면
  키가 같아 `once`가 막고, 대상이 바뀌면 새 상태라 새 키가 된다. id를 그대로 이으면
  컬렉션 100개에서 키가 3KB를 넘어 `(user_id, group_key)` UNIQUE 인덱스 상한에 걸려 해시했다
- 착지는 목록 화면으로 승격. **#9 묶음만 인벤토리**(컬렉션이 아니라 배지를 세므로)

**컬렉션 #9·#10·#11 우선순위 + R12**

- 완성 가능(남은 칸 전부 보유) → **#11만** / 잔여 1칸 미보유 → **#10만** / 그 외 → **#9만**
- `badges` 조회에 `name`을 추가해 #10이 부족한 배지를 이름으로 부른다(R12)
- **#11은 인라인(동기화)과 배치가 `selectCompletableDrafts()` 같은 함수를 쓴다.** 두 경로가
  각자 키를 만들면 같은 상태가 두 행이 된다 — 같은 입력에서 같은 키가 나와야 `once`가 막는다

**그 외**

- #24 완료/미완료 분기(묶음은 하나라도 미완료면 축하 제외), #20 인라인 묶음(잔여량은 **넣지
  않는다** — 20260825_005 존중), #22는 R4로 보상 문구 제거 + B4 묶음
- #41은 **#40이 미해소면 만들지 않는다** — 배치가 `strava_disconnected`의 최신 생성 시각을
  읽어 마지막 동기화보다 뒤면 건너뛴다. 문구도 원인이 다른 경우 전용으로 교체
- ⑥ R15 사람 단위 묶음 + 하루 상한 「사람 2명」, 묶음 착지는 그 사람 프로필
- #27 맞팔 제거 — `api/follows`가 역방향 조회 없이 항상 `followed`를 만든다.
  ENUM 값은 DB에 남기고 TS 타입에서만 제외
- R2(따옴표) · R3(「포인트」) 전면 적용. `missions.rewardPointsLine`의 `1200P` 표기도 함께 정리
- **레거시 6종 렌더 경로는 남겼다** — 코드 배포와 전량 삭제 사이 구간에 과거 행이
  「새로운 소식이 도착했어요」로 보이지 않게 한다

### 변경된 파일
```
jam-web/supabase/migrations/105_notification_recap_type.sql          (신규, 실행 안 함)
jam-web/supabase/seed_20260827_notifications_reset.sql               (신규, 실행 안 함)
jam-web/src/types/database.ts
jam-web/src/lib/i18n/ko.ts
jam-web/src/lib/notifications/types.ts
jam-web/src/lib/notifications/recap.ts                               (신규)
jam-web/src/lib/notifications/index.ts
jam-web/src/lib/notifications/message.ts
jam-web/src/lib/notifications/href.ts
jam-web/src/lib/notifications/groupKey.ts
jam-web/src/lib/notifications/batch/shared.ts
jam-web/src/lib/notifications/batch/collections.ts
jam-web/src/lib/notifications/batch/missions.ts
jam-web/src/lib/notifications/batch/dropSpot.ts
jam-web/src/lib/notifications/batch/following.ts
jam-web/src/lib/notifications/batch/account.ts
jam-web/src/lib/strava/sync.ts
jam-web/src/lib/drop-engine/index.ts
jam-web/src/lib/badge-engine/index.ts
jam-web/src/lib/points/index.ts
jam-web/src/lib/missions/checker.ts
jam-web/src/app/api/follows/route.ts
jam-web/src/app/(main)/notifications/NotificationsClient.tsx
jam-web/src/lib/notifications/__tests__/message-href.test.ts
jam-web/src/lib/notifications/__tests__/batch-drafts.test.ts
jam-web/src/lib/notifications/__tests__/kst-group-key.test.ts
jam-web/src/lib/points/__tests__/point-notification.test.ts
```

### 테스트 결과
- [x] `npx vitest run` — 62 파일 / 595 통과
- [x] `npm run test:node` — 실데이터 대조 48/48 통과
- [x] `npx tsc --noEmit` — 오류 0
- [x] `npx eslint` (변경 파일 전수) — 오류·경고 0
- [x] `npx next build` — 성공
- [x] 케이스북 표를 **문구 그대로 단언**하는 테스트 추가 (A1·A2·A3·A4·A5·A6·A8·B·C1·C2·C3·D1·E1·E3·F2·F5)
- [x] R11 묶음 9개 중 컬렉션 3종·#18·#20·#21·#23·#24 문구·착지 단언
- [x] 묶음 키 지문의 결정론·길이 상한 단언

### UX Writing 검증
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

- [x] 용어 일관성: 「Strava 동기화」·「포인트」·「완료」·「완성」 고정 용어만 사용
- [x] 톤앤매너: 배지=신남(획득했어요) / 미완료 종료=담백(끝났어요) / ⑧=전문
- [x] 에러 메시지: #41을 [현상 3일째 새 활동이 없어요] → [원인·해결 Strava에 활동이
      기록됐는지 확인해보세요] 구조로 교체
- [x] 문장 규칙: 해요체, 조사 마커로 받침 자동 판정, 마침표는 두 문장일 때만
- [x] 표기 규칙: 1~3일은 하루·이틀·사흘, 금액은 `toLocaleString('ko-KR')`
- [x] **R1~R15 준수** (두 케이스북) — R9는 R11에 흡수, R12는 컬렉션 #10에만 적용

### 배포 정보
- 배포일:
- 환경: production
- 커밋:

### 주요 의사결정 / 핵심 메모

| 사안 | 결정 | 근거 |
|---|---|---|
| 결산 묶음 단위 | **KST 하루** (활동 1건이 아니라) | 활동 2건 이상 R11 승격을 group_key가 그대로 구현하고, 「반쪽 결산」이 「덜 자란 결산」이 된다 |
| 결산 조립 | **점진 병합 유지** | 포인트·드랍 지급이 동기화 밖에도 있어 한 곳 조립이 불가능하다 |
| R11 묶음 키 | **대상 집합의 지문(해시)** | 날짜 키로 바꾸면 `once`가 지키던 재발송 억제가 사라진다 |
| 희귀 헤드라인 | **활동배지에서만** 고른다 | 아이템 배지는 착지가 도감이 아니라 인벤토리 인스턴스(A6)라 등급 문구로 승격하면 갈 곳이 어긋난다. 기존 동작과도 같다 |
| #18 묶음 인원 | 지점별 집계의 **합** | 지점별로 이미 본인 제외 고유 인원이라 실사용에서 벌어질 폭이 작다. 케이스북 예시(3곳·31명)도 합 |
| A5(반복 체크인) + 포인트 | 포인트가 있으면 획득 문형으로 되돌린다 | 「N번째 체크인 했어요」에는 포인트 꼬리를 붙일 문법 자리가 없다. 반복 체크인은 새 배지를 발급하지 않아 실제로는 포인트가 붙지 않는다 |
| 레거시 6종 렌더 | **남긴다** | 코드 배포와 전량 삭제 사이 구간에 과거 행이 「새로운 소식이 도착했어요」로 보이는 것을 막는다 |

### 잔여 이슈
- **PRD/DATA_MODEL 문서 갱신이 남아 있다.** 두 케이스북의 「확정 시 갱신할 문서」 표 중
  코드·문구는 전부 반영했으나 `PRD/Notification/PRD.md`(§3 소식 표·§5·§6-1)와
  `DATA_MODEL.md`(§2 ENUM·§4-2 group_key)는 이 티켓의 「변경 대상」에 없어 손대지 않았다
- **프로필 페이지 개선**(범위 밖) — 결산 일부가 프로필로 착지하는데 `user_activity_feed`에
  활동 참조가 없어 같은 활동의 이벤트가 평면으로 나열된다
- DB 실행 2건이 **미실행 상태**다 — 마이그레이션 105 → 코드 배포 → 전량 삭제 → 배치 1회
