# 배지 공유 기능 — PRD

> 생성일: 2026-08-21
> 관련 티켓: `20260821_003`(UI 셸, CLOSED), `20260821_004`(이미지 생성·스트라바 연동·OS 공유시트, CLOSED),
> `20260903_1047`(공유/저장 2버튼 분리, CLOSED)

## 1. 배경

과거 드랍 공유 카드 기능(`ShareCardModal`, `/api/drops/{id}/share`)은 2026-08-17 메뉴 개편으로
삭제됐다(티켓 `20260731_004`). 이는 이후 제대로 재개발하기 위한 임시 삭제였고, 이 PRD는 그
정식 재구현 스펙을 정의한다.

## 2. 한 줄 요약

획득한 액티비티/POI/아이템 배지 상세 페이지에서 배지를 투명 PNG 이미지로 만들어 OS 공유시트
(또는 다운로드)로 내보내거나, 스마트폰 사진 앱(카메라 롤)에 바로 저장할 수 있게 한다.

## 3. 대상 화면

`jam-web/src/app/(main)/badges/[id]/page.tsx` — activity/POI/item 3개 배지 타입이 공통으로
쓰는 단일 라우트. TopNav 우측에 공유 아이콘 버튼을 두고 3타입 공통으로 동작한다.

## 4. 핵심 플로우

1. 유저가 배지 상세 페이지 TopNav의 공유 아이콘(`ios_share`)을 누른다.
2. 전체화면 바텀시트가 열리고, 생성된 공유 이미지를 미리보기로 보여준다.
3. 바텀시트 하단에 "공유"·"저장" 버튼을 나란히 제공한다(`20260903_1047`).
   - **공유**: Web Share API 지원 환경(주로 모바일)은 OS 공유시트 호출, 미지원 환경(주로
     데스크톱)은 PNG 다운로드 링크로 대체
   - **저장**: 배지 이미지를 기기에 바로 보관하는 액션. 플랫폼별 실제 동작이 다르다
     (표준 웹 API로는 Photos 라이브러리에 직접 쓸 수 없다는 제약 때문 — §8 참고)
     - iOS: 이미지를 새 탭 전체화면 뷰어로 열고, 사용자가 길게 눌러 "사진에 추가"를 선택하면
       저장된다(안내 토스트로 유도)
     - Android: `<a download>`로 저장 → MediaStore가 스캔해 갤러리/사진 앱에 자동 노출
     - 데스크톱: PNG 다운로드(사진 앱 개념이 없어 다운로드가 곧 저장)

## 5. 이미지 생성 방식

- **클라이언트 Canvas API**로 생성한다. 어드민의 `bakePreviewToBlob.ts`(`jam-web/src/app/admin/badges/`)
  가 쓰는 "화면에 보이는 대로 `canvas.toBlob()`으로 캡처" 패턴을 확장한다. 별도 서버사이드
  렌더링 파이프라인은 신설하지 않는다.
- 매 공유 시점에 그때그때 새로 생성한다. DB에 캐싱하지 않는다 — `user_activity_badges.share_card_url`
  (과거 공유 카드 기능이 남긴 죽은 필드)은 이번 기능에서 재사용하지 않는다.

## 6. 템플릿 스펙 (피그마 제공)

피그마: https://www.figma.com/design/UXcBEgFagmO5ARwH5F0mMW/제목-없음

캔버스 공통: 1080×1920, 투명 배경 PNG.

### 6.1 액티비티/체크인 배지 공용 템플릿 (node `1:55`)
- 상단 중앙: 배지 이미지 483×483 영역, 비율 유지 `contain`으로 축소 표시
- 하단: 텍스트 3세트 (흰색, `Inter Bold` 폰트, 폴백 `Arial Bold`, 라벨 ~20px / 값 ~51px)
  - `DISTANCE` — 예: `160.29km`
  - `PACE` — 예: `12:00 /km`
  - `TIME` — 예: `12h 59m 59s` (피그마 원본 라벨이 "PACE"로 중복 표기된 오타였으나 `TIME`으로 확정)
- 최하단: `Jam` 로고 174×72

### 6.2 아이템 배지 전용 템플릿 (node `1:60`)
- 배지 이미지 483×483 영역(`contain`) + `Jam` 로고만. 텍스트 없음.

## 7. 데이터 소스

- 배지 이미지: 기존 배지 레코드의 `image_url`
- 거리(`DISTANCE`): `user_activity_badges.triggered_by_distance_km`(DB에 이미 저장됨, 재조회 불필요)
- 페이스(`PACE`)·시간(`TIME`): **DB에 저장돼 있지 않다.** 배지 발급 시 저장되는 건
  `triggered_by_strava_id`, `triggered_by_activity_name`, `triggered_by_distance_km`,
  `triggered_by_activity_date` 뿐이라, 공유 시점에 `triggered_by_strava_id`로 스트라바 API를
  재조회해야 한다(`lib/strava/api.ts`의 `getActivityById` 재사용, `moving_time`/`elapsed_time`에서
  페이스·시간 계산).
- 체크인 배지는 `user_checkin_badge_earns`에서 트리거 활동 정보를 조회(반복 획득 구조, 최신 획득 기준).

## 8. 예외 처리 (`20260821_004` 재작업 반영)

- **공유는 본인 배지에서만 가능하다**: 타인의 배지 상세(`isOwnBadge=false`)에서는 공유 버튼
  자체를 노출하지 않는다(비활성화가 아니라 미노출). 서버(`share-data` API)도 요청자가 배지
  소유자 본인이 아니면 `forbidden`으로 거부해, 클라이언트의 버튼 숨김을 우회한 직접 호출도
  차단한다.
- **미획득 배지 / 스트라바 토큰 만료·연동 해제**: 두 경우 모두 클릭 전에 판별 가능하므로
  공유 버튼을 사전 비활성화한다. 스트라바 연동 여부는 `page.tsx`(서버 컴포넌트)가
  `strava_connections` 레코드 존재 여부로 미리 조회해 내려준다(액티비티/체크인 배지만 해당,
  아이템 배지는 스트라바 데이터가 없으므로 영향 없음). 비활성화된 버튼을 클릭하면 작은
  팝오버로 케이스별 안내 문구를 보여준다. 바텀시트는 이 두 케이스가 모두 사전 차단된 뒤에만
  열리므로, 시트 내부에는 "미획득" 상태 분기가 없다.
- **런타임 API 실패(네트워크·레이트리밋·5xx 등)**: 사전 판별 대상이 아니므로 바텀시트를 연
  뒤 실제 호출이 실패하면 시트 내부에서 에러 상태로 안내한다.
- **Web Share API 미지원**: PNG 다운로드 링크로 대체.
- **"저장" 버튼의 플랫폼 제약(`20260903_1047`)**: 웹 표준상 사용자 승인 없이 iOS Photos
  라이브러리에 직접 쓰는 API가 없다 — `<a download>`는 Files 앱에만 저장되고 사진 앱에는
  들어가지 않는다. 이 제약 때문에 "저장"은 플랫폼별로 실제 동작이 다르며(§4 참고), iOS는
  저장 완료 여부를 코드로 확인할 수 없어 "저장했어요"로 단정하지 않고 안내 토스트만 띄운다.

## 8-1. API 계약 (`20260821_004` 구현 반영)

### `GET /api/badges/{id}/share-data?u={username}`

activity/poi 타입 배지의 공유 이미지에 필요한 페이스·시간을 스트라바에서 재조회한다.
item 타입은 스트라바 데이터가 필요 없어 이 API를 호출하지 않는다(클라이언트에서 배지 이미지 +
로고만으로 바로 생성). `u`는 선택 파라미터이지만, 공유는 본인 배지에서만 가능하므로(위 8장)
`u`가 로그인한 본인이 아닌 다른 유저를 가리키면 `forbidden`으로 거부한다 — 클라이언트 버튼
숨김에만 의존하지 않고 서버에서도 소유자 검증을 한다(20260821_004 재작업).

**200 응답**
```json
{ "distanceKm": 160.29, "paceSecPerKm": 300, "elapsedTimeSec": 46799 }
```
`paceSecPerKm`/`elapsedTimeSec`는 트리거 활동을 특정할 수 없는 경우 `null`일 수 있다.

**에러 응답** (`{ "error": "<코드>" }` + 아래 상태 코드)

| 상태 | 코드 | 의미 |
|---|---|---|
| 401 | `unauthorized` | 로그인 필요 |
| 403 | `forbidden` | `u`가 로그인한 본인이 아닌 다른 유저를 가리킴(타인 배지 공유 시도) |
| 404 | `badge_not_found` | 배지 없음 |
| 400 | `not_activity_or_poi` | item 배지에 잘못 호출 |
| 404 | `not_earned` | 조회 대상 유저가 아직 획득하지 않음 |
| 404 | `no_strava_trigger` | 획득했으나 연결된 스트라바 활동/거리 스냅샷이 없음(레거시·어드민 발급) |
| 404 | `strava_disconnected` | 스트라바 연동 해제 또는 토큰 갱신 실패 |
| 502 | `strava_fetch_failed` | 스트라바 API 조회 실패(레이트리밋·5xx 등) |

## 9. 범위 밖 (이번 PRD에 포함하지 않음)

- 공유 이미지 서버사이드 캐싱/재사용
- 공유 통계·트래킹
- 배지 외 다른 컨텐츠(아이템북, 컬렉션 등)의 공유 기능 확장 — 필요해지면 별도 PRD

## 10. 관련 결정 이력

- `share_card_url` 필드 재사용 안 함 (신규 설계) — 클라이언트 생성·비캐싱 방식과 일관.
- 체크보드 미리보기 프레임은 서비스 전용 구현으로 시작, 다른 화면에서 재사용 수요가 생기면
  MODULAR 승격 재검토 (티켓 `20260821_003` 개선 리뷰 참고).
