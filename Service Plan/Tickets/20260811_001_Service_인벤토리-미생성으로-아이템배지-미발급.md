---
id: 20260811_001
category: Service
status: CLOSED
created: 2026-08-11
closed: 2026-08-11
---

# [Service] handle_new_user() 인벤토리 생성 누락 — 신규 유저 아이템 배지 미발급

## 배경 / 문제 정의
`kangwonc` 계정이 Strava 활동을 정상 동기화했고 액티비티 배지(러닝 배지 등)도 정상 발급됐는데, 아이템 배지(드랍 엔진)만 하나도 발급되지 않는다는 문의가 접수됨. `engine_decision_log`에 badge 엔진 로그는 남아 있는데 drop 엔진 로그는 전무했고, `user_drop_state`에도 이 유저 행이 없었음.

**근본 원인 조사 결과**: `inventory` 테이블에 이 유저 행 자체가 없었다.
- 신규 가입 시 `users` 행과 함께 `inventory` 행을 자동 생성하는 `handle_new_user()` 트리거가 있고, 레포 마이그레이션(`001_initial_schema_rerun.sql`)에는 `INSERT INTO public.inventory` 구문이 포함돼 있음.
- 하지만 **운영 DB에 실제로 걸려있던 함수 정의를 조회해보니 이 구문이 통째로 빠진 채 재정의**되어 있었음(레포 커밋 이력에 없는 변경 — 운영 DB에서 직접, 또는 추적 안 된 방식으로 함수가 재정의됨).
- `tryItemDrop()`([drop-engine/index.ts:497](../../../../jam-web/src/lib/drop-engine/index.ts))은 `inventory`가 없으면 `console.error`만 남기고 조용히 종료 — DB 로그(`engine_decision_log`)에는 아무것도 남기지 않아 어드민에서 감지가 불가능했음.

**영향 범위**: `users.created_at` 기준 조사 결과, 2026-08-04 이후 가입한 계정 중 3명이 인벤토리 없이 방치되어 있었음 — `9irrun`(2026-08-04), `kangwonc`(2026-08-09), `jeeni`(2026-08-10). 이들은 첫 동기화 시점부터 아이템 드랍이 전혀 발생하지 않고 있었음(`jeeni`는 아직 첫 동기화 자체를 안 해서 처리된 활동 없음).

## 상세 요구사항

### 서비스/코드베이스 관점
- 운영 DB `handle_new_user()` 함수에 인벤토리 생성 구문 복원
- 레포 마이그레이션 파일로 반영해 운영 DB·레포 정합성 복구
- 피해 3계정 `inventory` 행 백필
- 이미 처리된 활동 중 드랍이 누락된 건에 대해 정산 발급(아래 참고)

## 구현 계획
1. `handle_new_user()`를 `CREATE OR REPLACE FUNCTION`으로 재정의 — `INSERT INTO public.inventory (user_id) VALUES (NEW.id) ON CONFLICT (user_id) DO NOTHING;` 복원
2. `supabase/migrations/079_fix_handle_new_user_missing_inventory.sql` 신규 마이그레이션 파일 추가
3. `9irrun`, `kangwonc`, `jeeni` 3계정에 `inventory` 행 직접 insert (컬럼 기본값 `max_slots=50`, `used_slots=0` 그대로 사용)
4. 정산 발급: `strava_activities.normalized`(각 활동의 정규화 스냅샷)를 이용해 `sync.ts`의 `dropTargets` 선정 로직(첫 싱크=최신 1건, 이후 싱크=배치당 최신 3건, 어뷰징 속도 필터 적용)을 그대로 재현하는 1회성 스크립트(`scripts/settle-missed-item-drops.ts`, 실행 후 삭제)를 작성해 `tryItemDrop()`을 실제로 호출

---
## 완료 기록

### 구현 내용 요약
계획대로 전부 반영. 운영 DB 함수 복원, 마이그레이션 파일 추가, 3계정 인벤토리 백필, 누락 활동 정산 발급까지 완료.

### 변경된 파일
```
jam-web/supabase/migrations/079_fix_handle_new_user_missing_inventory.sql  — 신규
(DB) public.handle_new_user() 함수 재정의 (jam-prod)
(DB) inventory 테이블에 9irrun/kangwonc/jeeni 3행 insert (jam-prod)
(DB) inventory_items 정산 발급 6건 (jam-prod) — 아래 참고
```

### 테스트 결과
- [x] 함수 복원 후 `pg_get_functiondef`로 인벤토리 insert 포함 확인
- [x] 3계정 `inventory` 행 생성 확인
- [x] 정산 스크립트 실행 로그로 드랍 발생 확인, `inventory_items` 조회로 최종 검증
  - `kangwonc`: 활동 3건(첫 싱크 1건 + 이후 싱크 2건) → 아이템 배지 4개 발급(바짝 마른 목캔디, 이슬 맺힌 나뭇잎[보너스], 장비병 초기 진단서, 초경량 카본 파편)
  - `9irrun`: 활동 1건(첫 싱크) → 아이템 배지 2개 발급(기름 튄 회식 영수증, 샤워실 샴푸 냄새[보너스])
  - `jeeni`: 처리된 활동 없음(아직 미동기화) — 인벤토리만 백필, 정산 대상 없음. 다음 동기화부터 정상 동작

### 배포 정보
- 배포일: 2026-08-11
- 환경: production (jam-prod, project_id: ceehnkzdbecxwzxrhhns) — DB 함수/데이터 변경은 즉시 적용, 마이그레이션 파일은 커밋으로 레포에 반영
- 커밋: (git push 시 기록)

### 주요 의사결정 / 핵심 메모
- **정산 시 실제 드랍 엔진 재호출 방식 채택**: 정산 시점에 "무엇이 나왔어야 했는가"를 역산하는 대신, 원래 싱크 배치 구조(첫 싱크=1건, 이후=최대 3건)를 그대로 재현해 `tryItemDrop()`을 실제로 호출하는 방식을 택함 — 드랍 결과가 확률적(rollRarityV2 등)이라 "정답"을 역산할 수 없고, 엔진의 실제 로직(세계관/아이템북 선택, pity, 온보딩 게이트 등)을 그대로 타는 것이 가장 정합적이라 판단.
- **정산 스크립트는 1회성으로 실행 후 삭제**: 하드코딩된 특정 유저 ID 대상 스크립트라 재사용성이 없어 레포에 남기지 않음(scripts/ 폴더의 기존 재사용 가능한 스크립트들과 성격이 다름).
- **운영 DB 함수가 레포와 어긋난 근본 원인은 특정하지 못함**: 언제/어떻게 `handle_new_user()`가 인벤토리 구문 없이 재정의됐는지는 `schema_migrations` 추적 기록이 4건뿐이라 규명 불가. 재발 방지를 위해 마이그레이션 파일로 명시 반영.

### 잔여 이슈 — 후속 전수 점검으로 해소 (2026-08-11 추가)

**전수 점검 결과**: `public` 스키마의 함수 7개(`activate_theme_preset`, `assign_random_serial`, `award_points`, `generate_serial_prefix`, `handle_new_user`, `handle_updated_at`, `pickup_drop`) 전체와 트리거 6개(`trg_on_auth_user_created`, `trg_random_serial`, `trg_serial_prefix`, `trg_strava_connections_updated_at`, `trg_trades_updated_at`, `trg_users_updated_at`, 그 외 Supabase 내장 storage/realtime 트리거)를 `pg_get_functiondef`/`pg_trigger`로 조회해 레포 마이그레이션 최신 정의와 1:1 대조. **`handle_new_user()` 외에는 드리프트 없음** — 전부 레포 정의와 일치, 모든 트리거 `tgenabled = 'O'`(정상 활성).

**근본 원인 규명**: `handle_new_user()`는 레포 마이그레이션 기준 `001_initial_schema*.sql`에서만 정의됐고(당시 컬럼명 `display_name`) 이후 어떤 마이그레이션 파일도 이 함수를 건드리지 않았다. 그런데 `020_username_onboarding.sql`(2026-07-14 커밋)이 `users.display_name` → `username`으로 컬럼명을 바꾸면서, 이 함수를 갱신하는 마이그레이션은 작성되지 않았다 — 즉 원래대로면 020 적용 직후부터 모든 신규 가입이 "존재하지 않는 컬럼(display_name)" 오류로 트리거 단계에서 실패했어야 함. 실제로는 020 이후에도 가입이 정상 동작했던 것으로 보아, **누군가 마이그레이션 파일 없이 운영 DB에서 직접 `handle_new_user()`를 `username` 컬럼 기준으로 재정의(핫픽스)했고, 이 과정에서 인벤토리 생성 구문이 함께 빠졌다.** 이 수정은 어떤 마이그레이션 파일에도, `schema_migrations` 추적 테이블(4건만 기록)에도 남지 않아 정확한 시점·작업자를 특정할 수 없음 — 이 자체가 문제의 핵심(추적 안 되는 운영 DB 직접 수정).

**근원 조치**: `04_PROJECT_SPEC.md`의 "절대 하지 마" 목록에 규칙을 추가/구체화 — 테이블 스키마뿐 아니라 **함수·트리거도 마이그레이션 파일 없이 운영 DB에서 직접 재정의 금지**를 명시. `execute_sql`은 조회·데이터 백필 전용으로 한정하고, 함수/트리거 등 로직 정의 변경은 반드시 마이그레이션 파일 작성 → 적용 → 커밋을 한 세트로 수행하도록 규칙화.

**변경된 파일 (추가)**
```
Service Plan/Specs/PRD/04_PROJECT_SPEC.md  — "절대 하지 마" 목록에 함수/트리거 직접 수정 금지 규칙 추가
```
