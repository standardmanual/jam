---
id: 20260815_024
category: UI
status: CLOSED
created: 2026-08-15
closed: 2026-08-15
---

# [UI] 미션 상황보기 Figma 리뉴얼 재구현

## 배경 / 문제 정의

티켓 20260815_021로 미션 상황보기 페이지를 리뉴얼했으나, 해당 구현이 담긴
워크트리 브랜치(`claude/mission-detail-layout-2394fe`)가 삭제되면서 staging에
반영되지 않은 상태. staging의 `MissionStatusClient.tsx`는 여전히 기존 Card 기반
텍스트 목록 형태.

동일 Figma 스펙(06_미션 상황)으로 재구현한다. 티켓 021의 구현 내용·의사결정을 그대로 계승.

## 상세 요구사항

### UI/UX 관점 (Figma 스펙 06_미션 상황 기준)

**헤더 정보 바** (페이지 상단):
- 좌: "랭킹 · 참가자 N명" (13px, #B2B2B2)
- 우: "미션: [목표값]" (13px, #B2B2B2)

**TOP 3 포디엄** (ranking 타입 전용):
- 배열: 2위(좌) - 1위(중앙, 최장바) - 3위(우)
- 아바타 원형 + 유저명 + 진행값 (포디엄 상단)
- 바 높이: 1위=130px, 2위=90px, 3위=60px
- 바 컬러: 1위=#0D3320(border #56C985), 2위=#1A3A2A(border #4CAF7D), 3위=#1E2E1E(border #5A7A5A)
- 아바타: 1위=60px(border #56C985), 2위/3위=48px(border 각각 #4CAF7D/#5A7A5A)
- 랭킹·진행값 컬러: 1위=#56C985, 2위=#4CAF7D, 3위=#8AAA8A

**전체 순위 목록** (4위 이후):
- 헤더: "전체 순위" (13px, #B2B2B2) + separator(#2A2A2A)
- 각 행: 순위(14px bold, #B2B2B2) + 아바타(36px, 원형, border #3A5A3A) + 유저명(14px, #FFF) + 진행값(14px bold, #FFF)
- 행 하단: 6px 프로그레스 바 (인덴트 32px, 배경 #1E1E1E, 그라디언트)
  - 그라디언트 패턴: 상위일수록 밝은 그린(4위=#00B259→#1AD973, 이하 점진 어두워짐)
- 행 구분선: #1E1E1E 1px

**나의 순위 카드** (하단 항상 고정):
- 배경 #0D2A1A, border 1px solid #2E7D52, border-radius 12px
- 순위 컬러: #56C985
- 유저명(bold, #FFF) + 서브텍스트 "[달성값] 달성" (11px, #B2B2B2)
- 진행값: #56C985
- 진행 바: 배경 #1A3A2A, fill: linear-gradient(#00CC66 → #33E580)

### 서비스/코드베이스 관점

- **파일 경로:**
  - `jam-web/src/app/(main)/missions/[id]/status/MissionStatusClient.tsx` — 전면 재작성
  - `jam-web/src/app/(main)/missions/[id]/status/page.tsx` — goalLabel prop 추가 여부 검토
- **미션 타입별 분기:**
  - `status_display_type === 'ranking'` → 포디엄 + 전체 순위 + 나의 순위 카드
  - `status_display_type === 'achievement'` → 기존 Card 기반 AchievementRow 유지
  - `status_display_type === 'individual'` → 기존 Card 기반 개인형 유지
- **API:** `/api/missions/[id]/status` 기존 그대로 (변경 없음)
- **브랜치:** 기존 main 브랜치 기준 `claude/jamwork-20260815_024-*` 신규 생성

### 참고 — 티켓 021 의사결정 계승

- 포디엄 3명 미만일 때: "—" 플레이스홀더로 빈 슬롯 처리
- 나의 순위 카드: Figma 스펙대로 목록 내 포함 여부 무관 중복 노출
- 프로그레스 바 너비: 1위 progressValue를 100%로 삼아 상대 비율 계산
- `entries` 없고 `me`도 없으면 "아직 참가자가 없어요" 빈 상태 처리
- `PODIUM_CONFIG` 상수로 1·2·3위 색상·크기 관리

## 구현 계획

1. `MissionStatusClient.tsx` 전면 재작성 (021과 동일 스펙)
2. `page.tsx` — 이미 `condition_json` / `goalLabel` 처리 중인지 확인 후 필요시 보완
3. main 기준 review 브랜치 push → staging 머지

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
main 기준으로 `MissionStatusClient.tsx` 전면 재작성 및 `page.tsx` goalLabel 추가.
Figma 스펙 06_미션 상황 기반, 티켓 021 의사결정 계승.

### 변경된 파일
- `jam-web/src/app/(main)/missions/[id]/status/MissionStatusClient.tsx` — 전면 재작성
- `jam-web/src/app/(main)/missions/[id]/status/page.tsx` — goalLabel 추출 로직 추가

### 테스트 결과
- [x] ranking 타입: 포디엄(2위-1위-3위 배열) + 4위~ 목록 + 나의 순위 카드 구현
- [x] 나의 순위 카드 하단 항상 고정 (Figma 스펙 준수)
- [x] achievement/individual 타입 기존 Card 기반 레이아웃 유지
- [x] TypeScript 오류 없음 (기존 reasons.test.ts jest 타입 오류는 사전 존재 이슈)

### UX Writing 검증
- [x] 용어 일관성: "랭킹", "참가자", "전체 순위", "달성", "미션" 등 고정 용어 사용
- [x] 톤앤매너: 13px #B2B2B2 서브 텍스트, Figma 색상 스펙 준수

### 배포 정보
- 배포일: 2026-08-15
- 환경: review 브랜치 (main 머지 후 staging → 배포)
- 커밋: 67bd6a4

### 주요 의사결정 / 핵심 메모
- staging 브랜치에는 이미 9c1786b 커밋으로 Figma 리뉴얼이 적용되어 있었음
  (티켓에서 "staging 미반영"이라 했으나 실제로는 적용됨)
- main 브랜치에는 여전히 구 Card 기반 구현 → 이번 티켓으로 main에 반영
- 스펙 vs staging 차이 수정: 순위 고정폭 24→20px, 프로그레스 바 paddingLeft 48→32px
- PODIUM_CONFIG valueColor 필드명 명시 (staging은 progressColor로 명명했으나 스펙 용어 valueColor 사용)

### 후속 수정 (커밋 2846d6e)

progressive-reviewer 개선 제안 3건 + Impeccable 훅 경고 처리:

- **Impeccable `[layout-transition]`**: `transition: width` → `transform: scaleX()` + `transformOrigin: left` (compositor-only 애니메이션, 레이아웃 스래시 제거)
- **`getRankGradient` 수렴 방지**: factor 최솟값 0→0.15, 감소율 0.08→0.05 (rank 17+ 단색 수렴 문제 해결)
- **하드코딩 문자열 → i18n**: `"미션: {goal}"` → `statusMissionGoal`, `"전체 순위"` → `statusAllRanks`, `"달성"` → `d.missions.achieved` (ko.ts에 2개 키 추가)
- **`isMeInEntries` 일관화**: `data?.type !== 'individual' && data?.me?.userId === userId`로 단순화, ranking 섹션에도 동일하게 적용

### 잔여 이슈
- 없음
