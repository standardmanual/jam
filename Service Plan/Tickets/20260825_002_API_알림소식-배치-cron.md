---
id: 20260825_002
category: API
status: CLOSED
created: 2026-08-25
closed: 2026-08-25
---

# [API] 알림(소식) 배치 cron — T2 11종

## 배경 / 문제 정의

[019](20260824_019_Infra_알림소식-스키마-인라인생성.md)가 스키마와 T1 인라인 생성을,
[021](20260824_021_UI_알림함-화면-TopNav-진입점.md)이 화면을 완성해 **둘 다 프로덕션에 배포**됐다.

지금 알림함은 T1이 만드는 소식만 담는다. **PRD §4의 T2 13종이 비어 있어** 알림함이 아직
제 모습을 갖추지 못했다. 이 티켓이 마지막 조각이다.

> **읽기 전 필독**
> - [PRD.md](../Specs/PRD/Notification/PRD.md) — §3 소식 정의, **§4 생성 트리거**, §4-1 배치 스케줄
> - [DATA_MODEL.md](../Specs/PRD/Notification/DATA_MODEL.md) — §4 묶음 모델, §6 payload

## 상세 요구사항

### 1. 단일 cron으로 통합

**신규 cron은 1개만 만든다.** 소식 종류마다 cron을 만들면 Vercel cron 예산을 소모하고
(과거 [20260723_004](20260723_004_Admin_배포가-안-되던-근본-원인Vercel-Hobby-플랜-cron-빈도-제.md)에서
cron 빈도 제한이 배포 실패 원인이었다) 실행 시각도 제각각이 된다.

```
경로     : /api/cron/notifications
스케줄   : 0 9 * * *   (UTC 09:00 = KST 18:00)
```

**KST 18:00인 이유** — [01_PRD.md](../Specs/PRD/01_PRD.md) §2의 핵심 시나리오가
"운동 후 귀가해서 저녁에 확인"이다. 저녁 접속 시점에 소식이 갓 만들어져 있는 게 가장 신선하다.
기존 cron 2개(UTC 00:00 `poi-cleanup`, UTC 18:00 `ambient-drop-monitor`)와도 겹치지 않는다.

`CRON_SECRET` Bearer 인증은 기존 cron 라우트와 동일하게 적용한다.

### 2. 생성 대상 — T2 11종

> **2026-08-25 2차 수정**: 원래 13종이었으나 지역 기반 2종(#32 `following_nearby_drop`,
> #34 `nearby_drops`)을 스펙에서 제거해 11종이 됐다. 사유는 아래 「완료 기록 → 주요 의사결정 7」.

| # | type | 판정 |
|---|---|---|
| 9 | `collection_slottable` | 미장착 소속 아이템 배지 보유 |
| 10 | `collection_near_complete` | 잔여 1칸 도달 |
| 18 | `drop_spot_active` | 내 활성 드랍이 있는 POI의 주간 고유 열람 인원 |
| 21 | `mission_deadline` | `ends_at` D-2 & 참가했으나 미완료 |
| 23 | `mission_rank_up` | `status_display_type=ranking`, **상승 시만** |
| 24 | `mission_ended` | `ends_at` 경과 |
| 29 | `following_rare_badge` | 팔로잉의 **legend/mythic만** |
| 30 | `following_collection_complete` | 팔로잉의 컬렉션 완성 |
| 31 | `following_mission_complete` | 팔로잉의 미션 완료 (묶음) |
| 41 | `sync_stalled` | 마지막 sync가 `SYNC_STALLED_DAYS` 경과 |
| 42 | `inventory_full` | 잔여 슬롯 부족 |

### 3. ⚠️ 반드시 지킬 계약 — 019·021 리뷰에서 누적된 것

이 절이 이 티켓의 핵심이다. **어기면 조용히 깨지는** 것들만 모았다.

#### 3-1. payload 키를 `types.ts` 계약 그대로 쓸 것

021이 각 type의 payload 키를 `src/lib/notifications/types.ts`에 **선반영해 뒀다.**
렌더러는 값이 빈 슬롯을 통째로 버리므로, 키를 하나라도 틀리면
**`''에 넣을 수 있는 아이템 배지가 3개 있어요`** 처럼 빈 따옴표만 남은 문장이 나간다.

| type | 필수 키 |
|---|---|
| `collection_slottable` | `item_book_id`, `book_name`, `count` |
| `collection_near_complete` | `item_book_id`, `book_name` |
| `drop_spot_active` | `poi_id`, `poi_name`, `visitor_count` |
| `mission_deadline` | `mission_id`, `mission_title`, `days`, `remaining`, `unit` |
| `mission_rank_up` | `mission_id`, `mission_title`, `rank` |
| `mission_ended` | `mission_id`, `mission_title` |
| `following_rare_badge` | `badge_id`, `badge_name`, `rarity` |
| `following_collection_complete` | `item_book_id`, `book_name` |
| `following_mission_complete` | `mission_id`, `mission_title`, `actor_ids` |
| `sync_stalled` | `days` |
| `inventory_full` | `max_slots`, `used_slots` |

021이 넣어둔 `missingMessageSlots()` 경고(`console.warn('[notifications] 빈 슬롯', …)`)가
서버 로그에 찍히는지 배포 직후 확인할 것.

#### 3-2. 시각을 한 번만 캡처해 모든 빌더에 넘길 것

`dailyGroupKey`·`sixHourGroupKey`는 `at`을 생략하면 **각각 `new Date()`를 개별 평가**한다.
배치가 KST 자정을 걸쳐 길게 돌면 같은 배치가 두 날짜 키로 갈린다.

→ **배치 시작 시각을 한 번 캡처해 모든 `group_key` 빌더와 날짜 계산에 넘긴다.**
`poi_views` 집계의 "KST 7일 전"도 반드시 같은 `kst.ts`를 경유할 것.

#### 3-3. 숫자 슬롯이 0이면 소식을 만들지 말 것

렌더러의 `num()`은 키가 없으면 0을 돌려준다. `0일째 활동을 못 불러오고 있어요`,
`0명이 다녀갔어요` 같은 문장이 그대로 나간다. ①은 배열 길이라 회귀 테스트로 잠겨 있지만
**T2의 숫자 슬롯(#18·#21·#23·#41·#42)은 열려 있다.**

→ **0이면 그 소식을 만들지 않는다**를 배치 쪽에서 보장하고 테스트로 잠글 것.

#### 3-4. `warning.ts`의 판정 상수를 import할 것

021이 배치와 공유하려고 export해 뒀다. 값을 복사하지 말 것.

```
SYNC_STALLED_DAYS             = 3    (#41)
INVENTORY_LOW_SLOTS_THRESHOLD = 3    (#42)
```

⚠️ **#42는 문구와 임계값이 어긋난다.** 상수는 "잔여 3칸 이하"인데 문구는
`인벤토리가 꽉 찼어요. 50개까지만 보관할 수 있어서 픽업이 안 될 수 있어요`다.
잔여 3칸인 유저에게 사실과 다른 문장이 간다. **둘 중 하나로 정할 것** —
(가) 생성 조건을 잔여 0칸으로 좁힌다 (나) 문구를 `거의 찼어요` + 잔여 칸 슬롯으로 바꾼다.
판단 근거를 완료 기록에 남길 것.

#### 3-5. ⑥ 팔로잉 활동은 하루 상한 2건

PRD §3 ⑥이 규정한다. 상한이 없으면 알림함이 남의 소식으로 도배되고 내 보상 소식이 묻힌다.
**희귀도 단독으로 선별**한다(PRD §9 — 친밀도 지표가 생기면 재검토).

#### 3-6. 팬아웃 비용

`createNotification()`이 호출마다 `createServiceClient()`를 새로 만들고 RPC를 1회 왕복한다.
T1(호출 14곳, 건당 1~3회)에는 충분하지만 **배치는 수백~수천 왕복**이 된다.
클라이언트 주입 인자나 `p_user_ids UUID[]` 벌크 RPC를 검토할 것. DB 변경이 필요하면
**SQL 파일 작성까지만** 하고 실행은 오케스트레이터가 처리한다.

#### 3-7. 멱등성 — 재실행해도 중복이 생기면 안 된다

cron은 재시도될 수 있다. `group_key` + `once` 모드로 DB가 막게 한다.

PRD §4-2 표에서 #10·#23·#24는 현재 `group_key` NULL로 분류돼 있는데, **NULL이면 중복 방지
수단이 DB에 없어** 배치가 매번 SELECT로 존재를 확인해야 하고 재시도 시 중복이 샌다.

→ `once` 모드에 키를 주는 방향을 검토할 것. 예: `collection_near_complete:{book_id}`,
`mission_rank_up:{mission_id}:{KST일자}`, `mission_ended:{mission_id}`.
채택 시 **DATA_MODEL §4-2 표를 함께 갱신**한다.

#### 3-8. 소식 생성 실패가 배치 전체를 죽이면 안 된다

한 유저의 소식 생성이 실패해도 나머지 유저는 처리돼야 한다. 다만 **조용히 삼키지 말 것** —
`console.error`로 남긴다(이 프로젝트의 기존 원칙, `createNotification()`이 이미 그렇게 동작).

### 4. 문구는 PRD §3 표 그대로

021이 렌더러를 이미 만들었다. 이 티켓은 **payload만 정확히 채우면** 문구는 자동으로 나온다.
새 문구를 만들지 말 것.

## 이 티켓의 범위 밖

- 문구 렌더러·착지점 계산 (021 완료)
- 스키마·T1 인라인 생성 (019 완료)
- `/notifications` 화면 (021 완료)

## 구현 계획

1. `src/lib/notifications/batch/` — 11종 판정 로직을 종류별 모듈로 분리 *(2차 수정 후)*
2. `src/app/api/cron/notifications/route.ts` — `CRON_SECRET` 인증 + 배치 오케스트레이션
3. `vercel.json`에 cron 추가 (2개 → 3개)
4. 단위 테스트 — 특히 **0 렌더 방지**, **KST 경계**, **멱등성**, **⑥ 하루 상한 2건**
5. `tsc` / `vitest` / `eslint` / `next build` 통과

### ⚠️ 주의 — 기존 사고 이력

- **`npm run db:types`를 그대로 돌리지 말 것.** Supabase CLI가 없어 셸 리다이렉트가 파일을
  0바이트로 날린 뒤 명령이 실패한다 (2026-08-24 실제 발생)
- **DB DDL은 SQL 파일 작성까지만.** 실행은 승인 후 오케스트레이터가 한다
- 마이그레이션 채번 시 실제 최대 번호를 확인할 것 (094가 중복된 이력이 있다)

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

`/api/cron/notifications` 1개(`0 9 * * *`, UTC 09:00 = KST 18:00)로 **T2 11종 전부**를 생성한다.
판정은 `src/lib/notifications/batch/` 아래 7개 단계 모듈로 나눴고, **각 단계는 "순수 선별 함수 +
DB 로더" 2층 구조**다. 순수 함수만 테스트하면 0 렌더·상한·멱등성이 전부 잠긴다.

> **2026-08-25 3차 보강** — 판정은 그대로 두고 **조회 계층과 관측성**만 손봤다.
> ① 페이징 조회에 안정 정렬 강제 ② 단계 실패 시 non-2xx ③ `scanned`·`drafts`·`durationMs`
> 지표 ④ `.in()` 청크 분할. 상세는 「주요 의사결정 8」.
>
> **2026-08-25 2차 수정** — 1차 구현의 13종 중 지역 기반 2종(#32·#34)을 들어냈다.
> ⑦ 발견 카테고리는 #34가 유일해 카테고리째 사라졌다(소식 28종 → 26종, T2 13종 → 11종).
> `discovery.ts` 모듈과 `nearby-drops` 단계를 삭제했고 **알림 경로의 역지오코딩 의존이
> 완전히 끊겼다.** 상세는 「주요 의사결정 7」.

| 단계 | 소식 |
|---|---|
| `collections` | 9 장착 가능 · 10 완성 임박 |
| `drop-spot` | 18 내 드랍 지점 활성 |
| `missions` | 21 마감 임박 · 24 종료 결과 |
| `mission-rank` | 23 순위 상승 |
| `following` | 29 · 30 · 31 (하루 상한 2건) |
| `sync-stalled` | 41 동기화 지연 |
| `inventory-full` | 42 인벤토리 포화 |

**§3 「반드시 지킬 계약」 대응**

- **§3-1 payload 키** — `NotificationDraft`를 `NotificationPayloadMap`에서 파생한 판별 유니온으로
  정의해 **키를 틀리면 `tsc`가 깨진다.** 실제로 `poi_name` → `poiName`으로 바꿔 컴파일 실패를
  확인했다. 여기에 더해 테스트가 모든 초안을 `missingMessageSlots()`에 통과시켜
  **버려진 슬롯이 0개**이고 평문에 `''`가 없음을 검증한다.
- **§3-2 시각 캡처** — `createBatchContext(startedAt)`가 `startedAt`·`today`를 한 번 만들고 7개
  단계가 그 값만 쓴다. 날짜 계산은 전부 `kst.ts` 경유(`kstDateString`·`kstDateOffset`·
  `kstWeekKey`). #18의 "KST 7일 전"도 `kstDateOffset`으로 계산해 `viewed_on`(KST DATE)과 맞췄다.
- **§3-3 0 렌더 방지** — #18(visitor_count) · #21(remaining) · #23(rank) ·
  #41(days) · #42(max_slots) 전부 0이면 만들지 않고, 각각 회귀 테스트가 있다.
  #9의 `count`도 같은 규칙으로 막았다.
- **§3-4 상수 import** — `warning.ts`의 `SYNC_STALLED_DAYS`·`INVENTORY_LOW_SLOTS_THRESHOLD`를
  import한다. 값 복사 없음(테스트가 상수를 참조해 임계값이 바뀌면 테스트도 같이 움직인다).
- **§3-5 #42** — 아래 「주요 의사결정」 참고.
- **§3-6 팬아웃** — `createNotification()`에 선택 인자 `client`를 추가해 배치가
  service_role 클라이언트를 **한 번 만들어 재사용**한다(T1 호출부는 생략 → 기존 동작 그대로).
  RPC 왕복은 동시 8개로 파이프라인화했다. 벌크 RPC는 채택하지 않았다 — 이유는 아래.
- **§3-7 멱등성** — **11종 전부 `group_key` + `once`.** `merge`를 쓰지 않은 이유까지 포함해
  DATA_MODEL §4-3을 신설했다.
- **§3-8 실패 격리** — `runStep()`이 단계를 격리하고, `emitDrafts()`가 소식 1건 단위로 격리한다.
  둘 다 `console.error`를 남긴다.

### 변경된 파일
```
신규
  jam-web/src/app/api/cron/notifications/route.ts
  jam-web/src/lib/notifications/batch/shared.ts
  jam-web/src/lib/notifications/batch/collections.ts      (#9 #10)
  jam-web/src/lib/notifications/batch/dropSpot.ts         (#18)
  jam-web/src/lib/notifications/batch/missions.ts         (#21 #23 #24)
  jam-web/src/lib/notifications/batch/following.ts        (#29 #30 #31)
  jam-web/src/lib/notifications/batch/account.ts          (#41 #42)
  jam-web/src/lib/notifications/batch/index.ts            (오케스트레이션)
  jam-web/src/lib/notifications/__tests__/batch-drafts.test.ts
  jam-web/src/lib/missions/ranking.ts                     (순위 정렬 단일 진실)
  jam-web/supabase/migrations/099_mission_rank_snapshots.sql   ⚠️ 미실행

수정
  jam-web/vercel.json                                     (cron 2 → 3)
  jam-web/src/lib/notifications/index.ts                  (client 주입 인자)
  jam-web/src/lib/missions/checker.ts                     (MISSION_PROGRESS_UNIT·getTarget export)
  jam-web/src/app/api/missions/[id]/status/route.ts       (공용 compareMissionRank 사용)
  jam-web/src/types/database.ts                           (mission_rank_snapshots)
  jam-web/src/lib/notifications/types.ts                  (payload 계약 2종 제거)
  jam-web/src/types/database.ts                           (NotificationType 26종 + ENUM 예약값 주석)
  jam-web/src/lib/notifications/message.ts                (#32·#34 문구 분기 제거)
  jam-web/src/lib/notifications/href.ts                   (#32·#34 착지점 제거)
  jam-web/src/lib/i18n/ko.ts                              (문구 2건·⑦ 섹션 제거)
  jam-web/src/app/(main)/notifications/NotificationsClient.tsx  (아이콘 분기 제거)
  Service Plan/Specs/PRD/Notification/PRD.md              (§3 ⑦ 카테고리 삭제·§7 제거 사유)
  Service Plan/Specs/PRD/Notification/DATA_MODEL.md       (§2 ENUM 예약값, §4-3, 2026-08-25 확정)

2차 수정에서 삭제
  jam-web/src/lib/notifications/batch/discovery.ts        (#34 — 모듈 통째 삭제)

3차 보강 (신규)
  jam-web/src/lib/notifications/__tests__/batch-query.test.ts        (정렬·청크·지표)
  jam-web/src/app/api/cron/notifications/__tests__/route.test.ts     (응답 계약)

3차 보강 (수정)
  jam-web/src/lib/notifications/batch/shared.ts           (fetchAllRows 정렬 강제·fetchAllRowsIn·지표)
  jam-web/src/lib/notifications/batch/index.ts            (StepOutput·scanned/drafts 합계·45초 경고)
  jam-web/src/lib/notifications/batch/collections.ts      (정렬·청크·scanned)
  jam-web/src/lib/notifications/batch/dropSpot.ts         (정렬·청크·scanned)
  jam-web/src/lib/notifications/batch/missions.ts         (정렬·scanned)
  jam-web/src/lib/notifications/batch/following.ts        (정렬·청크·scanned)
  jam-web/src/lib/notifications/batch/account.ts          (정렬·청크·scanned)
  jam-web/src/app/api/cron/notifications/route.ts         (단계 실패 시 500·요약 로그 지표)
```

### 테스트 결과
- [x] `vitest run src/lib` — 18 파일 **274 케이스 통과** (2차 수정 후. #34 테스트 3건 제거)
- [x] `tsx --test src/lib/missions/__tests__/*.ts` — 18/18 통과
- [x] `tsc --noEmit` 통과
- [x] `eslint src/lib/notifications src/lib/missions src/app/api/cron/notifications` — 경고 0
- [x] `next build` 통과 · `/api/cron/notifications` 라우트 등록 확인
- [x] **payload 키 오타 탐지 실증** — `poi_name`을 `poiName`으로 바꾸자 `tsc`가 TS2322로 실패
- [x] **2차 수정 실측** — 스텁 DB로 `runNotificationBatch()`를 실제 실행해
      **단계 7개**(`collections, drop-spot, missions, mission-rank, following, sync-stalled,
      inventory-full`), `nearby-drops` 부재, **외부 `fetch` 호출 0회**, `users` 테이블 조회 0회 확인
- [x] **2차 수정 임포트 그래프 추적** — cron 라우트에서 도달 가능한 24개 모듈 중
      `reverse-geocode` 도달 경로 없음

신규 테스트가 잠근 것: 0 렌더 방지(5종) · KST 자정 경계 · 주 단위 키 · 멱등성(전 초안이
`group_key` + `once`) · ⑥ 하루 상한 2건 · #23 첫 관측은 기준선만 · #42 잔여 0칸 · payload 계약.

**3차 보강 검증** (2026-08-25)

- [x] `vitest run src/lib src/app` — 21 파일 **305 케이스 통과** (신규 20건)
- [x] `tsx src/lib/missions/__tests__/checker-logic.test.ts` — 18/18 통과
- [x] `tsc --noEmit` · `eslint src/lib/notifications src/app/api/cron/notifications` — 경고 0
- [x] `next build` 통과 · `/api/cron/notifications` 라우트 등록 확인
- [x] **테스트가 실제로 잡는지 실증(3건)** — 일부러 되돌려 보고 확인했다
      · `strava_connections` 정렬 키를 비우자 「모든 페이징 조회에 정렬이 붙는다」 실패
      · `inventory_items`를 청크 분할 없이 되돌리자 청크 테스트 2건 실패
      · 라우트를 200으로 되돌리자 「단계 실패 시 500」 실패

3차 보강 테스트가 잠근 것: 페이지 경계 중복·누락(정렬 없는 페이징이 실제로 깨지는 것까지
재현) · 복합 PK 전순서 · `.in()` 청크 경계(200 / 201 / 0 / 중복 값) · 7개 로더 전수 정렬 검사 ·
단계 실패 시 500 · 요약 로그의 `scanned`·`drafts` 노출.

### UX Writing 검증
> 이 티켓은 문구를 만들지 않는다 — 021의 렌더러가 PRD §3 표대로 낸다.

- 11종 초안 전부를 `missingMessageSlots()`에 넣어 **버려진 슬롯 0개**를 테스트로 고정했다.
- 평문에 `''`(빈 따옴표만 남은 문장)가 들어가지 않는지도 함께 검증한다.
- 새 i18n 키를 추가하지 않았다. 2차 수정에서 `msgFollowingNearbyDrop`·`msgNearbyDrops`
  **2개를 제거**했다(해당 소식 자체가 사라졌으므로 고아 키를 남기지 않는다).

### 배포 정보
- 배포일: 2026-08-25
- 환경: staging → main (프로덕션, Vercel Ready 확인)
- 커밋: 8bfe3c0e(1차) · 90a73f74(2차) · 86790e51(3차, main HEAD 기준)
- DB: 마이그레이션 `099_mission_rank_snapshots` Supabase(jam-prod) 적용 확인 (`mission_rank_snapshots`, 20260824232255)

### 주요 의사결정 / 핵심 메모

**1. #42는 (가) "생성 조건을 잔여 0칸으로" 를 택했다** (§3-5)

이 티켓은 §4에서 **문구를 만들지 않기로 못박혀 있다.** (나)를 택하면 `ko.ts`의
`msgInventoryFull`을 고치고 PRD §3 ⑧ 표와 §5 강조 규칙 표(「42 … 슬롯 없음」)까지 함께
바꿔야 해서 021의 렌더러 범위를 침범한다. 그래서 문구는 그대로 두고 사실과 맞을 때만 보낸다.

`INVENTORY_LOW_SLOTS_THRESHOLD`는 버리지 않고 **후보 스캔**에 계속 쓴다(§3-4 준수). 그리고
T3 경고 재평가는 기존대로 잔여 3칸 이하까지 경고를 유지하므로 "칸 하나 비웠다고 경고가
사라지는" 문제도 생기지 않는다. 잔여 1~3칸 구간을 알리고 싶어지면 그때 문구를 새로 정의하는
별도 티켓으로 다룬다.

**2. #23을 위해 `mission_rank_snapshots` 테이블을 신설했다 (마이그레이션 099, 미실행)**

"상승 시만"은 직전 순위를 알아야 판정된다. 순위는 어디에도 저장되지 않고 status API가 매번
계산한다. 기존 소식 행을 기준선으로 쓰는 안은 **첫 기준선을 만들 수 없어**(소식이 있어야
기준선이 생기고 기준선이 있어야 소식이 생긴다) 영원히 발화하지 않는다.

- 첫 배치는 **기준선만 남기고 소식을 만들지 않는다** — 오르지도 않은 유저에게
  "5위로 올라섰어요"가 나가면 거짓말이다.
- 마이그레이션이 아직 적용되지 않아도 **`mission-rank` 단계만 실패**하고 나머지 10종은 정상
  생성된다. 무증상이 되지 않도록 `[notifications-batch] 단계 실패 — mission-rank` 로그가 남는다.
- 정렬 규칙을 `lib/missions/ranking.ts`로 뽑아 status 라우트와 배치가 **같은 비교 함수**를
  쓰게 했다. 복사해 뒀다면 "5위로 올라섰어요"를 눌러 들어간 화면이 6위를 보여줄 수 있다.

**3. 벌크 RPC(`p_user_ids UUID[]`)는 채택하지 않았다** (§3-6)

블래스트 반경 때문이다. 벌크 RPC를 만들면 **마이그레이션이 적용되기 전까지 11종 전부가
생성되지 않는다.** 반면 클라이언트 주입 + 동시 8개 파이프라인은 DDL 의존이 전혀 없고,
현재 규모(유저 수 기준 수백~수천 왕복)에서는 `maxDuration = 60` 안에 충분히 들어온다.
왕복이 병목이 되는 시점에 별도 티켓으로 벌크 RPC를 도입하는 편이 안전하다.

**4. 재발송 정책 — `group_key`에 "날짜"가 아니라 "상태의 시작 시점"을 넣었다**

배치는 매일 도는데 일자 키 + `once`를 쓰면 상태가 유지되는 동안 **매일 새 소식**이 나간다
(PRD §2-4가 금지한 반복 발송). 상태가 바뀔 때만 키가 바뀌게 하면 `once` 하나로
"해소될 때까지 침묵"이 구현된다.

- #9 : `collection_slottable:{book_id}:{최신 미장착 아이템의 획득 KST일자}`
  → 그 컬렉션에 넣을 **새 아이템**이 들어와야 다시 알린다.
  `{book_id}`만 쓰면 평생 1회가 되어 이후 아이템이 쌓여도 침묵한다.
- #41: `sync_stalled:{정체 시작 KST일자}` → 다시 동기화한 뒤 또 정체돼야 다시 알린다.
- #42만 예외로 발송 이력을 SELECT한다. 인벤토리에는 "언제부터 꽉 찼는가"를 알 수 있는 데이터가
  없어 상태 시작 시점을 키에 넣을 수 없다. 일자 키로 재시도 멱등성을 확보하고 최근 7일 내
  발송 이력이 있으면 건너뛴다.
- #18은 "주 1회"를 요일 게이트가 아니라 `{poi_id}:{KST주}` + `once`로 구현했다. 집계 창이
  지난 7일 롤링이라 어느 요일에 돌아도 결과가 같고, cron 실패로 하루를 건너뛰어도 복구된다.

**5. ⑥ 하루 상한 2건의 선별 — 희귀도 단독(PRD §9)**

mythic → legend → 컬렉션 완성(#30) → 미션 완료(#31), 동순위는 최근 이벤트 우선.
#30·#31은 희귀도 축이 없어 "얻기 어려운 순"으로 고정 우선순위를 줬다.
친밀도 지표가 생기면 PRD §9에 따라 재검토한다.

**6. #21에서 단위 없는 달성형 미션(poi_visit·item_collect)은 제외했다**

문구가 `{remaining} 남았어요`라 목표치가 0/1인 미션에서는 "1 남았어요"가 된다. 문장이 아니다.
#20(마일스톤)이 같은 이유로 이미 제외하고 있어 규칙이 일관된다. 이 유형에 마감 임박을
보내려면 새 문구가 필요하고, 그건 이 티켓 범위 밖이다.

**7. 지역 기반 소식 2종(#32·#34)을 제거했다 (2026-08-25 2차 수정)**

1차 구현의 지역 매칭은 **실데이터에서 절대 발화하지 않았다.** 비교하는 두 값의 단위가 다르다.

| 축 | 실제 값 |
|---|---|
| `users.region` (프로덕션 실측) | `'서울'`(1) · `'서울특별시'`(2) · `'부산'`(1) · `'제주'`(1) · `''`(5) — **시/도 단위** |
| `reverseGeocodeToRegionName()` (`reverse-geocode.ts:87`, `[area2, area3].join(' ')`) | `'성동구 성수동'` — **구/동 단위** |

정확 일치 비교라 어떤 경우에도 매칭되지 않고, **에러도 나지 않아 무증상**이었다. 게다가
region이 비어 있지 않은 유저가 5명 존재해 "지오코딩 건너뛰기" 비용 가드도 통과한다 —
앰비언트 드랍이 생기는 순간부터 매 배치가 NCP 역지오코딩을 최대 50회 호출하고 소식은
0건을 만들었을 것이다.

근본 해결에는 `poi.region` 컬럼 신설과 POI 3천여 건 백필이 필요하다. **스키마 변경 + 대량
백필 + 외부 API 상시 의존을 지는 대신 기능을 들어내는 쪽을 택했다.** 되살리려면
`poi.region`이 선행돼야 한다(PRD §7에 기록).

- ⑦ 발견 카테고리는 #34가 유일해 **카테고리째 삭제**됐다. 소식 28종 → 26종, T2 13종 → 11종.
- **DB ENUM은 건드리지 않았다.** Postgres는 ENUM 값 제거가 안전하지 않고 해당 타입 소식이
  프로덕션에 0건이라 방치해도 무해하다. DATA_MODEL §2에 "예약됐으나 사용하지 않음"으로 명시.
- TS `NotificationType`에서는 2종을 빼 **컴파일러가 재사용을 막게** 했다. 이 덕분에
  `message.ts`·`href.ts`·`NotificationsClient.tsx`의 잔여 분기를 빠짐없이 걷어낼 수 있었다.

**8. 3차 보강 — 조회 계층과 관측성 (2026-08-25)**

개선 리뷰가 짚은 4건이다. **판정 로직(11종)은 손대지 않았다.**

*(가) 페이징 조회에 안정 정렬을 강제했다.* Postgres는 `ORDER BY` 없는 쿼리의 행 순서를
보장하지 않는다. `LIMIT/OFFSET`만으로 넘기면 페이지마다 물리적 순서가 달라져 **같은 행이
두 번 오거나 아예 빠진다** — 그리고 에러가 나지 않아 무증상이다. 호출부가 `.order()`를
붙이도록 규약만 두면 새 조회를 추가할 때 또 빠뜨린다. 그래서 `fetchAllRows`가 **빌더를 직접
받아 정렬을 자기가 붙이는** 구조로 바꿨다(`orderBy`가 필수 인자라 잊을 수가 없다).
정렬 키는 유니크 컬럼(대개 PK)이고, 복합 PK(`user_item_book_completions`,
`mission_rank_snapshots`)는 컬럼을 전부 넘긴다. 현재 최대 테이블이 137행이라 아직 페이징이
발동하지 않지만 원리적 결함이라 먼저 막았다.

*(나) `drop-engine`의 동명 헬퍼와는 합치지 않았다.* 반환 계약이 다르다 — 배치 쪽은 실패 시
`throw`(단계 격리가 받는다), drop-engine 쪽은 `{ data, error }`를 돌려주고 호출부가 분기한다.
합치려면 drop-engine의 호출부 분기를 전부 고쳐야 하는데, 그쪽은 **동기화 때마다 도는 사용자
경로**라 회귀 시 비용이 배치보다 크다. 이 티켓이 얻는 것은 중복 30줄 제거뿐이라 **위험이
이득을 넘는다.** 대신 그쪽의 정렬 부재는 「잔여 이슈」에 남겼다.

*(다) 단계가 하나라도 실패하면 500을 반환한다.* Vercel의 cron 실패 감지는 상태 코드
기준이라, 200을 주면 **마이그레이션 099 미적용 같은 상태가 대시보드에서 성공으로 보인다.**
11종 전부 `once` + `group_key`라 Vercel 재시도가 와도 중복은 생기지 않는다. 500은 "전부
실패"가 아니라 "확인이 필요하다"는 신호이고, 다른 단계가 만든 소식은 그대로 남는다.

*(라) "정상 0건"과 "깨져서 0건"을 구분할 지표를 넣었다.* `BatchStepResult`에 `scanned`(훑은
행 수)·`drafts`(생성 시도 수)·`durationMs`를 더해 요약 로그에 싣는다. **제거한 지역 소식이
정확히 그 패턴**이었다 — 입력은 있는데 매칭이 0이라 소식이 0건인데, 로그만 봐서는 "오늘은
해당자가 없었다"와 구분되지 않았다. `scanned > 0 && drafts === 0`이 며칠 이어지는 단계가
눈에 띄면 그게 신호다. 단계별 `scanned`의 정의는 `batch/index.ts` 헤더 표에 적었다.
추가로 총 실행 시간이 45초를 넘으면 `console.warn`으로 느린 단계 3개를 남긴다 —
`maxDuration = 60`이라 타임아웃되면 요약 로그 자체가 남지 않아 부분 생성이 흔적 없이 지나간다.

*(마) `.in()` 목록을 200개 단위로 쪼갠다.* PostgREST는 목록을 URL에 싣는다. 아이템북 소속
배지는 실측 198개(약 7.3KB)로 Cloudflare 16KB 한계의 절반이지만, FACTIONS.md 목표인
세계관 10 × 90종 = 900종이 채워지면 약 33KB로 한계를 넘는다. 걸리면 조회가 throw →
`runStep`이 잡아 **그 단계만 무증상 0건**이 된다. `fetchAllRowsIn`으로 collections
(`inventory_items`) · following(`badges`) · account(`notifications`)를 포함해 같은 축의
`.in()` 조회를 일괄 처리했다. 값은 **중복 제거 후** 쪼갠다 — 같은 값이 두 청크에 걸리면
같은 행이 두 번 담기는 게 청크 분할이 새로 만드는 유일한 사고 경로라 거기서 막았다.

### 잔여 이슈

- **마이그레이션 099 미실행** — `mission_rank_snapshots`. 적용 전까지 #23만 생성되지 않는다
  (나머지 10종은 정상). 적용 후 `[notifications-batch] 단계 실패 — mission-rank` 로그가
  사라지는지 확인할 것.
- ~~`users.region`이 비어 있어 #32·#34는 발화하지 않는다~~ / ~~#34의 POI→지역 판정이 외부
  API에 의존한다~~ — **2026-08-25 2차 수정에서 삭제.** 1차 구현이 "전 유저의 region이 `''`"라고
  쓴 것은 **사실과 다르다**(프로덕션 실측: `'서울'` 1명 · `'서울특별시'` 2명 · `'부산'` 1명 ·
  `'제주'` 1명 · `''` 5명). 값이 있는 유저가 존재해 "region 설정 유저가 없으면 지오코딩을
  건너뛴다"는 비용 가드도 무효였다. 지역 기반 소식 2종을 제거해 이 이슈 자체가 사라졌다
  (「주요 의사결정 7」).
- **배포 직후 확인** — 021이 넣어둔 `missingMessageSlots()` 경고
  (`console.warn('[notifications] 빈 슬롯', …)`)가 서버 로그에 찍히지 않는지 확인할 것.
  테스트로 잠갔지만 실데이터(이름이 빈 배지·컬렉션 등)에서 새어 나올 수 있다.
- **`drop-engine`의 `fetchAllRows`도 정렬이 없다** (`src/lib/drop-engine/index.ts:160`).
  이 티켓 범위 밖이라 손대지 않았다(「주요 의사결정 8-나」). 특히 `badges` 조회는 활성
  아이템북 안에서만도 900개에 근접해 **1,000행을 넘는 순간 드랍 후보에서 배지가 새거나
  중복된다.** 별도 티켓으로 `.order('id')`를 붙일 것.
- **첫 실행 규모** — #24(종료 결과)는 최근 2일 내 종료 미션, #18은 활성 드랍이 있는 POI로
  제한돼 있어 첫 실행이 폭주하지 않는다. 다만 #9·#10은 **전 유저 × 전 컬렉션** 스캔이라
  첫 실행에서 소식이 한꺼번에 생길 수 있다(모두 `once`라 다음 날부터는 잠잠해진다).
