---
id: 20260825_033
category: BadgeEngine
status: CLOSED
created: 2026-08-25
closed: 2026-08-30
---

# [BadgeEngine] CONDITION_JSON_SPEC.md 온도 조건 부등호 방향 문서 오류

## 배경 / 문제 정의

티켓 [20260825_031](20260825_031_BadgeEngine_condition-json-데이터계약-검증-도입.md) 게이트
리뷰 중 발견. `Service Plan/Specs/BadgeEngine/CONDITION_JSON_SPEC.md`의 온도 조건 필드
설명이 실제 코드·어드민 UI 라벨과 **부등호 방향이 정반대**로 기술되어 있다.

| | 문서 기재 | 실제 코드(`src/lib/badge-engine/index.ts`) |
|---|---|---|
| `temperature_min_c` | "혹한(≤)" | 폭염 조건 — `average_temp ≥ temperature_min_c` |
| `temperature_max_c` | "폭염(≥)" | 한파 조건 — `average_temp ≤ temperature_max_c` |

`src/types/database.ts`의 `BadgeCondition` 타입 주석과 어드민 폼(`BadgeForm.tsx`) 라벨은
실제 코드와 일치하며, `CONDITION_JSON_SPEC.md`만 반대로 기술되어 있다. 이 문서는 "단일
출처(source of truth)"를 표방하므로 실사용 시 혼란을 유발할 수 있다.

## 상세 요구사항

### 문서 관점 (④ 배지 드랍 로직)

- `CONDITION_JSON_SPEC.md`의 온도 조건 필드 표(§2.6, 티켓 20260825_031 반영 후 절 번호는
  현재 문서 기준으로 재확인할 것) 설명을 실제 코드 기준으로 정정:
  - `temperature_min_c`: 최저 기온 조건 — Strava `average_temp` ≥ 조건값이어야 통과 (폭염 배지)
  - `temperature_max_c`: 최고 기온 조건 — Strava `average_temp` ≤ 조건값이어야 통과 (한파 배지)
- 정정 전 실제 코드(`matchesPerActivityCondition`·`describePerActivity`·`singleFieldFailure`
  3곳)를 다시 대조해 문서가 코드를 정확히 따르는지 확인할 것

## 구현 계획
> 문서 정정 단독 작업 — `/jam-docs` 규칙에 따라 진행

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
`CONDITION_JSON_SPEC.md` §2.6 온도 조건 필드 표의 부등호 방향을 실제 코드
(`src/lib/badge-engine/index.ts`의 `matchesPerActivityCondition`·`describePerActivity`·
`singleFieldFailure`)와 일치하도록 정정했다.
- `temperature_min_c`: "혹한(≤)" → "폭염(≥)"으로 수정
- `temperature_max_c`: "폭염(≥)" → "한파(≤)"로 수정

같은 문서 체계 내 `BADGE_ENGINE_UNIFIED.md`(77행)·`SERVICE_OPERATIONS.md`(297~298행)는
이미 올바른 방향으로 기재돼 있었음을 확인했다 — `CONDITION_JSON_SPEC.md`만 반대였다.

### 변경된 파일
```
Service Plan/Specs/BadgeEngine/CONDITION_JSON_SPEC.md
```

### 테스트 결과
- [x] 코드 3곳(`matchesPerActivityCondition` 128~132행, `describePerActivity` 168~174행,
  `singleFieldFailure` 216~231행) 대조 확인 — `temperature_min_c`는 `weatherTempC ≥ 조건값`,
  `temperature_max_c`는 `weatherTempC ≤ 조건값`이어야 통과
- [x] 저장소 내 다른 문서(`BADGE_ENGINE_UNIFIED.md`, `SERVICE_OPERATIONS.md`,
  `Content/ACTIVITY_BADGES.md`)와의 방향 일치 여부 grep으로 재확인 — 불일치 없음

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
해당 없음 (내부 스펙 문서, 사용자 노출 텍스트 아님)

### 배포 정보
- 배포일: 2026-08-30
- 환경: 문서 전용 변경 (코드 무변경, 배포 불필요)
- 커밋: (staging push 시 기록)

### 주요 의사결정 / 핵심 메모
> 티켓 20260825_031 게이트 리뷰 중 발견한 범위 밖 문서 오류. 별도 티켓으로 분리(2026-08-25,
> 사용자 결정) — 이후 미착수 티켓 일괄 처리(2026-08-30) 중 착수.

### 잔여 이슈
- 없음
