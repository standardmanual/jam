---
id: 20260901_0809
category: UI
status: CLOSED
created: 2026-09-01
closed: 2026-09-01
---

# [UI] 배지 트리 카드 레이아웃·잠금칩 획득완료 표시

## 배경 / 문제 정의

사용자가 `/badges/tree`(티켓 20260831_2208·2250) 실화면을 보고 스크린샷과 함께 3가지 UI 수정을
직접 요청했다. 경미 수정(2~3파일, 로직 소폭 추가, 오타 수준 아님)이라 `/jam-work` 풀 파이프라인
없이 오케스트레이터가 직접 처리한다.

## 상세 요구사항

1. **배지 이름·등급 표시를 설명 위로 이동**: 첨부 스크린샷처럼 카드 안에서 "등급 pill →
   배지 이름(볼드) → 설명" 순으로 보이게 한다. 기존에는 `BadgeGridCard`를 그대로 써서
   썸네일 아래(왼쪽 컬럼)에 이름·pill이 있고, 설명은 오른쪽 컬럼에 별도로 있었다.
2. **선행 배지를 이미 획득한 경우 "OOO 배지 획득 완료"로 표기**: 지금까지는 선행조건 충족
   여부와 무관하게 항상 "OOO 배지를 획득하면 열려요"만 보였다. (미션 게이팅은 이번 범위 아님 —
   "선행 배지"라고 명시했으므로 배지-대-배지 OR 게이팅만 대상.)
3. **선행 배지명 볼드 처리**: 잠금칩 문구 안의 배지/미션 이름을 볼드로 강조.

## 구현 계획

- `jam-web/src/components/badges/BadgeTreeFamily.tsx`: `BadgeGridCard` 사용을 중단하고
  썸네일(Link+Image, 기존과 동일한 흑백/반투명·터치타겟)만 직접 구성. 텍스트 컬럼 최상단에
  `RarityBadge` → 볼드 이름(Link) → 설명 → 잠금칩 순으로 배치.
- `jam-web/src/lib/badgeTree.ts`: `BadgeTreeLock`을 `{label, href}`에서
  `{kind: 'badge'|'mission', name, href, fulfilled}`로 확장. `buildBadgeActivityTrees`에
  `earnedBadgeIds: Set<string>` 3번째 인자 추가 — 배지-게이팅 잠금(`kind:'badge'`)마다 선행
  배지 family의 어느 등급이든(OR 규칙, 엔진과 동일) `earnedBadgeIds`에 있으면 `fulfilled: true`.
  미션 게이팅(`kind:'mission'`)은 `fulfilled: false` 고정(이번 범위 아님).
- `jam-web/src/components/badges/BadgeTreeLockChip.tsx`: `label` 문자열 대신 구조화된
  `{kind, name, href, fulfilled}`를 받아 이름을 `<strong>`으로 볼드 처리. `kind==='badge' &&
  fulfilled`면 `treeLockBadgeFulfilled`("{name} 배지 획득 완료") 템플릿 + 체크 아이콘,
  아니면 기존 잠금 아이콘 + 기존/미션 템플릿.
- `jam-web/src/lib/i18n/ko.ts`: `treeLockBadgeFulfilled` 신규 키 추가. `treeLockMissionHint`의
  플레이스홀더를 `{title}`→`{name}`으로 통일(노출 문구 자체는 불변, 코드 일관성 목적).
- `jam-web/src/app/(main)/badges/tree/page.tsx`: 기존에 계산하던 `earnedBadgeIdSet`을
  `buildBadgeActivityTrees` 호출에 그대로 전달(신규 조회 없음).

## 완료 기록

### 구현 내용 요약
계획대로 구현. `npx tsc --noEmit` 에러 0건, `eslint`(수정 파일 전체) 경고·에러 0건. 로컬
`next dev`에 `/api/dev-login`(dev-tester)로 로그인 후 `/badges/tree` 걷기 탭에서 실제 확인:
- 카드 순서가 "RARE/EPIC/MYSTIC pill → 볼드 이름 → 설명"으로 정확히 렌더링됨(스크린샷 참고
  레이아웃과 일치).
- "산책의 명상가"·"루틴의 수호자"의 배지-게이팅 잠금칩이 이 계정이 이미 보유한 "동네
  산책러"·"밤의 보행자"·"산책의 명상가"에 대해 체크 아이콘 + "OOO 배지 획득 완료"(볼드 이름)로
  정확히 바뀜을 확인.
- 종목 대표배지(미션 게이팅) 잠금칩은 기존과 동일하게 자물쇠 아이콘 + "'OOO 레벨업' 미션을
  완료하면 열려요"(볼드 이름)로 문구 불변 확인 — 미션 게이팅은 이번 범위가 아니므로 항상
  미충족으로 유지됨을 코드로 확인(`fulfilled: false` 고정).

### 변경된 파일
```
jam-web/src/components/badges/BadgeTreeFamily.tsx
jam-web/src/components/badges/BadgeTreeLockChip.tsx
jam-web/src/lib/badgeTree.ts
jam-web/src/lib/i18n/ko.ts
jam-web/src/app/(main)/badges/tree/page.tsx
```

### 테스트 결과
- [x] `npx tsc --noEmit -p .` — 에러 0건
- [x] `eslint`(수정 파일 5개) — 에러·경고 0건
- [x] 로컬 dev, 로그인 계정으로 실화면 확인(카드 레이아웃·획득완료 표시·볼드 처리 3건 전부)

### UX Writing 검증
- [x] 용어 일관성: "배지를 획득하면 열려요"·"배지 획득 완료" 모두 기존 "획득" 고정 용어 사용
- [x] 문장 규칙: 해요체 유지, 명사형 종결("배지 획득 완료")은 상태 라벨이라 가이드 §6 레이블
      규칙(마침표 불필요) 적용
- [x] 신규 문구는 `treeLockBadgeFulfilled` 1건뿐 — 사용자가 원문 그대로 준 문구("배지 획득
      완료")를 그대로 반영

### 배포 정보
- 배포일: 2026-09-01
- 환경: production
- 커밋: `553a65b6`(카드 레이아웃·획득완료 표시) → `cf83a37a`(등급칩 스트레치 버그) →
  `0bb2a318`(평면 목록 재구성, main) — Vercel 배포 `dpl_32kTZEwPYpVnpsvPmAxr68H7reZs`
  READY, `alias`에 `j-a-m.app` 포함 확인

### 주요 의사결정 / 핵심 메모
- 선행조건 OR 충족 판정은 "선행 배지 family의 어느 등급이든 보유"를 기준으로 했다 — 배지
  엔진의 크로스게이트 규칙(`Specs/BadgeEngine/BADGE_ENGINE_UNIFIED.md`: "이름 기반: 해당
  배지의 어느 등급이든 소유 시 통과")과 동일하게 맞춘 것으로, 임의 판단이 아니다.
- 미션 게이팅 잠금칩은 "선행 배지"라는 사용자 표현 범위 밖으로 보고 그대로 두었다. 필요하면
  후속 요청으로 별도 처리.
- `BadgeGridCard`(서비스 공용 컴포넌트)는 건드리지 않았다 — 그리드 레이아웃이 필요한 다른
  화면(`/badges`, `/badges/[id]` 선행배지 그리드)에 영향 없음.

### 잔여 이슈
- 없음

### 추가 수정 (2026-09-01, 같은 날 후속 요청)

1. **등급칩(RarityBadge)이 가로로 늘어나는 버그.** 텍스트 컬럼이 `flex-col`(기본
   `align-items: stretch`)이라 `inline-flex` span인 pill이 컬럼 폭 전체로 늘어났다.
   `self-start`로 콘텐츠 폭만큼만 차지하게 고정(`BadgeTreeFamily.tsx`, 이후
   `BadgeTreeCard.tsx`로 이어짐).

2. **트리 구성 방식을 "동일 배지 등급 순서(가족 단위 묶음)"에서 "전체 액티비티 배지의
   획득 단계"로 전면 재구성.** 기존에는 종목마다 배지를 가족(예: "동네 산책러")으로 묶어
   그 가족의 Common→Mystic 4장을 한 카드 박스 안에 쌓고, 그 가족들을 다시 선행조건 그래프의
   BFS 깊이로 "1단계"·"2단계" 헤더 아래 그룹핑했다. 사용자가 이 구조 대신 "동일배지의
   등급순서가 아니라 전체 액티비티 배지의 획득 단계별로 배치"하고 "1단계·대표배지 같은
   구분이나 구분선을 쓰지 말라"고 명시적으로 요청해 다음과 같이 재구성했다:
   - `src/lib/badgeTree.ts`: `BadgeActivityTree`가 더 이상 `stages`/`independentFamilies`를
     반환하지 않고, 종목당 하나의 평평한 `cards: BadgeTreeCard[]`(등급 하나 = 카드 한 장)를
     반환한다. 정렬 키는 1차 등급(Common→Rare→Epic→Mystic, 등급 자체가 이미 대략적인 획득
     단계다), 2차 선행조건 그래프 BFS 깊이(독립 배지는 그 등급 그룹 맨 뒤로), 3차 문서
     서술 순서(`ACTIVITY_BADGE_ORDER`/`INDEPENDENT_BADGE_ORDER`, 기존 로직 재사용).
     `representative`(대표배지 판정)는 내부 BFS 계산에는 여전히 쓰이지만 더 이상 화면에
     노출하는 필드가 아니다.
   - `src/components/badges/BadgeTreeFamily.tsx`(가족 단위 래퍼) 삭제,
     `src/components/badges/BadgeTreeCard.tsx`(카드 한 장 전용)로 대체 — "대표 배지" 태그·
     가족 래핑 박스 제거.
   - `src/components/badges/BadgeTreeConnector.tsx`(단계 사이 구분선) 삭제 — 더 이상
     사용처 없음.
   - `BadgeTreeClient.tsx`: 단계 헤더(`{n}단계`) 렌더링 루프를 제거하고
     `activeTree.cards.map(...)`로 평평한 목록을 그대로 렌더링.
   - `ko.ts`의 `treeStageLabel`·`treeRepresentativeLabel`·`treeIndependentTitle`
     3개 키 삭제(다른 사용처 없음, grep으로 확인).
   - 검증: `npx tsc --noEmit` 에러 0건, `npm run lint`(전체) 에러 0건·경고 26건(기존
     기준선과 동일, 신규 경고 없음). 로컬 dev-tester 계정으로 걷기 탭 실화면 확인 —
     Common 배지들이 먼저 쭉 나오고(독립 배지는 그 안에서 맨 뒤), 이어서 Rare가 이어지되
     그 안에서 대표배지("동네 산책러")가 먼저, 그다음 깊이 2("산책의 명상가") 순으로 나옴을
     확인. 구분 라벨·구분선 없음.
