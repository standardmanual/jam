---
id: 20260901_1852
category: Infra
status: CLOSED
created: 2026-09-01
closed: 2026-09-01
---

# [Infra] database.generated.ts 스키마 드리프트 점검·재생성

## 배경 / 문제 정의
티켓 [20260824_017](20260824_017_Infra_떠돌이신화-기능-전면제거.md)에서 발견:
`database.generated.ts`(Supabase 자동 생성 타입)가 실제 DB 스키마보다 뒤처져 있다.
이후 티켓 [20260831_1213](20260831_1213_Service_Supabase제네릭-생성타입전환-쓰기타입검사-복구.md)·
[20260831_1158](20260831_1158_Infra_수기타입-생성타입-불일치-전수점검.md)에서 관련 타입
불일치를 상당 부분 다뤘을 가능성이 있으므로, 이 티켓은 **먼저 현재 드리프트가 남아있는지
재확인**하고 남아있는 만큼만 처리한다.

## 상세 요구사항

### 서비스/코드베이스 관점
- Supabase MCP `generate_typescript_types`(또는 CLI `supabase gen types`)로 최신 타입을
  생성해 현재 `database.generated.ts`와 diff
- 차이가 없으면(이미 1213/1158에서 해소됐다면) 티켓에 "재확인 결과 드리프트 없음"으로
  기록하고 종료
- 차이가 있으면 재생성 반영 + 그로 인해 컴파일 에러가 나는 호출부 수정

## 구현 계획
- diff 우선 확인 → 필요시에만 재생성 및 후속 타입 수정

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
Supabase MCP `generate_typescript_types`(jam-prod, `ceehnkzdbecxwzxrhhns`)로 최신 스키마
타입을 생성해 로컬 `database.generated.ts`와 `diff`. 차이는 `backup_20260831_badges`·
`backup_20260831_engine_decision_log`·`backup_20260831_missions`·
`backup_20260831_today_cards`·`backup_20260831_user_activity_feed` **5개 백업 테이블**
뿐이었다 — 이름 그대로 2026-08-31에 어떤 위험한 마이그레이션 전에 만든 일회성 백업
스냅샷으로 추정되며, `grep`으로 `jam-web/src` 전체를 확인한 결과 코드에서 전혀 참조하지
않는다. 이전 티켓(20260831_1213·1158)이 실제 서비스가 쓰는 스키마의 드리프트는 이미 전부
해소했음을 확인 — **재확인 결과 실질적 드리프트 없음.**

### 변경된 파일
```
(없음 — 재확인만 하고 종료, 재생성 불필요)
```

### 테스트 결과
- [x] `generate_typescript_types` 결과와 로컬 파일 diff — 앱이 참조하는 테이블·컬럼은
      전부 일치. 차이는 앱 무관 백업 테이블 5개뿐

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
해당 없음

### 배포 정보
해당 없음 (코드 변경 없음)

### 주요 의사결정 / 핵심 메모
경미 조사(변경 없음 확인)라 SKILL.md 참고 규정에 따라 풀 파이프라인 없이 오케스트레이터가
직접 처리했다. `backup_20260831_*` 5개 테이블은 이 티켓 범위 밖 — 필요 없어졌다면 별도
정리 판단 필요(DB 삭제는 신중을 요하므로 임의로 지우지 않았다).

### 잔여 이슈
- `backup_20260831_*` 백업 테이블 5개가 DB에 남아있다. 더 이상 필요 없다면 별도 티켓으로
  삭제 검토(현재 앱 코드에서 참조 없음을 확인했으나, 삭제 여부는 사용자 판단 필요).
