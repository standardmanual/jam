---
id: 20260901_1944
category: UI
status: OPEN
created: 2026-09-01
---

# [UI] 배지·컬렉션 이미지 카드 배경 — 블롭 애니메이션 옵션 추가

## 배경 / 문제 정의

현재 배지(활동/아이템/체크인)·컬렉션의 배경 저작은 **단색(`background_color`) 하나뿐**이다.
과거에 있던 패턴/쉐이더/Paper 필터 제너레이터는 티켓 `20260901_1929`에서 제거 중이고(선행 작업),
그 결과 저작 UI에는 배경색 필드만 남는다.

여기에 **블롭(blob) 애니메이션 배경**을 두 번째 선택지로 추가한다. 사용자가 제시한 참조 스크립트
(Canvas 2D 기반 — 유기적 블롭 6개가 blur 합성되며 부유하는 애니메이션)를 서비스 렌더링 엔진으로
삼는다.

### 기존 배경 시스템과의 차이 (중요)

현재 `background_color`/`background_image_url`/`background_video_url`은 **상세화면 전체를 덮는
고정 배경 레이어**에 적용되고, 이미지 Hero 카드는 `bg-transparent`가 되어 그 배경이 비쳐 보인다
(`BadgeHeroSection.tsx:37`, 티켓 20260819_011에서 "단일 레이어" 원칙 확정).

**이번 애니메이션은 전체 배경이 아니라 이미지가 놓인 정사각형 Hero 카드 "안"에만 그린다**
(사용자 결정). 즉 기존 전체배경 레이어와는 별개의 새 렌더링 지점이다. 둘의 관계 정의가
이 티켓의 핵심 설계 포인트다.

### 과거 애니메이션 배경과의 차이 (중요)

티켓 `20260819_012`의 애니메이션 배경은 **어드민에서 MP4로 굽고 서비스는 `<video>`로 재생**하는
방식이었고, "유저단(모바일 웹)에는 무거운 렌더링을 올리지 않는다"가 명시 원칙이었다. 그 원칙은
**WebGL/Three.js 기준**이었고, 이번 블롭 애니메이션은 Canvas 2D + `ctx.filter` blur만 쓴다.

이번에는 **파라미터를 저장하고 서비스에서 라이브 실행**한다(사용자 결정). 근거:
- 저장 후 재편집이 가능하다 (기존 제너레이터는 완성 이미지만 저장해 재편집 불가였다 — 실사용
  불편의 주원인)
- 영상 파일 다운로드가 없어 네트워크 페이로드가 오히려 작다 (파라미터 JSON 수백 바이트)
- 렌더링 대상이 상세화면 Hero 카드 **1개**로 한정돼 동시 렌더 수가 통제된다

단, blur 필터는 모바일에서 비용이 있으므로 **성능 가드는 필수 요구사항**이다(아래 참조).

## 상세 요구사항

### 서비스/코드베이스 관점

#### 1. 데이터 모델 — 신규 컬럼

`badges` / `item_books` / `factions` 세 테이블에 애니메이션 파라미터 컬럼을 추가한다.
(세계관까지 포함하는 이유: 기존 배경 4컬럼이 세 테이블 모두에 있고, 세계관 → 컬렉션 → 배지
3단 캐스케이드 일괄 적용이 이미 동작 중이라 한 테이블만 빠지면 캐스케이드가 깨진다.)

- 컬럼명 제안: `background_animation` (`jsonb`, nullable)
- **`background_shader_id`를 재사용하지 않는다** — 그 컬럼은 죽은 레거시(값만 저장, 렌더링
  미연결)이고 선행 티켓 `20260901_1929`에서 저작 UI가 제거된다. 의미가 다른 값을 얹으면
  혼란만 커진다. 신규 컬럼으로 깨끗하게 분리한다.
- 저장 형태(제안 — 구현자가 최종 확정):
  ```json
  {
    "type": "blob",
    "colors": ["#ff6d30", "#a8aded", "#ffe5d1", "#ff4c00"],
    "bgColor": "#ffffff",
    "speed": 1,
    "seed": 21,
    "blur": 0.54,
    "scale": 0.66
  }
  ```
  `type`을 두는 이유: 향후 다른 애니메이션을 추가할 때 컬럼을 또 늘리지 않기 위함.
- 마이그레이션 파일을 작성한다. **번호는 push 직전에 원격 staging 기준으로 재확인할 것** —
  현재 최신은 120이나 병렬 세션이 선점할 수 있고, 실제로 094번 중복 사고 이력이 있다.
- 마이그레이션 추가 후 **`npm run db:types`로 `database.generated.ts`를 재생성해 같은 커밋에
  포함**한다. 생성 타입이 낡으면 올바른 코드가 잘못된 컴파일 오류를 낸다(실제 사고 이력 있음).
- **DB 실행은 하지 않는다** — SQL 파일 작성까지만. 실행은 오케스트레이터가 사용자 승인 후 수행.

#### 2. 배경색 ↔ 애니메이션 배타 선택

저작 UI에서 두 모드는 **배타**다(사용자 요구: "배경색 / 애니메이션 선택 두개의 옵션").
- 애니메이션을 선택해 저장하면 `background_animation`에 값이 들어간다
- 배경색을 선택하면 `background_animation`은 `null`이 된다
- 기존 `background_color`/`background_image_url` 우선순위 규칙과의 관계를 명시적으로 정해
  `badgeBackgroundTheme.ts`에 한 곳으로 모은다. 제안 우선순위:
  `background_animation` > `background_image_url` > `background_color`
- **주의**: 선행 티켓 `20260901_1929`가 저장 payload에서 `background_image_url`/
  `background_video_url`을 **생략**해 기존 DB 값을 보존하도록 바꾼다. 애니메이션을 켤 때
  과거에 구워둔 이미지 배경이 남아 있으면 우선순위 충돌이 난다 — 애니메이션 저장 시
  이 두 필드를 명시적으로 `null`로 덮을지 여부를 구현자가 판단하고 근거를 완료 기록에 남길 것.

#### 3. 서비스 렌더링 — 이미지 카드 안

- 신규 컴포넌트(예: `BlobAnimationBackground.tsx`)를 만들어 **정사각형 Hero 카드 내부**에
  절대배치 캔버스 레이어로 깐다. 이미지는 그 위(z-index 상위)에 그대로 둔다.
- 적용 지점:
  - 배지 상세: `jam-web/src/app/(main)/badges/[id]/BadgeHeroSection.tsx` (`:37` 카드 div)
  - 컬렉션 상세: `jam-web/src/app/(main)/collections/[id]/ItemBookHeroSection.tsx` (`:34`)
  - 두 Hero 컴포넌트는 어드민 프리뷰 프레임에서도 **그대로 재사용**되므로(티켓 20260819_011/014
    확립 패턴), 여기에만 넣으면 어드민 미리보기는 자동으로 따라온다 → **미리보기용 별도 구현
    금지**
- 카드가 `overflow-hidden` + `rounded-[var(--radius-cards)]`로 애니메이션을 라운드 안에 가둘 것
  (`ItemBookHeroSection`은 이미 `overflow-hidden`, `BadgeHeroSection`은 확인 필요)
- 배경 애니메이션이 있을 때 카드의 `bg-surface-elevated`/`bg-transparent` 분기를 어떻게 둘지
  정한다 — 애니메이션 자체가 카드 배경이므로 별도 배경색은 불필요할 가능성이 높다.
- **전체 배경 레이어(기존 시스템)는 이번 티켓에서 변경하지 않는다.** 애니메이션이 카드 안에만
  있고 전체배경은 기존 규칙대로 동작한다. 둘이 동시에 켜진 경우의 시각적 결과를 확인하고
  어색하면 완료 기록에 남길 것.

#### 4. 성능 가드 (필수)

블롭 애니메이션은 `ctx.filter = blur()`를 매 프레임 6회 호출한다 — 모바일에서 비용이 크다.
아래를 **모두** 구현한다:
- `prefers-reduced-motion: reduce`이면 애니메이션을 돌리지 않고 **정지 1프레임만 렌더**한다.
  기존 관행은 `globals.css:538`의 `.badge-background-video` 패턴(미디어쿼리로 `display:none`)이나,
  이 경우는 배경이 통째로 사라지면 카드가 비어 보이므로 **정지 프레임 유지**가 맞다.
- 카드가 뷰포트 밖이거나 탭이 백그라운드면 `requestAnimationFrame` 루프를 **정지**한다
  (`IntersectionObserver` + `document.visibilitychange`).
- 캔버스 백킹 스토어 해상도에 상한을 둔다(DPR 캡). 참조 스크립트도 모바일에서 DPR을 1.5로
  캡하고 프레임을 32ms로 제한하는 가드를 갖고 있다 — 동일 수준으로 적용한다.
- 언마운트 시 rAF·옵저버를 반드시 해제한다(누수 방지).

#### 5. 어드민 저작 UI

- 선행 티켓 `20260901_1929`가 `BackgroundGeneratorPreview.tsx`를 배경색 전용으로 축소한다.
  그 위에 **배경색 / 애니메이션 배타 선택 UI**를 얹는다. 1929가 지운 패턴/쉐이더/Paper 필터를
  되살리는 것이 아니다 — 새 단일 애니메이션 타입 하나만 추가한다.
- 애니메이션 선택 시 파라미터 컨트롤을 노출: 색상 4개 + 배경색 + 속도 + 시드 + blur + 크기
  (+ 시드 랜덤화 버튼). 참조 스크립트의 Aperture Size/Edge Softness는 원형 마스크 전용이라
  **채택하지 않는다**(카드 안 렌더링에는 불필요).
- 세 폼 모두에 적용: `BadgeForm.tsx`(배지 3종 공통 — 타입별 화면이 나뉘지 않음),
  `ItemBookForm.tsx`(컬렉션), `FactionForm.tsx`(세계관)
- 저작 중 미리보기는 실시간 반영(파라미터 조절 → 즉시 캔버스 갱신)

#### 6. 일괄 적용 캐스케이드 확장

`background_animation`을 기존 배경 필드와 함께 하위로 복사하도록 확장:
- `jam-web/src/app/api/admin/itembooks/[id]/apply-background/route.ts`
- `jam-web/src/app/api/admin/factions/[id]/apply-background/route.ts` (3단 캐스케이드)
- `092_faction_background_cascade_rpc.sql`에 RPC가 있다면 함께 갱신 필요 — 확인할 것
- 일괄 적용 버튼 활성화 조건(선행 티켓이 `Boolean(backgroundColor.trim())`으로 단순화)에
  애니메이션 값도 포함되도록 확장

#### 7. 저장 API

- `POST /api/admin/badges`, `PUT /api/admin/badges/[id]`
- `POST /api/admin/itembooks`, `PUT /api/admin/itembooks/[id]`
- 세계관 대응 라우트
- PUT은 `body.x !== undefined ? body.x : existing.x` 병합 패턴이므로, 애니메이션을 **끄는**
  경우(`null` 저장)가 이 패턴에서 정상 동작하는지 반드시 확인할 것 — `undefined`와 `null`
  구분이 깨지면 "애니메이션 해제"가 저장되지 않는다.

### UI/UX 관점

- **DS 재사용 판정(오케스트레이터 1.5단계 수행 결과)**: `design-system/components/`의
  buttons·cards·feedback·forms·icons·navigation·patterns 전 범주를 확인했고 캔버스/배경/
  애니메이션 계열 컴포넌트는 **존재하지 않는다**. 따라서 **서비스 전용 신규 구현**으로 진행한다.
  MODULAR 승격 여부는 개선 리뷰에서 판단한다(현재는 배경 데이터 모델에 강하게 결합돼 있어
  범용 컴포넌트로 올리기에 이르다).
- 어드민 화면 부분은 MODULAR 적용 대상이 아니다.
- 사용자 노출 문구(모드 선택 라벨, 안내 문구)는 UX Writing 가이드라인 준수 — 해요체, 간결.

## 구현 계획

1. 마이그레이션 작성(`background_animation` jsonb × 3 테이블) + `npm run db:types` 재생성
2. `badgeBackgroundTheme.ts`에 애니메이션 판정/우선순위 로직 추가 (렌더링 결정을 한 곳에 모음)
3. `BlobAnimationBackground.tsx` 신규 작성 — 참조 스크립트의 블롭 드로잉 로직 이식 +
   성능 가드(reduced-motion·IntersectionObserver·visibilitychange·DPR 캡·정리) 구현
4. `BadgeHeroSection.tsx` / `ItemBookHeroSection.tsx`에 카드 내부 배경 레이어로 연결
   (→ 어드민 프리뷰는 재사용으로 자동 반영)
5. 어드민 저작 UI: 배경색/애니메이션 배타 선택 + 파라미터 컨트롤 (3개 폼)
6. 저장 API 4~6곳에 `background_animation` 반영 (null 저장 경로 포함)
7. 일괄 적용 캐스케이드 2~3곳 확장 (+ RPC 확인)
8. 테스트 갱신, `npm run lint` / `npx tsc --noEmit` / `npm run build` 통과 확인

## 작업 시 유의사항 (구현 담당자에게)

- **선행 의존 (해소됨)**: 티켓 `20260901_1929`(배경 제너레이터 제거)는 2026-09-01 staging에
  반영 완료됐다(커밋 `564e75ff`, CLOSED `d9deb093`). 착수 가능 상태다.
  현재 `BackgroundGeneratorPreview`가 받는 props는 `backgroundColor` /
  `onBackgroundColorChange` / `renderPreview` 셋뿐이고, `liveNode`는 항상 `null`이다.
  `FactionForm`의 죽은 참조(`bake()` 등)도 함께 정리됐다.
- **병렬 세션 주의**: 커밋 시 반드시 본인이 수정한 파일만 명시적으로 `git add <file...>`로
  나열한다. `git add -A` / `git add .` **금지** — 워킹트리가 공유되어 다른 세션 작업을 삼킨다.
- 티켓 번호·마이그레이션 번호는 push 직전에 원격 staging 기준으로 재확인한다.
- `jam-developer`는 SQL 파일 작성까지만 하고 **DB에 직접 실행하지 않는다**.

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

`badges`/`item_books`/`factions`에 `background_animation`(jsonb) 컬럼을 추가하고, 배지·컬렉션
상세화면의 **정사각형 Hero 카드 안**에서 Canvas 2D로 블롭 애니메이션을 라이브 실행한다.
어드민 저작 UI는 배경색 / 애니메이션 배타 선택으로 바뀌었다.

1. **데이터 모델** — 마이그레이션 124. 세 테이블에 `background_animation JSONB` 추가 +
   `apply_faction_background_cascade` RPC가 `background_color`와 함께 복사하도록 확장.
   `background_shader_id`는 재사용하지 않았다(티켓 방침).
2. **렌더링 판단 일원화** — `badgeBackgroundTheme.ts`에 우선순위
   `background_animation` > `background_video_url` > `background_image_url` > `background_color`를
   구현. 애니메이션이 켜져 있으면 `getBadgeBackgroundStyle`은 빈 스타일을,
   `getBadgeBackgroundVideoUrl`은 null을, `hasBadgeBackgroundTheme`은 false를 돌려준다
   (전체 배경 레이어를 CSS·영상 모두 통째로 비운다).
3. **렌더러** — `lib/blobAnimation.ts`(React·DOM 비의존 순수 모듈: 파라미터 정의·정규화·
   `drawBlobFrame`) + `components/BlobAnimationBackground.tsx`(클라이언트 컴포넌트, 성능 가드).
4. **적용 지점** — `BadgeHeroSection` / `ItemBookHeroSection` 두 Hero 컴포넌트에만 연결했고,
   어드민 미리보기는 같은 컴포넌트를 재사용하므로 자동으로 따라온다(미리보기 전용 구현 없음).
5. **어드민 저작 UI** — `BackgroundGeneratorPreview`에 모드 라디오(배경색/애니메이션)를 얹고,
   파라미터 컨트롤은 `BlobAnimationFields.tsx`로 분리. 3개 폼(BadgeForm/ItemBookForm/FactionForm)에
   동일하게 배선.
6. **저장 API 6곳 + 캐스케이드 2곳** 확장.

7. **개선 리뷰 반영 라운드**(게이트 PASS 이후, 사용자 승인 하 일괄 반영) — 아래 «개선 리뷰 반영
   내역» 참고. 가독성(기본 배경색 `#ffffff` → `#555555`), 속도 슬라이더 텔레포트 수정, 첫 페인트
   폴백·페이드 인, 접근성 3신호 독립 처리, 모션 품질(무리수 하모닉·펄스 진폭), 재생 절제(뷰포트
   진입 후 8초 재생 → 감속 정지), 성능(1/3 해상도 오프스크린 블러), 어드민 UI shadcn 정합
   (네이티브 라디오 → Tabs), 용어표 갱신, 데이터모델 문서 정정.
8. **인터랙션 리뷰 반영 라운드**(게이트 WARN 대응) — **재생 절제(F1)를 되돌려 뷰포트 안에서는
   계속 재생한다**(사용자 결정). 접근성 역행 1건(reduced-transparency에서 대비 악화), 프레임당
   강제 리플로우, opacity 선언형/명령형 결합, 낮은 blur 오프스크린 아티팩트, 마운트 중복 프레임,
   제목 자간을 함께 정리했다. 아래 «인터랙션 리뷰 반영 내역» 참고.

### 변경된 파일
```
jam-web/supabase/migrations/124_background_animation_column.sql   (신규)
jam-web/src/lib/blobAnimation.ts                                   (신규)
jam-web/src/components/BlobAnimationBackground.tsx                 (신규)
jam-web/src/app/admin/badges/BlobAnimationFields.tsx               (신규)
jam-web/src/lib/__tests__/blobAnimation.test.ts                    (신규)
jam-web/src/lib/badgeBackgroundTheme.ts
jam-web/src/lib/__tests__/badgeBackgroundTheme.test.ts
jam-web/src/lib/badge-engine/__tests__/walking-badges-v4.test.ts
jam-web/src/types/database.ts
jam-web/src/types/database.generated.ts
jam-web/src/app/(main)/badges/[id]/BadgeHeroSection.tsx
jam-web/src/app/(main)/badges/[id]/page.tsx
jam-web/src/app/(main)/collections/[id]/ItemBookHeroSection.tsx
jam-web/src/app/(main)/collections/[id]/page.tsx
jam-web/src/app/admin/badges/BackgroundGeneratorPreview.tsx
jam-web/src/app/admin/badges/BadgeDetailPreviewFrame.tsx
jam-web/src/app/admin/badges/BadgeForm.tsx
jam-web/src/app/admin/itembooks/ItemBookDetailPreviewFrame.tsx
jam-web/src/app/admin/itembooks/ItemBookForm.tsx
jam-web/src/app/admin/factions/FactionForm.tsx
jam-web/src/app/api/admin/badges/route.ts
jam-web/src/app/api/admin/badges/[id]/route.ts
jam-web/src/app/api/admin/itembooks/route.ts
jam-web/src/app/api/admin/itembooks/[id]/route.ts
jam-web/src/app/api/admin/itembooks/[id]/apply-background/route.ts
jam-web/src/app/api/admin/factions/route.ts
jam-web/src/app/api/admin/factions/[id]/route.ts
jam-web/src/app/api/admin/factions/[id]/apply-background/route.ts

# 개선 리뷰 반영 라운드 추가분
Service Plan/Specs/PRD/02_DATA_MODEL.md          (J1 — 배경 컬럼·렌더링 우선순위·캐스케이드 정정)
Service Plan/Specs/UX_WRITING_GUIDELINE.md       (I4 — 저작 도구 용어표 신설)

# 인터랙션 리뷰 반영 라운드 추가분
jam-web/src/app/globals.css                      (캔버스 페이드 인 CSS — data-ready 토글)
```

### 테스트 결과

(인터랙션 리뷰 반영 라운드 기준 — origin/staging `208dd1cf` 리베이스 후 전용 워크트리에서 재측정)

- [x] `npx tsc --noEmit` — 오류 0건
- [x] `npm run lint`(전체) — 에러 0건 / 경고 13건 (전부 staging 사전 존재분: design-system 스토리·
      `<img>` 경고 등. 이번 변경으로 새로 생긴 경고 없음)
- [x] `npx vitest run`(전체) — 724건 중 723건 통과. 실패 1건은
      `design-system/components/patterns/BadgeRevealCarousel.stories.tsx` play 테스트로
      이번 변경과 무관한 사전 존재 실패(이번 티켓은 design-system을 건드리지 않았다)
- [x] `npm run build` — 성공
- [x] 신규 테스트: `blobAnimation.test.ts`(파라미터 정규화 + 기본 배경색 명도대비 + 속도 주기
      계산 + **reduced-transparency 면색 대비 12건**), `badgeBackgroundTheme.test.ts`에
      애니메이션 우선순위 7건 추가 (1차 FAIL 대응으로 '애니메이션 + 영상 동시 보유' /
      '캐스케이드 직후 하위 배지 상태' / '해제 시 영상 배경 복귀' 3건 추가)
- [ ] 실기기/스테이징 화면 확인 — DB 마이그레이션 미적용 + 이 브랜치가 아직 staging에 병합되지
      않은 상태라 `jam-stage.vercel.app`에는 변경이 반영돼 있지 않다. 마이그레이션 실행 +
      staging 병합 후 확인 필요. **특히 눈으로 확인해야 할 항목**: 기본 배경색 위 배지명 가독성,
      스크롤로 카드를 벗어났다 되돌아올 때 재개, 어드민 속도 슬라이더 드래그 시 블롭이 튀지
      않는지, **blur를 하한(0.01) 근처로 낮췄을 때 실루엣이 계단으로 보이지 않는지**(축소 경로
      임계값), 장시간 열어둔 상태의 프레임 안정성.

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

신규 문구는 전부 **어드민 저작 화면 전용**이다(유저 노출 화면에는 새 문구가 없다).

- [x] 용어 일관성: 배경색·애니메이션·카드 등 기존 저작 화면 용어 유지.
      **개선 라운드에서 정정** — "블롭 색상"·"시드"·"흐림"은 용어표에 없던 신규 용어였다.
      가이드 규정(표에 없는 용어는 표를 갱신한 뒤 사용)에 따라 `UX_WRITING_GUIDELINE.md` §1-3에
      «저작 도구 용어 (어드민 전용)» 표를 신설하고 **블러 / 시드 / 무작위**를 등재했다.
      화면 라벨 "흐림"은 표에 맞춰 "블러"로 바꿨다. "무작위"는 기존 어드민(`AmbientDropForm`)과
      이미 일치해 그대로 두고 표에만 고정했다. "블롭 색상"의 '블롭'은 애니메이션 타입 고유명이라
      용어표 대상이 아니다
- [x] 톤앤매너: 저작 도구 안내 톤 유지
- [x] 에러 메시지: 배경색 형식 오류 메시지는 기존 문구 그대로(3단계 구조 유지)
- [x] 문장 규칙: 해요체·간결. "배경색과 애니메이션 중 하나를 골라요.",
      "색상 4개가 블롭 6개에 순환해서 칠해져요." 등
- [x] 표기 규칙: 해당 없음

### 배포 정보
- 배포일:
- 환경: production
- 커밋:

### 주요 의사결정 / 핵심 메모

1. **애니메이션을 켤 때 `background_image_url`/`background_video_url`을 null로 덮지 않는다.**
   (티켓 상세요구 #2가 구현자 판단으로 남긴 항목)
   근거: 선행 티켓 20260901_1929가 저장 payload에서 이 두 필드를 의도적으로 생략해 기존 DB 값을
   보존하도록 바꿨다 — 제너레이터가 사라져 **다시 만들 수 없는 값**이기 때문이다. 여기서 파괴적
   null 덮어쓰기를 되살리면 그 결정을 뒤집는다. 대신 우선순위 충돌은 **렌더링 단계**에서 해결했다:
   `getBadgeBackgroundStyle`이 애니메이션이 있으면 빈 스타일을 돌려주므로, 과거 이미지 배경이
   DB에 남아 있어도 화면에는 절대 나타나지 않는다. 데이터는 보존하고 "무엇을 그릴지"만 한 곳
   (`badgeBackgroundTheme.ts`)에서 결정한다. `background_color`도 같은 이유로 남겨둔다 —
   모드를 배경색으로 되돌리면 이전 색이 그대로 살아나 저작 편의가 오히려 좋아진다.

2. **`hasBadgeBackgroundTheme`은 애니메이션 모드에서 false다.** 이 판정의 의미는 "고정 배경
   레이어에 무언가 그려지는가"이고, 애니메이션은 그 레이어가 아니라 카드 안에 그려진다. 따라서
   TopNav·본문은 배경 없는 화면과 동일하게(`--color-surface` / `PAGE_BG`) 유지된다.

   **[1차 게이트 리뷰 FAIL → 수정]** 1차 구현에서 이 분기를 `getBadgeBackgroundStyle`·
   `hasBadgeBackgroundTheme` 두 함수에만 넣고 `getBadgeBackgroundVideoUrl`을 빠뜨렸다. 그 결과
   "전체 배경 레이어와 카드 애니메이션이 동시에 켜지는 경우는 발생하지 않는다"고 적었던 기록은
   **사실과 달랐다** — CSS 배경만 비고 반복 MP4는 계속 재생돼, 화면 전체를 덮는 영상과 카드 안
   블롭이 동시에 돌았다. 프로덕션 DB에 `background_video_url`이 채워진 행이 badges 351건 /
   item_books 10건 / factions 1건으로 사실상 대부분이라 엣지 케이스도 아니었다.
   `getBadgeBackgroundVideoUrl`에도 동일한 `parseBlobAnimation` 분기를 넣어 **세 함수가 모두**
   "애니메이션이 있으면 전체 배경 레이어 없음"으로 답하도록 고쳤고, 회귀 방지 테스트
   (애니메이션 + 영상 동시 보유 / 캐스케이드 직후 상태 / 해제 시 영상 복귀)를 추가했다.
   수정 후 위 문장이 실제로 성립한다.

3. **성능 가드**(티켓 필수) 전부 구현: `prefers-reduced-motion: reduce` → 루프 없이 정지 1프레임
   유지(배경이 통째로 사라지면 카드가 비어 보이므로 display:none 방식은 쓰지 않았다),
   `IntersectionObserver`로 뷰포트 밖 정지, `visibilitychange`로 백그라운드 탭 정지,
   DPR 캡(모바일 1.5 / 그 외 2) + 모바일 프레임 간격 32ms, 언마운트 시 rAF·옵저버·리스너 전량 해제.

4. **`npm run db:types`를 실행하지 않았다.** 이 명령은 원격 Supabase의 **현재** 스키마를 읽는데,
   마이그레이션 124가 아직 적용 전이라 실행하면 새 컬럼이 빠진 파일이 생성돼 오히려 회귀한다.
   `database.generated.ts`의 badges/item_books/factions Row·Insert·Update 9곳에
   `background_animation: Json | null`을 수기로 추가했다(생성기가 쓰는 알파벳 순서·optional 규칙을
   그대로 따랐다). **마이그레이션 적용 후 `npm run db:types`를 한 번 돌려 수기 편집과 일치하는지
   확인할 것.**

5. **파라미터 정규화(`parseBlobAnimation`)를 관용적으로 설계했다.** jsonb는 스키마 검증이 없어
   깨진 값이 들어올 수 있는데, 배경은 장식이라 데이터 하나로 상세화면이 죽으면 안 된다. 타입이
   틀리면 `null`(애니메이션 없음), 개별 필드만 틀리면 기본값, 범위를 벗어난 숫자는 클램프한다.
   테스트 작성 중 `Number(null) === 0` 때문에 `blur: null`이 기본값이 아니라 최솟값으로 눌리는
   버그를 발견해 강제 변환을 제거했다.

6. **참조 스크립트의 aperture_size / edge_softness는 채택하지 않았다** — 원형 마스크 전용
   파라미터이고, 사용자가 "카드 전체 영역에 보이도록" 요청해 마스크 자체를 제거했다. 라운드
   클리핑은 카드의 `overflow-hidden`이 담당한다.

7. **마이그레이션 번호를 122 → 124로 재번호했다.** 1차 구현 이후 원격 staging에
   `123_backfill_feed_event_at_approx.sql`이 들어와, 122로 두면 이미 적용된 123보다 뒤에
   실행되는 순서 역전이 생긴다. 원격 staging 기준 최신은 123이므로 124를 쓴다(122는 빈 번호로
   남는다 — 되메우지 않는다). `database.generated.ts`는 컬럼 정의만 담아 번호와 무관하다.

8. **일괄 적용 캐스케이드에서도 `background_video_url`/`background_image_url`을 복사하거나
   지우지 않는다.** (1차 게이트 리뷰가 "부수 결함"으로 지적한 항목)
   근거는 의사결정 1과 같다 — 제너레이터가 사라져 다시 만들 수 없는 값이라 파괴적으로 덮지
   않는다. 대신 렌더링 우선순위가 충돌을 막는다: 캐스케이드 직후 하위 배지가 갖게 되는
   "부모의 애니메이션 + 자기 예전 영상" 상태에서 세 함수가 모두 "전체 배경 레이어 없음"으로
   답하는 것을 테스트로 고정했다. 라우트 2곳과 RPC 주석에도 이 관계를 명시했다.

### 개선 리뷰 반영 내역 *(게이트 PASS 이후, 사용자 승인 하 일괄 반영)*

기능 스펙은 게이트에서 이미 충족됐으므로 **기존 동작을 깨지 않는 것을 최우선**으로 반영했다.

**가독성**
- `DEFAULT_BLOB_ANIMATION.bgColor` `#ffffff` → **`#555555`**. `--color-text`가 `#ffffff`라
  어드민에서 '애니메이션'을 고르는 즉시 카드 안 배지명이 사라지던 결함이었다. `#555555`는 흰
  텍스트와 명도대비 약 7.46:1(WCAG AA·AAA 통과)이고, 서비스가 다크 테마인데 기본값만 라이트였던
  불일치도 함께 없앴다. 블롭 팔레트 4색은 사용자 지정 브랜드 색이라 유지. 회귀 방지로 명도대비를
  계산해 4.5:1 초과를 확인하는 테스트를 넣었다.
- 팔레트에 `#ffe5d1` 같은 밝은 색이 있어 블롭 위에서는 여전히 흰 텍스트가 묻힐 수 있다.
  `hasBadgeBackgroundTheme`의 의미("전체 배경 레이어에 무언가 그려지는가")는 **바꾸지 않고**,
  Hero의 텍스트에만 `getBadgeThemedTextStyle(true)` 상당의 그림자를 되살렸다(배지·컬렉션 양쪽).
  제목에는 `letter-spacing: 0.01em`을 함께 준다(본문은 줄바꿈이 달라져 제외).

**속도 슬라이더 텔레포트**
- `drawBlobFrame`의 5번째 인자를 "경과 ms"에서 **"애니메이션 위상 t(rad)"** 로 바꿨다. 예전에는
  그리기 시점에 `speed`를 곱해, 슬라이더를 움직이면 지나온 위상 전체가 재스케일돼(경과 30초에서
  speed 1→2면 t가 15→30 rad) 블롭 6개가 순간이동했다. 누적을 컴포넌트로 옮겨
  `tRef += delta * 0.001 * speed * BLOB_PHASE_RATE`로 바꿨다. 서비스에서는 speed가 상수라 결과가
  동일해 회귀 위험이 없다. (이 수정은 아래 F1이 되돌려진 뒤에도 그대로 유효하다.)

**첫 페인트 / 폴백**
- Hero 카드 div에 **서버에서** `style={{ backgroundColor: 애니메이션 bgColor }}`를 인라인으로
  준다(파라미터가 이미 서버 컴포넌트에 있어 추가 비용 0). `getContext('2d')` 실패나
  SSR~하이드레이션 구간에 카드가 통째로 비었다가 하드컷되던 문제의 폴백 — 영상 배경의 poster와
  같은 역할이다.
- 캔버스는 `opacity 0 → 1`로 `--duration-fast` / `--ease-smooth-out` 페이드 인한다. reduced-motion
  정지 프레임도 같은 경로로 들어온다.

**접근성 — 세 신호를 독립적으로 처리**
- `prefers-reduced-motion: reduce` → 정지 프레임의 위상을 항상 `t = 0`이 아니라 **고정 오프셋
  `BLOB_STILL_T = 6`** 으로 잡는다. t=0은 하모닉 위상이 seed에만 의존해 시드에 따라 블롭이 뭉치는
  구도가 나오고, 저작자가 그 화면을 확인할 방법도 없었다.
- `prefers-contrast: more` → 블롭을 그리지 않고 `bgColor` 단색으로 평탄화한다(위 텍스트 그림자는
  유지).
- `prefers-reduced-transparency: reduce` → 블롭 알파 1.0 / blur 절반.
- 세 미디어쿼리 모두 `change`를 구독해 런타임 변경에 반응하고 언마운트 시 해제한다. 대비·투명도는
  재생 여부가 아니라 "무엇을 그리는가"만 바꾸므로 `sync()`가 아니라 한 장 다시 그린다.

**모션 품질**
- 위치가 순수 sin/cos 쌍이라 눈에 보이는 8자/타원 레일을 왕복하고 6개가 같은 t를 공유해 은근히
  동기화됐다. 축마다 **황금비 배 2차 하모닉**을 겹쳤다(`0.65·sin(a·t+φ) + 0.35·sin(1.618·a·t+ψ)`).
  주파수 비가 무리수라 궤도가 닫히지 않아 루프감이 사라진다. 진폭 합은 1.0으로 예전과 같아 블롭이
  카드 밖으로 더 나가지 않는다. 블롭별 기본 주파수·seed 배수는 예전 값을 그대로 옮겼다.
- 스케일 펄스 진폭 ±0.5 → **±0.28**. 극점에서 X 0.5·Y 1.5까지 벌어져 "호흡"이 아니라 "펌핑"으로
  읽혔다. 두 축 위상차도 π/2(sin/cos 조합의 반대 위상)에서 **1.0 rad**으로 좁혀 살짝 커플링했다.
- 블롭 알파 0.85 → **0.7**. 겹칠수록 탁해지던 것을 완화해 팔레트가 살아난다.

**지속 재생 절제 (F1) — ⚠️ 다음 라운드에서 사용자 결정으로 전량 되돌림. 아래 «인터랙션 리뷰 반영
내역» 참고. 이 항목은 이력으로만 남긴다.**
- 뷰포트 진입 후 **8초 재생 → 2초 감속 → 정지**, 뷰포트 재진입 시 재시작. 430×430 카드가 화면의
  주인공인데 배경이 무한히 움직여 배지 아트와 경쟁했고, 배터리·발열도 누적됐다.
- 되돌리기 쉽도록 `PLAY_DURATION_MS` / `DECELERATE_MS` 상수 두 개와 `playbackFactor()` 함수
  하나로 분리했다 — 무한 재생으로 되돌리려면 `playbackFactor`가 항상 1을 반환하게 하면 된다.
- 감속 곡선은 `(1 - p)^4`. `--ease-smooth-out`을 **위치가 아니라 속도 배수**에 적용하므로 그
  easing의 도함수 형태를 쓴다 — 감속 시작 시점 배수가 1이라 속도 불연속(툭 끊김)이 없다.

**성능**
- 블롭을 **1/3 해상도 오프스크린 캔버스**에 (블러 반경도 자동으로 1/3) 그린 뒤 `drawImage`로 확대
  합성한다. `ctx.filter`의 blur는 프레임당 6회 적용되고 blur 0.8·DPR 2·430px면 반경이 약 103px라
  가장 비싼 지점이었는데, 축소로 비용이 약 1/9이 된다. 결과물이 어차피 강한 블러라 육안 차이가
  사실상 없다. 오프스크린 캔버스를 만들 수 없는 환경(테스트 등)은 예전 경로로 폴백한다.
- 프레임 스로틀 `coarse ? 32 : 16` → **`coarse ? 32 : 0`**. 16ms 고정은 120Hz 데스크톱을 60fps로
  묶었다. fine 포인터는 rAF에 맡긴다. 모바일 32ms는 유지했다 — 오프스크린으로 여유가 생겼지만
  모바일 GPU 편차가 크고, 위 재생 절제가 이미 총 재생 시간을 8~10초로 묶는다.
- 마운트 시 `render()` + `sync()`로 같은 프레임을 두 번 그리던 것을 `sync()` 하나로 정리했다.

**크래프트 / 구조**
- `BlobAnimationBackground`의 `className ?? '...'` **치환**을 **병합**으로 고쳤다. 호출부가 클래스를
  하나라도 넘기는 순간 `absolute inset-0` / `pointer-events-none`이 통째로 사라지는 구조였다.
- `BLOB_ANIMATION_RANGES.speed` `0.1~5`(step 0.1) → **`0.25~2.5`(step 0.05)**. 선형 슬라이더에 50배
  범위라 쓸모 있는 구간이 중앙에 몰려 있었고 0.1은 한 바퀴 약 126초로 사실상 정지였다. 라벨에
  `blobCycleSeconds()`로 계산한 "한 바퀴 약 N초"를 함께 노출한다. 범위 밖으로 이미 저장된 값은
  `parseBlobAnimation`의 클램프가 흡수한다(테스트로 고정).
- `drawSmoothBlob`의 `Math.max(radius * 0.1, ...)` 하한은 **현 상수 조합에서 발동하지 않는다**
  (세 항 진폭 합 0.75 → `1 + variance` 최솟값 0.25). 하모닉 변경은 위치 계산 쪽이라 이 조건은
  달라지지 않았다. 주석을 "지금은 동작하지 않는 방어선이며, 진폭 상수를 키울 때만 의미를 갖는다"로
  사실에 맞게 정정했다.
- `BadgeHeroSection`의 badge prop에 `background_animation`을 **필수**로 추가했다. 이 필드가 없으면
  내부 `hasBadgeBackgroundTheme(badge)`가 애니메이션 분기를 못 타, "판단은 `badgeBackgroundTheme.ts`
  한 곳"이라는 원칙이 이 컴포넌트에서만 깨진다(현재는 카드 클래스가 `backgroundAnimation || themed`라
  결과가 같아 무해했지만 구조적으로 어긋난 유일한 지점이었다). `BadgeRow`에서 Pick하지 않고
  `unknown`으로 선언한 이유는 `BadgeRow.background_animation`이 `Record<string, unknown> | null`이라
  아직 저장 전인 편집 중 `BlobAnimationParams`를 그대로 넘길 수 없기 때문이다 — jsonb 원본을 받아
  파싱은 전적으로 `badgeBackgroundTheme.ts`가 하는 `BackgroundThemeSource`와 동일한 규약으로 맞췄다.
- `parseBlobAnimation`이 `type`이 `'blob'`이 아닐 때 조용히 null을 반환하던 것에 **개발 모드 한정
  `console.warn`** 을 추가했다. 조용한 null은 애니메이션이 사라지는 동시에 전체 배경 레이어가
  예고 없이 되살아나는 결과라 화면만 보고 원인을 짚기 어렵다.

**어드민 UI**
- `BackgroundGeneratorPreview`의 네이티브 `input type="radio"` 2개(어드민 유일)를 shadcn 세트의
  **`Tabs`** 로 교체했다. 세트에 `radio-group`이 없어 두 안 중에 골랐다 —
  (a) `@radix-ui/react-radio-group` 의존성 신규 추가, (b) 이미 세트에 있는 `Tabs` 사용.
  **모드 선택이 곧 아래 설정 패널 전체의 교체**이므로 tab/tabpanel이 의미상 정확하고 새 런타임
  의존성도 늘지 않아 (b)를 택했다. 슬라이더·컬러피커는 세트에 대체재가 없어 네이티브 유지가
  합리적이라 그대로 뒀다.

**문서**
- `Specs/PRD/02_DATA_MODEL.md` — badges 절에 `background_animation` 행 추가(Hero 카드 **안**에서
  라이브 실행, 전체 배경 레이어와 렌더링 지점이 다르다는 점 명시) + «배경 렌더링 우선순위» 절 신설.
  **낡았던 "4필드 스냅샷을 복사" 서술을 현재 사실로 정정** — 마이그레이션 121(20260901_1929)에서
  `background_color` 1필드로 줄었고 이번에 `background_animation`이 더해져 지금은 2필드다.
  "캐스케이드는 영상·이미지를 복사도 삭제도 하지 않고 렌더링 우선순위로 충돌을 막는다"는 관계와,
  이번 게이트 FAIL이 정확히 그 규칙의 부분 적용에서 나왔다는 사실도 남겼다. item_books·factions
  절도 함께 갱신했다.
- `Specs/UX_WRITING_GUIDELINE.md` — «저작 도구 용어 (어드민 전용)» 표 신설(블러 / 시드 / 무작위).

**이번 라운드에서 의도적으로 적용하지 않은 것** (리뷰어가 명시적으로 보류)
- `resolveBackgroundLayer(source)` 단일 진입점 리팩터 — "다음에 이 모듈을 건드릴 때 후보로만".
- `BlobAnimationBackground`의 MODULAR 승격 — "프로덕션 2~3주 실사용 후 재평가".
- 실측 프레임 비용 기반 적응형 DPR 백오프 — 과한 엔지니어링.
- `BadgeRevealCarousel.stories.tsx` play 테스트 실패 / 배경색 모드 캐스케이드의 잔존 영상 /
  일괄 적용 다이얼로그 합쇼체 — 이번 티켓 회귀가 아닌 사전 존재분이라 별도 티켓 대상.

### 인터랙션 리뷰 반영 내역 *(게이트 WARN 대응 — 마지막 라운드)*

기능 스펙은 두 차례 PASS를 받았으므로 **회귀를 만들지 않는 것이 최우선**이었다.

**F1(8초 재생 후 감속 정지) 전량 되돌리기 — 사용자 결정**
- 뷰포트 안에서는 **계속 재생한다.** 무한 재생이 원래 요청("애니메이션 배경")에 맞고, 성능은
  이미 다른 수단으로 확보돼 있다(IntersectionObserver 뷰포트 밖 정지 + `visibilitychange`
  백그라운드 탭 정지 + DPR 캡 + 축소 오프스크린 블러).
- `playbackFactor()` / `PLAY_DURATION_MS` / `DECELERATE_MS` / `playedMs` 누적을 **전부 제거**했다
  (죽은 코드로 남기지 않았다). `tick()`은 `tRef += delta·speed·BLOB_PHASE_RATE`만 누적하고
  루프를 스스로 접지 않는다.
- 뷰포트 밖 정지 / 백그라운드 탭 정지 / reduced-motion 정지 프레임은 **그대로 유지**했다 —
  F1과 별개인 기존 성능·접근성 가드다.
- F1 제거로 인터랙션 리뷰가 지적한 부작용 5건이 함께 해소된다(별도 조치 없음): 어드민 미리보기가
  10초 뒤 얼어 속도 슬라이더가 죽던 문제, 스크롤 재진입마다 8초 버스트가 반복되던 문제, speed
  하한(0.25)에서 8초간 57°만 돌아 슬라이더 범위가 무의미해지던 문제, 감속 곡선이 8.8초에 사실상
  멈춘 뒤 1.2초간 프레임만 낭비하던 문제, "최종 화면"이 둘(t=6 / t≈4.2)이던 문제.
  → **`BLOB_STILL_T = 6`이 유일한 정지 구도**가 됐다.
- `BlobAnimationFields`의 "한 바퀴 약 N초" 라벨은 이제 실제로 완주하는 주기를 뜻하므로 그대로 뒀다
  (F1 때문에 거짓이던 안내가 참이 됐다).

**접근성 역행 수정 — `prefers-reduced-transparency: reduce`**
- 예전에는 이 신호에서 블롭 알파만 0.7 → **1.0**으로 올렸다. 이 카드의 전경 텍스트는 흰색
  (`--color-text`)인데 팔레트의 밝은 색(`#ffe5d1`)이 배지명 뒤를 지날 때 대비가
  **1.91:1 → 1.21:1로 악화**됐다 — 접근성 신호를 켠 사용자가 더 나쁜 화면을 받는 역행이다.
  apple-design §14의 의도는 "반투명 재질을 **불투명하게**"이지 "**더 밝게**"가 아니다.
- **택한 해법(가이드 제시안 중 2번)**: 알파는 1.0으로 올리되 블롭 색을 `bgColor` 쪽으로
  `BLOB_ALPHA`(0.7)만큼 미리 섞는다(`opaqueBlobFill`). 결과 색이 "일반 경로에서 블롭 한 장이
  배경 위에 놓였을 때의 합성색"과 **정확히 같아**, 밝기가 일반 경로보다 올라가지 않는다. 겹침으로
  더 밝아지던 경우는 오히려 사라진다(= 반투명 적층 완화라는 원 취지도 충족).
  `flatten`(블롭 제거)을 택하지 않은 이유는 그것이 `prefers-contrast: more`의 처리이고, 두 신호를
  독립적으로 다룬다는 이 구현의 원칙(개선 라운드에서 확립)을 무너뜨리기 때문이다.
- blur를 절반으로 줄이던 처리는 유지했다 — 밝기를 건드리지 않고 반투명·흐림 적층만 완화한다.
- 회귀 방지 테스트 12건: 팔레트 4색 각각에 대해 ① 일반 경로 1겹 합성색과 동일 ② 흰 텍스트 대비가
  예전(알파만 1.0) 방식보다 나아짐 ③ 일반 경로보다 나빠지지 않음.

**프레임당 강제 리플로우 제거**
- `render()`가 매 프레임 시작에 `canvas.clientWidth`/`clientHeight`를 **읽고** 마지막에
  `canvas.style.opacity`를 **썼다** — 스타일 쓰기 → 다음 프레임 레이아웃 읽기가 반복돼 프레임마다
  강제 동기 레이아웃이 걸렸다(블러 비용을 1/9로 줄인 성과를 갉아먹는 구조).
- 치수는 이미 구독 중인 `ResizeObserver` 콜백에서만 갱신하는 캐시(`cssWidth`/`cssHeight`)로 옮기고
  render는 캐시만 읽는다. 마운트 직후 첫 프레임을 위해 `measure()`를 한 번 직접 호출한다
  (ResizeObserver의 최초 콜백은 다음 프레임에나 오기 때문).
- opacity는 `ready` 플래그로 **첫 프레임에서 한 번만** 쓴다.

**opacity 선언형/명령형 결합 제거**
- `style={{ opacity: 0 }}`(React)와 `canvas.style.opacity = '1'`(명령형)이 같은 속성을 다투고
  있었다. 지금은 React가 동일 값 style을 diff에서 걸러 우연히 동작하지만, style에 값이 하나만 더
  추가되면 리렌더마다 캔버스가 투명으로 되돌아가는 회귀가 난다.
- `globals.css`에 `.blob-animation-canvas` / `[data-ready='true']` 규칙을 두고 컴포넌트는
  `canvas.dataset.ready = 'true'`만 한 번 토글한다. 인라인 style을 완전히 제거해 결합을 끊었다.
  (같은 파일의 `.badge-background-video` 선례와 동일한 방식이다.)

**낮은 blur에서 오프스크린 아티팩트**
- `BLOB_SCRATCH_SCALE = 1/3` + bilinear 3배 확대인데 슬라이더 하한은 `blur: 0.01`이다.
  430px·DPR 2에서 반경 약 1.3px — 선명한 곡선을 1/3로 그린 뒤 확대하는 꼴이라 실루엣이 계단으로
  보인다.
- **blur 반경이 `BLOB_SCRATCH_MIN_BLUR_PX = 6`px 이상일 때만 축소 경로**를 타게 했다. 근거는
  주석에 남겼다 — 3배 확대의 재구성 오차가 대략 확대 배율(≈3px)만큼 퍼지므로 그 2배를 안전
  여유로 잡았다. 임계값 미만이면 blur 비용 자체가 작아 축소로 아낄 것도 없다(축소는 반경이 클 때만
  이득이다). 기본값 blur 0.54는 반경 약 70px라 그대로 축소 경로를 탄다.

**마운트 중복 프레임 (절반만 정리돼 있던 것 마무리)**
- setup effect 끝의 `render()`는 앞 라운드에서 제거됐으나, `params` effect가 마운트 시에도 실행돼
  `redrawRef.current?.()`를 불러 첫 프레임이 여전히 두 번 그려졌다. `paramsSyncedRef`로 첫 실행만
  건너뛴다(무해했지만 주석·완료 기록 서술과 실제가 어긋나 있었다).

**자간 원복**
- `BadgeHeroSection`의 `cardTextStyle`에 걸었던 `letterSpacing: 0.01em`은 상속 속성이라 볼드 제목뿐
  아니라 `RarityBadge` 라벨까지 벌렸고, apple-design §15는 큰 표제에 negative tracking을 권하므로
  `--text-heading-sm` 볼드 제목에 +0.01em은 방향이 어긋난다. **제거하고 그림자만으로 가독성을
  확보**한다. `ItemBookHeroSection`의 36px 제목 자간도 같은 이유로 제거했다.

**펄스 주파수 동기화**
- 궤도는 무리수 하모닉으로 루프감을 없앴는데 펄스는 6개가 **전부 주파수 0.8**(위상만 오프셋)이라
  speed 1 기준 약 15.7초마다 여섯 개가 함께 부풀었다 줄어드는 신호가 남아 있었다 — 루프감이 가장
  지각되기 쉬운 지점이다. 황금비의 소수부(`(i·PHI) mod 1`)로 0.8~1.2배를 흩어 궤도와 일관되게
  했다. **진폭(`BLOB_PULSE`)은 그대로**라 형태가 커지는 범위는 달라지지 않아 리스크가 낮다.

**문서**
- 마이그레이션 `124_background_animation_column.sql` 헤더 주석의 저장 형태 예시가 아직
  `"bgColor": "#ffffff"`였다 — 기본값 변경(`#555555`)에 맞춰 정정했다(실행 결과에는 영향 없는
  설명 주석).
- `blobAnimation.test.ts`의 "기본 배경색 가독성" 테스트는 실제로는 **"블롭이 없는 곳의 대비"**를
  검증한다(텍스트 뒤 최상단 레이어는 블롭이고 밝은 팔레트에서는 1.9:1까지 떨어진다 — 실질
  가독성은 `getBadgeThemedTextStyle`의 그림자가 지탱한다). 이름을 «블롭이 덮지 않은 면(= bgColor)의
  기본 대비»로 바꾸고 주석에 이 사실을 명시해 다음 세션이 오해하지 않게 했다.

**이번 라운드에서 의도적으로 적용하지 않은 것** (가이드가 명시적으로 범위 밖으로 지정)
- `resolveBackgroundLayer` 단일 진입점 리팩터 / `BlobAnimationBackground`의 MODULAR 승격 /
  어드민 컬러 피커 대비 경고·텍스트 스크림·팔레트 명도 상한.
- 사전 존재 이슈(`BadgeRevealCarousel.stories.tsx` 실패, design-system 린트 경고 13건, 배경색 모드
  캐스케이드의 잔존 영상, 일괄 적용 다이얼로그 합쇼체).

### 잔여 이슈
- **컬렉션 대표 이미지가 불투명한 정사각형이면 애니메이션이 가려진다.** `ItemBookHeroSection`의
  이미지는 카드 전체를 `object-contain`으로 채우기 때문이다(배지는 200×200으로 작아 항상 보인다).
  현재 컬렉션 이미지는 대부분 여백이 있는 형태라 실사용 문제는 없을 것으로 보이나, 배경이
  안 보이는 컬렉션이 나오면 이미지 크기 규격을 별도 티켓으로 정해야 한다.
- ~~**로컬 `jam-web/.env.local` 심볼릭 링크가 자기 자신을 가리킨다**(`ELOOP`).~~
  → 오케스트레이터가 복구 완료(2026-09-01). 이후 vitest/build 정상 실행된다.
- **배경색 모드로 되돌리는 일괄 적용에는 예전 영상 배경이 그대로 남는다.** 캐스케이드가
  `background_color` + `background_animation`만 복사하므로, 부모를 배경색 모드로 바꿔 일괄
  적용해도 예전에 영상 배경이 구워진 하위 배지는 그 영상을 계속 재생한다(부모와 다른 화면).
  이번 티켓 이전(마이그레이션 121 / 티켓 20260901_1929)부터 있던 동작이고, 해소하려면 "다시 만들
  수 없는 값을 지운다"는 파괴적 결정이 필요해 이번 범위에서 바꾸지 않았다. 필요하면 별도 티켓.
- ~~`BadgeDetailPreviewFrame`/`ItemBookDetailPreviewFrame`이 `app/spike/background-generator/types`에
  의존한다.~~ → staging의 티켓 20260901_1851이 해당 스파이크 라우트를 삭제하며 `SERVICE_WIDTH`를
  `@/lib/backgroundGenerator/types`로 옮겼고, 개선 라운드의 리베이스에서 그 경로로 정리됐다.
