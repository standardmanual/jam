---
id: 20260905_0037
category: UI
status: OPEN
created: 2026-09-05
---

# [UI] 배지 트리 전면 리뉴얼

> 마스터: `20260905_0026`. 선행: `20260905_0031`(진행 계산) · `20260905_0036`(MODULAR 컴포넌트).

## 배경 / 문제 정의

현행 트리는 «등급 4단 = 가로 레일»을 **데이터 모델·정렬·레이아웃·집계 네 층에서 각각 독립적으로**
전제한다. 한 층만 고쳐도 나머지가 막는다 — **전면 리뉴얼이다.**

| 층 | 지점 | 무슨 일이 일어나는가 |
|---|---|---|
| 데이터 모델 | `badgeTree.ts:294-299` | 계열 눈금이 **`RARITY_ORDER` 4회 루프로만** 생성 — 등급 없는 배지는 stages에 못 들어가고 **화면에서 사라진다** |
| 집계 | `BadgeTreeClient.tsx:104-118` | `byRarity[stage.rarity].total += 1` — 키가 없으면 **런타임 TypeError로 페이지 크래시** |
| 레이아웃 | `BadgeStageRail.jsx:220-332` | 눈금을 flex 가로 배치, **overflow·flexWrap 둘 다 없음**. 8눈금 482px는 카드를 뚫는다 |
| 요약 | `BadgeTreeSummaryHeader.jsx:47` | `repeat(4, 1fr)` — 요약이 «등급 4칸» 그 자체 |
| 정렬 | `badgeTree.ts:34-80` | **배지 이름 72개 하드코딩**. 없는 이름은 `indexOf`가 `-1` → **그리드 맨 앞으로 튀어나온다** |
| 게이트 | `badgeTree.ts:252-253` | 선행 배지 링크가 그 계열의 **Common을 찾는다**. 무한레벨형(Common 없음)을 지목하면 **락이 조용히 사라진다** |
| 게이트 | `badgeTree.ts:240-247` | 미션 락이 있으면 그것만 반환 — «미션 AND 선행배지» 표현 불가 |
| 상태 | `badgeTreeConditionStatus.ts:14` | 4종뿐 — «1단 통과, 2단 대기»를 나타낼 값이 없다 |

**가장 결정적인 것은 계열(레일)의 정의 자체가 «같은 `name`을 4등급 순서로 늘어놓은 것»이라는 점이다.**
등급 축을 빼면 레일을 만들 방법이 없다. 레일은 세로 목록의 가로 압축이 아니라 **등급 서열의 시각화**다.

## 상세 요구사항

### 구조 (프로토타입 확정)
```
[종목 탭]  ← 가로 스크롤, 레이아웃 마진 밖으로 bleed
[요약: 38 / 114 · 다음 목표 4개]

다음 목표                                    ⌄
  [Lv.3] 자기 초월
  ▬▬▬▬▬▬▬▬░░
   9.8 / 12.4km                    2.6km 남음
  ...
받은 배지  38개                              ›
```

- **설계 분류를 노출하지 않는다** — 「누적·주기·최고 기록」은 유저 언어가 아니고
  배지 이름과 뜻이 겹친다. 배지 이름이 곧 구분자다
- 구분은 **「다음 목표 / 받은 배지」 상태 둘뿐**
- 목록은 **다음 목표가 가까운 순** — 이 화면의 질문이 «다음에 뭘 하면 되나»이므로
- 값 행에 **「N 남음」** — 이전에는 `178 / 210km`를 보고 32를 암산해야 했다

### 표현 3종
| 표현 | 대상 | 컴포넌트 |
|---|---|---|
| 단일 게이지 | 무한레벨 | `BadgeLevelGauge` |
| 4눈금 레일 | 등급형 | `BadgeStageRail` v2 |
| 카운터 행 | 반복형 | `BadgeStampRow` |

### 550종 대응
- 종목 탭이 1/5로 줄이고, 상태 섹션 접기가 다시 나눈다
- **펼친 섹션만 진행 계산을 요청** — 현재 `tree/page.tsx:200-237`이 배지마다 2패스를 돌아
  550종이면 요청당 최대 1,100회. 서버 컴포넌트 동기 계산이라 TTFB 직결
- 조회에 limit 없음(`tree/page.tsx:67-72`) — 550행은 PostgREST 기본 1000행 아래지만 여유가 절반뿐.
  체크인 배지는 이미 1,789종이고 `badges/page.tsx:46-52`에 **1000행 절단 사고 기록 + 청킹 선례**가 있다

### 게이트 표현
- 자물쇠 2개 병렬 — 교차(중성)와 미션(프라이머리)을 **색과 형태**로 구분
- 상태 모델 확장 — 「1단 통과, 2단 대기」

### 실루엣
- `BadgeSilhouette`로 5곳의 `grayscale(1)` 교체

## 구현 계획
1. `badgeTree.ts` 재작성 — 계열 정의를 `family_key`·`level`·`sort_order` 기반으로
2. `BadgeTreeClient` 재작성 — 상태 섹션 + 진행도 순 정렬
3. 요약 헤더 교체
4. 게이트 시트 다단계 대응
5. 성능 측정 — 550종에서 TTFB 실측

## UX Writing 검증
- [ ] 정책 용어(무한레벨·반복형·∞) 미노출
- [ ] 「획득」 고정
- [ ] 등급명 영문, 본문에 등급명 미기재
- [ ] 조건 표기 요소 순서 준수

## 잔여 이슈
- 550종 TTFB 실측 미수행 — 구조로는 개선되나 수치 확인 필요

---

## 탐색·재사용 판정 · 전제 실측 (2026-09-06, 오케스트레이터 직접 — `/jam-work` 1.5단계)

### 지금 프로덕션에서 실제로 벌어지고 있는 일

v5 630종이 시딩된 뒤 트리를 실측했다.

| | 종 | 계열 |
|---|---:|---:|
| 등급형 | 298 | — |
| 반복형 | 139 | — |
| **레벨형** | **193** | **26** |

**레벨형 193종 26계열이 지금 트리에서 아예 보이지 않는다.** `badgeTree.ts:258`의
`RARITY_ORDER` 루프가 등급 있는 배지만 `stages`에 담기 때문이다. 같은 파일 `:61` 주석이
그 사실을 이미 알고 있다 — v4에는 레벨형이 없어 무해했지만 지금은 카탈로그의 31%다.

`byRarity[stage.rarity]` 크래시(`BadgeTreeClient.tsx:115`)는 **일어나지 않는다** —
레벨형이 애초에 `stages`에 못 들어가서 그 코드에 도달하지 못한다. 크래시 대신 **조용한 실종**이다.

### 티켓 전제 정정 — 정렬 하드코딩은 이미 해결됐다

티켓이 「`badgeTree.ts:34-80` 배지 이름 72개 하드코딩」을 결함으로 적었으나
**티켓 `20260905_0027`이 이미 `sort_order` 기반으로 바꿨다**(마이그레이션 130).
같은 파일 주석에 「이전에는 이름 72개를 하드코딩했다」고 과거형으로 기록돼 있다.
`sort_order = 0`(미설정)을 맨 뒤로 미는 처리까지 들어가 있다. **구현 계획에서 뺀다.**

### 티켓 전제 정정 — 실루엣은 폐기됐다

「`BadgeSilhouette`로 5곳의 `grayscale(1)` 교체」는 **하지 않는다.**
**2026-09-06 사용자 확정: 미획득 배지는 원본 이미지를 그레이로 표시한다.**
0036이 실루엣으로 바꿨다가 되돌렸고 `BadgeSilhouette` 컴포넌트는 제거됐다.
서비스 5곳도 이미 `grayscale`이라 **손댈 것이 없다.**

### 여전히 유효한 전제

| 지점 | 확인 |
|---|---|
| `badgeTree.ts:258` `RARITY_ORDER` 루프 | 그대로 — 레벨형 실종의 원인 |
| `badgeTree.ts:170` 게이트가 계열의 `common`을 찾는다 | 그대로 — 레벨형엔 common이 없어 락이 사라진다 |
| `badgeTree.ts` 미션 락이 있으면 그것만 반환 | 그대로 — 「미션 AND 선행배지」 표현 불가 |
| `badgeTreeConditionStatus.ts:14` 상태 4종 | 그대로 |
| `BadgeStageRail` overflow·flexWrap 없음 | 0036에서 **4눈금 상한 강제**로 대응 |
| `BadgeTreeSummaryHeader` `repeat(4,1fr)` | 0036에서 `noRarity` 추가로 대응 |

### 재사용 판정 — 0036 산출물을 그대로 쓴다

새 컴포넌트를 만들지 마라. 0036이 이미 만들었고 **아직 서비스에 연결되지 않았다.**

| 표현 | 컴포넌트 | 상태 |
|---|---|---|
| 무한레벨 | `BadgeLevelGauge` | 신규, 미연결 |
| 등급형 4눈금 | `BadgeStageRail` v2 | 개편됨, 서비스 1곳 연결 |
| 반복형 | `BadgeStampRow` | 신규, 미연결 |
| 상태 섹션 | `BadgeStatusSection` | 신규, 미연결 |
| Lv.N 칩 | `BadgeLevelChip` | 신규, 미연결 |
| 진행 바 | `ProgressBar` `fillMode="track-gradient"` | 확장됨 |
| 요약 | `BadgeTreeSummaryHeader` + `noRarity` | 개편됨 |
| 게이트 시트 | `UnlockConditionSheetContent` + `relation`·`req.note`·`level` | 개편됨 |

**모자란 부분이 있으면 서비스에서 자체 구현하지 말고 DS를 확장하라** — 0036이
`ProgressBar`에서 같은 판단을 했다(자체 바를 그리면 표현이 갈린다).

## 0036이 넘긴 것 — 이 티켓에서 처리한다

1. ⚠️ **4눈금 상한이 프로덕션에서는 무방비다.** `BadgeStageRail`은 개발 빌드에서만
   throw한다. 지금은 `badgeTree.ts:258`의 `RARITY_ORDER` 4회 루프가 상한을 보장하는데,
   **이 티켓 구현 계획 1번이 바로 그 루프를 없앤다** — 보장이 사라지는 시점이 여기다.
   프로덕션 경로에 `slice(0, 4)`나 컨테이너 `overflow-x` 중 하나가 필요하다.
   (실측: 현재 데이터에 이름당 5눈금 이상인 계열은 **0건**이다 — 아직은 안전하다)
2. **개편 3종에 `.d.ts`가 없다**(`BadgeStageRail`·`BadgeTreeSummaryHeader`·
   `UnlockConditionSheetContent`). 새 props(`gates`·`earnCount`·`noRarity`·`relation`)의
   **오타가 컴파일에서 안 잡힌다** — `gate: [...]`로 써도 통과하고 화면에서만 조용히 사라진다.
   이 티켓이 그 props를 처음 쓰므로 여기서 `.d.ts`를 만든다.
3. **게이트 시트가 「미션 AND (배지 A OR 배지 B)」를 표현하지 못한다.** `relation`이 항목
   전체에 걸리는 단일 평면 값이고 `requirements`에 충족 여부가 없다. 티켓의 「1단 통과,
   2단 대기」 요구와 직결된다 — `requirements[].met?` 추가인지 `groups` 중첩인지
   **착수 시 정하고 DS를 확장하라.**
4. `req.note` 기본값이 「배지 · 어느 등급이든 1개」다 — 레벨형에 `note`를 안 넘기면
   **조용히 거짓 문장**이 나간다. 기본값을 「배지」로 낮추는 편이 안전하다.
5. `stop.gates`에 충족 여부가 없어 미션을 이미 깬 상태에서도 자물쇠 2개가 같게 그려진다.
6. `BadgeStatusSection`은 **빈 배열이 아니라 `null`을 넘겨야** `emptyText`가 뜬다.
   `{list.map(...)}`를 그대로 넘기면 빈 영역만 남는다(0036에서 `Children.count`로 고쳤으나
   호출부 규약은 그대로다).

## 모듈러-서비스 연결 범위 (`/jam-work` 1.6단계)

이 티켓이 **0036의 신규 4종을 서비스에 처음 연결한다** — 「미연결」에서 「연결된 컴포넌트」로
부류가 바뀐다. 완료 기록에 어떤 호출부가 붙었는지 남긴다.

⚠️ **`src/components/ui/`에 동명의 서비스 사본이 있는지 먼저 확인하라.** 이 저장소는
`TabBar`·`BottomSheet`·`BadgeGridCard` 등 **병존 구현 8종**이 있고, DS만 고치면 서비스는
따라오지 않는다.

---

## 구현 기록 (2026-09-06)

### 무엇을 고쳤나

| 층 | 변경 |
|---|---|
| 데이터 모델 | 계열 정의를 `name` → **`family_key`**로. 눈금 순서는 종류별(등급 4단 / `level` 오름차순 / 등급 4단). 레벨형 193종 26계열이 화면에 처음 나온다 |
| 게이트 | `BadgeTreeLock[]` 평면 → **`BadgeTreeGateGroup[]`**(그룹 안 OR, **그룹 사이 AND**). 미션 락이 있으면 그것만 반환하던 조기 return 제거 — 「미션 AND 선행배지」가 표현된다. v5 2단 교차 게이트(`cross_in_axis`·`cross_between_axis`·`gate_mission_badge`)를 처음 읽는다 |
| 레이아웃 | 계열 종류가 표현을 가른다 — `BadgeStageRail`(등급형) / `BadgeLevelGauge`(레벨형) / `BadgeStampRow`(반복형) |
| 구조 | 「계열 레일 + 트로피 그리드」 → **「다음 목표 / 받은 배지」 상태 섹션 2개**. 다음 목표는 계열 단위·진행 가까운 순, 받은 배지는 배지 단위 그리드(섹션 개수와 같은 단위) |
| 요약 | `noRarity` 버킷 연결 — 레벨형이 어느 칸에도 안 들어가 `totalCount`와 칸 합계가 어긋나던 것을 해소 |
| 조회 | `badges` 조회에 **1000행 청킹 페이지네이션**(`badges/page.tsx` 선례) + `id` 오름차순 고정 |
| 진행 계산 | 프로브 + 본계산 **2패스 → 1패스 + 라벨 백필**. `labelMap`은 축의 `label`/`unit`만 채우고 수치에 관여하지 않으므로 결과가 동일하다 |

### 0036이 넘긴 6건 처리 결과

1. **4눈금 상한** — `BadgeStageRail`이 프로덕션에서도 `slice(0, 4)`(`visibleStops`)로 자른다.
   `badgeTree.ts`도 등급형·반복형을 `MAX_FAMILY_STAGES`로 자른다(이중 방어).
2. **`.d.ts` 3종 신규** — `BadgeStageRail` · `BadgeTreeSummaryHeader` · `UnlockConditionSheetContent`.
   이 티켓이 `gates`·`noRarity`·`groups`를 처음 쓰므로 오타가 이제 컴파일에서 잡힌다.
3. **다단계 게이트** — `requirements[].met?`와 `groups` 중첩을 **둘 다** 넣었다.
   `groups`(2단: 그룹 안 relation, 그룹 사이 AND)가 조합을 표현하고, `met`이 「1단 통과, 2단 대기」를 드러낸다.
   3단 이상은 만들지 않았다 — 엔진의 결합 규칙(교차 2종은 OR, 미션 보상은 AND)이 2단으로 정확히 접힌다.
4. **`req.note` 기본값** — 「배지 · 어느 등급이든 1개」 → **「배지」**. 참인 문장은 `badgeTree.ts`가
   `lock.note`로 명시한다(선행 배지 = 어느 등급이든 1개 / 교차 = `min_rarity` 이상).
5. **`stop.gates[].met`** — 통과한 문은 체크+라임(형태까지 다르다). `aria-label`도 「미션 통과, 선행 배지 대기」로 갈린다.
6. **`BadgeStatusSection`에 `null`** — `nextGoals.length > 0 ? (...) : null` 형태로 넘겨 `emptyText`가 실제로 뜬다.

### 성능 실측 (2026-09-06, 로컬 · Node 합성 데이터)

| 항목 | 값 |
|---|---|
| 활동 400건 · 계열 630개, `computeUserPeriodMetrics` 1회 | 0.3ms |
| 진행 계산 **1패스**(이번 구조) | 1.0~1.1ms (0.002ms/계열) |
| 진행 계산 **2패스**(이전 구조) | 1.3~1.5ms |

**결론: 진행 계산은 TTFB의 병목이 아니었다.** 티켓이 적은 「630종이면 요청당 최대 1,260회 →
TTFB 직결」은 호출 **횟수**로는 맞지만 **시간**으로는 630종 2패스가 1.5ms다. 2패스를 없앤 것은
여전히 옳지만(같은 값을 두 번 구할 이유가 없다), 이 화면의 TTFB는 **DB 왕복 5건 +
`getActivityHistory`(가입 이후 전 활동)**가 지배한다. 다음에 TTFB를 더 줄여야 한다면 그쪽을 봐야 한다.
합성 조건의 반복·휴식 형태가 `unsupported`로 떨어져 그 두 축(활동 재순회)은 측정에서 빠졌다.

### 남긴 것 / 판단

- **상태 4종을 5종으로 늘리지 않았다.** 「1단 통과, 2단 대기」는 눈금 상태가 아니라 **게이트의**
  상태다 — `gateGroups[].fulfilled`(시트)와 `stop.gates[].met`(레일)로 표현했다. 눈금에 5번째
  라벨을 추가하면 `BadgeStageRail`의 캡션 한 줄이 「잠김」과 「부분 통과」를 동시에 말해야 한다.
- **트로피 그리드가 사라진 것이 아니라 「받은 배지」 섹션이 됐다.** 독립 배지는 이제 크기 1인
  계열이고, 미획득이면 「다음 목표」에 계열 행으로, 획득했으면 「받은 배지」 그리드에 들어간다.
- 교차 게이트의 `min_count > 1`은 그룹 `note`(「이 중 N개 이상 필요해요」)로 적었다 —
  OR/AND 두 단어로는 「3계열 중 2개」를 담을 수 없다.
