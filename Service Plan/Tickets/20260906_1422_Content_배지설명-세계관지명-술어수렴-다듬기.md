---
id: 20260906_1422
category: Content
status: OPEN
created: 2026-09-06
---

# [Content] 배지 설명의 세계관 지명이 한 가지 술어로 수렴한다

> 선행: `20260906_1305`(등급별 설명 436행 신규 작성)
> 개선 리뷰 제안에서 분리 (2026-09-06 사용자 승인)
> 라이팅 가이드: claude.ai artifact `fdffc886-3600-4dfb-9ff0-18f2f7d4a9d7` §06

## 배경 / 문제 정의

`20260906_1305`이 등급별 설명 436행을 새로 썼다. 개선 리뷰 판정은 **품질 자체는 높다**는
것이었다 — 등급이 오를수록 문장이 짧고 단정해지는 흐름이 표본 전부에서 지켜졌고, 계열 내
세로 읽기가 하나의 서사로 이어진다. UX 라이팅 가이드 위반도 0건이다.

다만 **한 계열만 볼 때는 안 보이고 여러 계열을 모은 유저에게만 보이는** 반복이 세 갈래 있다.
배지를 수집하는 서비스라 유저는 필연적으로 여러 계열을 나란히 본다.

**① 지명이 늘 「관찰자」로만 등장한다.**
지명 사용량 자체는 46회/436행(약 10%)으로 남발이 아니고 뜻도 맞다
(블랙 트랙=폭염·겨울·악천후, 화이트 룸=한계·심장·끝, 그루터기 살롱=숲·오르막·단골).
그런데 그 46문장의 술어가 「~을 알아봅니다/기억합니다」 11회, 「~의 문은 …에서 열립니다」
5~6회로 쏠린다. 지명이 **배경·장애물·거래처**로 쓰인 문장이 거의 없다.
같은 착상의 세 변주가 종목만 바뀌어 반복되기도 한다 —
「그루터기 살롱은 오래 비운 자리도 치우지 않습니다」(자전거) ·
「…그대로 남겨 둡니다」(트레일러닝) · 「돌아온 사람에게 자리를 비워 둡니다」(러닝).

**② 화이트 룸이 최상위 등급의 기본 기호가 됐다.**
mystic·최상위 레벨 문장 중 약 17건이 화이트 룸으로 끝난다. 개별로는 뜻이 맞지만,
계열을 여러 개 본 유저에게는 **「Mystic = 화이트 룸」이라는 기계적 패턴**으로 읽힌다.
가이드 §06이 경계한 「세 단어가 닳는다」가 바로 이것이다.

**③ 종목이 달라도 축이 같으면 문장 틀이 겹친다.**
자전거 `cycling:C1`과 등산 `hiking:C1`(둘 다 「하루 1회」이고 기준 설명도 동일)의 상위 문장이
rare=「습관」 / epic=「달력」 / mystic=「일정이 비켜간다」로 나란히 대응한다.
같은 조건이니 같은 주제인 것은 자연스럽지만, **두 종목을 함께 모으는 유저에게는 복사처럼
보인다.** 한쪽만 착상을 틀면 해소된다.

## 상세 요구사항

### 컨텐츠 관점

**전면 재작성이 아니다.** 436행 중 손댈 곳만 고친다 — 나머지는 이미 좋다.

- **지명 46문장의 술어를 분산한다.** 「알아본다·기억한다·문을 연다」 계열이 절반을 넘지 않게 한다.
  지명이 배경(그 앞을 지나간다) · 장애물(막아 세운다) · 거래처(값을 매긴다)로 쓰인 문장을
  섞는다. `FACTIONS.md`의 각 지명 설정에서 술어를 새로 길어 온다
- **화이트 룸 사용을 최상위에서 덜어낸다.** 17건 중 절반 이하로 줄이고, 줄인 자리는 지명 없이
  소재 자체로 단정하게 끝내는 문장으로 바꾼다. 「최상위 = 지명」이 규칙처럼 보이지 않아야 한다
- **같은 착상의 종목 간 변주를 정리한다.** 위 ①의 「비운 자리」 3종, ③의 `C1` 대응 등
  **종목을 넘어 대응 관계가 보이는 계열 쌍**을 찾아 한쪽의 착상을 튼다
- 최저 등급(Common / Lv1) 문장은 **여전히 건드리지 않는다** — 라이팅 가이드 §07 정본이다
- `20260906_1305`의 작성 규칙은 그대로 유효하다: 문어체 · 조건 숫자 미기재 · 등급명 미노출 ·
  배지 이름 미포함 · 계열 내 서사 연결 · 전역 문장 중복 0

**부수 항목 — 중복 문장 18종 51행**
`20260906_1305` 검증에서 확인된 것으로, **전부 최저 등급 행끼리의 중복**이고 v5 시딩 당시부터
있던 상태다(예: 「돌아왔다는 사실이 떠났던 사실을 덮습니다.」가 5종목 공통).
이번에 새로 쓴 436행에서 생긴 중복은 0건이다.
**이 티켓에서 함께 풀지, 최저 등급은 정본이므로 그대로 둘지 먼저 판단한다.**

### 반영 경로 (`20260906_1305`과 동일)

1. `Service Plan/Specs/Content/v5_catalog_writing.json`의 `등급별설명` 수정
2. `v5_seed_build.py` 재실행 → `rows.json`·`seed_v5_activity_badges.sql` 재생성
3. **`139_*.sql` UPDATE 마이그레이션을 따로 쓴다** — 시드 SQL은 멱등 INSERT라 재실행으로
   description이 갱신되지 않는다(→ 티켓 `20260906_1421`)
4. 마이그레이션 실행은 사용자 승인 후 오케스트레이터가 한다 (CLAUDE.md 규칙 5)

## 구현 계획

1. `등급별설명` 436행을 기계 집계 — 지명별 술어 분포 · 화이트 룸 위치 · 종목 간 대응 쌍
2. 집계로 뽑힌 대상만 재작성 (예상 50~80행)
3. 재작성분에 `20260906_1305`의 자가 검증 7항을 다시 돌린다
4. 시드 재생성 → `139_*.sql` 작성 → 승인 후 실행

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

**전면 재작성이 아니라 436행 중 17행만 고쳤다.** 나머지는 이미 좋다는 개선 리뷰 판정을 그대로 따랐다.

**① 지명 술어 분산** — 지명(그루터기 살롱·블랙 트랙·화이트 룸) 포함 44문장을 기계 집계했다.
「알아본다·기억한다·안다·문을 연다」 계열이 23행(52.3%)으로 과반이었다. 이 중 8행은 지명 자체를
빼고(②와 겹침), 4행은 지명은 유지한 채 술어만 FACTIONS.md 설정에서 새로 길어와 바꿨다 —
거래처(「값을 매기지 못합니다」·「값을 다시 매깁니다」), 장애물(「문턱을 낮춥니다」),
배경(「앞을 지나는 발소리가 낯설지 않습니다」). 결과: 지명 문장 36개 중 가족 술어 11개(30.6%).

**② 최상위 등급의 화이트 룸 고정 패턴 완화** — mystic(또는 그 계열의 최상위 레벨)이 화이트 룸으로
끝나는 문장이 15건(원 티켓 추정 17건과 근사)이었다. 8건을 지명 없이 소재 자체로 단정하는
문장으로 바꿨다(시간의 틈·거리·궤도·페이스·오르막·고도·심박). 남은 7건(46.7%)은 계열 내
서사(그루터기 살롱→블랙 트랙→화이트 룸 3단 escalation의 「밤의 보행자」 등 티켓 20260906_1305의
예시 문형)를 해치지 않는 선에서 유지했다.

**③ 종목 간 문장 틀 겹침 해소** — 자전거 `cycling:C1`과 등산 `hiking:C1`(둘 다 「하루 1회」,
rare=습관/epic=달력/mystic=일정 대응)에서 등산 3행을 「몸이 산으로 방향을 먼저 잡는다」는
착상으로 바꿨다(습관·달력·일정 단어를 전부 뺐다). 「비운 자리」 3종 변주
(`cycling:X2`·`running:X3`·`trail_running:X2`)는 자전거만 원문 유지, 러닝은 거래처(몫을 셈해
둔다), 트레일러닝은 장애물(문턱이 높아지지 않는다)로 갈랐다.

**부수 항목 — 중복 문장 처리 여부 판단**: 이번에 새로 손댄 17행 포함 전체 436행에서 신규 중복은
0건이다. 기존에 있던 최저 등급끼리의 중복(재검증 결과 11종 28행 — 티켓이 언급한 「18종 51행」과
표본 산정 기준이 다를 수 있으나 전부 최저 등급 행끼리라는 성격은 같다)은 **이번 티켓에서
건드리지 않았다.** 최저 등급은 라이팅 가이드 §07 정본이고, `20260906_1305`에서 이미 같은
판단(보존)을 내린 사안이라 재론하지 않았다.

### 변경된 파일
```
Service Plan/Specs/Content/v5_catalog_writing.json          (17행 수정 + _meta.지명_술어_다듬기 신설)
Service Plan/Specs/Content/v5_seed_build.py                 (수정 없음 — 등급/레벨별 설명을 정본에서
                                                               그대로 읽어 오는 기존 로직이 재사용됨)
Service Plan/Specs/Content/seed_v5_activity_badges.rows.json (재생성 — description 17행만 변경)
jam-web/supabase/migrations/seed_v5_activity_badges.sql     (재생성 — description 17행만 변경)
jam-web/supabase/migrations/139_badge_description_placename_predicate.sql (신규 · UPDATE 17행만 · 미실행)
```

### 테스트 결과
- [x] `python3 "Service Plan/Specs/Content/v5_seed_build.py"` 드라이런 — 194계열 630종 유지,
      436행(등급별설명) 유지, 검증 전항 통과("통과 — family_key 형식·유일성 / CHECK 허용 키 /
      계열 정합성 트리거 / rarity·level 배타 / 계열 내 중복 / 이름 유일성 / 미션 플래그 전부 이상 없음")
- [x] 재생성분 diff 확인 — `rows.json`·`seed_v5_activity_badges.sql` 모두 **description 17행만**
      변경(34줄 diff = 이전 17행 + 이후 17행). 그 외 필드·행 순서·행 수 변경 0
- [x] `v5_catalog_writing.json` 재포맷 무결성 — `json.load` 후 `json.dumps(indent=1)`로 되돌린
      결과가 커밋 전 원본과 바이트 단위로 동일함을 확인(수작업 편집이 포맷을 흐트러뜨리지 않았다)
- [x] 전역 신규 문장 중복 0건 — 새로 쓴 17문장을 편집 전 전체 604문장(설명 168 + 등급별설명 436)
      코퍼스와 대조, 중복 0
- [x] 등급명(에픽·미스틱·레어·커먼) 노출 0건, 등급별설명 숫자 포함 0건 — 604문장 전수 검사
- [x] `139_*.sql` **작성만 하고 실행하지 않았다.** family_key + (rarity 또는 level) 17건 지정,
      DELETE·재시딩 없음, 최저 등급 미대상

### UX Writing 검증
- [x] 지명 술어 분산 — 지명 문장 36개 중 「알아본다·기억한다·안다·문을 연다」 계열 11개(30.6%),
      절반 이하
- [x] 화이트 룸이 최상위 등급의 기본 기호로 읽히지 않음 — 최상위 화이트 룸 15건 → 7건(46.7%)
- [x] 종목 간 대응 쌍 해소 — `cycling:C1`↔`hiking:C1`, 「비운 자리」 3종 모두 착상 분리 확인
- [x] 문어체(~습니다) · 조건 숫자 미기재 · 등급명 미노출 · 배지 이름 미포함 · 전역 중복 0 —
      17행 전수 확인

### 배포 정보
- 배포일: (미배포 — `139_*.sql`은 사용자 승인 후 오케스트레이터가 실행)
- 환경: DB 변경 예정분은 실행 즉시 프로덕션 반영(Supabase 공용 단일 DB). 문서·SQL 파일 자체는
  이 커밋으로 staging에 반영됨
- 커밋: (이 완료 기록과 함께 커밋 예정)

### 주요 의사결정 / 핵심 메모

- **최상위 7건에 화이트 룸을 남긴 기준**: 계열 내 서사가 3단 escalation(그루터기 살롱→블랙
  트랙→화이트 룸)으로 완결되는 경우, 또는 지명을 빼면 하위 등급과의 연결이 어색해지는 경우를
  우선 유지했다. 「밤의 보행자」(A9)는 티켓 `20260906_1305`가 직접 예시로 든 문형이라 그대로 뒀다
- **「비운 자리」 3종 중 1종만 원문 유지**: 세 종목 모두 바꾸면 그루터기 살롱=휴식 복귀지라는
  뜻 자체가 흐려질 수 있어, 자전거(최초 작성분으로 추정)를 기준점으로 남기고 러닝·트레일러닝만
  틀었다
- **중복 18종 51행(부수 항목) 미처리 판단 근거**: 이번에 직접 집계한 결과는 11종 28행으로
  `20260906_1305` 완료 기록의 수치와 다르다(집계 방식 차이로 추정 — 정확한 재현 방법이 남지
  않았다). 다만 **전부 최저 등급 행끼리의 중복이라는 성격은 동일**하고, 티켓이 이미
  "최저 등급은 라이팅 가이드 §07 정본이라 건드리지 않는다"를 확정한 원칙과 정면으로 부딪히므로,
  이 티켓의 범위(등급별설명 436행)를 벗어난다고 보고 손대지 않았다

### 잔여 이슈
- 최저 등급끼리의 중복 문장(11종 28행, 위 메모 참조)은 여전히 남아 있다. 손대려면 최저 등급
  보존 원칙 자체를 재검토하는 별도 의사결정이 먼저 필요하다 — 이 티켓의 범위 밖이라 새 티켓
  없이 기록만 남긴다
- `139_*.sql` 실행은 사용자 승인 대기 중이다
