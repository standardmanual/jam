---
id: 20260906_2140
category: UI
status: CLOSED
created: 2026-09-06
closed: 2026-09-06
---

# [UI] 배지 트리 대시보드 — 정렬 엣지 통합·진행률 스캔 컬럼·상태 색 램프

## 배경 / 문제 정의

`/badges/tree`의 일은 **「전체 액티비티 배지를 조망하고, 지금 내가 어디이고, 앞으로
얼마나 더 가야 하는지」를 한 번에 알려주는 것**이다. 지금은 39장의 카드가 각자 다른
문법으로 그려져 **세로로 훑을 축이 하나도 없다.**

배포된 staging(`stage.j-a-m.app/badges/tree?activity=walking`, 걷기 탭 0/145)을
Chromium 375·320px에서 직접 실측한 값이다:

| 측정 항목 | 실측값 | 문제 |
|---|---|---|
| 계열명 시작 x | **32 / 88 px** | 레일 카드는 카드 패딩 엣지, 레벨·반복 카드는 썸네일(44)+갭(12) 뒤 |
| 계열명 글자 | **16 / 14 px** | 같은 위계인데 두 크기 |
| 그리드 셀 계열명 | x=16 · **12px** | 세 번째 문법(티켓 20260906_1425가 추가) |
| 레일 연결선 폭 | **37 / 25 / 21 / 14 px** | 한 화면 네 카드에서 제각각 |
| 한 레일 안 캡션 높이 | **14 / 29 / 43 px** | 눈금 열 높이가 86 / 101 / 115px로 어긋남 |
| 등급칩 · 레벨칩 | **8 px** | 등급이 이 화면의 핵심 정보인데 판독 불가 |
| 진행률 스캔 컬럼 | **없음** | 수치가 타입마다 다른 자리 |

**레일 결함의 기전이 실측으로 확정됐다.** 눈금 열이 `flex:none; minWidth:48`인데
캡션은 `maxWidth:92`까지 번진다 — **열 폭을 캡션 글자 수가 정한다.** 그래서 카드마다
레일 간격이 2.6배까지 벌어진다. 「텍스트 양에 따라 위치가 흔들린다」의 정체다.

색은 OKLCH로 환산해 쟀다:

```
lime  #d6f24a  L .911  H 118.6°   ← 다 채움
amber #f2cb00  L .851  H  94.9°   ← 채우는 중
                      ─────────
                      23.7° 차이, 밝기 0.06 차이
```

색상 23.7°·밝기 0.06으로는 「시작 전 / 진행 중 / 다 채움」 세 단계를 못 가른다.
게다가 amber는 **Epic 등급칩(H 73.2°)과 21.7°**로 붙어 있어 같은 카드에서 진행색과
등급색이 한 덩어리로 읽힌다. 그리고 **진행 중인 카드(42%·33%)의 진행률이 lime**으로
칠해져 있다 — lime은 이 서비스에서 「다 채웠다」 하나만 뜻한다.

## 상세 요구사항

### A. DS 토큰 추가 (`design-system/tokens/colors.css`)

**색값을 새로 만들지 않는다.** 다섯 줄 전부 기존 토큰을 가리키는 역할 별칭이라,
추가만으로는 `RarityBadge`를 쓰는 서비스 9개 파일을 포함해 **어떤 값도 달라지지 않는다.**

```css
/* ── 진행 상태 채널 (티켓 20260906_2140) ──────────────────────
 * 밝기가 「지금 얼마나 신경 쓸 곳인가」를, 색상이 「어떤 종류인가」를 맡는다.
 * lime(H 118.6°)과 amber(H 94.9°)가 23.7°밖에 안 떨어져 있어 색만으로는
 * 3단계를 가를 수 없다 — 가운데 단계에서 색을 빼고 밝기를 최대로 올린다.
 *
 * 왜 컴포넌트가 --color-text를 그냥 쓰면 안 되나: 「진행 중이면 가장 밝게」는
 * 본문 위계가 아니라 상태 규칙이다. 나중에 본문 색을 낮춰도 상태 채널은
 * 따라가면 안 된다(토큰을 역할 밖에서 빌려 쓰지 않는다). */
--status-progress-idle:   var(--color-text-secondary); /* 시작 전 */
--status-progress-active: var(--color-text);           /* 진행 중 — 지금 내 차례 */
--status-progress-near:   var(--status-short-solid);   /* 거의 다 (≥80%) */
--status-progress-done:   var(--status-done-solid);    /* 다 채움 · 획득 */

/* 채움 스윕 — 트랙 전체에 깔고 clip-path로 잘라 「어디까지 초록으로 넘어갔나」가
 * 곧 진행도가 되게 한다. ProgressBar.jsx의 PROGRESS_TRACK_GRADIENT를 CSS로 옮긴 것 —
 * 레일 연결선·링이 JS 상수를 import할 수 없어 한 화면에서 진행 표현이 셋으로 갈라져 있었다. */
--status-progress-sweep: linear-gradient(90deg in oklab,
    var(--status-short-solid), var(--status-done-solid));
```

- `ProgressBar.jsx`의 `PROGRESS_TRACK_GRADIENT`를 **새 토큰을 가리키게** 바꿔 그라데이션
  정의가 한 군데만 남게 한다. (기존 값은 `to right` + sRGB 보간 → `90deg in oklab`으로
  바뀐다. amber↔lime은 인접 색상이라 시각 변화는 미미하다. 이 1건이 이번 티켓에서 유일하게
  **기존 화면에 영향을 주는 변경**이므로 완료 기록에 명시할 것)

**상태 램프 적용 규칙** — 진행률 `fraction`(0~1) 기준:

| 상태 | 토큰 | 조건 |
|---|---|---|
| 시작 전 | `--status-progress-idle` | `fraction === 0` 또는 진행 계산 불가(muted) |
| 진행 중 | `--status-progress-active` | `0 < fraction < 0.8` |
| 거의 다 | `--status-progress-near` | `0.8 <= fraction < 1` |
| 다 채움·획득 | `--status-progress-done` | `fraction >= 1` 또는 `status === 'earned'` |

임계값 `0.8`은 CSS 토큰이 아니라 **공유 JS 상수**로 둔다
(`src/lib/badgeProgressText.ts`에 `NEAR_THRESHOLD = 0.8` 등). 정책 숫자라 색 토큰이 아니다.

### B. 신규 MODULAR 패턴 — `BadgeFamilyCardHeader`

**정렬 엣지가 갈라진 근본 원인은 네 패턴이 각자 헤더를 그린다는 것이다.**
매니페스트 확인 결과 공유 헤더 부품이 없다. 각 파일을 따로 고치면 지금은 맞지만 다음
패턴이 추가되는 순간 또 갈라진다 — 실제로 `BadgeProgressRingCard`(20260906_1425)가
세 번째 이름 위치를 만들어 냈다.

```
[계열 이름 1fr]  [87% EPIC auto — 카드 우측 패딩 엣지에 정렬]
[메타 한 줄 1fr] [자세히 ⌄ auto — 펼침 가능한 카드만]
```

props(안): `name` · `fraction` · `pctLabel`(예 `Rare`·`Lv.8`·`26회`) · `metaText`(ReactNode) ·
`expanded` · `onToggleExpand`(없으면 자세히 미표시). **문구 조립은 하지 않는다** —
완성 문자열만 받는다(기존 DS 원칙 유지).

- 이름: `--text-body`(16px) / 600 / `word-break: keep-all` + `overflow-wrap: anywhere`.
  **말줄임 금지** — 계열명이 곧 지표다(20260906_1323 §5 유지)
- 진행률 블록: **`87%`와 등급/레벨 라벨을 한 줄에 나란히 둔다.** 세로로 쌓지 않는다
  (2026-09-06 사용자 지시 — 예시 `자정의 정복자  87% EPIC`)
  - 퍼센트: `--text-body`(16px) / 600 / `tabular-nums` / 상태 램프 색
  - 라벨: `--text-micro`(11px) / `--status-progress-idle` / 대문자 / 퍼센트와 `--spacing-4` 간격
  - 블록 전체는 `white-space: nowrap`, 카드 **우측 패딩 엣지에 붙인다**
- 메타 줄: `--text-small`(14px) / `--color-text-secondary`

### C. 각 패턴을 새 헤더 위로 재구성

| 파일 | 변경 |
|---|---|
| `BadgeStageRail.jsx` | 자체 헤더 제거 → `BadgeFamilyCardHeader`. 눈금 배치는 **아래 C-1 참조**. 캡션 자리 **2줄 높이 상시 예약**. 썸네일 44→52px. 연결선 6→8px(높이). 캡션 `--text-micro`→`--text-caption`, 진행 앵커 캡션만 `--text-small`/700 |
| `BadgeLevelGauge.jsx` | 헤더 한 줄을 카드 폭 전체로 올리고 썸네일은 본문으로. 본문 `[52px 썸네일][값 행 + 10px 바]`. 현재값 `--text-body-l`(20px), `/ 다음값` `--text-small`, 진행 바 6→10px |
| `BadgeStampRow.jsx` | 위와 동일 구조. 본문 `[52px 썸네일][×N 칩 + 캡션 + 바]`. ×N 칩 `--text-small` |
| `BadgeProgressRingCard.jsx` | **원형 → 라운드 사각**. `border-radius: var(--radius-card)`(16px), 안쪽 `calc(var(--radius-card) - 4px)` 동심값. 진행은 그대로 테두리 `conic-gradient(in oklab, near → done)`. 이름 `--text-small` + **2줄 높이 예약**, 캡션 `--text-caption` |
| `DualAxisGauge.jsx` | **별도 카드에서 레일 카드 안의 구획으로**. 1px 구분선 + 소제목(`--text-micro`). 축 바 8→10px, 채움을 스윕으로 |
| `BadgeTreeSummaryHeader.jsx` | 아래 D 참조 |
| `RarityBadge.jsx` | `size` 프롭 추가 — `sm`(기본, 8px·4/9 패딩 = **현행 그대로**) / `md`(11px·5/10 패딩). 배지 트리만 `md`. **서비스 9개 호출부 무변경 보장** |
| `BadgeLevelChip.jsx` | 동일하게 `size` 프롭. 서비스 5개 호출부 무변경 |

#### C-1. 레일 눈금 배치 — 고정 간격 좌측 정렬 (2026-09-06 사용자 확정, 1차 게이트 FAIL 반영)

**초안의 `repeat(4,1fr)`은 틀린 전제였다.** 프로덕션 `badges` 실조회 결과 등급형 다단
계열의 눈금 수 분포는 **2눈금 148개 · 3눈금 6개 · 4눈금 2개**다. 4열을 고정하면 148개
카드가 왼쪽 절반만 쓰고, 눈금 수에 열 폭을 종속시키면(`repeat(n,1fr)`) 연결선이 한 화면에서
18px과 96px로 **5.4배** 갈린다(1차 구현 실측). 둘 다 이번 티켓의 목적을 깬다.

**확정 배치:**

```
┌────────────────────────────────┐
│ 오늘의 한 걸음        42% RARE │
│ 다음 Rare · 4.2/10km   자세히 ⌄│
│ ▣──▣──▣──▣                     │  4눈금
└────────────────────────────────┘
┌────────────────────────────────┐
│ 자정의 경계인         0% COMMON│
│ 다음 Common · 0/1회    자세히 ⌄│
│ ▣──▣                           │  2눈금 — 오른쪽은 비워 둔다
└────────────────────────────────┘
연결선 폭: 눈금 수·카드와 무관하게 항상 같은 값
```

- 썸네일 폭과 연결선 폭을 **둘 다 고정값**으로 두고 **왼쪽부터 나열**한다.
  `1fr`를 쓰지 않는다 — 남는 공간을 나눠 가지는 순간 눈금 수가 기하를 정하게 된다
- 남는 오른쪽 공간은 **그냥 여백으로 둔다.** 「이 계열은 2단계뿐」이라는 정직한 표현이고,
  진행 앵커가 카드 간 같은 x에 오는 이득이 그보다 크다
- **검증 기준**: 눈금 4·3·2개 레일을 한 화면에 세로로 쌓았을 때 연결선 폭이
  **하나의 값**으로 모여야 한다. 375px·320px 각각에서 실측해 티켓에 숫자를 남길 것
- 320px 대응: 4눈금이 가장 넓다. `4×썸네일 + 3×연결선 ≤ 카드 안쪽 폭`을 만족해야 한다
  (320px 기준 안쪽 256px). 1차 구현의 컨테이너 쿼리(좁을 때 썸네일·간격 동시 축소)는
  **유지해도 좋다** — 다만 축소 후에도 「한 화면 안에서 연결선 폭이 하나」가 깨지지 않아야 한다
- 캡션은 썸네일 폭보다 넓어질 수 있다. 인접 캡션이 겹치지 않도록 `max-width`를
  `썸네일 + 연결선` 이하로 제한하고 가운데 정렬한다

### D. 요약 헤더 재편 (`BadgeTreeSummaryHeader.jsx`)

현재 3열 셀 안의 순서가 **막대 → 라벨 → 값**이라 무엇에 대한 막대인지 모른 채 막대를
먼저 본다. 셀 폭이 96px이라 값이 12px·막대가 6px로 눌린다.

- **1층 히어로**: `획득 12 / 145`(`--text-h3` 28px) + 우측 `8%`(`--text-body-l`)
  - ⚠️ **전체 진행 막대는 넣지 않는다** — 티켓 20260906_1436의 결정을 **유지**한다
    (2026-09-06 사용자 재확인). 등급 버킷의 막대는 그대로 남는다
- **2층 버킷**: **2열** 그리드로 바꿔 셀 폭 ~148px 확보. 값 `--text-small`(14px), 막대 8px
- 셀 안 순서를 **라벨 + 값 → 막대**로 뒤집는다
- 라벨 앞에 등급색 8px 도트 (등급 식별을 색으로도)
- **등급 순서를 Common → Rare → Epic → Mystic → 레벨**로 (2026-09-06 사용자 확정).
  「앞으로 얼마나 더」를 읽는 순서와 같아진다. 레벨 칸은 2열 전체를 쓴다(다른 축이고,
  5개를 2열에 넣으면 마지막 하나가 혼자 남는다)

### E. 서비스 계층 (`src/`)

| 파일 | 변경 |
|---|---|
| `BadgeTreeClient.tsx` | ① **화면 제목 `<h1>배지 트리</h1>` 제거**, TopNav↔탭 사이 `--spacing-24` 확보 ② **`BadgeStatusSection` 접기/펴기 제거**(아래 별도 수정사항 1) ③ 그리드 섹션을 「한 번에 끝나는 배지」 **카드 한 장**으로 묶고 구성원 최고 진행률 자리에 배치 ④ 카드 간 간격 12→16px ⑤ 섹션 헤더에 「진행률 높은 순」 표기 |
| `BadgeFamilyRow.tsx` | 헤더에 넘길 `fraction`·`pctLabel`·`metaText` 조립. 2축 게이지를 레일 카드 안으로 |
| `BadgeProgressGridCell.tsx` | 라운드 사각 링 카드에 맞춰 props 전달 |
| `badgeProgressText.ts` | `NEAR_THRESHOLD` 상수 + 메타 줄 문자열 조립 |

### F. 별도 수정사항 (2026-09-06 사용자 지시)

1. **「다음 목표」 섹션의 접고 펴는 기능 제거.**
   `BadgeStatusSection`의 chevron·토글을 이 화면에서 쓰지 않는다. 섹션은 항상 펼쳐진
   상태로 제목 + 개수만 보여준다.
   ⚠️ `BadgeStatusSection`은 **서비스 1곳만** import한다(`BadgeTreeClient`). 컴포넌트를
   지우지 말고 **호출부에서 접힘을 쓰지 않는 형태**로 바꾸거나, 정적 헤더로 대체할지
   구현자가 판단해 티켓에 근거를 남길 것. 접힘 제거로 「펼친 섹션만 계산」 최적화
   (`BadgeStatusSection` 주석)가 무의미해지는데, 어차피 `defaultOpen`이라 현행도 항상
   계산하고 있다 — 회귀 없음을 확인할 것.

2. **자물쇠는 이모지가 아니라 TabBar 아이콘 스타일 SVG.**
   실측 결과 **코드에는 이모지가 없다** — 시안(아티팩트)의 🔒는 목업 플레이스홀더였다.
   현행 `LockGlyph`는 이미 `viewBox="0 -960 960 960" fill="currentColor"`(TabBar와 동일한
   Material Symbols 채움 스타일)이다. 따라서 요구사항은 두 가지다:
   - 구현에 **이모지를 절대 도입하지 않는다**
   - `BadgeStageRail.jsx:112`와 `BadgeProgressRingCard.jsx:51`에 **중복 선언된
     `LockGlyph`를 공유 모듈 하나로 통합**한다(체크 글리프도 같은 중복). 지금은 두 벌이라
     한쪽만 고치면 갈라진다
   - ⚠️ `src/components/ui/icons.tsx`의 `LockIcon`은 **stroke 아웃라인 스타일**이라 다르다.
     이걸 쓰지 말 것 — TabBar 스타일은 채움(fill)이다

3. **배지 이미지를 여백 없이 프레임에 꽉 채운다.** 보더 라운드로 잘리게 한다.
   - 현행: `padding: 3px`(레일·레벨·스탬프) / `4px`(2축) / `6px`(링) + `objectFit: contain`
   - 변경: `padding: 0` + `objectFit: cover` + 프레임에 `overflow: hidden`
   - ⚠️ 프레임의 `border-radius`와 이미지의 radius가 **따로 걸려 있으면** 모서리가 삐져
     나온다. 이미지에 radius를 걸지 말고 **프레임의 `overflow: hidden`으로만** 자른다

4. **진행률/등급을 계열명 오른쪽에, 한 줄로 둔다.**

   목표 형태 (2026-09-06 사용자 지시, 예시 그대로):

   ```
   자정의 정복자                    87% EPIC
   다음 Epic · 두 조건을 한 번의 활동에서   자세히 ⌄
   ```

   - **`87%`와 `EPIC`을 세로로 쌓지 않는다.** 한 줄에 나란히 둔다
   - 이 블록을 **카드 우측 패딩 엣지에 붙인다** — 모든 카드 타입에서 같은 우측 엣지를
     공유해야 세로로 훑는 스캔 컬럼이 성립한다(이번 티켓의 핵심)
   - chevron이 그 오른쪽을 차지하면 진행률이 안쪽으로 밀려 컬럼이 어긋난다.
     **「자세히 ⌄」는 헤더 2행(메타 줄) 오른쪽 끝**으로 옮겨, 1행 진행률과 2행 자세히가
     같은 우측 엣지를 쓰게 한다
   - 등급 라벨은 **텍스트**다 — `RarityBadge` 칩을 여기에 쓰지 않는다. 칩을 쓰면 레일
     안의 등급칩과 경쟁해 같은 카드에 등급 표기가 두 종류가 된다

   > 구현자 재량 아님 — 이 배치가 이번 티켓의 요구사항이다. 다른 배치가 낫다고 판단되면
   > 구현하지 말고 티켓에 근거를 적어 오케스트레이터에게 보고할 것.

5. **미획득 배지는 그레이스케일.**
   현행 `filter: grayscale(1)`이 이미 적용돼 있다(2026-09-06 사용자 확정 이력). **유지하고,
   위 3번(padding 0 + cover)으로 바꾸는 과정에서 필터가 유실되지 않는지 확인**한다.
   획득 배지는 원본 컬러.

### G. 문구 — 방금 갱신된 가이드라인을 따른다

`Service Plan/Specs/UX_WRITING_GUIDELINE.md`가 커밋 `00ead78f`로 갱신됐다.
**이번 티켓 범위에서 아래를 함께 반영한다:**

| 현행 | 변경 | 위치 |
|---|---|---|
| `조건 충족` | `조건을 다 채웠어요` | `badgeProgressText.ts:239,242` · `BadgeStageRail.jsx:76,83` |
| `미도달`(aria) | `아직` | 같은 4곳 |
| `잠금 해제 조건 보기` | `받는 방법 보기` | `BadgeStageRail.jsx:599` · `BadgeFamilyRow.tsx` |
| `잠금 해제 조건`(시트 제목) | `받는 방법` | `UnlockConditionSheetContent.jsx:220` |
| `한 번의 활동에서 모두 충족` | `한 번의 활동에서 동시에 채워야 해요` | `badgeConditionText.ts:164` |

`잠김`은 **유지**한다(가이드라인 고정 용어). 어드민 화면·내부 `reason` 로그는 대상 아님.

## 구현 계획

### UI 탐색·재사용 판정 (오케스트레이터 결정 — 이대로 따를 것)

`_ds_manifest.json` 전수 확인 결과:

- **재사용**: `RarityBadge`(size 프롭 비파괴 추가) · `BadgeLevelChip`(동일) ·
  `ProgressBar`(`fillMode="track-gradient"` 이미 존재) · `LockGlyph`/`CheckGlyph`(기존 SVG)
- **MODULAR 신규 확장 1개**: `BadgeFamilyCardHeader` — 위 B의 근거대로 **공유 부품이
  없어서** 정렬 엣지가 갈라졌다. 신규 패턴 1개로 구조적으로 해결한다.
  `ListRowCard`는 `[40px 아이콘][제목/부제][trailing]` 구조라 **맞지 않는다**(이 화면은
  아이콘이 헤더에 없고 진행률 슬롯이 고정 폭이어야 한다)
- **서비스 전용 신규**: 없음

### 순서

1. 토큰 5줄 + `PROGRESS_TRACK_GRADIENT` 재연결
2. `LockGlyph`/`CheckGlyph` 공유 모듈로 통합
3. `RarityBadge`·`BadgeLevelChip` `size` 프롭
4. `BadgeFamilyCardHeader` 신규 + Story
5. 기존 4개 패턴을 새 헤더 위로 재구성 + 각 Story 갱신
6. `BadgeTreeSummaryHeader` 재편 + Story
7. 서비스 계층(`BadgeTreeClient`·`BadgeFamilyRow`·`BadgeProgressGridCell`)
8. 문구 교체(G)

**Story 동반 필수** — `.githooks/pre-commit`이 컴포넌트만 고치고 Story를 빠뜨리면 경고한다.

### 1차 게이트 FAIL 이력 (2026-09-06)

1차 구현(`claude/jamwork-20260906_2140-badge-tree-layout`, 커밋 `5d6e0f00`)은 **10개 항목을
통과**했으나 4건으로 FAIL했다. 재시도는 **그 브랜치 위에서 이어서** 한다 — 통과한 부분을
다시 만들지 않는다.

| # | FAIL 사유 | 해결 |
|---|---|---|
| 1 | `repeat(${columnCount},1fr)`로 눈금 수가 열 폭을 정함 → 연결선 18/44/96px | 위 **C-1** 확정 배치로 재구현 |
| 2 | 실데이터(2눈금 148 / 3눈금 6 / 4눈금 2)에서 반드시 드러남 | 1번 해결 시 자동 해소. 세 종류를 한 화면에 놓고 실측 |
| 3 | 이탈 미고지 + `BadgeStageRail.jsx:76-77` 주석과 구현 요약이 「연결선 균일」이라 사실과 다르게 단언 | 주석·요약을 실제 동작과 맞추고, **티켓과 다르게 구현하면 반드시 `alerts`에 신고** |
| 4 | `npm run ds:check` 레드 회귀 — `[TOKEN_UNDEFINED] --ds-rail-thumb, --ds-rail-gap` | 해소할 것. `ds-sync-check.mjs:607`이 `<style>` 문자열 안 선언을 못 읽는 오탐이나, 이 브랜치가 게이트를 레드로 만들었으므로 그대로 둘 수 없다. **스크립트를 느슨하게 만들어 진짜 누락까지 놓치게 하지 말 것** — 지역 변수를 스크립트가 인식하는 형태로 선언하거나, `<style>` 블록 파싱을 정확히 추가하거나 둘 중 하나 |

### 검증

- 워크트리에서는 `next dev`·`next build`가 안 된다(심링크·`!` 경로로 fatal). **`vitest`는
  가능**하다. DS 단위 확인은 `npm run storybook`(로컬 :6006)으로 한다
- 정렬 엣지가 하나로 모였는지 **숫자로** 확인할 것. 목표: 375px에서 모든 계열명 시작 x가
  **32px 하나**, 글자 크기 **16px 하나**, 진행률 우측 엣지 하나
- 320px에서 가로 스크롤 0 유지(현행 통과 중)

### 하지 말 것 (선행 결정 유지)

- **전체 진행 막대를 되살리지 않는다** (20260906_1436 결정, 2026-09-06 재확인)
- 등급칩의 **색값**을 바꾸지 않는다 (`--color-rarity-*`는 폼 입력 에러색으로도 새어 있다)
- 계열명에 **말줄임을 쓰지 않는다** (20260906_1323 §5)
- 설계 분류(누적·주기·시간대)를 화면에 노출하지 않는다
- 전역 spacing·typography 토큰을 **신설하지 않는다**. 스케일 밖 수치는 가장 가까운 기존
  토큰으로 스냅하고, 기하값(52px 썸네일 등)은 **컴포넌트 로컬 상수**로 둔다
  (`RING_SIZE`·`GATE_SLOT_WIDTH` 선례)

---
## 완료 기록

### 구현 내용 요약

**두 라운드로 진행했다.** 1차(`5d6e0f00`)는 게이트 10개 항목을 통과하고 4건으로 FAIL,
2차(`1dc8530e`)가 그 4건을 해소했다.

- **정렬 엣지 통합** — 네 패턴이 각자 그리던 헤더를 신규 MODULAR 패턴
  `BadgeFamilyCardHeader` 하나로 모았다. `[계열명 1fr][87% EPIC auto]` / `[메타 1fr][자세히 ⌄ auto]`
  2행 구조로, 진행률 블록이 카드 우측 패딩 엣지에 붙어 카드 타입과 무관하게 같은 스캔 컬럼을 만든다.
- **레일 기하를 데이터에서 분리** — `THUMB_SIZE=52` · `LINK_WIDTH=16` · `PITCH=68` 컴포넌트
  로컬 상수 + 연결선 절대배치 고정 px. `1fr`과 퍼센트를 한 곳도 쓰지 않아 **눈금 수(2/3/4)도
  캡션 글자 수도 기하를 움직이지 못한다.** 남는 오른쪽은 여백으로 둔다.
- **상태 색 램프 4단** — lime(H 118.6°)과 amber(H 94.9°)가 색상 23.7°·밝기 0.06 차이라
  색만으로 3단계를 못 갈랐다. 가운데 단계(진행 중)에서 색을 빼고 밝기를 최대(`--color-text`)로
  올려, **밝기가 「지금 신경 쓸 곳인가」를 색상이 「어떤 종류인가」를** 맡게 나눴다.
- **토큰 5줄 추가** — 전부 기존 토큰의 역할 별칭이라 **기존 화면 값 변경 0건.**
  `PROGRESS_TRACK_GRADIENT`를 새 토큰으로 재연결해 그라데이션 정의를 한 곳으로 모았다.
- **글리프 단일 소스** — `LockGlyph`·`CheckGlyph`·`StarGlyph`·`ChevronDownGlyph` 사본 3벌을
  `icons/BadgeStatusGlyphs.jsx`로 통합. 이모지는 도입하지 않았다(원래도 코드엔 없었다).
- **요약 헤더 재편** — 1층 히어로 + 2층 2열 버킷, 셀 순서를 「라벨+값 → 막대」로 뒤집고
  등급 순서를 Common → Mystic → 레벨로. **전체 진행 막대는 넣지 않았다**(20260906_1436 유지).
- **화면 정리** — `<h1>` 제거 + TopNav↔탭 24px, 섹션 접기/펴기 제거, 눈금 1개 계열을
  「한 번에 끝나는 배지」 카드 한 장으로 묶어 구성원 최고 진행률 자리에 배치, 카드 간격 12→16px.
- **이미지** — `padding:0` + `objectFit:cover` + 프레임 `overflow:hidden`, 미획득 grayscale 유지.
- **문구 5건** — 조건 충족→조건을 다 채웠어요 / 미도달→아직 / 잠금 해제 조건(보기)→받는 방법(보기) /
  한 번의 활동에서 모두 충족→동시에 채워야 해요.

### 변경된 파일
```
design-system/tokens/colors.css                              (토큰 5줄 추가)
design-system/components/patterns/BadgeFamilyCardHeader.{jsx,d.ts,stories.tsx}   신규
design-system/components/icons/BadgeStatusGlyphs.{jsx,d.ts,stories.tsx}          신규
design-system/components/patterns/BadgeStageRail.{jsx,d.ts,stories.tsx,prompt.md}
design-system/components/patterns/BadgeLevelGauge.{jsx,d.ts,stories.tsx}
design-system/components/patterns/BadgeStampRow.{jsx,d.ts,stories.tsx}
design-system/components/patterns/BadgeProgressRingCard.{jsx,stories.tsx}
design-system/components/patterns/BadgeTreeSummaryHeader.{jsx,stories.tsx}
design-system/components/patterns/BadgeStatusSection.{jsx,d.ts,stories.tsx}
design-system/components/patterns/DualAxisGauge.{jsx,stories.tsx}
design-system/components/patterns/UnlockConditionSheetContent.{jsx,stories.tsx}
design-system/components/cards/{RarityBadge,BadgeLevelChip}.{jsx,d.ts,stories.tsx}
design-system/components/feedback/ProgressBar.{jsx,stories.tsx}
design-system/{readme.md,_ds_manifest.json}
src/app/(main)/badges/tree/BadgeTreeClient.tsx
src/components/badges/{BadgeFamilyRow,BadgeProgressGridCell,BadgeUnlockSheet}.tsx
src/lib/{badgeProgressText,badgeConditionText}.ts + 대응 __tests__
vitest.config.ts                                             (@ds 별칭 추가)
```
총 48파일.

### 모듈러-서비스 연결 범위 *(§1.6)*

- **토큰** — `colors.css`에 5줄 추가. `globals.css`가 `design-system/tokens/`를 직접 import하므로
  서비스에 즉시 반영되지만, **다섯 줄 모두 기존 토큰을 가리키는 역할 별칭이라 값 변경은 0건**이다.
- **연결된 컴포넌트** — 이번에 고친 DS 패턴 전부(`BadgeStageRail`·`BadgeLevelGauge`·`BadgeStampRow`·
  `BadgeProgressRingCard`·`BadgeTreeSummaryHeader`·`BadgeStatusSection`·`DualAxisGauge`·
  `UnlockConditionSheetContent`)를 서비스가 직접 import한다 — 배지 트리 화면에 즉시 반영된다.
  `RarityBadge`(서비스 9곳)·`BadgeLevelChip`(5곳)·`ProgressBar`(8곳)도 연결돼 있다.
- **병존 구현** — 해당 없음. 이번에 고친 것 중 `src/components/ui/`에 동명 사본이 있는 건 없다.
- **`PROGRESS_TRACK_GRADIENT` 변경이 다른 화면에 준 영향** — `to right`(sRGB) → `90deg in oklab`.
  `fillMode="track-gradient"`를 쓰는 곳(`BadgeLevelGauge`·`BadgeStampRow`·`DualAxisGauge`)에만
  적용되고, 기본 `fillMode="solid"`인 서비스 8곳은 `color` prop을 쓰므로 **영향 없다.**
  amber↔lime은 인접 색상이라 보간 공간을 바꿔도 시각 변화가 미미하다.

### 테스트 결과

Chromium(Playwright) 실렌더 — 눈금 4·3·2개 레일을 한 화면에 세로로 쌓아 측정.
개발자와 게이트 리뷰어가 **각자 독립으로** 실측해 같은 값을 얻었다.

| 뷰포트 | 연결선 폭 (4/3/2눈금) | 눈금 시작 x | 계열명 x·글자 | 진행률 우측 엣지 | 가로 스크롤 |
|---|---|---|---|---|---|
| 375px | **16 / 16 / 16px** | 32·100·168·236 (세 카드 동일) | **32px · 16px** | **343px** (동일) | 0 |
| 320px | **16 / 16 / 16px** | 32·100·168·236 (동일) | **32px · 16px** | **288px** (동일) | 0 |

- [x] 375px 계열명 시작 x가 32px 하나로 모임 (1차 실측 32/88/16 → 32 하나)
- [x] 375px 진행률 우측 엣지가 343px 하나로 모임
- [x] 연결선 폭 편차 5.4배 → **1.0배** (1차 17.75/43.66/95.5 → 16 하나)
- [x] 320px 가로 스크롤 0
- [x] 캡션·계열명을 길게 늘여도 기하 불변 (게이트 스트레스 검증)
- [x] `RarityBadge`·`BadgeLevelChip` 기본값(`sm`) 픽셀 동일 — 서비스 14곳 렌더 무변경
- [x] `npm run test` — vitest 62파일 1130건 + test:node 13건 통과
- [x] `npx tsc --noEmit` 오류 0
- [x] `npm run lint` 오류 0 · 경고 13 (staging 기준선과 동일, 신규 위반 0)
- [x] `npm run ds:check` 오류 0 · exit 0 (1차 회귀 해소)
- [x] Storybook에서 각 패턴 확인 — 신규 Story `FixedPitchAcrossStopCounts` 포함

### UX Writing 검증
- [x] 용어 일관성: 고정 용어만 사용
- [x] G절 문구 교체 5건 반영
- [x] 톤앤매너 · 해요체 · 마침표 규칙
- [ ] **「한 번에 끝나는 배지」를 용어표에 추가** → 이 티켓에서 함께 반영
      (`UX_WRITING_GUIDELINE.md` §1.3 신설 행)

### 배포 정보
- 배포일: 2026-09-06 (staging)
- 환경: staging
- 커밋: `5d6e0f00` · `1dc8530e` → 머지 `0014541d`

### 주요 의사결정 / 핵심 메모

- **`repeat(4,1fr)` 초안은 틀린 전제였다.** 「등급 4단」을 가정했으나 프로덕션 실조회 결과
  등급형 다단 계열이 **2눈금 148 / 3눈금 6 / 4눈금 2**다. 4열 고정은 148개 카드가 절반만 쓰고,
  `repeat(n,1fr)`은 연결선이 5.4배 갈린다. 사용자 확정으로 **고정 간격 좌측 정렬**을 택했다.
- **`LINK_WIDTH`를 14px가 아닌 16px로.** 진행 앵커 캡션 `4.2/10km`가 tabular-nums 기준 약 67px이라
  68px 레인에서만 한 줄에 들어간다(14px면 `4.2/10k`+`m`으로 쪼개짐). 대가는 320px 여유 0 —
  아래 잔여 이슈 참고.
- **컨테이너 쿼리를 제거했다.** 1차가 320px 대응으로 넣었는데, 고정값이 320px에서도 성립하므로
  불필요해졌다. 축소가 걸리는 순간 화면 폭이 다시 기하에 끼어들어 불변식이 흔들릴 여지가 남는다.
  부수적으로 `ds:check` 오탐(미정의 `--ds-rail-thumb`)의 근본 해소이기도 하다.
- **`NEAR_THRESHOLD`를 `src/lib`가 아니라 DS에 뒀다.** 임계값을 실제로 소비하는 쪽(램프 색 선택)이
  DS이고 DS는 `src/`를 import할 수 없다. 두 곳에 적으면 색과 문구가 다른 기준을 쓴다.
  `badgeProgressText.ts`가 같은 이름으로 재수출한다. 이 때문에 `vitest.config.ts`에 `@ds` 별칭이
  필요했다(tsconfig엔 이미 있었다).
- **`ds:check`를 느슨하게 만들지 않았다.** 스크립트는 한 줄도 손대지 않고 원인(미정의 변수) 자체를
  없앴다. 스크립트의 휴리스틱 한계는 별도 티켓 20260906_2334로 분리.
- **1차 FAIL의 1차 원인은 티켓 스펙이었다.** 개발자가 열 수를 바꾼 판단에는 근거가 있었고,
  잘못은 그것을 `alerts`에 신고하지 않고 주석에 「연결선 균일」이라 사실과 다르게 적은 쪽이다.
  2차에서는 티켓과 다른 결정 3건이 전부 신고됐다.

### 잔여 이슈

- **320px 여유가 0이다.** 4눈금 폭 `52×4 + 16×3 = 256px`이 320px 화면의 카드 안쪽 폭과 정확히 같다.
  **페이지·카드 패딩을 1px만 키워도 4눈금 레일이 카드를 넘는다.** 마지막 눈금의 `size="md"`
  MYSTIC 칩은 이미 안쪽 엣지를 8px 넘는다(카드 패딩 안이라 잘리지 않음). 완화하려면
  `THUMB_SIZE`·`LINK_WIDTH`를 함께 줄인다. → 개선 리뷰 제안대로 **기하 불변식을 렌더 없는
  산술 vitest로 못박으면** 이 결합이 주석이 아니라 실패하는 테스트로 남는다(미착수).
- **긴 캡션에서 눈금 열 높이가 다시 어긋난다.** 캡션 레인이 68px로 제한돼
  `이번 주 2/3회 · 3일 남음` 형태가 3~4줄로 접힌다(실측 편차 25px). 짧은 문구에서는 123px 하나로
  유지된다. 구조적 해법은 캡션을 수치만 두고 기간·남은 일수를 헤더 `metaText`로 올리는 것.
- **레일 게이트 자물쇠의 보조기술 읽는 순서**가 시각 순서와 어긋난다 → 티켓 20260906_2333
- **인터랙션 리뷰 지적 미반영** — 같은 레일 이웃 눈금의 누름 배율이 다름(전역
  `button:active{transform:scale(.96)}`와 `.ds-rail-stop:active{scale:.96}`이 곱해져 0.9216),
  「자세히」 탭 타깃 48×17px, 디스클로저에서 셰브론만 250ms 돌고 내용은 0ms,
  `in oklab` 미지원 브라우저에서 진행 채움 소실, reduced-motion에서 즉시 점프.
  제안형 리뷰라 머지를 막지 않았으나 별도 판단 필요.
- **스토리북 라이트 테마**에서 lime 1.15:1 · amber 1.43:1 — `colors.light.css`가 `--status-*`를
  재정의하지 않는다. 서비스는 다크 전용 고정이라 실사용 영향 없음.
- 범위 밖 발견물 6건을 티켓으로 분리: `20260906_2329`(매니페스트 minify) ·
  `20260906_2330`(Story play 테스트 미실행) · `20260906_2331`(폐지어·내부키 노출) ·
  `20260906_2332`(글리프 사본·prompt 폐지어) · `20260906_2333`(자물쇠 읽기 순서) ·
  `20260906_2334`(ds-sync-check 오탐)
