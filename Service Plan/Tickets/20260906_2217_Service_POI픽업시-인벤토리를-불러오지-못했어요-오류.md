---
id: 20260906_2217
category: Service
status: CLOSED
created: 2026-09-06
closed: 2026-09-06
---

# [Service] 지도 POI 아이템 배지 픽업 시 "인벤토리를 불러오지 못했어요" 오류

## 배경 / 문제 정의
사용자가 지도 POI에서 드랍된 아이템 배지를 픽업하려고 하면
"인벤토리를 불러오지 못했어요. 잠시 후 다시 시도해 주세요" 토스트가 뜨며 픽업이 실패한다.
재시도해도 같은 오류가 반복되는 것으로 보인다(문구가 "잠시 후 재시도"를 안내하지만
일시적 오류가 아닐 가능성).

## 상세 요구사항

### 서비스/코드베이스 관점
- 오류 문구는 `jam-web/src/lib/i18n/ko.ts:436` `drops.loadInventoryFailed`.
- 이 문구가 뜨는 경로는 [PoiCarouselModal.tsx](jam-web/src/components/PoiCarouselModal.tsx) 세 곳:
  1. `openInventory()` (179줄) — `/api/inventory/items` fetch 자체가 실패(네트워크/파싱 예외)
  2. `executeDrop()` 중 서버가 `inventory_not_found` 코드 반환 (207줄, 드랍 시)
  3. `executePickup()` 중 서버가 `inventory_not_found` 코드 반환 (261줄, **이번 신고 케이스**)
- 픽업 플로우([pickup/route.ts](jam-web/src/app/api/drops/[dropId]/pickup/route.ts))는
  114~123줄에서 픽업하려는 유저의 `inventory` 테이블 행을 조회하고, 없으면 `inventory_not_found`를
  반환한다. RPC `pickup_drop()` 내부에서도 146줄 매핑처럼 같은 코드를 돌려줄 수 있다.
  → **즉 이 유저는 `inventory` 테이블에 자신의 행이 없다는 뜻.**
- `inventory` 행은 회원가입 시 `handle_new_user()` 트리거(`001_initial_schema.sql`)가 생성한다.
  이 트리거는 과거 한 번 인벤토리 생성 구문이 누락된 채 운영 DB에 반영된 적이 있고
  (`079_fix_handle_new_user_missing_inventory.sql`), 이후 `129_inventory_policy_max_slots.sql`에서
  시그니처가 다시 갱신됐다. **신고한 유저가 언제 가입했는지, 현재 `handle_new_user()` 정의가
  실제로 inventory를 생성하는지, 그리고 해당 유저의 `inventory` 테이블에 행이 있는지부터
  Supabase에서 직접 확인할 것.**
- **오케스트레이터가 Supabase MCP(`ceehnkzdbecxwzxrhhns`, jam-prod — staging·프로덕션 공용
  단일 DB)로 직접 진단 완료. 원인 확정됨:**
  - `auth.users` 12명 / `public.users` 12명 / **`public.inventory` 0행** — 가입 유저 전원이
    인벤토리 레코드 자체가 없다. 신고 유저만의 문제가 아니라 **전체 유저 100% 결측**.
  - 트리거(`trg_on_auth_user_created`, `AFTER INSERT ON auth.users`, enabled)에 실제로 바인딩된
    함수(`handle_new_user()`, oid 17825)는 `INSERT INTO public.inventory (user_id, max_slots)`
    구문을 포함한 **정상 버전**이다 — DO 블록으로 동일 로직을 수동 실행해(즉시 롤백) 확인함.
    RLS는 함수 소유자가 `postgres`(`rolbypassrls=true`)라 무관.
  - 이 정상 버전은 **마이그레이션 129(`129_inventory_policy_max_slots.sql`, 실제 적용 시각
    `2026-09-04 07:41:58 UTC`)에서 도입**됐다. 최근 가입자(`ziyo88@gmail.com`,
    `2026-09-03 11:30:58 UTC`)도 이 적용 시각보다 앞선다 — **129 적용 이후 신규 가입자가
    아직 한 명도 없어 "트리거가 지금부터는 정상"이라는 사실만으로는 검증되지 않은 상태였다.**
  - `supabase_migrations.schema_migrations`에는 **079번 마이그레이션(`079_fix_handle_new_user_
    missing_inventory.sql`) 자체가 기록에 없다** — 078 다음이 082로 바로 뛴다. 즉 079는
    로컬 파일로는 존재하지만 **실제로 프로덕션에 적용된 적이 없다.** 이번에 함수가 정상인
    이유는 079가 아니라 129의 `CREATE OR REPLACE FUNCTION` 전체 재정의 덕분이다.
  - **결론**: 트리거는 이미 고쳐져 있다(129로). 남은 문제는 **129 적용 이전에 가입한 기존
    유저 12명 전원에 대한 `inventory` 행 백필**뿐이다. 트리거 자체를 다시 고칠 필요는 없다.
- **필요한 조치 (jam-developer가 SQL 파일로 작성 → 오케스트레이터가 승인 후 실행)**:
  ```sql
  INSERT INTO public.inventory (user_id, max_slots)
  SELECT u.id, COALESCE((SELECT max_slots FROM public.inventory_policy WHERE id = 1), 50)
  FROM auth.users u
  WHERE NOT EXISTS (SELECT 1 FROM public.inventory i WHERE i.user_id = u.id)
  ON CONFLICT (user_id) DO NOTHING;
  ```
  `jam-web/supabase/migrations/`에 `seed_` 접두사(1회성 데이터)로 저장 — 신규 컬럼/스키마
  변경이 아니라 데이터 백필이므로 `NNN_설명.sql`이 아니라 `seed_backfill_missing_inventory.sql`
  형태가 맞다.
- **회귀 방지**: 향후 같은 결측이 재발해도 사용자가 막히지 않도록, API 레벨(픽업/드랍/인벤토리
  조회 3곳)에서 `inventory_not_found`를 만났을 때 즉석에서 inventory 행을 생성하는 방어 로직
  추가를 검토. (근본 원인은 이미 백필로 해소되므로 이건 향후 재발 방지용 안전망 성격.)

### UI/UX 관점 (해당 시)
- 근본 원인이 "재시도해도 절대 안 풀리는 상태"라면, 현재 문구("잠시 후 다시 시도해 주세요")가
  오도할 수 있다. 원인 확정 후 문구 변경 필요 여부를 `UX_WRITING_GUIDELINE.md` 기준으로 판단.

## 구현 계획
> jam-developer가 위 확인 순서대로 Supabase MCP로 직접 조회 후, 근본 원인에 맞는 수정
> (트리거 복구 또는 백필 SQL 작성)을 진행한다. SQL 실행은 이 티켓 파이프라인 규칙상
> 오케스트레이터가 사용자 승인 하에 처리한다 — jam-developer는 SQL 파일 작성까지만.

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

**⚠️ 원인 재정정** — 위 상세 요구사항 절이 진단할 당시엔 "마이그레이션 129 적용 이전
가입자의 트리거 결측 백필 누락"으로 결론지었으나, 이후 어드민 인벤토리 최대치 화면에서
같은 증상이 다시 신고되어 재조사한 결과 **직접 원인은 따로 있었다**: 2026-09-06 실행된
유저 서비스 데이터 초기화(티켓 [20260905_0039](20260905_0039_Infra_유저-전체-초기화-배지정의-폐기.md),
v5 전환)의 결정 ②-A가 `inventory` 테이블 행을 **DELETE**했다 (아이템을 Orphaned로
전이시키기 위한 의도적 조치, `seed_reset_users_20260906.sql` Phase 4-A). 이 삭제 이후
`inventory` 행을 다시 만들어주는 경로가 없어 유저 12명 전원이 인벤토리 행 없는 상태가 됐다
— `handle_new_user()` 트리거(129에서 갱신)는 **신규 가입 시에만** 실행되므로 기존 유저에게는
적용되지 않는다. 129 이전 가입자 미반영 문제는 이 초기화가 겹쳐 일어난 별개 배경 사실일 뿐,
당시 실측(0행)의 즉각적 원인은 오늘의 초기화였다.

증상은 두 갈래로 나타났다:
1. POI 픽업 시 `inventory_not_found` → "인벤토리를 불러오지 못했어요" 토스트 (이 티켓의 신고 건)
2. 어드민이 `inventory_policy.max_slots`를 바꿔도 [inventory/page.tsx:39-40](../../jam-web/src/app/(main)/inventory/page.tsx)의
   `inventory?.max_slots ?? 50` 하드코딩 폴백 때문에 정책값이 아니라 항상 "0/50"으로 보임
   (관리자가 최대치를 10으로 바꿨는데도 화면은 여전히 0/50 → 사용자 재신고로 발견)

**조치**: 백필 SQL(`jam-web/supabase/migrations/seed_backfill_missing_inventory.sql`)을
작성해 실행 — `auth.users` 전원 중 `inventory` 행이 없는 유저에게 현재 정책값(`inventory_policy.max_slots`)
기준으로 행을 새로 INSERT (`ON CONFLICT DO NOTHING`, 기존 데이터 훼손 없음). 실행 후
12명 전원 `inventory` 행 생성 및 정책값(10) 반영 확인.

### 변경된 파일
```
jam-web/supabase/migrations/seed_backfill_missing_inventory.sql (신규, 실행 완료)
```

### 테스트 결과
- [x] 실행 전 `public.inventory` 0행 확인 (auth.users 12 / public.users 12 / inventory 0)
- [x] 백필 SQL 실행 후 `public.inventory` 12행, 전원 `max_slots = 10`(정책값) 반영 확인
- [ ] 실제 POI 픽업 플로우 재시도 확인 — 신고 유저가 직접 재시도해봐야 최종 확인됨
- [ ] 어드민 인벤토리 최대치 화면에서 정상적으로 "N/10 슬롯 사용중" 표시되는지 확인

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

- [x] 근본 원인이 확정 후 항상 재발하는 상태가 아니라 **1회성 데이터 결측**이었음이 확인됐다
  (백필로 완전히 해소) — 기존 "잠시 후 다시 시도해 주세요" 문구를 바꿀 필요는 없다고 판단

### 배포 정보
- 배포일: 2026-09-06
- 환경: production (Supabase jam-prod, `ceehnkzdbecxwzxrhhns` — staging·프로덕션 공용 단일 DB)
- 커밋: (SQL 실행이라 코드 커밋 없음 — 마이그레이션 파일만 staging에 커밋)

### 주요 의사결정 / 핵심 메모
- **왜 새 티켓을 만들지 않았는가**: 사용자가 "어드민 인벤토리 최대치 적용이 반영 안 된다"로
  새로 신고했을 때, 조사 결과 이 티켓과 동일한 근본 원인(inventory 행 결측)으로 확인되어
  이 티켓 안에서 함께 처리했다.
- **왜 트리거를 다시 고치지 않았는가**: `handle_new_user()`는 이미 정상(129에서 갱신)이라
  신규 가입자는 문제없다. 기존 유저 결측은 백필로 1회성 해소만 하면 된다.
- **회귀 방지 검토는 미착수** — 상세 요구사항에 제안된 "API 레벨에서 inventory_not_found
  조우 시 즉석 생성" 방어 로직은 이번 백필로 급한 불이 꺼져 별도 우선순위 판단이 필요하다.
  더 근본적으로는 **`inventory`처럼 유저당 정확히 1행이 항상 존재해야 하는 master record
  테이블을 앞으로 초기화 스크립트가 다시 건드릴 때, 행을 DELETE하고 끝내지 말고 반드시
  같은 스크립트 안에서 재생성까지 포함해야 한다**는 교훈이 남는다 — 이번 v5 초기화
  (`20260905_0039`)의 설계 시점에는 이 구분이 누락됐었다.

### 잔여 이슈
- v5 초기화(`20260905_0039`)가 DELETE한 나머지 테이블(`point_wallets`·`user_drop_state`·
  `user_combine_state` 및 15개 이력성 테이블)에 같은 유형 문제가 있는지 2026-09-06 추가 조사함
  — **결과: 없음.** `inventory`만 `handle_new_user()` 트리거가 "가입 시 항상 1행 생성"을
  보장하는 유일한 master record였고, 그 가정 위에 `.single()` 하드폴백 코드가 쌓여 있었다.
  `point_wallets`(`src/lib/points/index.ts:141`)·`user_drop_state`(`src/lib/drop-engine/index.ts:436`)·
  `user_combine_state`(`src/lib/combine/index.ts:270`)는 애초에 트리거 대상이 아니라
  `.maybeSingle()` + upsert 지연 생성 패턴으로 설계돼 있어 0행이 정상 상태다. 나머지 15개
  테이블도 전부 다건 조회(리스트/이력)라 `.single()` 사용 자체가 없다. 추가 조치 불필요.
- 상세 요구사항의 "API 레벨 방어 로직 추가" 제안은 이번엔 적용하지 않음 — 필요성 재판단 대상
