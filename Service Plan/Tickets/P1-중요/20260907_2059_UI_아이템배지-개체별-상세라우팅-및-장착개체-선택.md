---
id: 20260907_2059
category: UI
priority: P1
status: CLOSED
created: 2026-09-07
closed: 2026-09-07
---

# [UI] 아이템배지 개체별 상세 라우팅 + 컬렉션 장착 개체 선택 UI

## 배경 / 문제 정의

동일 배지를 여러 개 보유해도 **어디서 눌러도 같은 개체(같은 일련번호)의 상세 화면으로 간다.**
개체(`inventory_items`)는 DB상 이미 고유 일련번호로 구분되는데(티켓 20260829_2101에서 개체
정체성 확립), **URL이 전부 `badge_id` 기준이라 "어느 개체를 눌렀는가"가 화면에 도달하기 전에
소실**된다.

### 실측 재현 (sihyunrr@gmail.com, 2026-09-07 프로덕션 DB 조회)

| 배지 | 보유 | 일련번호 |
|---|---|---|
| 자외선 차단제 빈 통 | 3개 | `ZPRS545679` **[장착]** · `LJAE830165` · `LFCC439094` |
| 뜨거운 아스팔트 조각 | 2개 | `UPTF472117` **[장착]** · `ZHVQ777120` |
| 아지랑이의 환영 | 2개 | `MMBT829356` **[장착]** · `HGIB165351` |

현재 동작: 인벤토리의 `LJAE830165`를 눌러도, `LFCC439094`를 눌러도, 컬렉션에 장착된
`ZPRS545679`를 눌러도 **전부 `LFCC439094`가 표시된다.**

### 원인 (3중 복합)

1. **인벤토리 경로에서 개체 id 소실** — `InventoryGrid.tsx:63`이 `/inventory/{item_id}`로
   링크하지만, `inventory/[itemId]/page.tsx:32`가 `redirect('/badges/' + badge_id)`로
   개체 정보를 버린다. 티켓 [20260816_007](20260816_007_UI_아이템배지-상세-hero-공통-컴포넌트화.md)에서
   "아이템배지 상세는 어디서 접근하든 내용이 동일하다"는 전제로 도입한 redirect다.
   **이번 요구사항이 그 전제를 무효화한다.**
2. **컬렉션 경로는 개체 id를 애초에 안 싣는다** — `SlotGrid.tsx:151`이
   `href={/badges/${badge.id}}`. 슬롯이 참조하는 `inventory_item_id`가 있는데도 쓰지 않는다.
3. **상세 화면이 대표 개체 1개를 임의 선정** — `badges/[id]/page.tsx:304`가
   `obtained_at DESC` 첫 번째(=가장 최근 획득분)를 대표로 잡아 `ItemSerialCode`에 넘긴다.

### 파생 정합성 오류 (원인 2+3의 결합)

컬렉션 슬롯 자동 장착은 `obtained_at ASC`(가장 오래된 것,
`collections/[id]/page.tsx:186-194`)를 고르는데, 배지 상세 대표는 `obtained_at DESC`(가장 최신)를
고른다. **정렬 방향이 반대**라, 컬렉션에서 장착 배지를 눌러 들어가면 실제 슬롯에 꽂힌 개체가
아닌 다른 개체의 일련번호가 크게 표시된다. 위 실측이 정확히 이 상태다
(장착=`ZPRS545679`, 표시=`LFCC439094`).

### 관련 문제 — 장착 개체를 사용자가 고를 수 없다

동일 배지를 여러 개 보유해도 컬렉션 장착 시 **선택 UI가 없다.** 서버가
`obtained_at ASC` 첫 번째를 말없이 장착한다. 사용자가 특정 일련번호를 골라 넣을 수단이 없다.

## 상세 요구사항

### 서비스/코드베이스 관점

**A. 개체 식별을 URL에 싣는다 — `?item={inventory_item_id}` 쿼리 파라미터 방식**

- `/badges/[id]`에 `item` searchParam 추가. 기존 `?u={username}`와 같은 계열로 확장한다.
- `inventory/[itemId]/page.tsx`: `redirect('/badges/{badge_id}?item={itemId}')`로 변경
  (redirect 자체는 유지 — 뒤로가기 히스토리 동작을 그대로 보존한다).
- `SlotGrid.tsx:151`: 슬롯이 있으면 `?item={slot.inventory_item_id}`를 붙인다.
  이를 위해 `collections/[id]/page.tsx`가 슬롯의 `inventory_item_id`를 SlotGrid까지 전달해야
  한다(현재 장착 칸은 `inventoryItem: null`로 떨어진다 — `page.tsx:196-208`).
- `badges/[id]/page.tsx`의 대표 개체 선정 로직:
  1. `?item`이 있고 **유효하면**(해당 개체가 이 `badge_id`이고, 조회 주체가 소유) 그 개체를 쓴다
  2. 없거나 무효하면 기존 fallback(미드랍 중 최신) 유지 — 배지 탭 직접 진입 경로 무영향
- **`?item` 검증 필수**: 타인 소유·타 배지·파괴된(`destroyed_at`) 개체 id를 넘겨도 남의
  일련번호가 노출되지 않아야 한다. 무효 시 `notFound()`가 아니라 **조용히 fallback**으로
  떨어뜨린다(링크 공유 시 깨지지 않게).
- 타인 프로필 열람(`?u=`) 시 일련번호 비노출 정책(`showSerial = isOwnBadge`)은 **그대로 유지**한다.

**B. 컬렉션 장착 시 개체 선택 UI**

- `collections/[id]/page.tsx`: `inventoryMap`이 badge_id당 1개만 남기는 것을 **후보 배열 전체**로
  바꾼다(미장착 + `destroyed_at IS NULL`, `obtained_at ASC` 정렬 유지).
- `SlotGrid`의 장착 버튼:
  - 후보 **1개** → 지금처럼 즉시 장착 (불필요한 시트 노출 금지)
  - 후보 **2개 이상** → 개체 선택 시트를 열고, 각 후보의 **일련번호를 표시**한다
  - 선택 → 해당 `inventory_item_id`로 기존 `POST /api/itembooks/{id}/slot` 호출
- **API·RPC 변경 불필요** — `slot_item_into_book(p_user_id, p_item_book_id, p_inventory_item_id)`가
  이미 개체 id를 받아 소유·중복 장착을 검증한다(`migrations/111_item_slot_atomic_rpc.sql:77-108`).
  `UNIQUE(user_id, item_book_id, badge_id)` 제약도 그대로 유지된다(컬렉션당 같은 배지 1개).

### UI/UX 관점

**재사용 판정 (오케스트레이터 결정 — 이대로 따를 것)**

신규 MODULAR 컴포넌트를 만들지 않는다. **기존 조합으로 전부 해결된다.**

| 용도 | 사용할 것 | 근거 |
|---|---|---|
| 선택 시트 컨테이너 | `src/components/ui/BottomSheet`(서비스 구현) | 이미 8곳에서 사용. 드래그-투-클로즈 보유. DS 버전은 병존 구현이라 서비스 쪽이 기능 우위 |
| 개체 후보 그리드 | `InventoryGrid` `mode="select"` + `BadgeGridCard` | **`PoiCarouselModal.tsx:481`이 이미 "동일 배지 여러 개 중 하나를 골라 드랍"하는 완전히 동일한 형태의 선택 UI로 쓰고 있다.** 같은 문제에 같은 패턴을 쓴다 |
| 일련번호 표시 | `@ds/components/patterns/ItemSerialCode` | 배지상세(`height=50`)·드랍시트(`height=40`)에서 이미 사용 중. 그리드 카드용으로 더 작은 height를 넘긴다 |

- `InventoryGridItem`에 **`serial?: string`을 optional로 추가**한다 — 기존 호출부 2곳
  (`inventory/page.tsx`, `PoiCarouselModal`)은 무영향. 문자열 조립 포맷은 기존과 동일하게
  `(serial_prefix ?? '????') + String(serial_number).padStart(6,'0')`.
- 선택 시트 문구는 `UX_WRITING_GUIDELINE.md` 준수(해요체, 고정 용어 "획득·장착").

### 컨텐츠 관점
- 해당 없음 (컨텐츠 데이터 변경 없음)

## 구현 계획

**DB 변경 없음.** 마이그레이션·RPC·스키마 모두 그대로. UI/라우팅 계층만 수정한다.

예상 변경 파일:
```
jam-web/src/app/(main)/inventory/[itemId]/page.tsx      A: redirect에 ?item 추가
jam-web/src/app/(main)/badges/[id]/page.tsx             A: ?item 수신·검증·대표개체 선정
jam-web/src/app/(main)/collections/[id]/page.tsx        A+B: 슬롯 개체 id 전달, 후보 배열화
jam-web/src/app/(main)/collections/[id]/SlotGrid.tsx    A+B: 링크에 ?item, 선택 시트 분기
jam-web/src/components/inventory/InventoryGrid.tsx      B: serial optional 필드 + 표시
```

### 회귀 확인 포인트
- 배지 탭 → 배지 상세 직접 진입(`?item` 없음): 기존 동작 유지되는가
- 타인 프로필 열람(`?u=`): 일련번호 여전히 비노출인가
- 아이템배지가 아닌 일반 배지 상세: 영향 없는가
- 미보유 배지를 컬렉션에서 진입: 링크 없음(`isUndiscovered`) 동작 유지되는가
- 후보 1개일 때 시트가 뜨지 않고 즉시 장착되는가
- 드랍 바텀시트(`PoiCarouselModal`): `InventoryGridItem` 변경에 영향받지 않는가

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

**A. 개체 식별을 URL에 싣는다 (`?item={inventory_item_id}`)**

1. `inventory/[itemId]/page.tsx` — `redirect('/badges/{badge_id}?item={itemId}')`.
   redirect 자체는 유지해 뒤로가기 히스토리 동작을 보존했다.
2. `badges/[id]/page.tsx` — `searchParams`에 `item` 추가(`SearchParamValue` + `singleQueryParam`,
   기존 `?u=`와 동일 계열). 대표 개체 선정을 「`?item`으로 지목된 개체 → 없으면 기존
   fallback(미드랍 중 최신)」 순으로 바꿨다.
   **검증은 추가 쿼리 없이 끝난다** — `allItemInventory`가 이미
   `inventory_id = 조회주체의 인벤토리` + `badge_id = 이 배지`로 좁혀 조회한 목록이라,
   그 안에서 id가 일치하는지 + `dropped_at`·`destroyed_at`이 비어 있는지만 보면
   소유·배지일치·생존이 모두 걸러진다. 무효값은 `notFound()`가 아니라 조용히 fallback.
   (`destroyed_at`을 이 판정에 쓰기 위해 select에 컬럼 하나를 추가했다)
3. `collections/[id]/page.tsx` — 슬롯 조회에 `inventory_item_id`를 추가해 SlotGrid까지 전달.
4. `SlotGrid.tsx` — 장착된 칸의 링크에 `?item`(또는 `?u=` 뒤에 `&item=`)을 붙인다.
   이로써 「컬렉션 자동 장착은 `obtained_at ASC`, 배지 상세 대표는 `DESC`」라 서로 다른
   개체를 가리키던 파생 정합성 오류가 근본적으로 사라진다(정렬 방향을 맞추는 대신,
   실제로 꽂힌 개체 id를 그대로 넘기는 방식으로 해결).

**B. 컬렉션 장착 시 개체 선택 UI**

- `collections/[id]/page.tsx` — `inventoryMap`(badge_id당 1개)을 `inventoryCandidates`
  (badge_id당 **미장착 후보 배열 전체**, `obtained_at ASC` 유지)로 교체.
  후보 조회에 `destroyed_at IS NULL`을 추가했다(파괴된 개체는 RPC가 어차피 거절하므로,
  나열하기 전에 목록 단계에서 제외).
- `SlotGrid.tsx` — 「추가」 버튼이 후보 수로 분기한다. **1개면 종전과 동일하게 즉시 장착**,
  2개 이상이면 개체 선택 시트를 연다. 선택 시 기존 `POST /api/itembooks/{id}/slot`을
  그대로 호출한다(API·RPC·스키마 변경 없음).

**UI 재사용 — 신규 MODULAR 컴포넌트 없음** (오케스트레이터 판정대로)

| 용도 | 사용한 것 |
|---|---|
| 선택 시트 | `src/components/ui/BottomSheet` (body 포털 + z-50, 기존 층 그대로. footer 없어 `pushBottomOverlay` 신고 대상 아님) |
| 후보 그리드 | `InventoryGrid` `mode="select"` + `BadgeGridCard` (`PoiCarouselModal`의 드랍 선택과 같은 조합) |
| 일련번호 | `@ds/components/patterns/ItemSerialCode` (~~`height=14`~~ → 2차에서 `height=40`) |

`InventoryGridItem`에 `serial?: string`을 optional로 추가했다. **`serial`이 없으면 자식 노드가
종전과 완전히 동일**하도록 분기해, 기존 호출부 2곳(`/inventory`, 드랍 바텀시트)의 레이아웃이
바뀌지 않게 했다.

> ⚠️ 1차의 `height=14` 산정은 **틀렸다.** 총 폭 공식(5.31h)은 맞았지만 "3열 카드 콘텐츠 폭
> 80px" 전제가 실측이 아니었다. 2차에서 Chromium 실렌더로 재측정해 값과 열 수를 모두
> 바꿨다 — 아래 「2차」 절 참조.

---

**2차 (보완) — 게이트 리뷰 WARN 해소 + 판독성·피드백**

**1. 상세화면 안 개체 불일치 해소 (게이트 WARN 1)**

`ItemEarnHistory`에 `activeItemId`를 추가하고
`first = items.find(i => i.id === activeItemId) ?? items[0]`으로 잡아, 히어로 일련번호와 이력
첫 카드가 **같은 개체**를 가리키게 했다. 1차에서는 `const [first, ...rest] = items`라 첫 카드가
항상 `items[0]`(=`obtained_at DESC` 최신분)이었고, 컬렉션에서 장착 개체로 진입하면
히어로는 `MMBT829356`(09-06 획득)인데 획득일은 `2026.09.07`로 표시됐다(실데이터
"아지랑이의 환영"에서 재현). 상세 바텀시트의 열림 상태도 배열 index(`openIdx`) 대신 개체
id(`openId`) 기준으로 바꿔, `rest`가 재정렬돼도 정확한 개체를 연다.

**2. 일련번호 판독성 — 릴 정지 + 높이 상향 + 1열 전환**

- (2-a) `ItemSerialCode`에 **`animate` prop 추가(기본 `true`)**. `false`면 **기존
  reduced-motion 분기를 그대로 재사용**해 정적 렌더(`cells=[digit]`, `translateY` 0,
  SVG 블러 필터 미생성)한다. 전역 모션 토큰(`--reel-*`)은 건드리지 않았고, 새 정적 분기를
  따로 만들지도 않았다(릴을 끈 상태에서 지켜야 할 시각 결과가 reduced-motion과 동일하다).
  선택 시트 호출부에서만 `animate={false}`.
  **이유**: 시트를 여는 유일한 목적이 일련번호를 읽고 고르는 것인데 숫자가 약 1.9초간
  회전한다(`reel-dur` 1400ms + `stagger` 90ms × 5, 앞에 시트 진입 400ms가 겹침).
  `BottomSheet`가 닫히면 children을 언마운트하므로 **열 때마다 처음부터 재생**된다.
  릴은 "번호가 지금 확정되는 순간"의 연출이라, 이미 가진 값을 비교하는 화면과 목적이 반대다.
- (2-b) `GRID_SERIAL_HEIGHT_PX = 14` → `SELECT_SERIAL_HEIGHT_PX = 40`,
  **선택 시트만 1열**(`InventoryGrid`에 `columns?: 1 | 3` 추가, 기본 3).
  인벤토리 목록·드랍 시트의 3열은 그대로 둔다.

**실측 근거** — Chromium 실렌더 `offsetWidth`(esbuild + playwright, 추정 아님):

| 뷰포트 | 열 | 카드 offsetWidth | 클리핑 경계(padding box) | 콘텐츠 폭 | ItemSerialCode 폭 | 판정 |
|---|---|---|---|---|---|---|
| 360 | 3 | 104 | 100 | 76 | h14 → 74 | 들어가지만 글자가 캘리브레이션 범위 밖 |
| 360 | 3 | 104 | 100 | 76 | h20 → 106 | **6px 잘림** |
| 360 | 2 | 160 | 156 | 132 | h20 → 106 | 들어감 |
| 360 | 1 | 328 | 324 | 300 | h40 → 212 | 여유 |
| 390 | 1 | 358 | 354 | 330 | h40 → 212 | 여유 |
| 430 | 1 | 398 | 394 | 370 | h40 → 212 | 여유 |

- 1차의 "카드 콘텐츠 폭 80px" 전제는 실측이 아니었다. 실제 콘텐츠 폭은 **76px**이고,
  `overflow-hidden`의 **실제 클리핑 경계는 padding box인 100px**이다 — 90px 썸네일이
  패딩 영역까지 넘쳐도 잘리지 않는 이유가 이것이다.
- 3열에서는 height 20 이상이 물리적으로 불가하므로, 티켓 지시대로 **선택 시트만 1열**로 갔다.
- height **40**을 고른 이유: `ItemSerialCode`의 자간 보간 하한이 `TRACKING_SMALL_FONT = 20`
  (= height 40)이라 그 아래로는 작은 글씨용 자간 보정이 클램프돼 캘리브레이션 범위를 벗어난다.
  서비스 실사용 최소값(드랍 시트)도 40이다. `.t-reel-col` 마스크(셀 위아래 22% 페이드)가
  7px 글리프 획에 직접 닿던 문제도 릴 정지 + 높이 상향으로 함께 해소된다.

**3. 탭 반응 표시 + 만료 임박 칩 + 실패 노출**

- (3-a) `InventoryGrid`의 기존 `selectedItemId`를 선택 시트에서 실제로 넘긴다. 탭 즉시
  `bg-primary/15` 선택 톤이 켜지고 **그 뒤** 장착 요청이 나간다. 요청 진행 중에는 새로 추가한
  `disabled`로 중복 탭을 막는다(1차에는 어느 호출부도 `selectedItemId`를 넘기지 않아
  탭해도 카드에 아무 변화가 없었다).
- (3-b) 후보 조회(`collections/[id]/page.tsx`)에 `expires_at`을 추가해 후보에 실어 보낸다.
  `InventoryGridItem`은 이미 `expiresAt`으로 "곧 만료" 칩을 그리는데 값이 안 와서 칩이 뜨지
  않았다. 만료 임박 개체를 모르고 장착하는 것을 막는, 이 화면에서 가장 결정적인 정보다.
- (3-c) `handleSlot`을 **`requestSlot`(실패 사유를 문자열로 반환)** 으로 분리해
  **표시 위치를 호출부가 정하게** 했다. 그리드 「추가」 버튼 경로는 종전대로 그리드 상단 배너,
  시트 경로는 **성공했을 때만 시트를 닫고 실패는 시트 안 배너로 알린다.**
  1차 구현은 시트를 먼저 닫아서(350ms), `slotMode` 진입 시 그리드가 `scrollIntoView`된 상태와
  겹치면 실패 배너가 화면 밖일 수 있었다.

**MODULAR 하위 호환** — `animate` 기본값이 `true`라 기존 사용처 3곳(배지 상세 `height=50`,
드랍 시트 `height=40`, `dev-sample`)은 prop을 넘기지 않아 종전과 완전히 동일하게 스핀한다.
`.d.ts`·`.prompt.md`·`.stories.tsx`(릴 정지 Story 2종)를 함께 갱신했다.

### 변경된 파일
```
[1차]
jam-web/src/app/(main)/inventory/[itemId]/page.tsx     A: redirect에 ?item 추가
jam-web/src/app/(main)/badges/[id]/page.tsx            A: ?item 수신·검증·대표개체 선정
jam-web/src/app/(main)/collections/[id]/page.tsx       A+B: 슬롯 개체 id 전달, 후보 배열화
jam-web/src/app/(main)/collections/[id]/SlotGrid.tsx   A+B: 링크에 ?item, 선택 시트 분기
jam-web/src/components/inventory/InventoryGrid.tsx     B: serial optional 필드 + 표시
jam-web/src/lib/i18n/ko.ts                             B: 선택 시트 문구 2건

[2차 보완]
jam-web/design-system/components/patterns/ItemSerialCode.jsx          2-a: animate prop (기본 true)
jam-web/design-system/components/patterns/ItemSerialCode.d.ts         2-a: animate 타입
jam-web/design-system/components/patterns/ItemSerialCode.stories.tsx  2-a: 릴 정지 Story 2종
jam-web/design-system/components/patterns/ItemSerialCode.prompt.md    2-a: animate·height 하한 문서화
jam-web/src/app/(main)/badges/[id]/ItemEarnHistory.tsx                1: 첫 카드를 대표 개체 기준으로
jam-web/src/app/(main)/badges/[id]/page.tsx                           1: activeItemId 전달
jam-web/src/app/(main)/collections/[id]/page.tsx                      3-b: 후보에 expires_at
jam-web/src/app/(main)/collections/[id]/SlotGrid.tsx                  3-a/3-b/3-c: 선택 톤·만료칩·실패 노출
jam-web/src/components/inventory/InventoryGrid.tsx                    2-b/3-a: columns·height 40·disabled
```

### 테스트 결과
- [x] `npx tsc --noEmit` — 오류 0건
- [x] `npm run lint`(전체) — **에러 0건 / 경고 13건**, 전부 `design-system/` 기존 경고
      (변경 파일에서 발생한 경고 0건)
- [x] 회귀 확인 포인트 6가지 코드 레벨 점검
  - `?item` 없는 배지 탭 직접 진입 → `requestedItem = null` → 기존 fallback 그대로
  - `?u=` 타인 열람 → `showSerial = isOwnBadge`는 미변경, 일련번호 여전히 비노출
  - 아이템배지가 아닌 배지 → `?item`은 `badgeRow.type === 'item'` 분기 안에서만 소비
  - 미보유 배지 → `isUndiscovered = !isSlotted && candidates.length === 0`로 종전과 동치, 링크 없음
  - 후보 1개 → `handleSlotButton`이 시트를 열지 않고 바로 `handleSlot` 호출
  - 드랍 바텀시트 → `serial`을 넘기지 않으므로 자식 노드가 종전과 동일
- [x] **(2차) 회귀 확인 포인트 6가지 재점검** — 위 6가지 모두 여전히 성립.
      2차에서 추가로 확인한 항목:
  - `InventoryGrid` 기존 호출부 2곳(`/inventory` `mode="navigate"`, `PoiCarouselModal`
    `mode="select"`)은 `columns`·`disabled`·`serial`을 넘기지 않아 3열·릴 유지·자식 노드 동일
  - `ItemSerialCode` 기존 사용처 3곳(상세 h50 · 드랍시트 h40 · dev-sample h50)은
    `animate` 미전달 → 기본값 `true`로 종전과 동일하게 스핀
  - 타인 프로필(`?u=`) — `ItemEarnHistory`는 `isOwnBadge` 게이트 안에서만 렌더되므로
    `activeItemId` 추가가 일련번호 비노출 정책에 영향을 주지 않는다
  - `?item` 없는 직접 진입 — 첫 카드가 이제 히어로와 같은 개체(미드랍 최신분)를 그린다.
    1차에서는 `items[0]`(드랍된 개체일 수도 있음)이었으므로 **개선된 동작**이다
- [x] **(2차) 선택 시트 실렌더 검증** — 360/430 뷰포트에서 1열 + `height=40` +
      `animate=false` 조합으로 일련번호가 정지 상태로 판독 가능, 선택 톤(`bg-primary/15`)과
      "곧 만료" 칩 표시 확인 (esbuild + playwright 스크린샷)
- [ ] 실화면 확인 — 이 브랜치가 staging에 병합된 뒤 `jam-stage.vercel.app`에서 확인 필요
      (워크트리에서는 `next dev`가 뜨지 않아 앱 전체 로컬 실렌더 불가)

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

신규 노출 문구 2건 (`d.itembooks.selectItemTitle` / `selectItemBody`):

최종 문구 (3차에서 수정):

- `selectItemTitle`: `어느 배지를 장착할까요?`
- `selectItemBody`: `장착할 수 있는 배지가 {count}개예요. 일련번호로 구분해서 골라주세요.`

**3차 수정 사유 2건** (게이트 재리뷰 INFO 1 + 개선 리뷰 지적 반영):

1. **`{count}`가 가리키는 대상과 문구가 어긋났다.** `{count}`에 들어가는 값은
   `candidates.length`, 즉 **미장착 후보 수**인데 문구는 "보유하고 있어요"였다. 다른 컬렉션에
   이미 장착한 개체가 있으면 인벤토리에 보이는 보유 수와 달라져 오정보가 된다.
   → "장착할 수 있는 배지가 {count}개예요"로 바꿔 값과 문구를 일치시켰다.
2. **제목의 지시 대상이 모호했다.** 후보가 전부 같은 배지인데 "어떤 배지를"은 다른 배지를
   고르는 것으로 읽힐 소지가 있다. 한국어에서 「어느」는 특정 집합 안에서의 선택을 가리키므로
   「어느 배지를」로 바꿨다.
3. **마침표 규칙을 정정했다.** `UX_WRITING_GUIDELINE.md` §3은 "본문/안내문은 두 줄 이상
   문장에 정확히 마침표 사용"이다. 본문이 두 문장이므로 마지막에도 마침표를 찍는 것이 맞다.
   2차까지의 "본문 끝에 마침표 없음" 판정은 제목/버튼 규칙을 본문에 잘못 적용한 것이었다.

- [x] 용어 일관성: 기존 컬렉션 화면과 같은 「장착」 표기
      (`slotHint`·`completedBody`가 이미 「슬롯에 장착」을 쓴다)
- [x] 톤앤매너: 선택을 돕는 안내 — 담백한 해요체
- [x] 에러 메시지: 신규 에러 문구 없음 (기존 `slotFailed`·`networkError` 경로 그대로)
- [x] 문장 규칙: 해요체, 제목은 마침표 없이 물음표로, 본문은 두 문장 모두 마침표
- [x] 표기 규칙: 개수는 `{count}개`

### 배포 정보
- 배포일: 2026-09-07 (staging 병합)
- 환경: staging → 프로덕션 승격은 `/jam-ship`으로 별도 진행
- 커밋: `7863f063`(1차 구현) → `d559adbf`(2차 보완) → `12ba8bf7`(staging 머지)
  → 문구 수정·문서 갱신·CLOSED 커밋이 뒤따름

### 주요 의사결정 / 핵심 메모

**라우팅 방식으로 `?item=` 쿼리 파라미터를 택한 이유 (접수 시점 결정)**

검토한 대안:
- **(A) `/inventory/[itemId]` 개체 전용 화면 부활** — 티켓 20260816_007이 "단일 소스 유지"를
  위해 없앤 것을 되돌리는 셈이라 미채택. 화면 2벌 관리 부담이 그대로 재발한다.
- **(B) `/badges/[id]?item={inventory_item_id}`** — **채택.** 20260816_007의 단일 소스 원칙을
  깨지 않으면서 개체 식별만 얹는다. 기존 `?u=` 쿼리와 일관되고, `?item` 없는 진입은 기존
  동작을 그대로 유지하므로 회귀 표면이 좁다. redirect도 유지되어 뒤로가기 히스토리 동작이 보존된다.
- **(C) `/inventory/[itemId]`를 실화면으로 두고 컴포넌트만 공유** — (A)보다 낫지만 라우트가
  둘로 갈려 "어느 URL이 정본인가"가 다시 모호해진다. 미채택.

**「후보 1개면 시트를 열지 않는다」를 코드 분기로 둔 이유**

시트를 항상 열고 후보가 1개면 자동 선택하는 방식이 코드는 짧지만, 고를 것이 없는데 시트가
한 번 뜨는 것은 불필요한 단계다. 티켓 요구사항대로 호출부에서 갈랐다.

**정렬 방향을 맞추는 대신 개체 id를 넘기는 방식으로 고친 이유**

파생 정합성 오류(자동 장착 `obtained_at ASC` vs 상세 대표 `DESC`)는 양쪽 정렬을 같은 방향으로
맞춰도 겉으로는 해결된 것처럼 보인다. 하지만 그건 "우연히 같은 개체를 고르게" 만드는 것일 뿐,
**개체 선택을 사용자가 하게 되는 순간(요구사항 B) 다시 깨진다.** 실제로 꽂힌 id를 링크에
싣는 방식만이 선택 기능과 함께 성립한다.

**릴을 컴포넌트 안에서 끄지 않고 `animate` prop으로 뺀 이유 (2차)**

호출부가 셋뿐이라 "작은 height면 릴을 끈다" 같은 자동 규칙을 컴포넌트에 넣을 수도 있었지만,
릴을 켤지 말지는 **크기가 아니라 화면의 목적**이 정한다 — 드랍 시트는 같은 height 40에서
"번호가 지금 확정되는 순간"이라 릴이 있어야 하고, 선택 시트는 "이미 가진 번호를 비교하는
순간"이라 없어야 한다. 크기로 유추하면 이 둘이 갈리지 않는다. 그래서 호출부가 명시하는
prop으로 뺐다. 전역 모션 토큰(`--reel-*`)은 다른 화면의 릴에도 영향을 주므로 손대지 않았다.

**선택 시트만 1열로 간 이유 (2차)**

3열에서는 판독 가능한 크기(height 20 이상)의 `ItemSerialCode`가 물리적으로 안 들어간다
(360px 뷰포트 클리핑 경계 100px vs h20 106px, 실측). 2열이면 h20이 들어가지만, h20은
자간 보정 캘리브레이션 하한(h40) 아래라 여전히 보정이 클램프된다. 1열이면 h40이 여유 있게
들어가 캘리브레이션 범위 안에서 렌더된다. 선택 시트는 후보가 보통 2~3개라 1열의 세로
스크롤 비용이 작고, 인벤토리 목록·드랍 시트의 3열은 건드리지 않으므로 영향 범위도 좁다.

### 잔여 이슈

- **실화면 확인이 남아 있다.** 컴포넌트 단위 실렌더(360/430 뷰포트)로 1열 + `height=40`
  판독성·선택 톤·만료 칩까지는 확인했으나, 실제 컬렉션 화면에서 시트 진입 애니메이션·스크롤·
  `slotMode` 스크롤 위치와의 상호작용은 아직 눈으로 보지 않았다. staging 병합 후 확인할 것:
  (a) 컬렉션에서 장착 개체를 눌렀을 때 히어로 일련번호 = 슬롯에 꽂힌 개체
  (b) 이력 첫 카드 획득일 = 히어로 개체 (재현 케이스: "아지랑이의 환영" `MMBT829356`)
  (c) 선택 시트와 `slotMode`의 `scrollIntoView`가 겹칠 때 시트 위치·실패 배너 가시성

- **1열 카드 형태는 재검토 여지가 있다.** 328px 폭 카드에 90px 썸네일만 가운데 놓이는 세로
  스택이라 여백이 크다. 게이트 재리뷰는 `ListRowCard`(썸네일 좌 + 이름·일련번호 우)가 문법상
  자연스럽다고 봤다. 다만 이 티켓이 `InventoryGrid mode="select"` + `BadgeGridCard`를
  "이대로 따를 것"으로 못박은 결정이라, 뒤집으려면 실화면을 본 뒤 별도로 판단한다.

- **눈에 보이는 부수 변화 1건**: 후보 조회에 `destroyed_at IS NULL`이 추가되면서, 보유 개체가
  전부 파괴된 배지는 컬렉션 칸이 「추가」 버튼 대신 미보유(`???`)로 표시된다. 종전에는 버튼이
  보였다가 누르면 RPC가 `item_not_found`로 거절했으므로 **동작 개선**이지만, 회귀 목록에
  없던 변화라 기록해 둔다.

### 파생 티켓 (범위 밖 발견물 — 자동 생성)

- [20260907_2138 (P3, API)](20260907_2138_API_아이템북GET응답-단일개체-후보배열-불일치.md)
  — `api/itembooks/[id]/route.ts`가 후보 배열이 아닌 단일 개체를 반환해 페이지 로직과 어긋남
  (현재 소비자 0건이라 사용자 영향 없음)
- [20260907_2138 (P2, UI)](20260907_2138_UI_BadgeGridCard-썸네일고정폭-카드클리핑.md)
  — 썸네일 90px 고정이라 3열에서 좌우가 잘림. 이번 건과 무관한 기존 동작이지만, 이 카드 안의
  폭 예산을 추정으로 잡으면 틀린 값이 나오는 원인이기도 하다(1차의 `height=14` 오산정)
- [20260907_2138 (P3, Content)](20260907_2138_Content_UX용어표-장착-일련번호-미등재.md)
  — UX Writing 고정 용어 표에 「장착」·「일련번호」 미등재
