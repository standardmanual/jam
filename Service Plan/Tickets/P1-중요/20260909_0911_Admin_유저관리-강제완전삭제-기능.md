---
id: 20260909_0911
category: Admin
priority: P1
status: CLOSED
created: 2026-09-09
closed: 2026-09-09
---

# [Admin] 유저 관리 강제 완전 삭제 기능

## 배경 / 문제 정의
지금까지 특정 유저를 완전히 삭제해야 할 때(예: `jadeny23.jy@gmail.com` 삭제 사례) 오케스트레이터가
Supabase MCP로 SQL을 직접 실행했다. 반복적으로 필요한 작업인데 어드민 화면에 기능이 없어 매번
수동 SQL 조회·삭제를 거쳐야 한다.

기존 `/admin/users` 화면에는 [UsersTable.tsx](../../../jam-web/src/app/admin/users/UsersTable.tsx)의
`ResetUserButton`(유저 콘텐츠만 초기화, 계정은 유지)이 있지만, **계정 자체를 완전히 삭제**하는
기능은 없다. `UsersTable.tsx` 주석의 "일괄 삭제/정지는 정책상 민감해 범위 밖"이라는 언급은
DataTable 전환 티켓(20260826_015)의 스코프 언급일 뿐, 개별 유저 완전 삭제 자체를 금지한 결정은
아니다.

## 상세 요구사항

### 서비스/코드베이스 관점
- `/admin/users` 목록의 액션 컬럼에 "완전 삭제" 버튼 추가 (기존 `ResetUserButton`과 나란히,
  시각적으로 명확히 구분되는 더 강한 위험 표시 — 예: 더 진한 빨강·아이콘)
- 신규 API 라우트 `DELETE /api/admin/users/[id]` (또는 `POST /api/admin/users/[id]/delete`,
  `getAdminUser()` 가드는 기존 라우트들과 동일 패턴 사용)
  - `public.users` 삭제 → FK `ON DELETE CASCADE`로 관련 데이터(strava_connections,
    user_activity_badges, inventory, inventory_items, trades 등) 자동 정리
  - **`auth.users`도 함께 삭제** (Supabase Admin API `supabase.auth.admin.deleteUser(userId)`
    사용 — `execute_sql`로 직접 `DELETE FROM auth.users`한 것과 동일 효과를 서비스 코드 안에서
    안전하게 재현. `createServiceClient()`가 service_role 키를 쓰므로 Admin API 호출 가능한지
    확인할 것)
  - 삭제 대상 유저가 없으면 404, 성공 시 삭제된 이메일/주요 카운트를 응답에 포함(로그용)
  - `console.info`로 관리자 이메일과 대상 유저 정보 감사 로그 남기기 (기존 reset 라우트 패턴과 동일)
- 어드민 본인 계정 삭제 방지 로직은 불필요(유저 삭제는 항상 "다른 유저" 대상 — 필요 시
  `getAdminUser()`로 조회한 admin.id와 대상 id가 같으면 400으로 막는 방어 코드 정도는 추가해도 됨)

### UI/UX 관점 (필수)
- 삭제 버튼 클릭 시 확인 다이얼로그(기존 `ResetUserButton`의 모달 패턴 재사용) 표시
- 다이얼로그 문구는 "초기화"보다 훨씬 강하게: 이 작업이 계정 자체와 모든 데이터를
  **영구적으로, 되돌릴 수 없이** 삭제한다는 점을 명시
- **"삭제합니다"라는 텍스트를 정확히 입력해야만 삭제 버튼이 활성화**되는 텍스트 확인 입력란 추가
  - 입력값이 정확히 "삭제합니다"와 일치하지 않으면 삭제 버튼은 disabled 상태 유지
  - 입력란 위/아래에 "삭제하려면 아래에 '삭제합니다'를 입력하세요"라는 안내 문구를 명확히 표시
  - 대상 유저를 식별할 수 있도록 이름/이메일을 다이얼로그 본문에 노출
- 삭제 완료 후에는 결과 메시지를 보여주고 목록에서 사라지도록 `router.refresh()` 호출
  (기존 `ResetUserButton` 패턴 참고)

## 구현 계획
- `jam-web/src/app/admin/users/DeleteUserButton.tsx` 신규 생성 (ResetUserButton.tsx 구조 참고,
  텍스트 확인 입력 로직 추가)
- `jam-web/src/app/api/admin/users/[id]/route.ts`에 `DELETE` 핸들러 추가 (기존 파일에 이미
  `GET`/다른 메서드가 있는지 확인 후 병합 또는 신규 라우트 분리 — jam-developer가 기존
  `route.ts` 구조를 먼저 확인하고 판단)
- `UsersTable.tsx`의 액션 컬럼에 `DeleteUserButton` 추가
- Supabase Admin API(`auth.admin.deleteUser`)가 `createServiceClient()`로 정상 동작하는지
  로컬에서 실측 확인 (DEV_PROCESS_GUARDRAILS.md 패턴 13 — `ADMIN_EMAILS` 로컬 환경변수로 검증)

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
- `/admin/users` 액션 컬럼에 `ResetUserButton`과 나란히 `DeleteUserButton`("완전 삭제") 추가.
  진한 빨강(`border-red-700`/`bg-red-700`) 테두리·버튼으로 초기화 버튼과 시각적으로 구분했다.
- 삭제 확인 다이얼로그는 "삭제합니다"를 정확히 입력해야만 삭제 버튼이 활성화되도록 구현했다
  (입력값이 상수 `CONFIRM_TEXT`와 정확히 일치할 때만 `disabled` 해제). 입력란 위/아래에 안내
  문구를 표시하고, 대상 유저의 이름·이메일을 본문에 노출한다.
- `DELETE /api/admin/users/[id]` 라우트 신규 추가. `getAdminUser()` 가드 후, 어드민 본인 계정
  삭제는 400으로 방어한다. 대상 유저가 없으면 404.
- 삭제는 `supabase.auth.admin.deleteUser(userId)` 한 번만 호출한다. `public.users.id`가
  `auth.users(id)`를 `ON DELETE CASCADE`로 참조하므로(001_initial_schema), 이 한 번의 호출로
  `auth.users` → `public.users` → 그 아래 모든 참조 테이블(strava_connections, inventory,
  inventory_items, user_activity_badges 등)이 한 트랜잭션으로 연쇄 삭제된다. "public.users를
  먼저 지우고 auth.users를 뒤이어 지우는" 2단계 방식은 그 사이 상태가 갈라지는 창을 만들 수
  있어 채택하지 않았다(티켓 원안의 "public.users 삭제 → 자동 정리" 서술은 이 연쇄 삭제 메커니즘
  설명으로 해석해 단일 호출로 구현).
- 응답에는 삭제 전 집계한 `deletedEmail`·`deletedActivityBadges`·`deletedInventoryItems`를
  포함하고, `console.info`로 관리자 이메일·대상 정보 감사 로그를 남긴다(기존 reset 라우트 패턴).
- `UsersTable.tsx` 상단 주석을 갱신 — "일괄 삭제는 범위 밖"이라는 기존 서술은 유지하되, 개별
  유저 완전 삭제는 이번 티켓으로 추가됐다는 점을 명확히 했다.

### 변경된 파일
```
jam-web/src/app/api/admin/users/[id]/route.ts (신규)
jam-web/src/app/admin/users/DeleteUserButton.tsx (신규)
jam-web/src/app/admin/users/UsersTable.tsx
```

### 테스트 결과
- [x] `npx tsc --noEmit` 통과 (에러 0건)
- [x] `npm run lint` 전체 실행 — 0 에러 / 13 경고 (모두 `design-system/**`의 기존 경고, 변경
  파일과 무관)
- [x] 로컬 `next dev`(`ADMIN_EMAILS=dev-tester@jam.local`) + `/api/dev-login` 실렌더 확인:
  `/admin/users` 페이지에 "완전 삭제" 버튼이 각 행에 렌더됨을 HTML로 확인
- [x] 임시 테스트 계정(Admin API로 생성, `handle_new_user` 트리거로 `public.users` 자동
  생성분)을 대상으로 실제 `DELETE /api/admin/users/[id]` 호출 → 200 응답, 삭제 후 서비스
  클라이언트로 `auth.users`/`public.users` 양쪽 모두 사라졌음을 직접 조회해 확인(연쇄 삭제 실측)
- [x] 가드 케이스 실측: 존재하지 않는 id → 404, 어드민 본인 id → 400, 미인증 요청 → 미들웨어가
  `/login`으로 리다이렉트(기존 어드민 라우트 공통 동작)

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

- [x] 용어 일관성: 어드민 화면 전용 문구이며(가이드라인상 어드민 저작 화면은 사용자 노출 원칙
  적용 대상 아님), 기존 `ResetUserButton`의 어휘·구조(초기화 확인/취소, 결과 메시지)를 그대로
  따랐다
- [x] 톤앤매너: 삭제·소각 등 되돌릴 수 없는 행동에 맞춰 냉정하고 명확한 톤 유지("영구적으로
  실행되며... 절대 되돌릴 수 없습니다")
- [x] 에러 메시지: 어드민 내부 도구 특성상 기존 reset/admin-role 라우트와 동일하게 간결한
  현상 중심 메시지 사용(3단계 CTA 구조는 소비자 화면 전용, 어드민은 적용 대상 아님)
- [x] 문장 규칙: 해요체 아님(어드민 화면은 기존에도 "-습니다" 평서체 사용), fluent-korean
  출력 지침에 따라 조사·어미 생략 없이 완결된 문장으로 작성
- [x] 표기 규칙: 해당 없음(날짜/금액/기간 표기 없음)

### 배포 정보
- 배포일: 2026-09-09
- 환경: production
- 커밋: d51d9f66 (staging 머지), main 승격은 `/jam-ship`으로 진행

### 주요 의사결정 / 핵심 메모
- 티켓 원안은 "`public.users` 삭제 → cascade" + "`auth.users`도 함께 삭제(Admin API)"를
  구분된 두 동작처럼 서술했지만, 실제 FK 방향(`public.users.id → auth.users.id ON DELETE
  CASCADE`)을 확인한 결과 `auth.admin.deleteUser()` 단일 호출로 두 단계가 한 트랜잭션에서
  전부 처리된다. 두 단계를 명시적으로 나눠 호출하면 중간에 실패할 경우 "auth는 남고 public만
  지워진 상태"가 생길 수 있어, 더 안전한 단일 호출 방식을 택했다(medium 판단 개입 지점).
- 어드민 본인 계정 삭제 방지는 티켓이 "불필요"라고 했지만 "추가해도 됨"이라는 선택지를 줬기에,
  방어 코드 한 줄(400)로 추가했다 — 범위 확장이 아니라 티켓이 명시적으로 허용한 옵션 선택.

### 잔여 이슈
-
