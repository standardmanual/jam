---
id: 20260910_1129
category: Service
priority: P1
status: OPEN
created: 2026-09-10
closed:
---

# [Service] 서비스 용어 일괄 변경 — 세계관 → 트라이브, faction → tribe

## 배경 / 문제 정의

티켓 `20260909_2119`(온보딩 재설계)에서 유저 대면 명칭을 **"트라이브"**로 확정하고
온보딩 화면에만 먼저 적용했다. 그 결과 지금은 **온보딩만 새 용어이고 나머지 전부가
옛 용어인 불일치 상태**다:

- 어드민 화면은 "세계관 관리", "소속 세계관", "최소 세계관 다양성"으로 노출된다
- 서비스 화면·주석·타입·변수는 전부 `faction` 계열 식별자를 쓴다
- `Specs/`·`Business/`의 현재 상태 문서도 "세계관"으로 서술되어 있다

운영자와 개발자가 같은 개념을 두 이름으로 부르게 되어, 문서를 검색할 때도 두 용어를
모두 시도해야 하는 상태다. 용어를 하나로 수렴시킨다.

## 상세 요구사항

### 핵심 제약 — DB는 건드리지 않는다

Supabase는 staging·프로덕션 **공용 단일 DB**다. 컬럼·테이블을 리네임하면 아직 배포되지
않은 프로덕션 코드가 즉시 깨지므로, 이번 티켓에서 **DB 스키마는 전혀 바꾸지 않는다.**

따라서 코드에는 두 이름이 의도적으로 공존한다:

```ts
const [tribeId, setTribeId] = useState(book?.faction_id ?? '')  // 우변은 DB 컬럼
...
faction_id: tribeId || null,                                     // 키는 DB 컬럼, 값은 코드 변수
```

**절대 바꾸면 안 되는 것** (바꾸면 런타임에 DB와 어긋나 즉시 장애):

| 대상 | 건수 | 비고 |
|---|---|---|
| `faction_id` 컬럼명 | 125건 | 쿼리 문자열·객체 키·Row 타입 필드 전부 |
| `'factions'` 테이블명 문자열 | 62건 | `.from('factions')` 등 |
| `tier{1,2,3}_min_factions` 컬럼 | 24건 | 조합 정책 싱글톤 |
| `faction_adjacency` 테이블 | — | 인접 관계 |
| `faction_background_cascade` RPC | — | 배경 일괄 적용 함수 |
| `src/types/database.generated.ts` | 전체 | Supabase 자동 생성 파일, 손대지 않음 |
| `supabase/migrations/*.sql` | 220건 | 실행 완료된 기록, 소급 수정 금지 |
| `supabase/seed_*.sql` | — | 실행 완료된 1회성 데이터, 파일명 포함 유지 |

### 변경 대상

**1. 소스코드 식별자 (`jam-web/src/`)** — DB에 묶이지 않은 것만

- 변수·함수: `factionId` → `tribeId`, `factionMap` → `tribeMap`, `pickFaction` → `pickTribe`,
  `loadFactions` → `loadTribes` 등
- 타입: `FactionRow` → `TribeRow`, `FactionAdjacencyRow` → `TribeAdjacencyRow`,
  `ItemBookWithFaction` → `ItemBookWithTribe`
- 컴포넌트: `FactionForm` → `TribeForm`, `FactionsTable` → `TribesTable`
- **주의**: `FactionRow`의 **필드명**(`faction_id` 등)은 DB 컬럼이므로 유지한다.
  바뀌는 건 타입의 **이름**뿐이다.

**2. 라우트·디렉터리**

- `src/app/admin/factions/` → `src/app/admin/tribes/`
- `src/app/api/admin/factions/` → `src/app/api/admin/tribes/`
- `components/admin/adminNavItems.ts`의 경로·라벨 동시 수정

**3. 한글 UI 문구·주석 (138건)**

"세계관" → "트라이브". 어드민 화면 라벨·placeholder·다이얼로그 문구·에러 메시지 포함.
서비스 화면은 이미 트라이브로 노출되므로 잔여 지점만 정리한다.

**4. MODULAR 디자인 시스템**

`design-system/components/cards/ShapeTag.{jsx,d.ts,stories.tsx}`의 `faction` prop →
`tribe`, `FACTION_COLORS` → `TRIBE_COLORS`. `design-system/readme.md`의 "세계관" 표기.

**모듈러-서비스 연결 범위 판정** (`ds-connection-scope.md` 기준): `ShapeTag`는 **미도입 8종**에
해당한다. `grep -rn "ShapeTag" jam-web/src` 결과가 0건으로, 서비스에 대응 개념이 없는
스토리북 전용 컴포넌트다. 따라서 prop 이름을 바꿔도 **서비스 호출부 영향이 없고**,
`src/components/ui/`에 함께 고쳐야 할 병존 구현도 없다.

**UI 재사용 판정** (1.5단계): 이번 티켓은 순수 리네이밍으로 **신규 컴포넌트를 만들지
않는다.** 기존 컴포넌트의 이름과 prop만 바뀌므로 MODULAR 탐색·재사용 판정은 해당 없음.

**5. 문서 — 현재 상태 문서만**

- `Service Plan/Specs/` 45건 (11개 파일). `Specs/Content/FACTIONS.md` → `TRIBES.md`로 파일명 변경
- `Service Plan/Business/` 9건. `Business/04 JAM! 세계관.md` → `04 JAM! 트라이브.md`
- `jam-web/PRODUCT.md`, `jam-web/docs/storybook/*.md`
- `jam-web/scripts/recraft/*.js` 주석·변수

문서에서도 DB 식별자(`factions` 테이블, `faction_id` 컬럼)를 **지칭하는 문장은 사실이므로
그대로 둔다.** 예: `02_DATA_MODEL.md`의 컬럼 정의표.

### 변경 제외

- **완료된 과거 티켓 315건 (파일명 12개 포함)** — 티켓은 작업 이력이다. 당시 용어와 당시
  DB 상태를 기록한 문서이므로 소급 수정하지 않는다. 특히 `20260716_001_Infra_factions-
  item_books-배지-마이그레이션.md`처럼 실제 테이블명을 서술한 기록은 지금도 사실이다.
- `Service Plan/Archive/` — 읽기 전용 폐기 기록
- `Service Plan/Assets/` — 파이프라인 산출물, 문서 아님
- `jam-web/public/storybook/` — 빌드 산출물, 소스 변경 후 재빌드로 갱신

## 구현 계획

1. **DB 경계 먼저 고정** — 위 "절대 바꾸면 안 되는 것" 목록을 정규식으로 보호 대상으로
   삼고, 나머지만 치환한다. 단순 `sed 's/faction/tribe/g'`는 절대 쓰지 않는다.
2. 디렉터리 이동(`git mv`) → import 경로 수정 → 식별자 치환 → 한글 문구 치환 순으로 진행
3. `npx tsc --noEmit`으로 타입 검증, `npm run build`로 빌드 검증
4. 드랍엔진·조합 테스트(`src/lib/drop-engine/__tests__/`, `src/lib/combine/__tests__/`) 통과 확인
5. 문서는 코드 변경이 끝난 뒤 일괄 반영

### 검증 체크리스트 (게이트 리뷰 필수 항목)

- [ ] `grep -rn "faction_id" jam-web/src | grep -v database.generated` 건수가 **125건 그대로**인가
- [ ] `grep -rn "from('factions')\|'factions'" jam-web/src` 가 살아있는가
- [ ] `min_factions` 24건이 그대로인가
- [ ] `database.generated.ts`에 diff가 없는가
- [ ] `supabase/migrations/`·`seed_*.sql`에 diff가 없는가
- [ ] 완료된 과거 티켓에 diff가 없는가
- [ ] `npx tsc --noEmit` 통과
- [ ] `npm run build` 통과
- [ ] 어드민 `/admin/tribes` 진입 가능, 사이드바 활성 판정 정상

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

DB 식별자를 정규식으로 마스킹해 보호한 뒤에 나머지만 치환하는 스크립트를 작성해서 적용했다.
보호 대상은 `*faction_id*`·`*min_factions*`·`*faction_adjacency*`·`*faction_background_cascade*`·
`faction_name`·`faction_constant_missing`·`'factions'`·`"factions"`·`badges/factions`(Storage 경로)
9종이며, 무차별 치환은 사용하지 않았다.

1. 디렉터리·파일을 `git mv`로 옮겼다: `admin/factions/` → `admin/tribes/`,
   `api/admin/factions/` → `api/admin/tribes/`, `FactionForm.tsx` → `TribeForm.tsx`,
   `FactionsTable.tsx` → `TribesTable.tsx`.
2. `jam-web/src/` 전역에서 DB에 묶이지 않은 식별자를 `faction` → `tribe`로 치환했다.
   `src/types/database.ts`는 테이블 키(`factions`·`faction_adjacency`)가 살아 있어야 하므로
   자동 치환 대상에서 빼고 타입 이름(`FactionRow` → `TribeRow`,
   `FactionAdjacencyRow` → `TribeAdjacencyRow`)과 주석만 직접 고쳤다.
3. 한글 문구 "세계관"을 "트라이브"로 바꾸면서, 받침이 사라지며 어긋난 조사를 함께 고쳤다
   (`트라이브을` → `트라이브를`, `트라이브은` → `트라이브는`, `트라이브과` → `트라이브와`,
   `트라이브이 ` → `트라이브가 `, `트라이브으로` → `트라이브로`). 이 조사 오류 중 일부는
   티켓 20260909_2119에서 이미 유입돼 있던 기존 오류였고 이번에 함께 정리했다.
4. `design-system/components/cards/ShapeTag.{jsx,d.ts,stories.tsx}`의 `faction` prop을 `tribe`로,
   `FACTION_COLORS`를 `TRIBE_COLORS`로 바꾸고 `design-system/readme.md` 표기도 맞췄다.
5. 문서는 파일명 2건을 `git mv`로 바꾸고(`Content/FACTIONS.md` → `TRIBES.md`,
   `Business/04 JAM! 세계관.md` → `04 JAM! 트라이브.md`), 이 두 파일을 가리키던 링크·경로
   참조를 전부 갱신했다. 문서 안에서 DB 테이블·컬럼·마이그레이션 파일명을 지칭하는 문장은
   사실이므로 그대로 두었다.

### 변경된 파일
```
[디렉터리·파일 이동]
jam-web/src/app/admin/factions/           → jam-web/src/app/admin/tribes/ (6개 파일)
jam-web/src/app/api/admin/factions/       → jam-web/src/app/api/admin/tribes/ (4개 파일)
Service Plan/Specs/Content/FACTIONS.md    → Service Plan/Specs/Content/TRIBES.md
Service Plan/Business/04 JAM! 세계관.md   → Service Plan/Business/04 JAM! 트라이브.md

[소스코드] jam-web/src/ 내 68개 파일 (types/database.ts 포함)
[디자인시스템] ShapeTag.{jsx,d.ts,stories.tsx}, readme.md
[문서] PRODUCT.md, docs/storybook/{01,03}, scripts/recraft/register-style.js,
       Specs/{PRD/01,PRD/02,PRD/04,PRD/AdminUI/PHASES,PRD/AdminUI/REDESIGN,
       SERVICE_OPERATIONS,UX_WRITING_GUIDELINE,BadgeEngine/BADGE_ENGINE_UNIFIED,
       Content/TRIBES,Content/COMBINE_RECIPES,Content/v5_*},
       Business/{01 철학, 서비스플랜/02 서비스 플랜}, .claude/skills/jam-docs/SKILL.md
```

### 테스트 결과
- [x] `faction_id` 125건 유지 (`grep -rn faction_id src | grep -v database.generated`)
- [x] `min_factions` 24건 유지
- [x] `'factions'` 테이블명 문자열 24건 유지
- [x] `src/types/database.generated.ts`·`supabase/` diff 0건
- [x] `Service Plan/Tickets/` 기존 티켓 diff 0건
- [x] `npx tsc --noEmit` 통과
- [x] `npm run lint` 통과 (오류 0건, 경고 13건 — 전부 이번 변경과 무관한 기존 경고)
- [x] `npx vitest run` 드랍엔진·조합·배지엔진·엔진로그·알림 28개 파일 629개 테스트 전부 통과
- [x] `npm run build` 통과, 라우트가 `/admin/tribes`·`/api/admin/tribes`로 생성됨

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

- [x] 용어 일관성: 유저 대면·어드민 노출 명칭을 "트라이브" 하나로 통일했다
- [x] 톤앤매너: 기존 문장 구조를 그대로 두고 명칭만 교체해 톤은 변하지 않았다
- [x] 에러 메시지: 기존 구조를 유지했다
- [x] 문장 규칙: 명칭 교체로 어긋난 조사를 전부 교정했다
- [x] 표기 규칙: 해당 없음

### 배포 정보
- 배포일:
- 환경: production
- 커밋:

### 주요 의사결정 / 핵심 메모

- **`INVALID_FACTION` 에러 코드는 그대로 두었다.** 온보딩 API가 돌려주고 클라이언트가 비교하는
  문자열 상수인데, 티켓의 변경 대상 목록에 없고 배포가 갈리는 순간에 서버와 클라이언트가
  서로 다른 코드를 쓰게 될 여지가 있어 이번 범위에서 제외했다.
- **`faction_name`을 보호 대상에 추가했다.** 티켓 표에는 없지만 드랍엔진이
  `activity_feed`의 meta(jsonb)에 실제로 기록하는 키이고, 피드 화면이 이 키의 존재로
  "아이템 획득"과 "아이템 드랍" 문구를 갈라 쓴다. 이름을 바꾸면 이미 쌓인 데이터를 읽지 못한다.
- **`faction_constant_missing`도 보호했다.** `engine_logs`에 적재되는 이벤트 값이라 바꾸면
  과거 로그와 조회 조건이 어긋난다.
- **Storage 경로 `badges/factions/`를 보호했다.** 이미 그 경로에 올라간 배지 이미지가 있어서
  경로를 바꾸면 기존 이미지를 찾지 못한다. `TribeForm.tsx`의 업로드 `folder="factions"`도 같은 이유다.
- **`Specs/Content/v5_catalog_*`의 "세계관"은 라이팅 가이드 문맥이었지만 함께 바꿨다.**
  `UX_WRITING_GUIDELINE.md`가 같은 문맥을 이미 "트라이브 속 사건"으로 서술하고 있어 표기를
  맞췄다. `v5_catalog_verify.py`와 그 산출물인 `v5_catalog_verified.json`은 같은 문자열을
  공유하므로 양쪽을 동시에 고쳤다.

### 잔여 이슈
- `jam-web/public/storybook/`은 빌드 산출물이라 이번 변경에 포함하지 않았다. Storybook을
  다시 빌드할 때 반영된다.
- DB 컬럼·테이블명은 계획대로 `faction` 계열을 유지하므로, 코드에는 `tribeId`와 `faction_id`가
  의도적으로 공존한다.
