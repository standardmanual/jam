---
id: 20260912_1105
category: Admin
priority: P1
status: OPEN
created: 2026-09-12
closed:
---

# [Admin] 게이트미션 화면 JAM! 카테고리 라벨 구분 테스트 실패

## 배경 / 문제 정의
티켓 `20260912_1053`(체크인 배지 지도 마커 토글) 게이트 리뷰 중 발견된 범위 밖 결함이다.
`src/app/admin/gate-missions/__tests__/gate-family-options-contract.test.ts`의 테스트
3개가 이번 변경과 무관하게 `origin/staging` 기준으로 이미 실패 중이다. `git stash`로
해당 티켓의 변경분을 걷어내고 재현해도 동일하게 실패함을 확인했다 — 회귀가 아니라
기존부터 깨져 있던 결함이다.

실패 원인은 `page.tsx`에서 `adminCategory: f.adminCategory` 패턴을 찾지 못하는 것으로,
티켓 `20260910_2055`(BadgeEngine, JAM! 카테고리용 `admin_category` 컬럼 신설, AC5:
게이트미션 화면의 카테고리 라벨 구분)가 구현한 기능이 실제로는 깨져 있을 가능성이 있다.

## 상세 요구사항

### 서비스/코드베이스 관점
- `gate-family-options-contract.test.ts` 3개 테스트 실패 원인을 조사
- 티켓 `20260910_2055`의 AC5(게이트미션 화면 JAM! 카테고리 라벨 구분)가 실제 화면에서도
  깨져 있는지 확인 — 테스트만 실패하는 것인지, 실제 기능도 미동작인지 구분 필요
- 원인에 따라 `page.tsx` 수정 또는 테스트 갱신

## 구현 계획
> 조사 후 원인에 따라 결정

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

### 변경된 파일
```
-
```

### 테스트 결과
- [ ]

### 주요 의사결정 / 핵심 메모

### 잔여 이슈
-
