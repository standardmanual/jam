---
id: 20260906_1436
category: UI
priority: P2
status: CLOSED
created: 2026-09-06
closed: 2026-09-06
---

# [UI] 배지 트리 화면 — 전체 통계 막대 제거·레일 등급 표시 조정·진행 수치 색상 변경

> 선행: `20260906_1323`(배지 트리 화면 정리 9건 — 통계 2행 3열 배치·레일 `not-reached` 조건값
> 표시를 이번 티켓과 같은 화면에 먼저 적용했다)
> 대상 화면: `/badges/tree` 하나. 다른 화면은 건드리지 않는다.

## 배경 / 문제 정의

`20260906_1323` 반영 후 화면을 다시 확인한 사용자가 지적한 3건이다. 전부 **표시 층의
문제**이고 발급 로직·진행 계산에는 손대지 않는다.

1. **전체 통계 칸의 막대가 불필요하다.** `20260906_1323` §6에서 "막대·라벨·값의 세로 위치가
   세 칸에서 정확히 맞는다"는 근거로 전체(`전체 0/145`) 칸에도 등급 칸과 같은 6px 진행 막대를
   넣기로 했는데, 사용자가 다시 보니 이 칸은 막대 없이 레이블+값만 있는 편이 더 조화롭다는
   판단이다. **이번 티켓은 그 결정을 되돌린다.**
2. **레일(`BadgeStageRail`) 표시가 세 군데 어색하다.**
   - 「자세히」로 펼친 목록의 상태 줄이 `not-reached`일 때 여전히 `'—'`만 보여준다.
     `20260906_1323` §7이 접힌 레일 눈금의 캡션은 조건값으로 바꿨지만, 펼친 목록 쪽은
     범위를 "레일 눈금 캡션 한 곳"으로 못박아 손대지 않았다 — 그 티켓 완료 기록의
     잔여 이슈에 정확히 이 항목이 남아 있다.
   - 펼친 목록에서 배지 이미지 아래 등급색 3px 바가 뜨는데, 같은 줄 오른쪽에 이미
     `RarityBadge` 칩이 등급을 텍스트로 보여주고 있어 중복이다.
   - 반대로 펼치기 전(접힌) 레일에서는 등급을 3px 색 바 하나로만 표시한다 — 등급명이
     `aria-label`에만 있고 화면에는 안 보인다. 이 자리를 칩으로 바꿔 화면에서도 등급이
     바로 읽히게 한다.
   - 눈금 사이 연결선("레일바")이 간격 중앙이 아니라 한쪽으로 치우쳐 보인다.
3. **진행 중인 수치값의 색이 눈에 거슬린다.** 지금 계산 중인(=아직 조건을 못 채운) 배지의
   진행 수치를 옐로우(`--status-short-solid`, `#f2cb00`)로 표시하는데, 이를 화이트 계열로
   바꾼다.

## 상세 요구사항

### 1. 전체 통계 칸 — 막대 제거, 레이블·값 재배치

**대상**: `design-system/components/patterns/BadgeTreeSummaryHeader.jsx`

- `cells` 중 `key: 'total'`(`primary: true`) 칸만 6px 진행 막대(74~84행의
  `<div style={{height:6, ...}}>` 블록)를 렌더하지 않는다. 등급 칸(Mystic·Epic·Rare·Common)의
  막대는 그대로 둔다 — 이번 요청은 "전체" 칸 하나에 한정된다.
- 막대가 빠진 자리에서 레이블(`전체`)과 값(`{earned} / {total}`)이 **조화롭게** 배치되도록
  정렬·여백을 다시 잡는다. 지금 레이블은 `marginTop:8`(막대 기준), 값은 `marginTop:4`인데
  막대가 없으면 이 간격이 어색할 수 있다 — 등급 칸과 세로 정렬(baseline)이 흔들리지 않는
  범위에서 값을 조정한다.
- `20260906_1323` §6의 "요약 헤더 첫 칸에도 6px 막대를 둔다" 결정을 **이 칸에 한해 되돌린다**는
  점을 구현 요약에 명시할 것 (문서상 결정 번복 이력을 남긴다).
- 스토리(`BadgeTreeSummaryHeader.stories.tsx`) 갱신 — 전체 칸에 막대가 없는 상태를 반영.

### 2. 레일 영역 — `BadgeStageRail.jsx`

#### 2-1. 펼친 목록에도 `not-reached` 조건값 표시

- 582행 `{STATUS_LABEL[stop.status]}` — 펼친 목록(`expanded`, 552~599행)의 상태 줄이 지금은
  `stop.status`만 보고 라벨을 고른다. 접힌 레일(492~533행)이 이미 하는 것과 같은 규칙을
  적용한다: `stop.status === 'not-reached' && stop.conditionText != null`이면 상태 라벨
  `'—'` 대신 `stop.conditionText`를 보여준다. 조건값이 없으면 기존 `'—'` 그대로.
- `stopAriaLabel`과 같은 방식으로 접근성 텍스트도 갱신이 필요한지 확인할 것 — 지금 펼친
  목록의 상태 `<p>`는 시각 텍스트 자체가 스크린리더에도 그대로 읽히므로, 텍스트만 바꾸면
  자동으로 해결될 가능성이 높다(접힌 레일처럼 aria 전용 라벨을 따로 만든 구조가 아니다).

#### 2-2. 펼친 목록 — 배지 이미지 아래 등급 바 제거

- `StopThumbnail`(144~198행)의 `barColor` 렌더(190~195행)를 **펼친 목록 호출부(560행)에서만**
  끈다. 접힌 레일 호출부(491행)는 2-3에서 다시 다루므로 당장은 손대지 않아도 되지만, 결과적으로
  2-3이 접힌 쪽 바도 없애므로 최종적으로는 `StopThumbnail`이 바를 그리는 경우 자체가 사라질
  수 있다 — 구현 시 2-2·2-3을 함께 보고 `StopThumbnail`에 바 렌더 로직을 통째로 남길지
  제거할지 판단할 것(둘 다 안 쓰면 죽은 코드가 된다).
- 펼친 목록은 이미 `RarityBadge` 칩(563행)으로 등급을 보여주므로 바가 없어도 정보 손실이
  없다.

#### 2-3. 접힌 레일 — 등급 바 제거하고 등급칩 표시

- 접힌 레일 눈금(371~538행, `StopThumbnail` 호출 491행)의 3px 등급 바를 없애고, 대신
  **등급칩**을 표시한다. 칩은 44px 썸네일 폭 안에 들어가야 하므로 펼친 목록의 `RarityBadge`
  (텍스트 포함, 폭 가변)를 그대로 쓸 수 없다 — 압축된 형태(색 배경 + 짧은 라벨 또는 색만
  다른 소형 칩)가 필요할 가능성이 높다. 정확한 시각 형태(크기·라벨 유무·배치 위치 — 썸네일
  아래인지 옆인지)는 구현 시 `RarityBadge.jsx`의 기존 변형을 먼저 찾아보고, 없으면 신규 소형
  변형이 필요한지 `/jam-design`으로 재사용 여부부터 판단할 것.
- `rarity`가 없는 눈금(레벨형은 애초에 이 컴포넌트를 안 쓰므로 실질적으로 발생하지 않지만)
  방어적으로 `barColor` 조건과 동일하게 `rarity`가 있을 때만 칩을 그린다.
- 등급칩 도입 후 `stopAriaLabel`의 등급 정보(현재 `rarityLabel`이 `stopName`에 포함되어
  aria로는 이미 전달됨, 63~70행 `STATUS_ARIA_LABEL`)는 중복 변경 없이 그대로 둘 것.

#### 2-4. 눈금 사이 연결선("레일바") 간격 중앙 정렬

- 421~483행의 연결선 `<span>`(`marginTop: 19`, `flex: isGateBefore ? '0 0 44px' : '1 1 14px'`)이
  화면에서 한쪽으로 치우쳐 보인다는 지적이다. 다만 정확한 원인(수직 위치 기준점 문제인지,
  좌우 여백 배분 문제인지)은 이번 조사에서 코드만으로 단정하기 어렵다 — 2-3의 등급 바 제거로
  눈금 하단 구성(썸네일+바 유무)이 바뀌면 연결선의 적정 `marginTop`도 같이 달라질 수 있으므로,
  **2-3을 먼저 반영한 뒤 실제 화면(스크린샷)으로 치우침을 재확인하고 고칠 것.**
- 확인 시 참고: `marginTop: 19`는 44px 썸네일의 세로 중앙(22px) 근처를 겨냥한 값으로 보이며,
  좌우 폭은 `flex: '1 1 14px'`로 눈금 사이 남는 공간을 균등 배분한다 — 게이트 자리
  (`isGateBefore`, `flex: '0 0 44px'`)가 있는 구간만 폭이 고정이라 그 앞뒤 일반 연결선과
  시각적으로 안 맞을 가능성을 우선 의심할 것.

### 3. 진행 중인 배지 수치값 — 옐로우 → 화이트

**대상**: `BadgeStageRail.jsx`(502~508행 `captionColor`), `BadgeLevelGauge.jsx`(57행
`valueColor`) — 둘 다 "조건을 아직 못 채운(진행 중)" 상태의 수치 텍스트에
`var(--status-short-solid)`(옐로우 `#f2cb00`)를 쓴다.

- ⚠️ **토큰 값 자체(`--status-short-solid`)를 바꾸지 않는다.** 이 토큰은
  `design-system/tokens/colors.css`에 "배지 트리 상태 채널" 공용 시맨틱으로 선언돼 있고
  `ProgressBar.jsx`(DS 공용)·`DualAxisGauge.jsx`·`src/components/badges/BadgeTrophyGridCard.tsx`
  (`/badges` 등 다른 화면)에서도 참조한다. 토큰을 바꾸면 배지 트리 밖 화면(`/badges` 트로피
  그리드 등)의 진행 막대·게이지 색까지 함께 바뀐다 — 이번 요청은 "배지 트리 화면"으로
  범위가 못박혀 있으므로, **두 컴포넌트의 해당 텍스트 색만** 화이트 계열
  (`--color-text` 등, 정확한 토큰은 구현 시 확정)로 개별 override한다.
- 진행 막대·연결선의 **채움색**(예: `BadgeStageRail`의 `connectorBackground` 그라데이션,
  411~419행)까지 바꾸라는 요청인지, 텍스트(수치) 한정인지 사용자 표현("수치값")상으로는
  텍스트 한정으로 읽힌다 — **막대·연결선 채움색은 이번 범위에서 유지**하고 캡션·값 텍스트
  색만 바꾼다. 구현 착수 시 화면으로 재확인할 것.
- `progressComplete`(획득 조건은 채웠지만 게이트만 남은 상태, 501행)의 라임 색은 이번 변경
  대상이 아니다 — "진행 중"(조건 미충족) 상태 하나만 바꾼다.

## 구현 계획

> 개략적 순서 — 착수 시 최신 화면 스크린샷으로 2-4·3의 세부를 재확인한 뒤 확정할 것.

1. `BadgeTreeSummaryHeader.jsx` — 1번(전체 칸 막대 제거·재배치) + 스토리 갱신.
2. `BadgeStageRail.jsx` — 2-1(펼친 목록 조건값) → 2-2/2-3(등급 바→칩, 두 호출부 함께 정리)
   → 2-4(연결선 위치, 2-3 반영 후 재확인) → 3(캡션 색) 순으로 처리. 같은 파일을 여러 항목이
   건드리므로 항목 간 되돌아가지 않도록 이 순서를 지킬 것.
3. `BadgeLevelGauge.jsx` — 3번(값 색)만 해당.
4. 등급칩 형태가 신규 UI면 `/jam-design`으로 재사용 판정 먼저.
5. 스토리 갱신: `BadgeStageRail.stories.tsx`(등급칩·펼친 목록 조건값 케이스),
   `BadgeLevelGauge.stories.tsx`(진행 중 값 색).
6. 검증: `npx tsc --noEmit` · `npx eslint`(변경 파일) · `npm run ds:check`.

### 하지 않는 것

- 발급 엔진·`computeBadgeProgress`·진행 계산 로직 변경
- `--status-short-solid` 등 공용 시맨틱 토큰 값 변경 (다른 화면에 영향)
- `RarityBadge.jsx`의 기존 변형(펼친 목록용) 변경 — 신규 소형 변형이 필요하면 별도 컴포넌트로
- 진행 막대·연결선 채움색 변경 (수치 텍스트 색만)
- `/badges`·`/badges/[id]` 등 다른 화면 변경
- `20260906_1323`에서 다룬 나머지 8건 재작업

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

1. **`BadgeTreeSummaryHeader.jsx`** — 전체(`primary`) 칸만 6px 막대를 렌더하지 않도록
   되돌렸다(`20260906_1323` §6 결정 번복). 막대가 빠진 만큼 레이블 `marginTop`을
   `8→14`(막대 6px + 여백 8px 자리)로 올려 등급 칸과 레이블의 세로 위치는 그대로 맞춘다.
2. **`BadgeStageRail.jsx`**
   - §2-1: 펼친 목록의 상태 줄이 `not-reached`이고 `conditionText`가 있으면 「—」 대신
     조건값을 보여준다(접힌 레일과 동일 규칙, `expandedStatusText`).
   - §2-2/§2-3: `StopThumbnail`의 등급색 3px 바(`RARITY_BAR_COLOR`)를 완전히 제거하고
     `showRarityChip` prop을 추가했다. 접힌 레일 호출부만 `showRarityChip`을 켜 등급을
     `RarityBadge` 압축 칩으로 보여주고(`common`은 기존 관례대로 칩 없음), 펼친 목록은
     이미 옆에 칩이 있어 끈다(기본값 `false`).
   - §2-4: 게이트 연결선의 `flex`를 `0 0 44px`(고정)에서 `1 1 44px`(grow:1 유지)로
     바꿨다. 실측 결과 원인은 정확히 이거였다 — 게이트만 grow:0이라 남는 공간이 일반
     연결선에만 쏠려 레일 전체가 한쪽(뒤쪽)으로 치우쳐 보였다. grow를 모두 1로 맞추자
     시각적으로 균등해졌다(아래 검증 방법 참고).
   - §3: 진행 중(조건 미충족) 캡션 색을 `--status-short-solid`(옐로우)에서 `--color-text`
     (화이트)로 바꿨다. 채움색(연결선 그라데이션, 415행)은 그대로 옐로우 — 텍스트만 변경.
3. **`BadgeLevelGauge.jsx`** — 진행 중(미완료) 현재값 색을 같은 규칙으로
   `--status-short-solid` → `--color-text`로 바꿨다. `ProgressBar` 채움색은 불변.
4. 세 컴포넌트의 `.prompt.md`(MODULAR 사용 문서)도 3px 바·전체 칸 막대 등 사라진 설명을
   갱신해 실제 동작과 어긋나지 않게 했다(요청 범위 밖이지만 같은 파일군의 최신화라 함께
   반영 — 스펙 추가가 아니라 기존 문서의 정확도 유지).
5. **UI 재사용 판정**: 오케스트레이터 지시대로 `RarityBadge.jsx`를 그대로 재사용했다.
   44px 폭보다 넓은 칩(`MYSTIC` 등)은 신규 변형 없이 방치했고, 실렌더 확인 결과 좌우
   연결선 여백이 자연스럽게 흡수해 위화감이 없었다 — 소형 변형 신설은 불필요했다.

### 추가 수정 — 인터랙션 리뷰 ① 탭 타깃 높이 뒤틀림 (사용자 승인, 이 티켓에 포함)

게이트 리뷰 PASS 후 인터랙션 리뷰가 지적한 문제: `RarityBadge`는 `rarity==='common'`일 때
자체적으로 `null`을 반환한다(노이즈 축소 관례). §2-3에서 `showRarityChip && rarity &&
<RarityBadge rarity={rarity}/>`로 구현했더니, 칩이 안 그려지는 Common 눈금만 그 자리
자체(칩 높이 16px + `gap:4`)가 생기지 않아 44px, 나머지 등급은 60px가 됐다. 같은
`role="group"` 행이 `alignItems:'flex-start'`라 Common 캡션만 다른 3개보다 위에서
시작해 — 이번 티켓이 원래 고치려던 "치우침"과 같은 종류의 새 문제였다.

**수정**: `StopThumbnail`의 등급칩 자리를 `showRarityChip`이 켜져 있으면 항상 예약하도록
바꿨다 — `RarityBadge`가 렌더할 내용이 없어도(`common`) 높이 16px(`RARITY_CHIP_SLOT_HEIGHT`,
패딩 4px×2 + 8px 텍스트) 자리는 그대로 차지한다. 칩이 화면에 안 보이는 것(common 정책)과
레이아웃 높이가 흔들리는 것을 분리했다.

- 변경: `BadgeStageRail.jsx`(`RARITY_CHIP_SLOT_HEIGHT` 상수 추가, `StopThumbnail` 등급칩
  래퍼를 `minHeight` 고정 `<span>`으로 교체)
- 회귀 스토리 추가: `BadgeStageRail.stories.tsx`의 `RarityChipSlotAligned` —
  common·rare·epic·mystic 4눈금의 `.ds-rail-stop` 첫 자식(썸네일 블록) `offsetHeight`가
  전부 같은 값인지 단언
- esbuild+Playwright 실렌더 재확인 — Common(획득)·Rare(획득)·Epic(—)·Mystic(—) 4개
  캡션이 같은 y좌표에서 정렬됨을 스크린샷으로 확인

### 검증 방법 (스크린샷 실렌더)

구글 로그인 없이 실제 `/badges/tree` 화면을 보지 않고, esbuild로 세 컴포넌트만 번들해
`design-system/styles.css`(다크 토큰)를 그대로 링크한 정적 HTML을 만들고 Playwright로
스크린샷을 찍어 확인했다(로컬 `next dev`/`next dev --webpack` 모두 이 워크트리 경로에서
node_modules 심링크·경로의 `!` 문자 때문에 기동 실패 — 별도 `alerts` 참고). 이 방법으로
레일 치우침의 실제 원인(게이트 연결선 grow:0)을 픽셀 단위로 확인하고 수정 전/후를
비교했다. 임시 산출물은 스크래치패드에만 두고 저장소에는 남기지 않았다.

### 변경된 파일
```
jam-web/design-system/components/patterns/BadgeTreeSummaryHeader.jsx
jam-web/design-system/components/patterns/BadgeTreeSummaryHeader.stories.tsx
jam-web/design-system/components/patterns/BadgeTreeSummaryHeader.prompt.md
jam-web/design-system/components/patterns/BadgeStageRail.jsx
jam-web/design-system/components/patterns/BadgeStageRail.stories.tsx
jam-web/design-system/components/patterns/BadgeStageRail.prompt.md
jam-web/design-system/components/patterns/BadgeLevelGauge.jsx
jam-web/design-system/components/patterns/BadgeLevelGauge.stories.tsx
jam-web/design-system/components/patterns/BadgeLevelGauge.prompt.md
```

### 테스트 결과
- [x] `npx tsc --noEmit` — 오류 없음
- [x] `npx eslint`(변경 파일 9개) — 오류·경고 없음
- [x] `npm run lint`(전체) — 0 errors, 13 warnings (전부 변경 파일과 무관한 기존 경고 —
      `IconButton.stories.tsx`·`BadgeFrame.jsx` 등)
- [x] `npm run ds:check` — 변경 컴포넌트 3종 관련 신규 경고 없음(기존 경고만 그대로)
- [x] `npx vitest run` — 60 files / 1066 tests 전부 통과 (스토리 `play` 함수는
      `vitest.config.ts`의 명시적 exclude로 실행 대상이 아니다 — 티켓 20260906_1348,
      한글 경로 인코딩 버그로 storybook 프로젝트 자체를 제거한 기존 결정. `npm run
      storybook`으로 육안 확인하는 용도로만 남는다)
- [x] esbuild+Playwright 실렌더 스크린샷으로 5개 시나리오(전체 통계·게이트 앞 레일·
      4눈금 진행중 레일·펼친 목록·레벨 게이지) 육안 확인 — 위 "검증 방법" 참고
- [x] (추가 수정 후 재검증) `npx tsc --noEmit`·`npx eslint`(변경 2파일) — 오류 없음
- [x] (추가 수정 후 재검증) `npm run lint`(전체) — 0 errors, 13 warnings(동일, 신규 없음)
- [x] (추가 수정 후 재검증) `npx vitest run` — 60 files / 1066 tests 전부 통과(회귀 없음)
- [x] (추가 수정 후 재검증) `npm run ds:check` — 오류 0·경고 30·참고 8 (수정 전과 동일,
      Tailwind 클래스를 건드리지 않아 변화 없음)
- [x] (추가 수정 후 재검증) esbuild+Playwright 실렌더 — Common·Rare·Epic·Mystic 4눈금의
      캡션이 같은 y좌표에서 정렬됨을 스크린샷으로 확인

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

신규 사용자 노출 텍스트 없음(색·레이아웃·표시 여부만 변경, 문구는 기존 그대로 재사용).

- [x] 용어 일관성: 신규 문구 없음
- [x] 톤앤매너: 해당 없음
- [x] 에러 메시지: 해당 없음
- [x] 문장 규칙: 해당 없음
- [x] 표기 규칙: 해당 없음

### 배포 정보
- 배포일: 2026-09-06
- 환경: staging
- 커밋: `cef815bc` (staging 머지 커밋, 브랜치 `claude/jamwork-20260906_1436-badge-tree-fix`)

### 주요 의사결정 / 핵심 메모

- **레일 치우침 원인을 실측으로 확정**: 티켓이 예상한 대로 "게이트 자리만 폭이 고정이라
  앞뒤 일반 연결선과 안 맞을 가능성"이 실제 원인이었다. esbuild+Playwright로 컴퓨티드
  스타일(`flexGrow`)까지 직접 찍어 확인했다 — 게이트가 `flex-grow:0`, 일반 연결선이
  `flex-grow:1`이라 남는 공간이 전부 일반 연결선에만 들어가 레일 뒤쪽이 늘어져 보였다.
  게이트도 `flex-grow:1`로 맞추되 최소 폭(자물쇠 2개 자리, 44px)은 `flex-basis`로 유지했다.
- **StopThumbnail의 등급 표시 방식**: 3px 바를 완전히 걷어내고 `showRarityChip` prop으로
  칩 렌더 여부를 호출부가 고르게 했다(펼친 목록은 이미 옆에 칩이 있어 끔). 바 렌더 로직
  자체를 컴포넌트에 남기지 않는 쪽을 택했다 — 티켓이 우려한 "둘 다 안 쓰면 죽은 코드"
  상황을 피했다.
- **`RarityBadge` 재사용 그대로 확정**: 오케스트레이터 판정대로 신규 소형 변형을 만들지
  않았다. `MYSTIC`처럼 44px보다 넓은 칩도 실렌더에서 위화감이 없어 별도 변형은 불필요
  하다고 판단했다.
- **전체 칸 막대 제거 시 레이블 위치**: `marginTop`을 8→14로 올리는 것으로 등급 칸과의
  세로 정렬을 그대로 유지했다(막대 6px + 기존 여백 8px 자리를 레이블 자체의 marginTop으로
  흡수). 별도 컨테이너 높이 보정 없이 값 하나만 바꿔 해결됐다.

### 잔여 이슈
- 없음 (§2-4 원인 규명까지 이번 티켓 범위에서 완료)
