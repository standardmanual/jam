---
id: 20260906_2332
category: UI
priority: P2
status: CLOSED
created: 2026-09-06
closed: 2026-09-08
---

# [UI] DS 글리프 사본 잔재와 prompt 문서의 폐지어를 정리한다

## 배경 / 문제 정의

티켓 20260906_2140이 `LockGlyph`·`CheckGlyph`·`StarGlyph`·`ChevronDownGlyph`를
`design-system/components/icons/BadgeStatusGlyphs.jsx` 단일 소스로 통합했다.
**한 벌이 남았다** — `BadgeStatusSection.jsx:33`이 아직 로컬 `ChevronDownGlyph`를 선언한다.
`BadgeStatusGlyphs`가 이미 같은 이름을 export하고 `BadgeFamilyCardHeader`는 거기서
import하므로, 이 파일만 갈라져 있다. 「한쪽만 고치면 갈라진다」는 통합의 이유가 그대로 남는다.

**별개로 `*.prompt.md`에 폐지어가 남아 있다.** 이 파일들은 claude.ai/design 재생성 입력이라
**여기 남은 폐지어가 다음 생성물로 되돌아온다.**

| 파일 | 잔존 |
|---|---|
| `BadgeStageRail.prompt.md` (24-26·37행) | 「충족」 5회 |
| `BadgeProgressRingCard.prompt.md:13` | ariaLabel 예시가 **「미도달」** — 현재 코드(「아직」)와 이미 어긋남 |
| `UnlockConditionSheetContent.prompt.md:19` · `.d.ts:13,51` | 「충족」·「잠금 해제 조건」 |

폐지어 기준은 `UX_WRITING_GUIDELINE.md` §1.3 (2026-09-06 갱신, 커밋 `00ead78f`).

## 상세 요구사항

### 서비스/코드베이스 관점
- `BadgeStatusSection.jsx:33`의 로컬 `ChevronDownGlyph`를 지우고 `BadgeStatusGlyphs`에서
  import한다. **호출부의 `size`가 명시돼 있는지 확인** — 기본값이 다르면 픽셀이 바뀐다
- 위 표의 `*.prompt.md`·`*.d.ts` 폐지어를 현행 문구로 교체한다
  (「충족」→「다 채웠어요」 계열, 「미도달」→「아직」, 「잠금 해제 조건」→「받는 방법」)
- 다른 `*.prompt.md`에도 같은 폐지어가 있는지 전수 grep

### UI/UX 관점
- 렌더 결과가 한 픽셀도 바뀌지 않아야 한다 (글리프 통합은 형태 동일)

## 구현 계획

글리프 통합 1건 + 문서 문구 교체. Story 동반 필요(`BadgeStatusSection`).

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
1. **글리프 통합** — `BadgeStatusSection.jsx:33`의 로컬 `ChevronDownGlyph`를 지우고
   `BadgeStatusGlyphs.jsx`에서 import하도록 교체. 두 구현의 SVG(viewBox·path·fill·
   aria-hidden)가 완전히 동일함을 먼저 대조 확인했고, 호출부(161행)가 이미 `size={20}`을
   명시해 기본값 차이 위험도 없었다.
2. **`*.prompt.md`·`*.d.ts` 폐지어 정리** — 티켓이 지목한 3개 파일(`BadgeStageRail`·
   `BadgeProgressRingCard`·`UnlockConditionSheetContent`) 외에 전수 grep으로
   `DualAxisGauge.prompt.md`(티켓 미지목)와 `BadgeStageRail.d.ts`·
   `BadgeProgressRingCard.d.ts`·`UnlockConditionSheetContent.d.ts`(티켓이 prompt.md만
   지목, `.d.ts`는 놓침)에서도 같은 폐지어를 추가로 발견해 함께 정리했다. 「충족」은
   문맥에 따라 「채움」·「채웠는지」로, 「미충족」은 「아직」으로, 「미도달」은 실제 코드
   (`badgeProgressText.ts:254`의 `not-reached` 라벨 `'아직'`)와 맞춰 「아직」으로,
   「잠금 해제 조건/시트」는 가이드가 정한 「받는 방법」으로 교체했다.

### 변경된 파일
```
jam-web/design-system/components/patterns/BadgeStatusSection.jsx
jam-web/design-system/components/patterns/BadgeStageRail.prompt.md
jam-web/design-system/components/patterns/BadgeStageRail.d.ts
jam-web/design-system/components/patterns/BadgeProgressRingCard.prompt.md
jam-web/design-system/components/patterns/BadgeProgressRingCard.d.ts
jam-web/design-system/components/patterns/DualAxisGauge.prompt.md
jam-web/design-system/components/patterns/UnlockConditionSheetContent.prompt.md
jam-web/design-system/components/patterns/UnlockConditionSheetContent.d.ts
```

### 테스트 결과
- [x] `BadgeStatusSection` 렌더 픽셀 무변경 — 로컬 Storybook(`npm run storybook`)에서
      스토리 렌더 확인, 두 chevron(펼침/접힘) 모두 정상 표시. SVG 원본 대조로 이미
      동일함을 사전 확인했으므로 픽셀 변화 없음이 이중으로 보장됨
- [x] `*.prompt.md`·`*.d.ts` 전수 grep(`충족|미도달|잠금 해제`)에서 0건 확인
- [x] `npx tsc --noEmit` — 오류 0건
- [x] `npm run lint`(전체) — 0 errors, 13 warnings(기존 `design-system/**` 경고와
      동일 개수·동일 파일, 무관)
- [x] Storybook 콘솔 확인 — `PREVIEW_API` 에러 1건이 있었으나, 이번 변경과 무관한
      스토리(`BadgeStageRail`)에서도 동일하게 나타나 사전부터 있던 스토리북 자체
      이슈임을 확인(회귀 아님)

### UX Writing 검증
- [x] 폐지어 「충족」·「미도달」·「잠금 해제」 제거 — 전수 grep 0건으로 확인
- [x] `BadgeProgressRingCard.prompt.md`의 ariaLabel 예시가 실제 코드
      (`badgeProgressText.ts:254`)와 어긋나 있던 것도 「아직」으로 맞춰 바로잡음

### 배포 정보
- 배포일: 2026-09-08
- 환경: staging
- 커밋: (아래 커밋 참조)

### 주요 의사결정 / 핵심 메모
**티켓이 놓친 지점까지 전수 검토** — 티켓 본문은 3개 `*.prompt.md` 파일만 지목했지만,
"claude.ai/design 재생성 입력"이라는 범위 정의(prompt.md + 대응 `.d.ts`)를 기준으로
`design-system` 전체를 grep해, `.d.ts` 3곳과 `DualAxisGauge.prompt.md`까지 추가로
찾아 함께 정리했다. 반대로 `.jsx` 코드 주석(예: `BadgeStatusGlyphs.jsx:31`의 "충족된
축")은 claude.ai/design 재생성 입력이 아니고 사용자에게도 노출되지 않아 이번 범위에서
제외했다 — 이 판단은 티켓의 "재생성 입력이라 되돌아온다"는 문제 정의 자체에서 따른 것이다.

**부수 발견 (범위 밖)** — 이번에 손댄 5개 DS 패턴 컴포넌트(`BadgeStatusSection`·
`BadgeStageRail`·`DualAxisGauge`·`UnlockConditionSheetContent`·`BadgeProgressRingCard`)를
서비스가 실제로 `@ds/components/patterns/*`로 직접 import해 쓰고 있음을 확인했다
(`BadgeTreeClient.tsx`·`BadgeFamilyRow.tsx`·`BadgeUnlockSheet.tsx`·
`BadgeProgressGridCell.tsx`). `.claude/skills/jam-work/SKILL.md` 1.6절의 "연결된
컴포넌트 9종" 목록에는 이 5개가 빠져 있어 문서가 낡아 있다 — 별도 문서 정리 티켓으로
남긴다(아래 참고).

### 잔여 이슈
- `jam-work` SKILL.md 1.6절 "연결된 컴포넌트" 목록 갱신 필요 — 티켓
  `20260908_0920`으로 별도 생성함(P3, 문서 정리, 코드 변경 없음)
