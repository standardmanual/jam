---
id: 20260907_1243
category: Infra
priority: P2
status: OPEN
created: 2026-09-07
closed:
---

# [Infra] POI 카테고리 체계 재정리 + 기존 POI 재분류

## 배경 / 문제 정의
현재 `poi_categories`는 14개 카테고리로 구성되고 키워드는 `text[]`로, 카테고리 하나에 지역
범위(구+동 고정)가 일괄 적용된다. 이 구조로는 다음이 표현되지 않는다:

- **키워드마다 필요한 지역 단위가 다름** — 관공서 카테고리의 주민센터는 동 단위, 구청은 구
  단위, 시청은 시/도 단위로 검색해야 결과가 나오는데 지금은 전부 "구+동"으로 고정
- **"자동수집은 하되 지도에는 안 보이게"가 불가능** — `poi_categories`에 그런 플래그가 없고,
  수집 INSERT 로직([drops/route.ts:66-74](../../jam-web/src/lib/poi/naver.ts))에도 반영할 지점이 없음

실측(2026-09-07)으로 확인된 사실:
- 산(mountain) 자동수집은 32그리드 검색해 **0건** — 현재 산 847건은 전부 수동 등록(T1)이었고
  자동수집 파이프라인은 애초에 산출물이 없었다
- 기차/지하철(train_subway) 967건 중 자동수집 산출은 40건뿐 — 927건은 수동 등록
- 대중교통(transit, 버스정류장)은 그리드당 수확 0.09로 낮고, 지도 비노출 결정과 겹쳐 자동수집
  중단 + 기존 16건 삭제로 확정
- 관광명소(tourist_attraction) 기존 97건 중 새 키워드(박물관/미술관/전망대) 기준에 맞는 건 3건뿐,
  35건은 공원류, 59건은 판정 불가 — 카테고리 전환 시 재분류 필요성의 근거

이 논의의 전체 맥락(카테고리별 실측 수확률, 최종 확정 표 도출 과정)은 이 티켓을 만든 대화
세션에 있다 — 아래 "확정 카테고리 표"가 그 결론이다.

## 상세 요구사항

### 서비스/코드베이스 관점

**스키마 변경**
- `poi_categories.keywords`를 `text[]` → `jsonb`로 전환. 각 원소는
  `{"keyword": string, "scope": "dong" | "gu" | "sido"}` 형태
- `poi_categories.display_on_map`(boolean, default true) 컬럼 추가 — false면 자동수집은 계속하되
  지도에는 노출하지 않음
- `jam-web/src/lib/poi/reverse-geocode.ts`가 현재 `area2+area3`(구+동)만 합쳐 문자열 하나로
  반환([reverse-geocode.ts:86-88](../../jam-web/src/lib/poi/reverse-geocode.ts)) — `area1`(시/도)까지
  포함해 `{ sido, gu, dong }` 형태로 분리 반환하도록 변경. 호출부(`naver.ts`,
  `fetchNearbyNaverPoisForCategories`)가 키워드별 `scope`에 맞는 지역 단위를 조합하도록 수정
- `jam-web/src/lib/poi/categories.ts`의 `loadPipelineCategories` — jsonb 키워드 구조에 맞게
  파싱 로직 수정. **tier 컬럼은 스키마에서 제거하지 않고, 전체 `pipeline_linked=true` 카테고리를
  `tier=1`로 통일**해 티어 분기를 무력화한다(코드 로직은 `tier=1|2` 필터를 그대로 두되 데이터로만
  해소 — 42~50행이 `tier===1||tier===2` 필터라 이 변경으로 충분함)
- `jam-web/src/app/api/drops/route.ts`의 POI INSERT 페이로드([66-74행](../../jam-web/src/app/api/drops/route.ts))에
  `is_active: category.display_on_map` 반영 — 지금은 이 필드가 아예 없어 전부 활성으로 들어감
- `jam-web/src/lib/poi/radius-policy.ts` — stadium·school·park 반경 정책 등록 검토(현재 미등록
  카테고리는 기본 500m 적용, 배지 연동 전이면 당장 무해하나 향후 배지 연동 시 재검토 필요)

**카테고리 구조 변경**
- 신규: `stadium`(경기장), `school`(학교), `unassigned`(미정)
- 삭제: `transit`(대중교통/버스정류장) — 카테고리 자체와 기존 POI 16건 전부 삭제
- 삭제: `other`(기타) — 기존 3건은 `unassigned`로 이관 후 카테고리 삭제
- 통합: `bike_route`·`trail` → `route`(자전거길/트레일)로 통합 (기존 POI 0건이라 무손실)
- `mountain`·`train_subway`: `pipeline_linked=false`로 전환(자동수집 중단). 기존 POI는 활성
  유지, **재분류 대상에서 제외**(아래 재분류 작업은 이 두 카테고리를 건드리지 않음)

**확정 카테고리 표**

| slug | label | 키워드(지역단위) | 자동수집 | 지도노출 |
|---|---|---|---|---|
| government | 관공서 | 주민센터(동), 구청(구), 군청(구), 시청(시도) | O | 노출 |
| convenience | 편의점/마트 | 편의점(동), 마트(동), 시장(동) | O | 노출 |
| tourist_attraction | 관광명소 | 박물관(구), 미술관(구), 전망대(구) | O | 노출 |
| nature | 자연 | 국립공원(시도), 자연휴양림(시도), 해수욕장(시도), 수목원(시도) | O | 노출 |
| stadium | 경기장 | 경기장(구), 종합운동장(구) | O | 노출 |
| school | 학교 | 대학교(구) | O | 노출 |
| park | 공원 | 공원(동) | O | 비노출 |
| hospital | 병원 | 병원(동) | O | 비노출 |
| pharmacy | 약국 | 약국(동) | O | 비노출 |
| food | 음식점/카페 | 카페(동), 음식점(동) | O | 비노출 |
| mountain | 산 | 없음(수집중단) | X | 노출, 847건 유지 |
| train_subway | 기차/지하철 | 없음(수집중단) | X | 노출, 967건 유지 |
| route | 자전거길/트레일 | 없음 | X | 비노출, 수동등록 |
| unassigned | 미정 | 없음 | X | 비노출 |

> 관공서는 "구청/군청/시청 3개를 매 그리드마다 다 검색하면 2개는 항상 헛돈다"는 문제가 있음 —
> 역지오코딩 `area2` 접미사로 분기(`~구`→구청만, `~군`→군청만, `~시`→시청만) 시 그리드당 2콜
> 절약 가능. 구현 여유가 되면 함께 처리, 아니면 후속 최적화로 미룸

**재분류 대상 (599건)**
- 산·기차/지하철 제외한 기존 자동수집 POI 전체 (2026-09-07 기준 599건)
- 원본 검색 키워드가 DB에 저장돼 있지 않아 이름만으로는 판정 불가 — 이름+좌표로 네이버
  지역검색 API에 재조회해 원본 분류 문자열을 받아 새 카테고리 표에 매핑
  ([20260907_1242 티켓](20260907_1242_Infra_POI-네이버원본분류검증게이트및-어드민검토큐.md)의
  `naver_category` 저장 기능을 전제로 함 — 그 티켓을 먼저 완료)
- 애매한 것은 전부 `unassigned`로 배정
- 599콜은 네이버 일일 한도(25,000) 대비 여유 있음

### UI/UX 관점
- 어드민 `/admin/poi/categories` 화면이 jsonb 키워드 구조(키워드별 지역단위 드롭다운)를
  지원하도록 수정
- `display_on_map` 토글 UI 추가

## 구현 계획
1. **선행**: [20260907_1242 티켓](20260907_1242_Infra_POI-네이버원본분류검증게이트및-어드민검토큐.md)
   완료 (naver_category 저장)
2. 마이그레이션: `keywords` jsonb 전환, `display_on_map` 컬럼 추가, 카테고리 구조 변경(신규 3·
   삭제 2·통합 2), tier 전체 1로 통일
3. `reverse-geocode.ts` 반환 타입 변경 → 호출부(`naver.ts`) 지역단위 조합 로직 수정
4. `categories.ts` jsonb 파싱 반영
5. `drops/route.ts` INSERT에 `is_active` 반영
6. `transit` 카테고리 POI 16건 삭제, `other` 3건 `unassigned` 이관
7. 599건 재분류 스크립트 실행 (네이버 재조회 → 매핑 → 검토 필요분은 unassigned)
8. 어드민 `/admin/poi/categories` UI 갱신
9. `npm run db:types` 재생성

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
상세 요구사항대로 스키마 재정리(마이그레이션 144)·파이프라인 코드(reverse-geocode/naver/
categories/drops)·어드민 UI(CategoryManager)·599건 재분류 스크립트를 구현했다. 마이그레이션
144와 재분류 스크립트 둘 다 **작성만 하고 실행하지 않았다** — DB 변경은 사용자 승인 후
오케스트레이터가 처리(CLAUDE.md 규칙).

- 마이그레이션 144: `poi_categories.keywords` text[]→jsonb 전환(`{keyword, scope}[]`,
  `jsonb_typeof` CHECK 제약 추가) + `display_on_map`(boolean, default true) 컬럼 추가 +
  stadium/school/route 신규 + 확정 카테고리 표대로 키워드·tier=1·display_on_map 재정의 +
  mountain/train_subway pipeline_linked=false 전환(기존 POI 불변) + transit 카테고리·POI
  16건 완전 삭제 + other→unassigned 이관 후 삭제 + bike_route/trail→route 통합
- `reverse-geocode.ts`: `reverseGeocodeToRegionName`(문자열 반환)을 `reverseGeocodeToRegions`
  (`{sido, gu, dong}` 객체 반환)로 교체 — area1(시/도)까지 분리 반환
- `naver.ts`: 신규 `buildRegionPrefix(regions, scope)` — scope에 따라 sido→gu→dong 계층을
  누적 조합(예: scope='gu'면 "{sido} {gu}"까지만, 동은 뺀다). `fetchNearbyNaverPoisForCategories`가
  키워드별 scope로 개별 접두어를 만들도록 수정
- `categories.ts`: `PoiCategoryConfig`에 `displayOnMap` 추가, `loadPipelineCategories`가
  jsonb 키워드·`display_on_map`을 함께 로드(생성 타입 미반영으로 `unknown` 경유 캐스팅,
  기존 `requires_review`와 동일 패턴)
- `drops/route.ts`: POI INSERT에 `is_active: category.displayOnMap` 반영(이전에는 이 필드
  자체가 없어 전부 활성으로 저장됐음)
- `radius-policy.ts`: 삭제된 `transit` 항목 제거(더 이상 존재하지 않는 카테고리라 무의미).
  stadium/school/park 반경 정책 신규 등록은 보류 — 아직 배지 미연동이라 기본값(500m)으로
  충분하고, 배지 연동 시점에 재검토하도록 주석으로 남김("검토" 지시를 결정으로 처리)
- `category-gate.ts`: 삭제된 `transit` 패턴 제거
- 어드민 `/admin/poi/categories`: 신규 `KeywordListEditor.tsx`(키워드별 scope 드롭다운,
  shadcn Select 사용) — 생성 폼·행 인라인 수정 공유. `display_on_map` 토글 체크박스 추가
  (pipeline_linked 여부와 무관하게 항상 노출 — route/unassigned 같은 수동 전용 카테고리도
  지도 노출 여부를 이 값으로 결정하므로)
- `api/admin/poi-categories/route.ts`·`[slug]/route.ts`: keywords 검증을
  `{keyword, scope}[]` 형태로 갱신, `display_on_map` 필드 추가
- `types/database.ts`: `PoiKeywordScope`·`PoiCategoryKeyword` 신규, `PoiCategoryRow.keywords`
  타입 변경 + `display_on_map` 추가. 이 타입 변경의 파급으로 `admin/poi/{page,new/page,[id]/page,
  categories/page,review/page}.tsx` 5곳의 `(categoriesRaw ?? []) as PoiCategoryRow[]` 캐스팅이
  `as unknown as`로 바뀌어야 컴파일된다(키워드 타입이 string[]→객체배열로 바뀌어 단일 `as`의
  "충분한 겹침" 조건이 깨짐) — 전부 반영
- 599건 재분류: `lib/poi/reclassify.ts`(매핑 규칙 + 실행 로직) + 실행 스크립트
  `scripts/reclassify-poi-categories.ts`(기본 미리보기, `--apply`로만 실제 UPDATE, 다른
  1회성 스크립트와 동일한 dry-run 컨벤션). 이름으로 네이버 재검색 후 기존 좌표와 300m
  이내인 후보만 "같은 곳"으로 인정, 그 원본 분류를 순서 있는 규칙 목록(`RECLASSIFY_RULES`,
  구체적 패턴이 먼저)에 대조해 새 카테고리로 매핑. 매칭 실패·패턴 미해당은 전부
  unassigned로 배정(티켓 확정 사항)

### 변경된 파일
```
jam-web/supabase/migrations/144_poi_category_restructure.sql   (신규, 미실행)
jam-web/src/types/database.ts
jam-web/src/lib/poi/reverse-geocode.ts
jam-web/src/lib/poi/naver.ts
jam-web/src/lib/poi/categories.ts
jam-web/src/lib/poi/radius-policy.ts
jam-web/src/lib/poi/category-gate.ts
jam-web/src/lib/poi/reclassify.ts                               (신규)
jam-web/src/lib/poi/__tests__/naver.test.ts                     (신규)
jam-web/src/lib/poi/__tests__/reclassify.test.ts                (신규)
jam-web/src/app/api/drops/route.ts
jam-web/src/app/api/admin/poi-categories/route.ts
jam-web/src/app/api/admin/poi-categories/[slug]/route.ts
jam-web/src/app/admin/poi/CategoryManager.tsx
jam-web/src/app/admin/poi/KeywordListEditor.tsx                 (신규)
jam-web/src/app/admin/poi/page.tsx
jam-web/src/app/admin/poi/new/page.tsx
jam-web/src/app/admin/poi/[id]/page.tsx
jam-web/src/app/admin/poi/categories/page.tsx
jam-web/src/app/admin/poi/review/page.tsx
jam-web/src/app/(main)/badges/BadgesClient.tsx                  (주석 예시값만 수정)
jam-web/scripts/reclassify-poi-categories.ts                    (신규, 미실행)
```

### 테스트 결과
- [x] `lib/poi/naver.ts` 신규 유닛 테스트 6건(`buildRegionPrefix` — scope별 계층 조합,
  null 폴백, 부분 결측) 작성·통과
- [x] `lib/poi/reclassify.ts` 신규 유닛 테스트 9건(`matchNaverCategoryToTargetSlug` —
  구체 패턴 우선순위, 대학병원 예외 처리, 미매칭/빈 문자열 null) 작성·통과
- [x] `cd jam-web && npm run test` — 66 test files / 1158 tests 전부 통과(기존 1143 +
  신규 15건, 회귀 없음)
- [x] `cd jam-web && npm run lint` — 0 errors, 13 warnings(전부 `design-system/` 기존
  경고, 이번 변경과 무관 — 티켓 1242 완료 기록과 동일한 경고 목록)
- [x] `cd jam-web && npx tsc --noEmit` — 0 errors
- [ ] `npm run db:types` — CLI 부재로 실행 불가(마이그레이션 143 완료 기록과 동일한 사유).
  `database.generated.ts`는 여전히 `keywords: string[]`, `requires_review`/`display_on_map`
  미인식 상태 — 기존 확립된 패턴(narrow `unknown` 경유 캐스팅)으로 우회했고 `npx tsc --noEmit`
  전체 통과로 확인
- [ ] 실제 화면 확인 — 마이그레이션 미적용 상태라 `/admin/poi/categories` 실렌더 확인 불가
  (jsonb 컬럼·display_on_map이 실제 DB에 없어 조회가 실패함). 마이그레이션 적용 후
  프로덕션에서 직접 확인 필요(어드민은 스테이징 검증 불가 — MEMORY 참고)
- [ ] 재분류 스크립트(`scripts/reclassify-poi-categories.ts`) 실행 결과 — 티켓 지시대로
  실행하지 않았다. DB 조회 도구 자체가 이 역할에 없어(CLAUDE.md 규칙 3) 599건의 실제 이름/
  좌표에 접근할 수 없고, 네이버 API를 호출하는 dry-run조차 "실행"에 해당해 하지 않았다 —
  예상 영향 범위(카테고리별 배정 건수)를 실측치로 제시하지 못한다. 마이그레이션 144 적용
  후 `npx tsx scripts/reclassify-poi-categories.ts`(플래그 없음 = 미리보기)로 먼저 확인할 것

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

- [x] 용어 일관성: 어드민 내부 화면이라 고정 용어(획득·드랍·픽업·체크인·포인트) 대상 문구
  없음. "지도에 노출/비노출"은 이 티켓에서 새로 정의한 어드민 전용 운영 용어
- [x] 톤앤매너: 기존 POI 카테고리 관리 화면과 동일한 어드민 실무 도구 톤(간결·단정) 유지
- [x] 에러 메시지: 키워드 검증 실패 메시지("키워드는 { keyword, scope } 형태여야 하며…")는
  어드민 개발자/운영자 대상 API 오류라 기존 `poi-categories` API와 동일한 축약형 유지
- [x] 문장 규칙: 체크박스 라벨("지도에 노출")은 기존 화면의 다른 라벨("드랍/픽업 자동검색
  파이프라인에 연동" 등)과 동일하게 명사구·해요체 미적용 컨벤션 그대로 따름
- [x] 표기 규칙: 지역 단위 라벨(동/구/시·도)은 새 카테고리 표 원문 표기 그대로 사용

### 배포 정보
- 배포일: (미배포 — 마이그레이션 144 사용자 승인 대기 중)
- 환경: (해당 없음 — DB 변경 미실행, 코드는 review 브랜치 push까지만)
- 커밋: (아래 "push한 브랜치명" 참고)

### 주요 의사결정 / 핵심 메모
> 개발 과정에서 검토·결정된 사항, 선택하지 않은 대안과 그 이유.

- park을 비노출로 결정 — 사용자가 명시적으로 선택. 기존 관광명소 소속 공원류 35건이 함께
  비노출 전환되므로 도심 노출 POI가 크게 줄어드는 점은 인지된 트레이드오프
- 관광명소 키워드를 "관광명소"(포괄어)에서 "박물관/미술관/전망대"(구체 유형)로 좁힌 것도
  사용자 확정 — 기존 97건 중 3건만 부합하는 실측 결과를 알린 뒤에도 유지 결정
- nature·stadium·school 자동수집 지속 여부 — 이 시설들이 도심 500m 반경 안에 있을 가능성이
  낮아 자동수집 구조상 한계가 있다는 의견을 제시했으나, 사용자가 자동수집 유지로 확정.
  대신 오염 방지를 위해 [20260907_1242 티켓](20260907_1242_Infra_POI-네이버원본분류검증게이트및-어드민검토큐.md)의
  검증 게이트 도입이 세트로 결정됨
- tier 컬럼 자체를 스키마에서 제거하는 대신 전체 tier=1 통일로 처리 — 코드 변경 없이 티어
  분기를 무력화할 수 있어 채택. 컬럼 완전 제거는 후속 정리로 미룸
- **region prefix를 sido→gu→dong 누적 계층으로 설계**(구현 판단, 티켓 문서에 정확한 조합
  규칙까지 명시돼 있지 않았음): "주민센터=동, 구청=구, 시청=시/도"를 문자 그대로 "그 단위
  하나만" 쓰는 안도 검토했으나, 기존 코드가 이미 "구+동"을 함께 써서 동작하던 걸 감안해
  scope가 커질수록 하위 단위를 버리고 상위로 넓히는 누적 방식을 택함 — dong scope는 기존
  동작(구+동)에 시/도만 추가돼 회귀 위험이 낮고, gu/sido scope는 "구"명 전국 중복 문제
  (예: 중구가 여러 도시에 있음)를 시/도 접두어로 방지
- **599건 재분류 매핑 규칙(`RECLASSIFY_RULES`)은 티켓 문서에 없던 신규 설계**: 확정
  카테고리 표는 "수집에 쓸 키워드"만 정의했지 "네이버 원본 분류 문자열 → 새 카테고리"
  역매핑은 없었다. 확정 표의 키워드를 그대로 매칭 패턴으로 재사용하되, 순서(구체→일반)와
  "대학교 패턴이지만 대학병원은 hospital로" 같은 예외 1건을 추가로 판단했다 — 실측 데이터
  없이 작성한 초안이라 스크립트 완료 보고 문서화 참고
- **transit 카테고리 삭제 시 FK 위반 위험을 발견해 방어 코드 추가**(티켓 본문에 없던 내용):
  `user_activity_badges.triggered_by_poi_id`가 `ON DELETE NO ACTION`이라 이 필드가 삭제
  대상 POI를 가리키면 DELETE 자체가 실패한다(`lib/admin/poi-references.ts`, 2026-09-07
  실측 문서). 마이그레이션에 삭제 전 NULL 처리 스텝을 추가했다. `ambient_drop_config.
  category_slug`(싱글톤, ON DELETE SET NULL)도 삭제 시 조용히 "전체 카테고리 대상"으로
  동작이 넓어질 수 있어 실행 전 확인 쿼리를 마이그레이션 주석에 남겼다(alerts 참고)
- `badgeImageDesigns.ts`의 `poiCategories: ['train_subway', 'transit']`(배지 이미지 생성
  검색 필터 옵션)는 손대지 않음 — transit 삭제 후 그 필터 옵션이 0건을 반환할 뿐 에러는
  나지 않고, 이 파일은 카테고리 재정리 범위(poi_categories 파이프라인 체계)와 다른 배지
  이미지 생성 도구 설정이라 범위 밖으로 판단

### 잔여 이슈
- 마이그레이션 144 미실행 — 사용자 승인 후 오케스트레이터가 적용해야 이후 단계(재분류
  스크립트 실행, 어드민 화면 실확인)가 가능하다
- 599건 재분류 스크립트 미실행 — 마이그레이션 144 적용 후 `--apply` 없이(미리보기) 먼저
  실행해 카테고리별 배정 건수·unassigned 비율을 확인하고, 결과가 합리적이면 `--apply`로
  반영 권장
- `RECLASSIFY_RULES` 매핑 규칙은 실측 데이터 없이 작성한 초안 — 미리보기 실행 결과로
  unassigned 비율이 예상보다 높으면(예: 특정 원본 분류 패턴이 빠짐) 패턴 보정 필요
- `radius-policy.ts`에 stadium/school/park 반경 정책 미등록 — 이 세 카테고리에 체크인
  배지를 연동하는 시점에 반드시 재검토(현재는 배지 미연동이라 기본값 500m로 무해)
- 관공서 카테고리의 "구청/군청/시청 3중 검색 낭비" 최적화(area2 접미사로 ~구/~군/~시 분기)는
  티켓이 명시한 대로 후속 최적화로 미룸 — 이번 티켓에서 구현하지 않음
- `Service Plan/Specs/Content/POI.md`의 "[ ] 카테고리 체계 재정리·기존 POI 재분류 결과"
  체크리스트 항목은 이 티켓을 참조하도록 이미 표기돼 있음 — 문서 갱신은 오케스트레이터의
  승인 후 문서화 단계에서 처리(jam-developer 역할 범위 밖)
