---
id: 20260910_1719
category: BadgeEngine
priority: P2
status: CLOSED
created: 2026-09-10
closed: 2026-09-10
---

# [BadgeEngine] 사용량 배지(팔로워·팔로잉·일일동기화)에 반복형을 만들면 조용히 등급형처럼 오동작한다

## 배경 / 문제 정의

티켓 [20260910_1557](../P1-중요/20260910_1557_BadgeEngine_JAM카테고리-서비스사용량-배지엔진-신설.md)
(`/jam-work` engine 파이프라인)의 게이트 리뷰·개선 리뷰 양쪽에서 독립적으로 발견한 범위 밖 이슈.

신설된 `usageBadges.ts`는 등급형(rarity 있음)·레벨형(level 있음, family_key로 묶임) 두 종류만
지원한다 — 티켓 20260910_1557의 Acceptance Criteria 11이 이 둘만 명시했기 때문에 범위 판단
자체는 합리적이다. 문제는 **어드민 폼(`conditionFormFields.ts`)에 이 제약에 대한 안내가 전혀
없다**는 점이다.

어드민이 `follower_count`/`following_count`/`daily_sync_count` 조건에 `repeat_count`(반복
획득)까지 같이 넣어 반복형 배지를 만들면, 에러 없이 저장은 되지만 `isLeveledBadge()`가
`rarity == null` 여부로만 이진 판정하기 때문에 실제로는 등급형 경로(`gradedByName`)로 흘러가
"이름 그룹 내 최상위 tier 1개만 발급, `repeat_count` 조건은 무시, 재발급 없음"으로 조용히
오처리된다. 저장 시점 에러도, 발급 시점 로그도 없어 어드민이 원인을 알기 어렵다.

## 상세 요구사항

### 서비스/코드베이스 관점

두 가지 방향 중 하나(또는 둘 다)로 처리한다.

1. **저장 시점 가드**: 어드민 폼에서 `follower_count`/`following_count`/`daily_sync_count` 중
   하나와 `repeat_count`를 함께 저장하려 하면 명시적으로 막고, "이 지표는 등급형·레벨형만
   지원합니다"라는 안내를 보여준다 (`findBadgeConditionSaveError`,
   `src/lib/admin/badge-validation.ts` 확장 후보).
2. **문서·도움말**: 어드민 폼(`conditionFormFields.ts` 또는 `BadgeForm.tsx`)의 해당 조건 입력
   근처에 "반복형 미지원" 안내 문구를 추가한다.

우선순위는 1이 근본적이지만(조용한 오동작 자체를 막음), 비용이 크면 2만으로도 최소한 "왜
안 되는지"는 어드민이 알 수 있게 된다.

## 구현 계획
> 지금은 발견 기록 단계 — 실제 구현 착수 시 위 두 방향 중 범위를 확정한다.

1. `findBadgeConditionSaveError`에 "measurable/meta 사용량 키 + `repeat_count` 동시 존재
   금지" 가드 추가 검토
2. 어드민 폼 도움말 문구 추가
3. 회귀 테스트: 기존 `repeat_count`를 쓰는 다른 배지(활동 기반)에는 영향 없는지 확인

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

방향 1(저장 시점 가드)로 처리 — `findUsageMetricRepeatConflictError`(`badge-condition-guards.ts`
신설)가 사용량 지표 3종 중 하나와 `repeat_count`가 함께 있으면 저장을 막고 한국어 에러를
반환한다. `findConditionShapeSaveError` → `findBadgeConditionSaveError`
(`badge-validation.ts`) 체인에 연결되어, 어드민 POST/PUT 라우트와 `BadgeForm.tsx`의 저장 전
클라이언트 검증 양쪽에서 동일하게 걸린다.

방향 2(도움말)는 티켓이 후보로 지목한 `conditionFormFields.ts`/`BadgeForm.tsx`가 아니라
`conditionRegistry.ts`의 `repeat_count` 필드 `help`를 확장하는 방식으로 처리했다 — 이
코드베이스의 조건 폼 도움말 단일 출처가 그쪽이라(1557도 동일 판단), 값 파싱 전용인
`conditionFormFields.ts`에는 애초에 `help` 개념이 없다.

### 변경된 파일
```
jam-web/src/lib/admin/badge-condition-guards.ts       — findUsageMetricRepeatConflictError 신설
jam-web/src/lib/admin/badge-validation.ts              — 위 가드를 체인에 연결
jam-web/src/lib/badge-engine/conditionRegistry.ts      — repeat_count 필드 help에 안내 추가
jam-web/src/lib/admin/__tests__/badge-condition-guards.test.ts — 신규 가드 테스트
jam-web/src/lib/admin/__tests__/badge-validation.test.ts       — 체인 연결 테스트
Service Plan/Specs/BadgeEngine/CONDITION_JSON_SPEC.md  — §4 조합 제한 규칙 추가
Service Plan/Specs/BadgeEngine/BADGE_ENGINE_UNIFIED.md — 헤더 이력·§1 표 반영
```

### 테스트 결과
- [x] `npx vitest run` — 79 test files / 1304 tests 전부 통과(신규 테스트 포함)
- [x] `npx tsc --noEmit` — 오류 0건
- [x] `npm run lint` 전체 — 0 errors, 13 warnings(전부 `design-system/` 기존 파일, 무관)
- [x] `npm run build` — 프로덕션 빌드 성공
- [x] 회귀 확인 — 기존 `repeat_count` 기반 활동 배지, 사용량 지표 단독(등급형·레벨형 정상
      케이스) 모두 그대로 저장됨을 확인
- [x] 게이트 리뷰어가 프로덕션 DB를 서비스 롤 키로 직접 조회(activity 배지 633건 전수) —
      사용량 지표+`repeat_count` 조합을 가진 기존 배지 **0건**. 1557 배포~이 가드 배포 사이의
      짧은 창에서 실제 피해 데이터는 생기지 않았다

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
- [x] 어드민 저장 에러 메시지("등급형·레벨형만 지원하는 지표가 충족 횟수와 함께 있습니다 —
      {필드}. 충족 횟수 조건을 빼고 저장해주세요.")는 [현상→원인→해결책] 구조를 따르고,
      같은 파일의 기존 형제 가드 함수들과 톤·포맷이 일치한다. 어드민 저작 화면은
      UX_WRITING_GUIDELINE.md의 "충족" 용어 제한 예외 대상.

### 배포 정보
- 배포일: 2026-09-10
- 환경: staging → production (같은 날 `/jam-ship`으로 main 승격까지 진행)
- 커밋: `f4a55f46`(머지, `BADGE_ENGINE_UNIFIED.md` 헤더 충돌을 20260910_1719 섀도우밴 건과
  수동 병합) · DB 변경 없음

### 주요 의사결정 / 핵심 메모
- 안내 문구는 `repeat_count` 쪽에만 달았다 — 사용량 지표 3종 쪽에도 대칭으로 "반복 획득과
  함께 쓸 수 없어요"를 추가하면 양방향 발견성이 완전해지지만, 이건 1557이 만든 필드의 `help`
  텍스트를 건드리는 것이라 범위 밖으로 남겨뒀다(개선 리뷰 제안, 소규모 후속 필요 시 검토).

### 잔여 이슈
- (참고, 범위 밖으로 별도 티켓화됨) 개선 리뷰에서 발견한
  [20260910_1804 사용량 지표·measurable 필드 혼합 시 이중 발급 위험](../P2-일반/20260910_1804_BadgeEngine_사용량지표-measurable혼합-이중발급위험.md) —
  `repeat_count`가 아닌 다른 활동 필드(예: `distance_km`)와 섞였을 때는 이번 가드가 막지 않는다.
