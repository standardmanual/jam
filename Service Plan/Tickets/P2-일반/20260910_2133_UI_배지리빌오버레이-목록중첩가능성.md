---
id: 20260910_2133
category: UI
priority: P2
status: IN_PROGRESS
created: 2026-09-10
---

# [UI] 배지 리빌 오버레이 — 목록 페이지에서 여러 인스턴스 동시 노출 가능성

## 배경 / 문제 정의

티켓 [20260910_2056](20260910_2056_BadgeEngine_JAM카테고리-팔로우리빌애니메이션연동.md)
인터페이스 리뷰(MEDIUM)에서 발견된 범위 밖 이슈.

팔로워/팔로잉 목록 페이지(`[username]/followers`·`following`)는 행마다 독립된
`FollowButton` 인스턴스를 렌더하고, 각 인스턴스가 로컬 `revealOpen` state로
`BadgeRevealOverlay`를 직접 연다 — N행이면 이론상 N개의 오버레이가 동시에 존재할 수 있다.

`BadgeRevealCarousel`은 `document.body` 포털이 아니라 제자리에서
`position:fixed; zIndex:60`으로 그려진다(`design-system/components/patterns/
BadgeRevealCarousel.jsx:373-375`, `createPortal` 미사용). 프로젝트 z-index 문서
(`Service Plan/Specs/PRD/2026-08-15 DESIGN_RENEWAL_SPEC.md:325-328,385-387`)의
"동일 z에서는 DOM 순서가 승자를 정한다" 규칙에 따라, 목록에서 두 사람을 연달아 팔로우해
각각 배지를 얻으면 두 오버레이가 동시에 열릴 수 있고 위로 뜨는 쪽은 클릭 순서가 아니라
목록 배열 순서로 정해진다. **가려진 쪽은 화면에 안 보이는 채로 `aria-live` 공지와 배경
스크롤락만 남는다** — 사용자가 오버레이를 닫을 길이 없는 상태에 빠질 수 있다.

`SyncButton`(TopNav의 단일 전역 인스턴스)은 이 문제가 없었는데, 이번 티켓으로 처음으로
"여러 인스턴스가 동시에 뜰 수 있는" 위치에 `BadgeRevealOverlay`가 놓였다.

## 상세 요구사항

### 서비스/코드베이스 관점

**제안 변경**

리빌 상태를 개별 `FollowButton` 인스턴스가 아니라 목록 페이지 레벨로 끌어올린다 —
`SyncButton`이 `TopNav`에 단일 인스턴스로 두는 것과 같은 패턴. `FollowButton`은 획득
배지를 콜백(`onEarnBadges` 등)으로 상위(목록 페이지)에 전달하고, 오버레이는 페이지에
하나만 둔다.

### 구현 계획

1. `[username]/followers/page.tsx`·`following/page.tsx`(또는 공용 목록 컴포넌트)에
   `revealOpen`/`earnedBadges` state와 `BadgeRevealOverlay`를 페이지 레벨로 이동
2. `FollowButton`에 콜백 prop 추가, 내부 오버레이 렌더 제거
3. 연달아 팔로우해 여러 배지를 얻는 경우의 큐잉/병합 정책 결정(구현 단계 판단 — 순차 표시
   vs 합쳐서 한 번에 표시)

### Acceptance Criteria

1. 목록에서 두 사람을 연달아 팔로우해 각각 배지를 얻어도 오버레이가 한 번에 하나만 뜬다.
2. 오버레이를 닫으면 다음 대기 중인 배지(있다면)가 정상적으로 이어서 뜨거나, 합쳐서 한 화면에 표시된다(정책은 구현 단계에서 확정).
3. 기존 단일 팔로우 → 단일 리빌 흐름에는 회귀가 없다.

### Files Reference

| 파일 | 변경 |
|---|---|
| `jam-web/src/app/(main)/[username]/FollowButton.tsx` | 오버레이 렌더 제거, 콜백 prop 추가 |
| `jam-web/src/app/(main)/[username]/followers/page.tsx` | 리빌 state·오버레이를 페이지 레벨로 이동 |
| `jam-web/src/app/(main)/[username]/following/page.tsx` | 동일 |

### Out of Scope

- 프로필 화면 팔로우 버튼 2곳의 리빌 미연동 — 별도 티켓([20260910_2132](20260910_2132_UI_팔로우리빌-프로필화면2곳미연동.md))
- `BadgeRevealCarousel`을 `document.body` 포털로 바꾸는 근본 리팩터링(더 큰 범위 — z-index 문서의 "알려진 취약점" 전반에 영향, 이번 티켓은 목록 페이지 케이스만 다룬다)

## 구현 기록 (2026-09-14)

**변경 내용**

- `FollowButton.tsx`: 내부 `BadgeRevealOverlay`·`revealOpen`/`earnedBadges` state 제거. 획득
  배지가 있으면 신설한 `onEarnBadges(badges, moreCount)` 콜백을 호출하도록 변경(필수 prop —
  이 컴포넌트를 쓰는 곳은 팔로워·팔로잉 목록 2곳뿐이라 옵셔널로 남겨 둘 이유가 없었음).
- `FollowUserList.tsx`(신규, `[username]/` 공용): 팔로워·팔로잉 페이지가 각자 인라인으로 하던
  행 렌더링(아바타·이름·`FollowButton`)을 이 클라이언트 컴포넌트로 옮기고, 여기에 리빌 큐
  state와 `BadgeRevealOverlay` 단일 인스턴스를 둔다.
- `followers/page.tsx`·`following/page.tsx`: 행 매핑 JSX를 `<FollowUserList users=... currentUserId=... />` 호출로 교체(서버 컴포넌트라 상태를 가질 수 없어 클라이언트 컴포넌트로 위임).

**큐잉 정책: 순차 표시(FIFO)**

두 사람을 연달아 팔로우해 여러 이벤트가 쌓이면 배열에 순서대로 push하고, 오버레이를 닫을 때마다
큐 맨 앞을 pop해 다음 이벤트를 보여준다. 합쳐서 한 번에 보여주는 방식 대신 순차 표시를 택한
이유: 각 팔로우 응답의 `moreCount`(서버가 10장 상한으로 잘라낸 잔여 개수)는 그 팔로우 1건에
대한 값이라, 여러 이벤트를 합치면 "몇 개를 더 봐야 하는지"가 실제와 달라진다. 연달아 팔로우가
일반적인 사용 패턴이 아니라 큐가 길어질 가능성도 낮다고 판단했다.

**검증**

- `npm run lint` 전체 실행 — 0 errors, 14 warnings(모두 이번 변경과 무관한 기존 경고: Storybook
  redundant-name 3건, 미사용 변수 4건, `<img>` 사용 권고 4건, `no-unused-expressions` 2건,
  `recraft` 스크립트 1건).
- 코드 리뷰 기준 확인: 단일 팔로우 흐름은 기존과 동일하게 `onEarnBadges` → 큐에 1건 push →
  즉시 오버레이 표시로 동작해 회귀 없음.

**변경 파일**

- `jam-web/src/app/(main)/[username]/FollowButton.tsx`
- `jam-web/src/app/(main)/[username]/FollowUserList.tsx` (신규)
- `jam-web/src/app/(main)/[username]/followers/page.tsx`
- `jam-web/src/app/(main)/[username]/following/page.tsx`

DB 변경 없음.
