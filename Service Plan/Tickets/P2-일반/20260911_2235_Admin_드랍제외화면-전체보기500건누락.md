---
id: 20260911_2235
category: Admin
priority: P2
status: CLOSED
created: 2026-09-11
closed: 2026-09-14
---

# [Admin] 드랍 제외 관리 화면 — 필터 없이 전체 보기 시 500건 이후 배지 누락

## 배경 / 문제 정의
티켓 20260911_2220(드랍 정책 — 아이템배지 드랍 제외 기능)의 게이트 리뷰 중 발견된
범위 밖 이슈다. `/admin/drop-policy/exclusions` 화면의 `DropExclusionBadgesTable`에
넘기는 배지 조회(`page.tsx`)에 `.limit(500)`이 걸려 있다. 활성 아이템북 소속
아이템배지가 이미 900개에 근접하므로, 트라이브·컬렉션 필터를 걸지 않고 "전체 보기"
상태로 조회하면 이름순 500개를 넘는 배지는 **안내 문구 없이 조용히 목록에서 빠진다.**
운영자가 특정 배지를 전체 목록에서 찾다가 못 찾는 상황이 재현될 수 있다.

## 상세 요구사항

### 서비스/코드베이스 관점
- `jam-web/src/app/admin/drop-policy/exclusions/page.tsx`의 배지 조회 쿼리 검토
- 다음 중 하나로 해결:
  1. 페이지네이션 추가 (다른 어드민 목록 화면과 동일한 패턴이 있으면 재사용)
  2. 500건 초과 시 "일부만 표시 중, 필터를 사용하세요" 안내 문구 표시
  3. limit을 늘리고 검색/정렬 UI로 보완

## 구현 계획
- 담당자가 기존 어드민 테이블(예: `/admin/badges`)의 페이지네이션 패턴을 확인 후
  동일 패턴 적용을 우선 검토

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
`/admin/badges`의 페이지네이션 패턴(`Pagination` 컴포넌트 + `count: 'exact'` +
`.range()`)을 그대로 재사용해 아이템배지 조회에 500건 단위 페이지네이션을 적용했다.
`.limit(500)`을 `.range(from, from+499)`로 바꾸고 총 개수 표시("총 N개")와
`Pagination` 링크를 추가했다. 필터(트라이브·컬렉션)가 바뀌면 `page` 쿼리 파라미터를
초기화해 1페이지로 되돌아가도록 `ExclusionFilterBar`도 함께 수정했다.

### 변경된 파일
```
jam-web/src/app/admin/drop-policy/exclusions/page.tsx
jam-web/src/app/admin/drop-policy/exclusions/ExclusionFilterBar.tsx
```

### 테스트 결과
- [x] `npm run lint` 전체 실행 — 0 errors, 14 warnings(모두 변경 파일과 무관한 기존 경고)

### 배포 정보
- 배포일: 2026-09-14 (staging 머지)
- 환경: staging → production 예정 (`/jam-ship`으로 별도 승격)
- 커밋: `499b8153`(머지 시점)

### 잔여 이슈
- 게이트 리뷰(WARN)에서 발견: 실제 총 건수보다 큰 `page` 파라미터로 직접 접근하면
  `count`가 `null`이 되어 "총 0개"로 오표시되는 엣지 케이스가 있다(정상 클릭 동선에서는
  발생하지 않음). 별도 티켓([20260914_1145](../P3-낮음/20260914_1145_Admin_드랍제외화면-초과페이지-count-null-오표시.md))으로 분리했다.
