---
id: 20260912_1025
category: Content
priority: P2
status: CLOSED
created: 2026-09-12
closed: 2026-09-12
---

# [Content] 미슐랭 POI 체크인 배지 생성 — JAM MICHELIN 디자인 42개 일괄 생성

## 배경 / 문제 정의

[[20260912_0115]]에서 `poi_categories`에 `michelin` 카테고리를 신설하고 서울 미쉐린 가이드
레스토랑 42곳을 `poi` 테이블에 등록했다(전부 `is_active=false`·`pending_review=true`,
`linked_badge_id`는 아직 없음).

이번 티켓에서는 이 42곳을 **체크인 배지 타입(`type='checkin'`)으로 실제 배지화**한다.
기존 `mountain`(847개)·`train_subway`(929개)와 동일하게 **POI 1개 = 배지 1개** 1:1 생성
패턴을 따른다(`BADGE_ENGINE_UNIFIED.md` §3.14).

배지 이미지는 신규 피그마 템플릿("미쉐린 가이드" 문장을 두른 골드/레드 라벨 디자인, 하단에
Jam 로고)을 사용하며, 템플릿의 `POI`라는 이름의 텍스트 레이어에 각 레스토랑 이름을 주입해
생성한다.

- 디자인 출처: Figma `UXcBEgFagmO5ARwH5F0mMW` node-id `15-127`
  (텍스트 레이어 id `15:113`, 레이어명 `POI`, 예시 문구 `강민철 레스토랑`)
- 사용자가 텍스트 길이 확인을 완료함(42곳 상호명이 템플릿 폭 안에 들어감을 확인) — 다만
  실제 구현 시 최장 상호명 기준으로 렌더 결과를 한 번 더 육안 확인한다.

## 상세 요구사항

### 서비스/코드베이스 관점

1. **배지 레코드 생성**: `poi.category='michelin'` 42곳 각각에 대해 `badges` 테이블에
   `type='checkin'`, `rarity='common'` 배지를 1:1 생성하고 `poi.linked_badge_id`를 연결한다.
   - 배지 이름 = POI 이름 그대로
   - 설명은 기존 자동 생성 규칙(mountain/transit)과 톤을 맞춘 문구를 새로 정의한다
     (예: `"{POI명}에서 체크인했습니다"` 계열 — 정확한 문구는 `UX_WRITING_GUIDELINE.md`
     기존 체크인 문구 규칙을 따라 구현 단계에서 확정)
   - 아이콘은 신규 생성하는 배지 이미지 자체를 사용 (기존 `anyway_star.png` 공용 아이콘과
     달리, 이번 디자인은 레스토랑 이름이 라벨 안에 들어가는 형태라 배지별로 이미지가 다르다 —
     지하철역/산 배지와 동일한 방식)
2. **체크인 매칭 반경 등록**: `src/lib/poi/radius-policy.ts`의
   `EXACT_MATCH_RADIUS_BY_CATEGORY`에 `michelin` 항목이 없으면 기본값 500m가 적용된다.
   레스토랑은 도심 밀집 지역에 인접해 있는 경우가 많아 500m는 인근 다른 매장과 오탐 위험이
   있으므로, `mountain`(150m)·`train_subway`(50m) 사례를 참고해 적정 반경(권장: 50m,
   식당 단위 정밀도 기준)을 등록한다. §3.14 문서의 "카테고리를 표에 등록하지 않으면 기본값이
   적용되어 오탐이 재발한다"는 경고를 그대로 따른다.
3. **배지 이미지 생성**: `jam-web/scripts/badge-image-gen/configs/michelin-poi-badge.config.js`
   신규 작성.
   - Figma MCP(`get_design_context`, `get_metadata`)로 node `15:127` 배경 요소 좌표·텍스트
     노드(`15:113`) 사양(x/y/width/height/fontSize/color/align)을 정확히 추출해 배경 SVG를
     조립한다 (`backgrounds/michelin-poi-badge.svg`, 기존 `metro-poi-badge.svg` 방식 재사용)
   - dataSource는 `poi(category='michelin')` ↔ `badges(linked_badge_id)` 조인, 소프트삭제
     배지 제외 (기존 `metro-poi-badge.config.js` 패턴 참고)
   - 텍스트 렌더링은 기존 `text.measure: 'font'`(폰트 실측 모드) 재사용 — 상호명 길이 편차가
     역명보다 클 수 있으므로 `autoShrink: true` 필수
   - 대표 케이스(최장/최단 상호명) 샘플 생성 후 육안 확인
4. **DB 반영**: 생성된 이미지를 `badges.image_url`에 일괄 UPDATE (SQL 파일로 남기고
   service_role 키로 직접 실행 — `jam-web/supabase/migrations/seed_*.sql` 또는
   `seed_update_michelin-poi-badge_images.sql`)
5. **POI 활성화**: 어드민 `/admin/poi/review`에서 미슐랭 POI 42곳을 전부 승인
   (`is_active=true`, `pending_review` 처리). 배지 이미지가 먼저 프로덕션에 반영된 뒤 활성화
   해야 한다 — [[20260824_020]]에서 발견된 "이미지 파일 미배포 상태에서 DB부터 바꾸면 깨져
   보인다"는 배포 순서 문제를 반복하지 않는다.

### 컨텐츠 관점

- 배지 설명 문구는 `UX_WRITING_GUIDELINE.md`의 체크인 배지 톤(신남)을 따른다.
- `Service Plan/Specs/Content/` 하위 체크인 배지 관련 문서(있다면)에 미슐랭 배지 42종 추가
  사실을 반영한다.

## 구현 계획

1. Figma MCP로 node `15:127`/`15:113` 사양 재확인(이미 1차 확인 완료) → 배경 SVG 조립
2. `radius-policy.ts`에 `michelin` 반경 등록
3. `badges` 테이블에 42개 신규 체크인 배지 생성 + `poi.linked_badge_id` 연결 SQL 작성·실행
4. `configs/michelin-poi-badge.config.js` 신규 작성
5. 대표 케이스 샘플 생성 → 육안 확인 → 42개 전체 생성
6. `badges.image_url` 일괄 UPDATE
7. 이미지 프로덕션 배포 확인 후 어드민에서 POI 42곳 활성화
8. 관련 문서(BadgeEngine §3.14 표, Content POI 문서) 갱신

---
## 완료 기록

### 구현 내용 요약
1. Figma MCP(`get_metadata`, `get_design_context`)로 node `15:127`(텍스트 노드 `15:113`
   'POI') 사양을 추출 — 823.87×825.299, 배경은 16개 벡터 요소(외곽 3중 보더 + 상단 월계수
   장식 + 하단 소용돌이 장식 + 빨간 텍스트판 + Jam 로고 + MICHELIN GUIDE 아치 텍스트),
   텍스트 x=82 y=403.999 w=665 h=76 fontSize=107.2 Pretendard Black `#F4ECCA` 중앙정렬.
   각 요소를 개별 SVG asset으로 다운로드해 절대좌표 그대로 조립(`michelin-poi-badge.svg`)
2. `src/lib/poi/radius-policy.ts`에 `michelin: 50` 등록 (매장 단위 좌표라 train_subway와 동일값)
3. `164_michelin_checkin_badges.sql` 실행 — 미슐랭 42곳에 `type='checkin'` 배지 1:1 생성 +
   `poi.linked_badge_id` 연결 (이름 매칭, 42곳 이름 유일성 사전 확인). 42/42/42 검증
4. `configs/michelin-poi-badge.config.js` 신규 작성 — 기존 `metro-poi-badge.config.js`
   패턴 재사용(`measure:'font'`, `autoShrink:true`, `minFontSize:40`)
5. 대표 케이스(최장 "가겐 by 최준호", 최단 "라연"/"빈호") 샘플 생성 후 육안 확인 → 42개 전체 생성
6. `staging → main` 순으로 배포해 이미지 파일을 프로덕션(`j-a-m.app`)에 반영, 200 응답 확인
7. `update_michelin-poi-badge_images.sql` 실행 — `badges.image_url` 42/42 커스텀 경로로 갱신
8. `165_michelin_poi_activate.sql` 실행 — POI 42곳 `is_active=true`·`pending_review=false` 확인
9. `BADGE_ENGINE_UNIFIED.md` §3.14/반경표, `Content/POI.md` 체크리스트 갱신

### 변경된 파일
```
jam-web/scripts/badge-image-gen/backgrounds/michelin-poi-badge.svg (신규)
jam-web/scripts/badge-image-gen/configs/michelin-poi-badge.config.js (신규)
jam-web/scripts/badge-image-gen/fonts/Pretendard-Black.ttf (신규)
jam-web/public/badges/poi/michelin/*.png (신규 42개)
jam-web/supabase/migrations/164_michelin_checkin_badges.sql (신규, 실행 완료)
jam-web/supabase/migrations/165_michelin_poi_activate.sql (신규, 실행 완료)
jam-web/supabase/seed/update_michelin-poi-badge_images.sql (신규, 실행 완료)
jam-web/src/lib/poi/radius-policy.ts (수정)
Service Plan/Specs/BadgeEngine/BADGE_ENGINE_UNIFIED.md (수정)
Service Plan/Specs/Content/POI.md (수정)
```

### 테스트 결과
- [x] 워크트리 전체 `npm run lint`(0 errors) · `npm run typecheck`(0 errors) — 회귀 없음
- [x] 배지 42개 파일명(UUID)과 DB `badges(category='michelin', type='checkin')` 42건 1:1 일치
- [x] 최장·최단 상호명 육안 확인 — 라벨 안에 정상 배치, 잘림 없음
- [x] `https://stage.j-a-m.app` → `https://j-a-m.app` 순으로 이미지 200 응답 확인
- [x] `badges.image_url` 42/42 커스텀 경로 반영, `poi.is_active` 42/42 활성화 확인

### UX Writing 검증
- 배지 설명 `"{POI명}에서 식사했습니다"` — 기존 체크인 배지(mountain "올랐습니다"/
  transit "지나갔습니다")와 동일하게 상황별 동사 + 해요체 규칙 준수. 부사격 조사 "에서"로
  을/를 받침 분기 불필요하게 설계.

### 배포 정보
- 배포일: 2026-09-12
- 환경: production (j-a-m.app / standard-manual/jam), DB는 staging·프로덕션 공용 단일 DB
- 커밋: de6ea7cf (staging/main 공통)

### 주요 의사결정 / 핵심 메모
- **게이트 1차 FAIL 사유와 조치**: jam-developer 서브에이전트는 Figma MCP 도구 접근 권한이
  없어 배지 이미지 생성(티켓의 핵심 산출물)을 시작하지 못했다. 오케스트레이터가 직접 Figma
  디자인을 추출해 배경 SVG·config·이미지 42개를 완성한 뒤 재게이트에서 PASS 받았다 — 구조적
  한계(서브에이전트 도구 제약)는 오케스트레이터가 보완해야 하는 사례로 기록.
- **배경 SVG 조립 방식**: Figma가 각 장식 요소를 개별 SVG asset URL로 내려주므로, `get_metadata`의
  절대좌표(또는 inset % → px 환산값)를 그대로 translate에 사용해 16개 요소를 하나의 SVG로
  합쳤다. 각 asset의 자체 width/height가 계산된 배치 크기와 정확히 일치함을 확인해 스케일
  보정 없이 translate만으로 충분했다.
- **배포 순서**: [[20260824_020]]의 "이미지 먼저 배포, DB는 그 다음" 원칙을 그대로 따랐다 —
  staging에서 이미지 응답을 먼저 확인하고, main 승격(프로덕션 배포)까지 마친 뒤에야
  `image_url` UPDATE와 POI 활성화를 실행했다.
