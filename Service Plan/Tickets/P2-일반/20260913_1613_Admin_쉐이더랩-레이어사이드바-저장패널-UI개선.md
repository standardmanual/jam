---
id: 20260913_1613
category: Admin
priority: P2
status: CLOSED
created: 2026-09-13
closed: 2026-09-13
---

# [Admin] 쉐이더 랩 — 레이어 사이드바·저장 패널 UI 개선

## 배경 / 문제 정의

사용자가 `/better-interface`로 `/admin/shader-lab`을 검토해달라고 요청했고, 실제 사용 중
다음 4가지 문제를 직접 보고했다.

1. 레이어 추가 드롭다운(29개 옵션)이 PC·모바일 모두에서 화면에 잘려 선택할 수 없다.
2. 저장/불러오기 패널이 항상 펼쳐진 카드 하나를 통째로 차지해 레이어 목록이 아래로 밀린다.
3. 레이어 행의 아이콘(표시·위로·아래로·삭제)이 자리를 너무 차지해 레이어 이름이 잘린다.
4. 레이어/미리보기/속성 카드의 설명 문구가 불필요하다.

better-interface 검토 결과, 1·3번은 코드 레벨 원인이 확인된 HIGH 등급 결함이었다.

## 검토에서 확인된 원인과 결정된 방향

### 1. 레이어 추가 드롭다운 (HIGH)

`jam-web/src/components/admin/ui/select.tsx`의 `SelectContent` 내부 `SelectPrimitive.Viewport`가
`position === 'popper'`일 때 높이를 `h-[var(--radix-select-trigger-height)]`(트리거 버튼
자신의 높이, 약 40px)로 고정해뒀다. 바깥 `SelectContent`는 이미 충돌 인식 가용 높이
(`--radix-select-content-available-height`)로 `max-h`를 걸어뒀는데, 안쪽 뷰포트가 그 공간을
못 쓰고 트리거 높이에 갇혀 29개 항목이 사실상 한 줄씩만 보이는 좁은 창에 갇힌다.

**수정 방향**: `select.tsx`는 어드민 전체가 공유하는 원시 컴포넌트다. popper 모드의
`Viewport` 높이 고정(`h-[var(--radix-select-trigger-height)]`)을 제거하고
`w-full min-w-[var(--radix-select-trigger-width)]`만 남긴다. 다른 화면의 Select에 미치는
영향(짧은 목록이 트리거보다 커 보이지 않는지)을 실렌더로 확인할 것.

### 2. 레이어 추가 드롭다운 그룹화 (MEDIUM)

`jam-web/src/lib/admin/shaderLab/effectRegistry.ts`에 이미 `kind: 'source' | 'effect'`
필드가 있다(1281번째 줄부터 각 레이어 정의). `select.tsx`가 이미 export하지만 안 쓰이는
`SelectGroup`/`SelectLabel`을 `ShaderLabLayerSidebar.tsx:46-58`의 드롭다운에 적용해
"소스"(6종)·"이펙트"(23종) 2그룹으로 나눈다.

**이번 범위에는 포함하지 않음**: `command.tsx`(검색형 콤보박스)로 완전히 바꾸는 것은
범위가 더 크다 — 1·2번 수정 후에도 여전히 찾기 불편하면 별도 티켓으로 판단한다.

### 3. 레이어 행 아이콘 과밀 (HIGH)

`ShaderLabLayerSidebar.tsx:67-126`. 260px 사이드바 컬럼(`page.tsx:95`
`lg:grid-cols-[260px_1fr_320px]`) 안에서 카드 패딩(48px)·아이콘 4개(표시·위로·아래로·삭제,
각 28px=112px)·gap(24px)을 빼면 레이어 이름에 남는 폭이 약 60px뿐이다. 한글 레이어 이름
대부분이 2~3자만 보이고 잘린다. `title` 속성은 있지만 마우스 호버에서만 동작해 터치·키보드
사용자는 전체 이름을 볼 방법이 없다.

**수정 방향**: 표시(눈) 토글만 인라인에 남기고, 위로·아래로·삭제는
`jam-web/src/components/admin/ui/dropdown-menu.tsx`로 묶어 "⋮ 더보기" 버튼 하나로
압축한다. 보이는 버튼이 4개 → 2개로 줄어 이름에 폭이 돌아온다. 위로/아래로가 비활성화되는
경계(첫 번째/마지막 항목)는 메뉴 안에서도 `disabled`로 유지한다.

### 4. 저장/불러오기 위치 (MEDIUM)

현재 `page.tsx:115-123`에서 레이어 카드 바로 아래 항상 펼쳐진 별도 카드로 저장/불러오기
전체(이름 입력+저장 버튼+목록+안내문, `ShaderLabSavePanel.tsx`)를 보여준다. 저장/불러오기는
가끔 쓰는 보조 동작인데 상시 카드 하나를 통째로 차지해 자주 쓰는 레이어 목록이 화면
아래로 밀린다.

**수정 방향**: 사용자 제안대로 페이지 제목("쉐이더 랩", `page.tsx:86`) 우측에 이름 입력
+ 저장 버튼 + "목록" 버튼만 컴팩트하게 배치하고, "목록" 버튼을 누르면
`jam-web/src/components/admin/ui/popover.tsx`로 여는 팝업 안에 기존 목록 UI(불러오기/삭제,
`ShaderLabSavePanel.tsx`의 목록 렌더링 부분)를 넣는다. `ShaderLabSavePanel.tsx`의 상태
관리(로직)는 그대로 재사용하고 UI 배치만 "헤더 인라인 폼 + 팝오버 목록"으로 재구성한다
(컴포넌트를 분리할지 하나로 유지한 채 조건부 렌더링할지는 구현 시 판단).

### 5. 설명 텍스트 정리 (LOW)

다음 3곳의 `CardDescription`은 삭제한다 — 카드 제목과 실제 동작만으로 이미 자명하고
어떤 행동도 안내하지 않는 문장이다.

- `page.tsx:118` "작업 유실 방지용 최소 저장이에요." (저장/불러오기 카드 — 4번 수정으로
  이 카드 구조 자체가 바뀌므로 함께 처리)
- `page.tsx:129` "이 캔버스가 그대로 결과물로 내보내져요(WYSIWYG)."
- `page.tsx:140` "선택한 레이어의 값을 조정해요."

### 6. 레이어 순서 설명은 삭제 대신 압축 (MEDIUM — 정보 손실 주의)

`page.tsx:100` "목록 위쪽일수록 나중에(위에) 그려져요."는 **그대로 삭제하지 않는다.** 이
순서(위=나중에 그려짐)는 런타임이 배열을 뒤집어 합성하기 때문(`page.tsx:40-46` 주석 참고)에
직관과 반대다 — 이 문장이 유일한 안내 수단이다.

**수정 방향**: 문장을 상시 노출하는 대신, 레이어 카드 제목("레이어") 옆에 작은 정보
아이콘(`IconInfoCircle` 등 기존 아이콘 세트인 `@tabler/icons-react`에서 선택)을 두고,
마우스 호버(또는 `title` 속성)로 같은 문장을 보여준다. 화면은 깔끔해지고 정보는 완전히
사라지지 않는다.

## 상세 요구사항

### 서비스/코드베이스 관점

1. `select.tsx`의 popper 모드 Viewport 높이 고정 제거.
2. `ShaderLabLayerSidebar.tsx` 추가 드롭다운에 `SelectGroup`/`SelectLabel`로 소스/이펙트
   2그룹 적용.
3. `ShaderLabLayerSidebar.tsx` 레이어 행: 표시 토글만 인라인, 위로/아래로/삭제는
   `DropdownMenu`(⋮ 트리거)로 통합.
4. 저장/불러오기를 페이지 헤더 우측 컴팩트 폼 + `Popover` 목록으로 재구성
   (`page.tsx`/`ShaderLabSavePanel.tsx`).
5. `page.tsx`의 CardDescription 3곳 삭제.
6. `page.tsx`의 레이어 순서 설명을 정보 아이콘 + 호버 텍스트로 대체.

### UI/UX 관점

- 어드민 화면이므로 MODULAR 탐색 절차는 생략한다(기존 정책).
- 신규 의존성 추가 없음 — `dropdown-menu.tsx`/`popover.tsx`/`SelectGroup`/`SelectLabel`
  모두 프로젝트에 이미 있는 컴포넌트다.

### 컨텐츠 관점

- 해당 없음.

## 구현 계획

1. `select.tsx` Viewport 높이 고정 제거 → 어드민 전체 다른 Select 화면(배지 폼 등)에
   회귀가 없는지 로컬 실렌더로 확인.
2. `ShaderLabLayerSidebar.tsx` 드롭다운 그룹화 + 레이어 행 아이콘 `DropdownMenu` 통합.
3. `page.tsx`/`ShaderLabSavePanel.tsx` 저장/불러오기 헤더 이동 + Popover 목록.
4. `page.tsx` CardDescription 3곳 삭제, 레이어 순서 설명을 정보 아이콘으로 대체.
5. 로컬 `ADMIN_EMAILS` 실렌더 확인(드롭다운 29개 항목 스크롤·그룹 라벨, 더보기 메뉴
   위/아래/삭제 동작, 저장/불러오기 팝오버, 정보 아이콘 호버) — 워크트리 제약으로
   `next dev`/`build` 불가 시 esbuild+playwright 병행 검증.

## 유예된 판단

1. 레이어 추가 드롭다운을 검색형 콤보박스(`command.tsx`)로 완전히 바꾸는 것 — 그룹화
   적용 후에도 불편하면 별도 티켓.

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

애초 계획한 6개 항목을 전부 구현했다. jam-developer 서브에이전트가 배정 워크트리에서
Edit/Write가 "다른 세션 소유"로 거부되는 인프라 버그(HALT)로 파일을 전혀 쓰지 못해,
오케스트레이터가 직접 구현했다(빈 워크트리 정리 후 같은 review 브랜치를 이어받음).

1. `select.tsx`: popper 모드 `SelectPrimitive.Viewport`의 `h-[var(--radix-select-trigger-height)]`
   고정을 제거했다.
2. `ShaderLabLayerSidebar.tsx`: `SHADER_LAB_LAYER_TYPES`를 `effectRegistry.ts`의 `kind`로
   소스/이펙트 두 배열로 나눠 `SelectGroup`+`SelectLabel`로 그룹화. 레이어 행은 표시 토글만
   인라인에 남기고 위로/아래로/삭제를 `DropdownMenu`("⋮ 더보기")로 통합했다(삭제는
   `text-destructive`로 구분). 행 내부 `gap`도 `gap-1.5`→`gap-0.5`로 좁혀 아이콘 사이 여백을
   줄였다(사용자가 구현 도중 추가로 지적한 사항).
3. `page.tsx`: 저장/불러오기를 제목 우측으로 옮기고(`ShaderLabSavePanel`), 레이어/미리보기/
   속성 카드의 `CardDescription` 3곳을 삭제했다. "레이어" 카드에는 `IconInfoCircle` +
   `title` 속성으로 순서 설명을 압축 보존했다.
4. `ShaderLabSavePanel.tsx`: 기존 fetch 로직은 그대로 두고 UI만 "이름 입력+저장 버튼+'목록'
   버튼(Popover 트리거) → PopoverContent 안 기존 목록"으로 재구성했다. 오류 메시지는 팝오버
   상태와 무관하게 트리거 행 아래 항상 노출한다.
5. **구현 도중 발견해 함께 고친 사전 조사에 없던 버그**: `dropdown-menu.tsx`의
   `DropdownMenuContent`에 `select.tsx`/`popover.tsx`에는 이미 있는 "어드민 테마 스코프로
   포털" `container` prop이 빠져 있었다. Radix Portal 기본값(`document.body`)으로 렌더링되면
   `[data-admin-theme]` 스코프 안에만 정의된 `--destructive`/`--foreground` 등 shadcn 실값이
   미정의 상태로 남아, "삭제" 메뉴 항목의 빨간색과 저장 목록 팝오버의 텍스트 색이 실제로는
   적용되지 않는 것을 실측(esbuild+playwright, `getComputedStyle`)으로 확인했다.
   `dropdown-menu.tsx`에 동일한 `container` prop을 추가하고, `ShaderLabLayerSidebar.tsx`/
   `ShaderLabSavePanel.tsx`에서 기존 `BadgeForm.tsx` 등과 동일한
   `useState(() => document.querySelector('[data-admin-theme]'))` 패턴으로 `themeContainer`를
   구해 `SelectContent`/`DropdownMenuContent`/`PopoverContent`에 전달했다.

### 변경된 파일
```
jam-web/src/components/admin/ui/select.tsx           (popper 뷰포트 높이 고정 제거)
jam-web/src/components/admin/ui/dropdown-menu.tsx    (container prop 추가 — 신규 발견 버그 수정)
jam-web/src/app/admin/shader-lab/ShaderLabLayerSidebar.tsx  (그룹화+더보기 메뉴+themeContainer)
jam-web/src/app/admin/shader-lab/page.tsx            (헤더 재배치+설명 3곳 삭제+정보 아이콘)
jam-web/src/app/admin/shader-lab/ShaderLabSavePanel.tsx     (Popover 재구성+themeContainer)
```

### 테스트 결과
- [x] `npx tsc --noEmit` — 오류 0건
- [x] `npm run lint`(전체) — 0 errors, 13 warnings(전부 `design-system/**` 기존 경고, 무관)
- [x] `npx vitest run`(전체) — 91개 파일 중 90개 통과, 1452/1455 테스트 통과. 실패 3건은
      `gate-family-options-contract.test.ts`로 이 티켓과 무관한 기존 실패(티켓 20260912_1105
      추적 중)
- [x] 실렌더 검증(esbuild+playwright, 워크트리 `next dev`/`build` 제약으로 대체) — 실제
      소스 파일을 그대로 번들하고, 실제 `globals.css`를 postcss로 컴파일해 실제 어드민
      레이아웃과 동일한 `[data-admin-theme]` 래퍼 구조로 마운트해 검증:
      - 레이어 추가 드롭다운: "소스"/"이펙트" 그룹 라벨 존재, 29개 항목 전부 렌더링,
        리스트박스 높이가 트리거 높이(약 40px)에 갇히지 않고 1002px까지 펼쳐짐(수정 전
        `h-[var(--radix-select-trigger-height)]` 고정 시 불가능했던 상태)
      - 레이어 행: 버튼 개수 4→2(이름 제외), 이름 버튼 폭 약 60px→124px로 확대
      - 더보기 메뉴: 첫 항목의 "위로 이동"에 `data-disabled` 정상 부여, "삭제"가
        `oklch(0.577 0.245 27.325)`(실제 destructive 빨강)로 렌더링됨을 `getComputedStyle`로
        직접 확인(수정 전에는 `--destructive` 미정의로 근처 검정에 가까운 색이 나왔었음)
      - 저장 팝오버: 목록·불러오기·삭제·안내 문구 정상 렌더링, 텍스트 색상이
        `--foreground`(`oklch(0.147 0.004 49.25)`) 정상 적용 확인
      - 콘솔/페이지 오류 0건
      - 검증에 쓴 esbuild 번들·postcss 스크립트·playwright 스크립트·PNG는 스크래치
        산출물이라 커밋하지 않음(관례 동일)

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

- [x] 용어 일관성: "위로 이동"/"아래로 이동"/"삭제"/"목록" 등 기존 사이드바·저장 패널이
      쓰던 용어를 그대로 유지, 신규 고정 용어 없음
- [x] 톤앤매너: 어드민 도구 톤(간결·설명형) 유지
- [x] 에러 메시지: 기존 저장/불러오기/삭제 실패 메시지 문구는 변경하지 않음
- [x] 문장 규칙: 해요체·간결함 유지
- [x] 표기 규칙: 해당 없음

### 배포 정보
- 배포일: 2026-09-13 (staging 병합·push 완료, 오케스트레이터)
- 환경: staging (`stage.j-a-m.app`) - 프로덕션(main) 승격은 이 티켓 범위 밖, `/jam-ship`
  진행 시 사용자 승인 필요
- 커밋: `d0d28653`(review 브랜치 `claude/jamwork-20260913_1613-shaderlab-ui-improve`,
  origin/staging 기점에서 fast-forward로 정상 병합). 게이트 리뷰 판정: PASS(2026-09-13,
  conservative-reviewer가 6개 항목 전부 diff와 대조 확인, tsc/lint/vitest 재실행 결과
  일치).

### 주요 의사결정 / 핵심 메모
1. 레이어 행 아이콘의 `gap`을 `gap-1.5`(6px)에서 `gap-0.5`(2px)로 줄였다 — 티켓에는 없던
   조정이나, 구현 진행 중 사용자가 "아이콘 간 간격이 너무 넓다"고 추가로 지적해 반영했다.
2. `command.tsx`(검색형 콤보박스)로의 전환은 티켓이 명시한 대로 이번 범위에 포함하지
   않았다 — 그룹화만으로 충분한지는 실사용 후 재판단.
3. `dropdown-menu.tsx`의 `container` prop 누락은 이 티켓 착수 전 사전 조사에는 없던
   내용이다. `select.tsx`/`popover.tsx`가 이미 20260826_016/018에서 확립한 패턴을
   `dropdown-menu.tsx`만 놓치고 있었던 것으로 보이며, 이번이 이 프로젝트에서
   `DropdownMenuContent`가 `[data-admin-theme]` 스코프 색 토큰(`text-destructive`)에 처음
   의존한 사례라 지금까지 드러나지 않았던 것으로 판단된다.

### 잔여 이슈
- 어드민 화면 자체(실제 브라우저에서 마우스로 드롭다운 열기·팝오버 클릭 등)의 `next dev`
  실렌더는 여전히 못 했다(워크트리 제약, 기존과 동일). esbuild+playwright로 실제 소스
  파일과 실제 컴파일된 CSS를 그대로 사용해 검증했으나, Next.js 라우팅·서버 컴포넌트 경계를
  통과한 최종 렌더는 아니다.
- 레이어 추가 드롭다운을 검색형 콤보박스로 바꾸는 것은 여전히 유예된 판단(위 참고).
