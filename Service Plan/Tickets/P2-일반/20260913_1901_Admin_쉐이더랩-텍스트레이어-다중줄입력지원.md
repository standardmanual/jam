---
id: 20260913_1901
category: Admin
priority: P2
status: OPEN
created: 2026-09-13
---

# [Admin] 쉐이더 랩 — 텍스트 레이어 다중 줄(줄바꿈) 입력 지원

## 배경 / 문제 정의

텍스트 레이어의 "텍스트" 입력값이 줄바꿈을 포함한 여러 줄을 지원하지 않는다. 사용자가
줄바꿈 포함 다중 줄 입력을 요청했다.

## 원본 저장소 조사 결과

- **UI**: `node_modules/@basementstudio/shader-lab/dist/src/renderer/text-pass.js`
  (설치된 실제 런타임)의 `rebuildTextTexture()`는 `text` 파라미터 문자열 전체를
  `[...text]`로 문자 단위 분해해 **한 줄로만** 배치한다 — `\n` 문자가 섞여 들어와도
  줄바꿈으로 처리하지 않고 그냥 폭 0(또는 이상값)짜리 글리프로 취급해 한 줄에
  이어 그려진다(실측: 개행 문자를 포함해 렌더링하면 한 줄에 뭉쳐 보임).
- **원본 저장소 자체도 다중 줄 미지원**: GitHub `basementstudio/shader-lab`의
  `src/lib/editor/config/layer-registry.ts`(`textParams`)를 조회해보니 `text` 필드가
  `maxLength: 32`인 단순 `type: "text"`(단일 줄) 그대로이고 `lineHeight` 등 다중 줄
  관련 파라미터 자체가 없다 — JAM!이 원본에 없는 기능을 추가하는 순수 확장이다.
- **적용 방식**: 사용자 확정 — `patch-package`로 설치된 패키지의 `text-pass.js`를
  직접 고친다(fork 유지·전체 재구현 등 다른 대안 대신 이 방식으로 결정).

## 상세 요구사항

### 서비스/코드베이스 관점

1. `patch-package` 개발 의존성 설치 + `package.json`에 `postinstall: "patch-package"`
   스크립트 추가 — CI·다른 PC에서 `npm install` 시 패치가 자동 재적용되도록.
2. `node_modules/@basementstudio/shader-lab/dist/src/renderer/text-pass.js`의
   `rebuildTextTexture()`를 다중 줄 레이아웃으로 교체한다.
   - `text.split('\n')`으로 줄 단위 분리
   - 줄마다 기존과 동일한 글리프 레이아웃(`visualLeft`/`visualRight`/글자별 x좌표)을
     독립 계산 — 가로 정렬(`anchor`의 horizontal: left/center/right)은 줄마다
     자기 폭 기준으로 적용(일반적인 워드프로세서 정렬과 동일)
   - 줄 간격은 `fontSize`의 고정 배율(1.2, 통상적인 본문 줄간격 기본값)로 계산 —
     별도 파라미터로 노출하지 않는다(사용자 요청 범위 밖, 필요 시 후속 확장)
   - 세로 정렬(`anchor`의 vertical: top/center/bottom)은 전체 텍스트 블록(줄 수 ×
     줄간격 + 첫 줄 ascent/마지막 줄 descent) 기준으로 계산 — 단일 줄일 때는 원본과
     100% 동일한 좌표가 나오도록 만든다(회귀 방지)
3. `npx patch-package @basementstudio/shader-lab`로 패치 파일 생성
   (`patches/@basementstudio+shader-lab+3.0.2.patch`) — 커밋 대상.
4. `jam-web/THIRD_PARTY_NOTICES.md`에 이번 패치 사실(대상 파일·변경 요지·이유) 고지
   추가.
5. `jam-web/src/lib/admin/shaderLab/paramFields.ts`의 `TextFieldDefinition`에
   `multiline?: boolean` 옵션 추가.
6. `jam-web/src/lib/admin/shaderLab/effectRegistry.ts`의 `text` 필드(468행 부근)에
   `multiline: true` 지정, `maxLength`를 다중 줄에 맞게 상향 조정(현재 32는 한 줄
   기준값이라 여러 줄엔 너무 작음 — 착수 시 적정값 판단해 근거를 완료 기록에 남길 것).
7. `jam-web/src/app/admin/shader-lab/ShaderLabParamField.tsx`의 `type === 'text'`
   분기에서 `field.multiline`이 true면 `Textarea`(shadcn, 이미 프로젝트에 있음)를,
   아니면 기존 `Input`을 렌더링하도록 분기 — ASCII 레이어의 `customChars` 등 다른
   `text` 타입 필드는 영향받지 않아야 한다(회귀 방지).

## 구현 계획

1. `patch-package` 설치 + `postinstall` 스크립트 추가.
2. `text-pass.js`의 `rebuildTextTexture()`를 다중 줄 알고리즘으로 교체 후
   `npx patch-package @basementstudio/shader-lab`로 패치 파일 생성.
3. `paramFields.ts`/`effectRegistry.ts`/`ShaderLabParamField.tsx` 수정.
4. `THIRD_PARTY_NOTICES.md` 갱신.
5. esbuild+playwright 실측: (a) 단일 줄 입력 시 패치 전/후 렌더링 결과가 픽셀
   동일한지(회귀 없음 증명), (b) 여러 줄 입력이 실제로 줄바꿈되어 렌더링되는지,
   (c) anchor 9종 조합 중 대표값(top-left/center/bottom-right)에서 다중 줄 정렬이
   올바른지.
6. `npx tsc --noEmit`, `npm run lint`, `npx vitest run` 재확인.

## 유예된 판단

- 줄 간격(line-height) 커스터마이징 UI는 이번 범위 밖(고정 배율 1.2 사용).
- 자동 줄바꿈(word-wrap, 폭을 넘으면 자동 개행)은 이번 범위 밖 — 사용자가 명시적으로
  입력한 `\n`만 줄바꿈으로 처리한다.

---
## 완료 기록

### 구현 내용 요약

계획한 순서대로 구현했다.

1. **`patch-package` 도입**: 개발 의존성으로 설치하고 `package.json`에
   `postinstall: "patch-package"`를 추가했다. `npm install` 시 자동으로 패치가
   재적용되는지 실측으로 검증했다(아래 테스트 결과 참고).
2. **`text-pass.js` 다중 줄 알고리즘**: `TextPass.rebuildTextTexture()`를
   `text.split('\n')` 기준으로 줄 단위 분리하도록 고쳤다. 원본은 문자 전체를 한 줄로만
   배치했는데, 줄마다 독립적으로 글리프 레이아웃(가로 정렬 기준폭)을 계산하고, 줄 간격은
   `fontSize * 1.2`(고정 배율, 통상적인 본문 줄간격 기본값)로, 세로 정렬은 전체 텍스트
   블록(줄 수 × 줄간격 + 첫 줄 ascent/마지막 줄 descent) 기준으로 계산하도록 확장했다.
   `lines.length === 1`일 때(개행 없는 입력) 정확히 원본과 같은 좌표가 나오도록 설계했고,
   실측(아래)으로 바이트 단위 완전 동일함을 확인했다.
3. **패치 파일 생성**: `npx patch-package @basementstudio/shader-lab`로
   `patches/@basementstudio+shader-lab+3.0.2.patch`를 생성했다.
4. **`THIRD_PARTY_NOTICES.md` 갱신**: Apache License 2.0 §4(b)(수정 파일 고지 의무)에
   따라 패치 대상 파일·변경 요지·패치 파일 위치를 추가했다.
5. **UI 반영**: `paramFields.ts`의 `TextFieldDefinition`에 `multiline?: boolean` 옵션을
   추가하고, `effectRegistry.ts`의 `text` 필드에 `multiline: true`를 지정, `maxLength`를
   32→100으로 올렸다(여러 줄을 감안, 근거는 "주요 의사결정" 참고). `ShaderLabParamField.tsx`의
   `type === 'text'` 분기에서 `field.multiline`이 true면 shadcn `Textarea`를, 아니면 기존
   `Input`을 렌더링하도록 나눴다 — ASCII 레이어의 `customChars` 등 다른 `text` 타입 필드는
   `multiline`이 없어(기본 false) 영향받지 않는다.

### 변경된 파일
```
jam-web/package.json
jam-web/package-lock.json
jam-web/patches/@basementstudio+shader-lab+3.0.2.patch (신규)
jam-web/THIRD_PARTY_NOTICES.md
jam-web/src/lib/admin/shaderLab/paramFields.ts
jam-web/src/lib/admin/shaderLab/effectRegistry.ts
jam-web/src/app/admin/shader-lab/ShaderLabParamField.tsx
```

### 테스트 결과
- [x] `npx tsc --noEmit` — 클린
- [x] 변경 파일 3개 `npx eslint` — 클린
- [x] `npx vitest run` — 1452/1455 통과. 실패 3건은 이번 변경과 무관한 기존 실패
      (`gate-family-options-contract.test.ts`, 별도 티켓 20260913_1852로 이미 분리됨)
- [x] **postinstall 자동 재적용 검증**: `@basementstudio/shader-lab` 패키지 디렉터리를
      삭제 후 `--ignore-scripts`로 원본 재설치 → `npm run postinstall` 실행 →
      `text-pass.js`에 패치가 다시 적용됨을 확인(새 클론·CI 환경 재현성 증명)
- [x] **회귀 없음(바이트 단위 완전 동일) 실측**: esbuild+playwright로 동일 파라미터
      (`text: 'HELLO'`, `anchor: 'center'`, `fontSize: 200`)를 (a) 원본(패치 전)
      `text-pass.js`와 (b) 패치 후 `text-pass.js` 각각으로 렌더링해 PNG로 추출,
      SHA-256 해시를 비교한 결과 완전히 동일함(`bdf410ab...`)을 확인했다.
- [x] **다중 줄 렌더링 실측**: 3줄 텍스트("AAA\nBBB\nCCC")를 anchor
      top-left/center/bottom-right 3종으로 각각 렌더링해 스캔라인 분석(밝은 픽셀이
      있는 연속 구간 = "밴드" 카운트)으로 검증 — 전부 3개 밴드(줄 3개가 정확히 분리)를
      확인했고, top-left는 첫 밴드가 캔버스 최상단(y=0)에, bottom-right는 마지막 밴드가
      캔버스 최하단(y=251/256)에 위치함을 확인해 세로 정렬도 정상 동작함을 실증했다.
      스크린샷으로 육안 확인도 병행했다.

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

- [x] 용어 일관성: 신규 노출 문구 없음(기존 "텍스트" 라벨 그대로 유지, 입력 방식만 확장)
- [x] 톤앤매너: 해당 없음
- [x] 에러 메시지: 변경 없음
- [x] 문장 규칙: 해당 없음
- [x] 표기 규칙: 해당 없음

### 배포 정보
- 배포일: (게이트 리뷰·사용자 승인 후 병합·배포 예정)
- 환경: production
- 커밋: (병합 시 기록)

### 주요 의사결정 / 핵심 메모

- **`patch-package` 방식 채택**: 원본 패키지를 fork하거나 텍스트 레이어 전체를
  재구현하는 대신, 설치된 npm 패키지 하나의 메서드만 직접 고치고 패치 파일로 관리하는
  방식을 사용자가 확정했다(이전 세션 AskUserQuestion). `postinstall`로 자동 재적용되게
  만들어 새 클론·CI 환경에서도 패치가 유실되지 않는다.
- **줄 간격을 파라미터로 노출하지 않음**: 사용자 요청은 "줄바꿈 포함 다중 줄 입력"까지였고
  줄 간격 커스터마이징은 요청 범위 밖이라, `fontSize * 1.2`(통상적인 본문 줄간격 기본값)
  고정값을 사용했다. 필요하면 후속 티켓으로 별도 파라미터 노출을 검토할 수 있다.
- **자동 줄바꿈(word-wrap)은 구현하지 않음**: 사용자가 명시적으로 입력한 `\n`만 줄바꿈으로
  처리한다 — 폭을 넘으면 자동으로 개행하는 기능은 이번 범위 밖(티켓 "유예된 판단" 절 그대로
  확정, 변경 없음).
- **`maxLength: 100`**: 여러 줄(줄바꿈 포함)을 감안해 기존 32에서 상향했다. 배지·미션·
  컬렉션 대표이미지 1:1 정적 렌더 용도라 너무 길면 가독성이 떨어지므로, 한 줄당 대략
  20자 × 5줄 안팎을 기준으로 넉넉히 잡은 경험값이다(엄격한 산식은 아님).
- **회귀 검증 방식**: 단순히 "코드 로직상 단일 줄일 때 동일한 수식이 나온다"는 논리적
  근거에 그치지 않고, 실제로 패키지 디렉터리를 원본으로 되돌려 렌더링한 이미지와 패치
  적용 후 렌더링한 이미지를 SHA-256 해시로 비교해 바이트 단위 완전 동일함을 실측했다
  ([[project-canvas-bake-local-verify]] 패턴 재사용).

### 잔여 이슈
- 없음
