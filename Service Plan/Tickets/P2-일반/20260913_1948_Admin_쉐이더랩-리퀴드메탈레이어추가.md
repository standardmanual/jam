---
id: 20260913_1948
category: Admin
priority: P2
status: CLOSED
created: 2026-09-13
---

# [Admin] 쉐이더 랩 — 리퀴드 메탈 레이어 추가

## 배경 / 문제 정의

사용자가 `https://shaders.paper.design/liquid-metal`(Paper Shaders의 "Liquid Metal" 데모)을
참고해 쉐이더 랩에 리퀴드 메탈 레이어 추가를 요청했다. 프로젝트에는 이미
`@paper-design/shaders-react@0.0.80`이 설치돼 있다(과거 티켓 20260819_007/20260912_1532에서
"유일하게 허용되던 셰이더 라이브러리"로 확립됐으나, 실제 코드에서 사용된 적은 없음 — 이번이
첫 실사용).

## 기술적 난제와 통합 방식 결정

`@basementstudio/shader-lab`(WebGPU + Three.js + TSL)과 `@paper-design/shaders`(순수 WebGL2
GLSL 프래그먼트 셰이더)는 렌더링 파이프라인이 완전히 다르다. 검토한 대안:

1. **GLSL을 TSL로 재작성**: `liquid-metal.js`(630줄, 포아송 디스크 샘플링·이미지 전처리 등
   복잡한 로직)를 통째로 재작성 — 리스크가 크고 원본과의 시각적 차이가 생길 위험이 높아
   기각.
2. **`custom-shader-pass.js`(원본의 커스텀 셰이더 확장 지점) 재활용**: 소스 코드를 실행 시점에
   컴파일하는 방식이라 매력적이었으나, 이 레이어는 순수 TSL 함수만 반환할 수 있고
   `sourceCode`/`entryExport` 외에 임의 UI 파라미터(색상·반복 등)를 노출할 방법이 없다 —
   원본 앱도 이 레이어를 일반 사용자 UI에 노출하지 않고 내부용으로만 쓴다. 기각.
3. **오프스크린 WebGL 브릿지(채택)**: `@paper-design/shaders`의 `ShaderMount`(순수 JS 클래스,
   React 의존성 없음)로 숨겨진 `<div>`에 GLSL 셰이더를 직접 마운트하고, 그 안에서 자체
   생성되는 `<canvas>`(별도 WebGL2 컨텍스트)를 `THREE.CanvasTexture`로 감싸 shader-lab의
   TSL `texture()` 노드에 공급한다. `text-pass.js`가 Canvas 2D 결과를 `CanvasTexture`로
   공급하는 것과 원리가 동일 — WebGPU와 WebGL은 서로 다른 canvas를 쓰는 한 한 페이지 안에
   공존 가능하다.

**패치 방식**: 새 pass 클래스(`liquid-metal-pass.js`)를 `patch-package`로 패키지에 신규
파일로 추가하고, `pipeline-manager.js`의 `createPass()`에 새 case(`layer.type ===
"liquid-metal"`)를 추가한다.

## 범위 결정 — 이미지 업로드는 제외

원본 `LiquidMetal`은 로고 이미지를 업로드해 그 위에 리퀴드 메탈 질감을 입히는 것이 핵심
용도이지만, 이미지를 마스크로 변환하는 `toProcessedLiquidMetal()`이 비동기 canvas 전처리
(edge gradient + opacity 추출)를 수행하는 무거운 로직이라 이번 범위에서 제외한다. 대신
셰이더가 기본 제공하는 `shape`(도형 프리셋: 없음/원/데이지/다이아몬드/메타볼) 기반으로만
동작하는 "소스" 레이어로 구현한다 — `fullScreenPreset`("Backdrop", `shape: "none"`)이 이미
이미지 없이 전체 화면을 채우는 표준 사용법으로 라이브러리에 내장돼 있어 이 범위로도
정상적인 시각 효과를 낼 수 있음을 확인했다. 이미지 업로드 지원은 후속 확장으로 유예한다.

## 상세 요구사항

### 패치 (`patches/`)

1. **신규 파일 `liquid-metal-pass.js`** (`PassNode` 확장, `text-pass.js` 패턴 참고):
   - 생성자: 뷰포트 좌상단에 겹치되 `opacity: 0; pointer-events: none; position: fixed`로
     시각적으로 숨긴 `<div>`를 만들어 `document.body`에 붙인다. `display: none`이나 화면 밖
     좌표(`left: -9999px`)는 쓰지 않는다 — `ShaderMount`의 `IntersectionObserver`가
     "화면 밖"으로 판정해 렌더링을 자동 정지시키기 때문(실측 확인 필요).
   - `updateParams(params)`: 최초 호출 시 `new ShaderMount(container,
     liquidMetalFragmentShader, uniforms, ...)`으로 마운트, 이후에는 `shaderMount.
     setUniforms(...)`만 호출. `u_image`는 항상 1x1 투명 픽셀(data URL 상수), `u_isImage:
     false` 고정(이미지 미지원 범위).
   - `render(...)`: `shaderMount.canvasElement`를 최초 1회 `THREE.CanvasTexture`로 감싸고
     매 프레임 `needsUpdate = true`(ShaderMount 자체 RAF가 그 canvas를 계속 갱신하므로).
   - `resize(width, height)`: container div의 CSS 크기를 갱신(ShaderMount의
     `ResizeObserver`가 자동으로 canvas 해상도를 맞춤).
   - `needsContinuousRender()`: `true`(줄무늬가 항상 흐르는 것이 이 레이어의 정체성 — 이전
     티켓 20260913_1819에서 제거한 "정적 이미지에 불필요한 자동 애니메이션"과는 성격이
     다르다. 플루이드·픽셀 트레일과 같은 카테고리).
   - `dispose()`: `shaderMount.dispose()` + container 엘리먼트 제거.
2. **`pipeline-manager.js` patch**: import 추가 + `layer.kind === "source" && layer.type ===
   "liquid-metal"` 케이스에서 `new LiquidMetalPass(layer.id)` 반환.
3. `npx patch-package @basementstudio/shader-lab`로 패치 파일 갱신.

### 의존성

4. `@paper-design/shaders`(코어, react 아님)를 `package.json` dependencies에 명시적으로
   추가 — 지금은 `@paper-design/shaders-react`의 전이 의존성으로만 node_modules에 존재해
   직접 import가 버전 관리 밖에 있다.

### UI/서비스 코드 (`jam-web/src/lib/admin/shaderLab/`, `jam-web/src/app/admin/shader-lab/`)

5. `effectRegistry.ts`: `SHADER_LAB_LAYER_TYPES`에 `'liquid-metal'` 추가(kind: 'source').
   파라미터 필드(원본 `LiquidMetalParams` 대조, 한국어 라벨):
   - `colorBack`(배경색, color), `colorTint`(틴트색, color)
   - `shape`(모양, select: 없음/원/데이지/다이아몬드/메타볼)
   - `repetition`(줄무늬 밀도, number 1~10), `softness`(경계 부드러움, number 0~1)
   - `shiftRed`/`shiftBlue`(색수차, number -1~1), `distortion`(왜곡, number 0~1)
   - `contour`(윤곽 강도, number 0~1), `angle`(방향, number 0~360)
   - sizing 관련(`fit`/`scale`/`rotation`/`offset*`/`origin*`/`worldWidth`/`worldHeight`)은
     `fullScreenPreset` 고정값을 그대로 써서 UI에는 노출하지 않는다(범위 최소화).
6. `layerFactory.ts`/`composition.ts`/`ShaderLabLayerSidebar.tsx`는 제너릭 코드라 별도 수정
   불필요할 가능성이 높음(착수 시 재확인).
7. `THIRD_PARTY_NOTICES.md`: (a) `@basementstudio/shader-lab` 패치 고지에 신규 파일 추가
   내용 보강, (b) `@paper-design/shaders`의 Apache License 2.0 NOTICE("Powered by Paper
   Shaders: https://shaders.paper.design") 신규 고지 추가.

## 구현 계획

1. `liquid-metal-pass.js` 작성 → `pipeline-manager.js` patch → `patch-package`로 패치 파일
   생성.
2. `package.json`에 `@paper-design/shaders` 의존성 추가.
3. `effectRegistry.ts`에 레이어 타입·필드 추가.
4. `THIRD_PARTY_NOTICES.md` 갱신.
5. esbuild+playwright 실측: (a) 레이어가 실제로 렌더링되는지(빈 화면이 아님을 확인),
   (b) 시간이 지나면 프레임이 계속 바뀌는지(애니메이션 확인 — 두 시점의 canvas 픽셀이
   달라야 함), (c) `shape` 값을 바꾸면 렌더링 결과가 달라지는지, (d) 레이어를 제거
   (`dispose()`)했을 때 숨겨진 DOM 엘리먼트가 실제로 정리되는지(DOM 누수 확인).
6. `npx tsc --noEmit`, `npm run lint`, `npx vitest run` 재확인.

## 유예된 판단

- 이미지 업로드(로고를 리퀴드 메탈 질감으로 감싸는 원본 핵심 기능)는 이번 범위 밖.
- `speed`(애니메이션 속도) 파라미터는 고정값(1)으로 두고 UI에 노출하지 않는다 — 필요하면
  후속 확장.

---
## 완료 기록

### 구현 내용 요약

계획한 순서대로 구현했다. 통합 과정에서 예상보다 훨씬 많은 실측 디버깅이 필요했다 —
"실제 디버깅 경위" 절 참고.

1. **`liquid-metal-pass.js`(patch-package로 신규 추가)**: `PassNode`를 확장한 새 pass
   클래스. 뷰포트 좌상단에 겹치되 `opacity:0`으로 숨긴 `<div>`에 `@paper-design/shaders`의
   `ShaderMount`(순수 JS 클래스)를 마운트해 GLSL 셰이더를 실행하고, 그 WebGL 캔버스를 매
   프레임 일반 2D 캔버스로 복사(`drawImage`)한 뒤 그 2D 캔버스를 `THREE.CanvasTexture`로
   감싸 WebGPU 파이프라인에 공급한다.
2. **`pipeline-manager.js` patch**: `layer.kind === "source" && layer.type ===
   "liquid-metal"` 케이스를 추가해 `LiquidMetalPass`를 생성하도록 연결.
3. **`types.d.ts` patch**: 공개 타입 `ShaderLabSourceLayerType`에 `"liquid-metal"`을 추가
   (없으면 `effectRegistry.ts`의 `as const satisfies readonly ShaderLabLayerType[]` 체크가
   타입 에러를 낸다).
4. **`package.json`**: `@paper-design/shaders`(코어, react 아님) 의존성을 `0.0.80` 정확한
   버전으로 명시 추가 — 지금까지는 `shaders-react`의 전이 의존성으로만 존재해 직접 import가
   버전 관리 밖에 있었다.
5. **`effectRegistry.ts`**: `liquid-metal` 소스 레이어 타입과 10개 파라미터 필드(모양·배경
   색상·틴트 색상·줄무늬 밀도·경계 부드러움·왜곡·윤곽 강도·방향·빨강/파랑 채널 분산) 추가.
   기본값은 라이브러리 내장 "Backdrop" 프리셋을 기준으로 채웠다.
6. **`THIRD_PARTY_NOTICES.md`**: `@basementstudio/shader-lab` 패치 고지 보강(신규 파일
   추가 사실) + `@paper-design/shaders`의 Apache License 2.0 NOTICE 신규 고지.

### 실제 디버깅 경위 (실측 검증 도중 발견한 문제 3건)

esbuild+playwright로 초기 실측을 돌렸을 때 "빈 화면 아님(notBlank)=true"인데도 애니메이션이
전혀 흐르지 않고 `shape` 변경도 결과에 영향을 주지 않아, 렌더링 자체가 실패했음을 발견했다.
원인을 좁혀나간 과정:

1. **`u_image` 유니폼 타입 오류**: 코어 `ShaderMount`(react 래퍼가 아닌 순수 클래스)에
   문자열(URL)을 넘겼더니 "Unsupported uniform type for u_image: string" 경고만 내고
   무시했다 — react 패키지의 `ShaderMount` 컴포넌트가 문자열→`HTMLImageElement` 변환을
   대신 해줄 뿐, 코어 클래스 자체엔 그 로직이 없다. **수정**: 1x1 투명 픽셀을 `new Image()`로
   미리 로드해 `HTMLImageElement` 인스턴스로 넘기도록 고쳤다.
2. 이 수정 후에도 여전히 빈 화면이었다. **`shaderMount.canvasElement`(WebGL 캔버스)에
   직접 `readPixels`를 실행**해보니 배경색(`#AAAAAC` → `[170,170,172,255]`)이 정확히
   렌더링되고 있음을 확인했다 — WebGL 셰이더 자체는 완벽했다. 문제는 그 이후, WebGPU
   파이프라인으로 텍스처를 넘기는 단계였다.
3. `THREE.CanvasTexture`가 WebGL 컨텍스트를 가진 canvas를 직접 소스로 받았을 때
   WebGPURenderer 쪽에서 텍스처가 갱신되지 않는 것으로 의심해, text-pass.js가 이미 검증한
   "2D 캔버스 브릿지" 패턴(매 프레임 `drawImage`로 일반 2D 캔버스에 복사한 뒤 그 2D 캔버스를
   텍스처 소스로 사용)으로 우회했다 — 이 mirror canvas의 `getImageData`로도 RGBA가
   `[170,170,172,255]`로 정확함을 재확인했다. 그런데도 여전히 최종 WebGPU 출력은 검정이었다.
4. **진짜 근본 원인(핵심 버그)**: `PassNode`의 생성자(`super(layerId)`)가 실행되는 도중
   서브클래스의 `buildEffectNode()`가 이미 한 번 호출된다는 것을 코드 추적으로 발견했다.
   그 시점엔 서브클래스 생성자가 아직 `this.textureNode`를 설정하기 전(`undefined`)이라,
   `buildEffectNode()`의 `if (!this.textureNode)` 분기(완전 투명한 `vec4(0,0,0,0)`)로
   `effectNode`/`material.colorNode`가 **영구 고정**돼버렸다 — 이후 `this.textureNode`
   필드를 설정해도 이미 굳어진 노드 그래프는 바뀌지 않는다. `text-pass.js`/`fluid-pass.js`가
   생성자 마지막에 `this.rebuildEffectNode()`를 명시적으로 다시 호출하는 이유가 바로 이것
   이었다 — 이 한 줄을 빠뜨렸던 것이 근본 원인이었다. 추가 후 즉시 정상 렌더링 확인.

이 과정에서 시도했다가 근본 원인이 아니었던 것으로 판명된 수정도 있다: `preserveDrawingBuffer:
true`(WebGL 백버퍼 보존 — 이론적으로 필요한 이유는 있지만 이번 증상의 원인은 아니었다),
`generateMipmaps`/`minFilter` 등 텍스처 필터 명시(text-pass.js와 동일하게 맞췄지만 이것도
원인이 아니었다), `textureNode.value`를 매 프레임 재할당(원인 아님). 셋 다 안전한 방어적
설정이라 코드에는 그대로 남겨뒀다(`preserveDrawingBuffer`·텍스처 필터 명시).

### 변경된 파일
```
jam-web/package.json
jam-web/package-lock.json
jam-web/patches/@basementstudio+shader-lab+3.0.2.patch (신규)
jam-web/THIRD_PARTY_NOTICES.md
jam-web/src/lib/admin/shaderLab/effectRegistry.ts
```

### 테스트 결과
- [x] `npx tsc --noEmit` — 클린
- [x] `npx eslint .`(전체) — 에러 0건, 경고 13건(기존 baseline)
- [x] `npx vitest run` — 1452/1455 통과. 실패 3건은 이번 변경과 무관한 기존 실패
      (`gate-family-options-contract.test.ts`, 별도 티켓 20260913_1852 — 이 브랜치는
      origin/staging에서 분기해 아직 그 티켓이 반영되지 않은 상태라 여전히 나타난다)
- [x] **postinstall 자동 재적용 검증**: `@basementstudio/shader-lab` 패키지 디렉터리를
      삭제 후 `--ignore-scripts`로 원본 재설치 → `npm run postinstall` 실행 → 3개 파일
      (신규 `liquid-metal-pass.js`, 수정된 `pipeline-manager.js`/`types.d.ts`) 모두
      정상 재적용됨을 확인
- [x] **실제 렌더링 실측**(esbuild+playwright, `@basementstudio/shader-lab` 실제
      WebGPU 런타임 사용):
      - shape="none"(전체 화면) 레이어가 실제로 화면을 채움(빈 화면 아님)을 확인
      - 시간이 흘러도 프레임이 계속 바뀜(줄무늬 애니메이션이 흐름)을 확인 — 근본 버그
        수정 전에는 이 항목이 계속 실패했다
      - shape="diamond"로 바꾸면 shape="none"과 다른 결과가 나옴을 확인(파라미터 반영
        확인) — 두 케이스 모두 이미지 로드(비동기)가 끝난 뒤 충분한 프레임을 진행한
        시점끼리 비교해야 공정한 비교가 됨을 실측 과정에서 확인(초기 로딩 프레임끼리
        비교하면 둘 다 검게 나와 "차이 없음"으로 오판될 수 있음)
      - `dispose()` 호출 시 숨겨진 `<div>` 엘리먼트가 실제로 DOM에서 제거됨을 확인
        (dispose 전 2개 → dispose 후 0개)
      - 스크린샷으로 리퀴드 메탈 특유의 금속 질감 줄무늬 패턴이 정확히 렌더링됨을 육안
        확인(shape=none/diamond 둘 다)

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

- [x] 용어 일관성: 신규 필드 라벨(모양·배경 색상·틴트 색상·줄무늬 밀도·경계 부드러움·왜곡·
      윤곽 강도·방향·빨강/파랑 채널 분산)은 기존 다른 레이어(플루이드·메시 그라디언트 등)의
      용어 패턴을 그대로 따랐다
- [x] 톤앤매너: 해당 없음(어드민 도구 내부 라벨)
- [x] 에러 메시지: 신규 에러 메시지 없음
- [x] 문장 규칙: 필드 설명("전체 화면을 채우려면 '없음'을 선택하세요.")은 해요체 유지
- [x] 표기 규칙: 해당 없음

### 배포 정보
- 배포일: 2026-09-13 (staging 병합·push 완료, 오케스트레이터)
- 환경: staging (`stage.j-a-m.app`) - 프로덕션(main) 승격은 이 티켓 범위 밖, `/jam-ship`
  진행 시 사용자 승인 필요
- 커밋: `203874c4`(티켓 20260913_1901과 통합 머지 — 아래 "병합 시 추가 반영" 절 참고)

### 주요 의사결정 / 핵심 메모

- **이미지 업로드 제외**: 원본 `LiquidMetal`의 핵심 용도(로고를 리퀴드 메탈로 감싸기)를
  포기하고 `shape` 프리셋 기반으로만 구현했다 — `toProcessedLiquidMetal()`의 비동기 canvas
  전처리(edge gradient 추출)가 무거워 범위를 넘어선다고 판단했다. 필요하면 후속 티켓으로
  확장 가능.
- **오프스크린 WebGL→WebGPU 브릿지 방식 채택**: GLSL을 TSL로 재작성(630줄 분량, 포아송
  디스크 샘플링 등 복잡한 로직 포함)하는 대신, 원본 라이브러리를 그대로 npm 의존성으로
  설치해 별도 WebGL 캔버스에서 실행하고 텍스처로 브릿지했다 — 원본과 동일한 시각적 결과를
  보장하면서 재구현 리스크를 없앴다.
- **2D 캔버스 미러링이 필요했던 이유**: `THREE.CanvasTexture`가 WebGL 컨텍스트를 가진
  canvas를 직접 소스로 받으면 WebGPU 렌더러 쪽에서 텍스처가 갱신되지 않는 현상을 실측으로
  확인했다(정확한 내부 메커니즘까지는 규명하지 못했으나, 재현 가능한 회피책은 확인함).
  매 프레임 CPU 사이드 `drawImage` 복사가 들지만, 브라우저의 canvas-to-canvas 복사는
  대체로 GPU 가속되어 실사용 성능에 문제가 없을 것으로 판단했다(실측상 애니메이션이
  매끄럽게 흐름을 확인).
- **`rebuildEffectNode()` 누락이 근본 원인이었던 교훈**: `PassNode`를 상속하는 새 pass를
  작성할 때, 생성자에서 `super()` 호출 시점에 이미 서브클래스의 `buildEffectNode()`가
  한 번 호출된다는 사실(JS 클래스 생성 시점의 다형성 특성)을 놓치기 쉽다 — 필드를 설정한
  뒤 반드시 `this.rebuildEffectNode()`를 명시적으로 재호출해야 한다는 것을 text-pass.js/
  fluid-pass.js 소스에서 이미 그렇게 하고 있었음에도 처음엔 놓쳤다. 향후 이 패키지에 새
  pass를 추가할 때 동일한 함정에 빠지지 않도록 이 완료 기록에 명시해둔다.

### 병합 시 추가 반영 (게이트 리뷰 WARN 반영, 티켓 20260913_1901과 통합)

1차 게이트 리뷰가 WARN으로 지적한 사항 셋을 병합 커밋(203874c4)에서 함께 처리했다:

1. **패치 파일 충돌 해소**: 이 티켓과 20260913_1901이 `patch-package`로 같은 패치 파일
   (`patches/@basementstudio+shader-lab+3.0.2.patch`)을 독립적으로 만들어놔서, staging에
   함께 병합하면 git이 add/add 충돌로 잡을 상황이었다. 두 review 브랜치를 먼저 머지하고
   (`claude/jamwork-20260913_1948-shaderlab-liquid-metal`에 1901 브랜치를 머지), 두 패치를
   `node_modules`에 모두 적용한 뒤 `patch-package`를 재실행해 4개 파일(신규
   `liquid-metal-pass.js`, 수정된 `pipeline-manager.js`/`types.d.ts`/`text-pass.js`) 전부가
   담긴 하나의 통합 patch 파일로 재생성했다.
2. **`buildLiquidMetalUniforms()`의 fallback 기본값 일치**: 라이브러리 "Default" 프리셋
   기준이던 fallback을 `effectRegistry.ts`의 UI 기본값("Backdrop" 프리셋: shape=none/
   colorBack=#AAAAAC/colorTint=#ffffff/repetition=1.5/softness=0.05/distortion=0.1/
   contour=0.4/angle=90/shiftRed=0.3/shiftBlue=0.3)과 정확히 일치시켰다.
3. **`dispose()` 레이스 컨디션 방어**: `media-pass.js`의 `mediaLoadNonce`와 같은 목적의
   `disposed` 플래그를 추가해, 생성자가 시작한 비동기 이미지 로드 콜백이 `dispose()` 이후
   실행돼 새 `ShaderMount`를 만들어버리는 걸 막았다.

재게이트 리뷰(conservative-reviewer)가 통합 패치 파일의 4개 diff 전부가 병합 과정에서
손상 없이 보존됐음을 바이트 단위로 확인했고, postinstall 재현성도 원본 재설치→재적용으로
직접 재현해 파일 해시 일치를 확인했다. `THIRD_PARTY_NOTICES.md`도 두 티켓의 패치 고지를
한 섹션으로 통합했다(같은 npm 패키지에 대한 패치라 patch-package 관례상 파일이 하나임을
명시).

### 잔여 이슈
- 없음
