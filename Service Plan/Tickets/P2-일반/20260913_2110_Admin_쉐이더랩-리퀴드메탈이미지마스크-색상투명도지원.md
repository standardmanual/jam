---
id: 20260913_2110
category: Admin
priority: P2
status: OPEN
created: 2026-09-13
---

# [Admin] 쉐이더 랩 — 리퀴드 메탈 이미지 마스크 지원 + 텍스트/리퀴드 메탈 색상 투명도

## 배경 / 문제 정의

티켓 20260913_1948에서 만든 리퀴드 메탈 레이어를 사용자 피드백에 따라 다음과 같이
수정한다:

1. **배경 전체 적용은 유지** — 이미지를 올리지 않으면 지금처럼 전체 화면에 적용된다.
2. **이미지(벡터 포함)를 올리면 그 이미지 위에 적용** — 원본 라이브러리의 핵심 기능
   (`toProcessedLiquidMetal()`)을 이번엔 실제로 통합한다. 이전 티켓(20260913_1948)에서는
   구현 복잡도 때문에 범위에서 제외했었다.
3. **`shape`(모양) 파라미터를 완전히 제거** — 원/데이지/다이아몬드/메타볼 프리셋은 더 이상
   필요 없다(이미지 유무로만 분기).
4. ~~텍스트 레이어 위에서는 텍스트에 적용~~ — 사용자가 취소함(범위 밖).
5. **배경색(`colorBack`)에 투명도 조절 추가.**

또한 별도로, "모든 색상 설정에 투명도를 지원해달라"는 요청을 조사한 결과, 원본
`@basementstudio/shader-lab`의 TSL 기반 레이어(그라디언트·하프톤·ASCII·디더링 등,
색상 필드 약 30개)는 `hexToRgb`류 유틸이 hex를 항상 6자리로 잘라 알파를 구조적으로
버린다는 것을 확인했다 — 전체 지원은 각 pass 파일을 개별 patch해야 하는 매우 큰 작업이라
사용자와 협의해 **텍스트 레이어(`textColor`)와 리퀴드 메탈(`colorBack`) 두 곳으로만
범위를 좁혔다.**

## 원본 조사 결과 (근거)

- **Canvas 2D의 8자리 hex(`#RRGGBBAA`) 지원**: 실측 확인 — `ctx.fillStyle = '#ff000080'`로
  그린 픽셀의 alpha가 정확히 128(50%)로 나옴. 텍스트 레이어(`text-pass.js`)는 Canvas 2D
  `fillStyle`을 그대로 쓰므로 **패치 없이 UI에서 8자리 hex를 넘기기만 하면 `textColor`
  알파가 그대로 반영된다.**
- **`@paper-design/shaders`의 `getShaderColorFromString`**: 이미 8자리 hex를 완전히
  지원함(코드 확인 완료, 티켓 20260913_1948에서 이미 검증됨) — 리퀴드 메탈의 `colorBack`도
  패치 없이 UI 변경만으로 알파가 반영된다.
- **`toProcessedLiquidMetal(file)`**: 순수 브라우저 API(Canvas 2D, `Image`)로 동작하며
  SVG를 명시적으로 지원한다(`isSVG` 분기, `svgMaxSize=4096`). 포아송 방정식을 512px
  작업 크기·40회 반복(SOR)으로 풀어 이미지 형태의 부드러운 엣지 마스크를 만들고,
  `canvas.toBlob()`으로 PNG blob을 반환한다 — Web Worker 없이 메인 스레드에서 동작한다.
- **원본 `LiquidMetal` React 컴포넌트의 이미지 처리**: `toProcessedLiquidMetal(url).then(
  (result) => URL.createObjectURL(result.pngBlob))`로 처리된 PNG를 Object URL로 만들어
  `u_image`에 넘긴다. 코어 `ShaderMount` 클래스는 문자열 URL을 받으면 무시하고 경고만
  내므로(20260913_1948에서 이미 확인), Object URL을 `new Image()`로 로드해
  `HTMLImageElement`로 변환한 뒤 넘겨야 한다(투명 픽셀 처리와 동일한 패턴).
- **기존 image 레이어의 asset 업로드 흐름**: `ShaderLabPropertiesPanel.tsx`가
  `layer.type === 'image'`일 때 파일 입력을 보여주고 `patch({ asset: { kind: 'image', src,
  fileName } })`로 저장한다. `pipeline-manager.js`의 `applyLayerState()`는
  `pass instanceof MediaPass`일 때만 `asset`을 `setMedia()`로 전달하는 특별 분기가 있다 —
  `LiquidMetalPass`에도 동일한 성격의 분기(`pass instanceof LiquidMetalPass`)를 patch로
  추가해야 한다.

## 상세 요구사항

### 리퀴드 메탈 — 이미지 마스크 지원

1. `effectRegistry.ts`의 `liquidMetalFields`에서 `shape` select 필드 제거.
2. `ShaderLabPropertiesPanel.tsx`에서 `layer.type === 'liquid-metal'`일 때도 이미지 업로드
   UI를 노출(문구는 image 레이어와 다르게 — "이미지를 올리면 그 형태에 리퀴드 메탈을
   입혀요. 올리지 않으면 배경 전체에 적용돼요").
3. `liquid-metal-pass.js` patch:
   - `setImageAsset(src)` 메서드 신규 추가 — `toProcessedLiquidMetal(src)` 호출 →
     `pngBlob`을 Object URL로 변환 → `new Image()`로 로드해 `HTMLImageElement`로 확보,
     원본 종횡비(`imageData.width / imageData.height`)도 함께 저장.
   - 이미지가 있으면 `u_image=업로드이미지`, `u_isImage=true`,
     `u_imageAspectRatio=저장한종횡비`. 없으면 기존처럼 투명 픽셀 + `u_isImage=false`
     (배경 전체 모드).
   - `u_shape`는 항상 `LiquidMetalShapes.none`(0)으로 고정.
4. `pipeline-manager.js` patch: `applyLayerState()`에 `pass instanceof LiquidMetalPass`일
   때 `asset`을 `pass.setImageAsset(asset?.src ?? null)`로 전달하는 분기 추가(MediaPass
   분기와 병렬).

### 색상 투명도 (텍스트 레이어 `textColor` + 리퀴드 메탈 `colorBack`)

5. `paramFields.ts`의 `ColorFieldDefinition`에 `alpha?: boolean` 옵션 추가(기본
   false — 나머지 36개 색상 필드는 영향 없음).
6. `ShaderLabParamField.tsx`의 `type === 'color'` 렌더러에서 `field.alpha`가 true면
   hex6 컬러피커 + 알파 슬라이더(0~1) 조합 UI로 렌더링(내부 값은 8자리 hex
   `#RRGGBBAA`로 합쳐서 저장). false/undefined면 기존 그대로.
7. `effectRegistry.ts`: `textFields`의 `textColor`에 `alpha: true`(기본값 `#ffffffff`),
   `liquidMetalFields`의 `colorBack`에 `alpha: true`(기본값 `#AAAAACFF`).
8. `text-pass.js`/`liquid-metal-pass.js`는 패치 불필요(위 조사 결과대로 8자리 hex를
   그대로 통과시키면 런타임이 알파를 이미 반영함) — 실측으로 재확인만 한다.

## 구현 계획

1. `paramFields.ts`/`ShaderLabParamField.tsx`에 알파 지원 UI 인프라 추가.
2. `effectRegistry.ts`: `textColor`/`colorBack`에 `alpha: true` 지정, `liquidMetalFields`
   에서 `shape` 제거.
3. `liquid-metal-pass.js` patch: `setImageAsset()` 추가, shape 로직 제거.
4. `pipeline-manager.js` patch: `LiquidMetalPass`용 asset 전달 분기 추가.
5. `ShaderLabPropertiesPanel.tsx`: 리퀴드 메탈에도 이미지 업로드 UI 노출.
6. esbuild+playwright 실측:
   - PNG 이미지 업로드 시 그 형태에 리퀴드 메탈이 입혀지는지(이미지 없을 때와 렌더링
     결과가 다른지)
   - SVG 이미지 업로드도 정상 처리되는지(라이브러리의 SVG 특별 처리 분기 확인)
   - `colorBack` 알파를 0.3 정도로 낮췄을 때 실제로 배경이 투명하게(아래 레이어가
     비쳐 보이거나, 최종 알파 채널 값이 낮게) 나오는지
   - 텍스트 레이어 `textColor` 알파를 낮췄을 때 실제로 텍스트가 반투명하게 나오는지
   - 이미지 asset을 제거(다시 없앰)했을 때 배경 전체 모드로 정상 복귀하는지
7. `npx tsc --noEmit`, `npm run lint`, `npx vitest run` 재확인.

## 유예된 판단

- 나머지 약 30개 색상 필드(그라디언트·하프톤·ASCII·디더링 등)의 투명도 지원은 이번
  범위 밖(원본 런타임이 알파를 구조적으로 버리는 큰 제약, 사용자와 협의해 제외 확정).
- 리퀴드 메탈의 `colorTint`는 투명도 요청 대상이 아니었으므로 그대로 둔다.

---
## 완료 기록

### 구현 내용 요약

1. **색상 투명도 인프라**: `ColorFieldDefinition`에 `alpha?: boolean` 옵션을 추가했다.
   `true`면 `ShaderLabParamField.tsx`의 color 렌더러가 hex6 컬러피커 + 알파 슬라이더(0~1,
   1% 단위 표시) 조합으로 그리고, 두 값을 합쳐 8자리 hex(`#RRGGBBAA`)로 저장한다. 기본값
   `false`는 기존 6자리 hex 렌더링 그대로라 다른 색상 필드(약 36개)는 영향받지 않는다.
2. **적용 범위**: `effectRegistry.ts`에서 텍스트 레이어 `textColor`(`alpha: true`, 기본값
   `#ffffffff`)와 리퀴드 메탈 `colorBack`(`alpha: true`, 기본값 `#AAAAACFF`) 두 곳에만
   켰다 — 원본 `@basementstudio/shader-lab`의 TSL 기반 레이어 대부분은 `hexToRgb`류
   유틸이 hex를 항상 6자리로 잘라 알파를 구조적으로 버리는 반면, 텍스트 레이어는 Canvas 2D
   `fillStyle`(8자리 hex 표준 지원, 실측 확인), 리퀴드 메탈은 `getShaderColorFromString`
   (8자리 hex 완전 지원, 코드 확인)을 써서 패치 없이 알파가 그대로 반영되기 때문이다.
3. **리퀴드 메탈 이미지 마스크**: `liquid-metal-pass.js`를 patch해 `setImageAsset(src)`를
   추가했다 — `toProcessedLiquidMetal(src)`(`@paper-design/shaders`, 순수 브라우저 API)로
   이미지를 포아송 방정식 기반 엣지 마스크 PNG로 변환한 뒤 Object URL → `HTMLImageElement`로
   로드해 `u_image`/`u_isImage=true`/`u_imageAspectRatio`로 셰이더에 넘긴다. 이미지가 없으면
   기존처럼 투명 픽셀 + `u_isImage=false`(배경 전체 모드)를 유지한다. `u_shape`는 항상
   `LiquidMetalShapes.none`으로 고정하고 `effectRegistry.ts`에서 `shape` select 필드를
   완전히 제거했다(원/데이지/다이아몬드/메타볼 프리셋 폐기).
4. **파이프라인 연결**: `pipeline-manager.js`의 `applyLayerState()`에 `pass instanceof
   LiquidMetalPass` 분기를 추가해(`MediaPass` 분기와 병렬) 레이어의 `asset`을
   `pass.setImageAsset(asset?.kind === 'image' ? asset.src : null)`로 전달한다.
   `createLayerSignature()`가 이미 `layer.asset?.src`를 시그니처에 포함하고 있어서, 이미지
   업로드·교체·제거 모두 자동으로 dirty 감지되어 재적용된다.
5. **UI**: `ShaderLabPropertiesPanel.tsx`에서 `layer.type === 'liquid-metal'`일 때도
   이미지 업로드 UI를 노출한다(선택 사항, "제거" 버튼 포함). 안내 문구는 이미지 유무에 따라
   "올린 이미지의 형태에 리퀴드 메탈이 입혀져요(벡터 이미지도 지원해요)." /
   "이미지를 올리지 않으면 배경 전체에 적용돼요."로 분기한다.
6. **레이스 컨디션 방어**: 비동기 이미지 로드 도중 레이어가 dispose되거나 다른 이미지로
   빠르게 교체되는 경우를 막기 위해 `imageLoadNonce` 카운터(다른 pass 파일들의 기존 패턴과
   동일)와 `this.disposed` 가드를 둬, 응답이 늦게 도착한 이전 요청이 최신 상태를 덮어쓰지
   않게 했다.

### 변경된 파일
```
jam-web/patches/@basementstudio+shader-lab+3.0.2.patch  (liquid-metal-pass.js·pipeline-manager.js patch 갱신)
jam-web/src/app/admin/shader-lab/ShaderLabParamField.tsx  (color 필드 alpha 분기 UI 추가)
jam-web/src/app/admin/shader-lab/ShaderLabPropertiesPanel.tsx  (리퀴드 메탈 이미지 업로드 UI 추가)
jam-web/src/lib/admin/shaderLab/effectRegistry.ts  (textColor/colorBack alpha 지정, shape 필드 제거)
jam-web/src/lib/admin/shaderLab/paramFields.ts  (ColorFieldDefinition.alpha 옵션 추가)
```

### 테스트 결과
- [x] `npx tsc --noEmit` — 오류 없음
- [x] `npx eslint`(변경 파일 4개) — 경고·오류 없음
- [x] `npx vitest run` — 1452 passed / 3 failed(전부 `gate-family-options-contract.test.ts`,
      본 작업과 무관한 기존 이슈 — 티켓 20260913_1852에서 별도 추적 중)
- [x] esbuild+playwright 실측(워크트리는 `next dev`/`build` 구조적으로 불가해 이 패턴을
      계속 사용한다):
  - PNG 도넛 마스크(중앙에 알파=0 구멍) 적용 시 중앙 픽셀이 `colorBack` 기본값
    `(170,170,172)`에 정확히 고정되고 8프레임에 걸쳐 분산 0.000 — 이미지 밖 영역은
    배경색으로 고정되고 스트라이프 애니메이션이 적용되지 않음을 확인
  - SVG(evenodd fill-rule 도넛) 마스크도 동일하게 중앙 픽셀 `(170,170,172)` 고정, 분산
    0.000 — `toProcessedLiquidMetal()`의 `isSVG` 특별 처리 분기(`svgMaxSize=4096` 업스케일)
    가 정상 동작함을 확인
  - 이미지 없음 상태(배경 전체 모드)에서는 중앙 픽셀이 8프레임 동안
    `(32,33,47)→(86,87,100)`으로 계속 변화(분산 2620~2780) — 스트라이프 애니메이션이 캔버스
    전체(중앙 포함)에 적용됨을 확인
  - asset 제거 후 같은 중앙 픽셀 분산이 2870 수준으로 즉시 회복 — 배경 전체 모드로 정상
    복귀함을 확인(이번 세션에서 새로 검증한 부분 — 이전 시도는 코너 픽셀 기준이라 캔버스
    테두리 감쇠 마스크에 가려 분산 차이가 드러나지 않았다. 원인 규명 후 캔버스 정중앙 픽셀
    기준으로 재설계해 해결)
  - `colorBack` 알파 0.1~1.0 비교(20260913_1948에서 이미 검증 완료분 재확인) — 코너 픽셀
    RGB 합이 알파값에 따라 다르게 나옴을 확인
  - `textColor` 알파 1.0/0.2 비교(20260913_1948에서 이미 검증 완료분 재확인) — 가장 밝은
    픽셀 RGB가 알파에 비례해 낮아짐을 확인(255→153)

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

- [x] 용어 일관성: "이미지(선택)" · "이미지 선택" · "제거" — 기존 image 레이어 UI와
      동일한 용어 사용
- [x] 톤앤매너: 어드민 도구 설정 UI라 중립적 톤 유지
- [x] 에러 메시지: 이번 변경에 신규 에러 메시지 없음(이미지 처리 실패는 조용히 무시하고
      이전 상태를 유지 — 다른 pass 파일들의 기존 패턴과 동일)
- [x] 문장 규칙: 해요체("올린 이미지의 형태에 리퀴드 메탈이 입혀져요", "배경 전체에
      적용돼요"), 마침표 정확
- [x] 표기 규칙: 해당 없음(날짜·시간·금액 없음)

### 배포 정보
- 배포일: (staging 병합·push 시점에 기록)
- 환경: staging
- 커밋: (병합 후 기록)

### 주요 의사결정 / 핵심 메모

- **"모든 색상에 투명도" 요청을 "텍스트+리퀴드 메탈"로 좁힘**: 사용자가 처음 "모든 색상
  설정에 투명도"를 요청했으나, 원본 런타임의 그라디언트·하프톤·ASCII·디더링 등 약 30개
  색상 필드가 `hexToRgb`류 유틸로 hex를 항상 6자리로 잘라 알파를 구조적으로 버린다는 조사
  결과를 보고한 뒤 사용자가 범위를 직접 좁혔다. 전체 지원은 각 pass 파일을 개별 patch해야
  하는 훨씬 큰 작업이라 이번 범위에서 제외했다(유예된 판단 절 참고).
- **`shape` 프리셋 완전 제거**: 이미지 유무로만 분기하도록 사용자가 명시적으로 요청해
  원/데이지/다이아몬드/메타볼 4종 프리셋을 되살릴 계획 없이 삭제했다. `u_shape` uniform은
  셰이더 시그니처 호환을 위해 남기되 항상 `LiquidMetalShapes.none`으로 고정한다.
- **"텍스트 레이어 위에서는 텍스트에 적용" 취소**: 사용자가 최초 요청 중 이 항목만 별도로
  취소해 리퀴드 메탈은 배경/이미지 마스크 두 모드로만 남았다 — 텍스트 레이어 자체에 리퀴드
  메탈 텍스처를 입히는 기능은 없다.
- **asset 제거 실측 재설계(이번 세션 핵심 디버깅)**: 처음엔 코너 픽셀(0,0)의 시간에 따른
  분산으로 "이미지 제거 → 배경 전체 모드 복귀"를 검증하려 했으나, 이미지 마스크 있음/없음
  두 상태 모두 분산이 0으로 나와 구분이 안 됐다. 셰이더 소스(`liquid-metal.js`)를 직접
  분석한 결과, "배경 전체" 모드의 코너 영역은 `pixel_thickness = min(250/캔버스크기, 0.5)`
  로 계산되는 테두리 그라디언트 마스크의 영향권 안에 있어(작은 캔버스일수록 이 값이 상한
  0.5에 도달해 캔버스 절반 가까이가 감쇠 영향을 받음) 코너 자체가 정적이라는 근본 원인을
  찾았다. 반대로 캔버스 정중앙은 이 마스크가 항상 완전히 벗어나 있어(`smoothstep`이 항상
  포화) 배경 전체 모드에서는 always-animated, 이미지 모드에서는 이미지 알파에 따라
  0/1로 결정되는 깨끗한 이분법적 신호가 된다. 중앙 픽셀 기준으로 재설계해 이 문제를 해결했다.
- **디버그 로그 임시 추가·제거**: 위 재설계 전에 `liquid-metal-pass.js`에
  `window.__liquidMetalDebugInfo` 임시 로그를 넣어 `setImageAsset()`/`resolveImageState()`가
  코드 레벨로는 이미 정상 동작하고 있음을 먼저 확인한 뒤, 그 확인을 바탕으로 검증 방법론만
  다시 설계했다. 디버그 로그는 확인 직후 제거하고 patch를 재생성했다(운영 코드에 남지 않음).

### 잔여 이슈
- 나머지 약 30개 색상 필드(그라디언트·하프톤·ASCII·디더링 등)의 투명도 지원은 이번 범위
  밖으로 유예됐다 — 필요해지면 각 pass 파일의 hex 파싱 유틸을 개별 patch해야 한다.
