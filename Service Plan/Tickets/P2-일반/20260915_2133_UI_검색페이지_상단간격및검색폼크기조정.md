---
id: 20260915_2133
category: UI
priority: P2
status: CLOSED
created: 2026-09-15
closed: 2026-09-15
---

# [UI] 검색 페이지 TopNav-검색폼 간격 추가 + 검색폼 크기 축소

## 배경 / 문제 정의
`/search` 페이지(`https://j-a-m.app/search`)에서 두 가지 문제가 있다 (사용자 실사용 피드백):
1. TopNav와 검색창 사이에 간격이 전혀 없다 (`pt-0`)
2. 검색폼(입력폼 + 버튼) 자체가 너무 크다 — 체감상 30% 정도 줄여도 될 정도

## 상세 요구사항

### UI/UX 관점
- 대상 파일: `jam-web/src/app/(main)/search/page.tsx`,
  `jam-web/src/app/(main)/UserSearchBar.tsx`
- **간격**: `search/page.tsx`의 컨테이너 `pt-0`을 다른 페이지(홈 `page.tsx`, `inventory/page.tsx`
  등)와 동일한 `pt-[var(--spacing-24)]`로 변경해 TopNav와 검색폼 사이 간격을 준다
- **검색폼 크기**: 세로 크기(패딩·버튼 높이)를 약 30% 축소. **가로폭은 유지한다**
  (`flex-1 min-w-0` 등 가로 관련 클래스는 그대로 둔다)
- **주의**: `UserSearchBar` 컴포넌트는 홈 화면(`(main)/page.tsx`)에서도 재사용된다.
  컴포넌트 자체를 직접 줄이면 홈 화면 검색바도 함께 작아지므로, `/search` 페이지에서만
  작게 렌더링되도록 컴포넌트에 옵션(예: `size` prop)을 추가하고 홈 화면 호출부는 기존
  동작 그대로 유지되게 한다

### UI 재사용 판정 (오케스트레이터 결정)
기존 서비스 컴포넌트(`UserSearchBar`, `Button`)를 그대로 확장 — MODULAR `Button.tsx`에
이미 `xs` 사이즈(조밀한 컨텍스트 전용, 44px 규칙의 명시적 예외)가 정의되어 있어 신규
컴포넌트나 MODULAR 확장 없이 기존 `size` prop 체계로 해결 가능. 신규 UI 추가가 아니므로
디자인 시스템 탐색 절차는 이 판단으로 대체한다.

## 구현 계획
- `UserSearchBar`에 `size?: 'default' | 'compact'`(기본값 `'default'`) prop 추가
  - `compact`일 때: 폼 컨테이너 패딩 축소(`p-[var(--spacing-8)]` → 더 작은 값),
    입력 세로 패딩 축소(`py-2` → 더 작은 값), 버튼을 `size="xs"`로 전환
  - `default`(생략 시)는 기존 마크업과 동일 — 홈 화면 영향 없음
- `search/page.tsx`에서 `<UserSearchBar defaultValue={q ?? ''} size="compact" />`로 호출,
  컨테이너 `pt-0` → `pt-[var(--spacing-24)]`로 수정
- 가로폭에 영향 주는 클래스(`flex-1`, `min-w-0`, 컨테이너 `px-[var(--spacing-16)]`)는
  건드리지 않는다

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
- `search/page.tsx` 컨테이너 `pt-0` → `pt-[var(--spacing-24)]`로 변경해 TopNav-검색폼 간격 확보
- `UserSearchBar`에 `size?: 'default' | 'compact'`(기본값 `'default'`) prop 추가
  - `compact`: 폼 컨테이너 패딩 `p-[var(--spacing-8)]` → `p-[var(--spacing-4)]`,
    입력 세로 패딩 `py-2` → `py-1`, 버튼 `size="sm"` → `size="xs"`
  - `default`는 기존 마크업 그대로 — 다른 호출부 영향 없음(현재 `search/page.tsx`가 유일한 호출부)
- `search/page.tsx`에서 `<UserSearchBar ... size="compact" />`로 호출
- 가로폭 관련 클래스(`flex-1`, `min-w-0`, 컨테이너 `px-[var(--spacing-16)]`)는 변경하지 않음

### 변경된 파일
```
jam-web/src/app/(main)/UserSearchBar.tsx
jam-web/src/app/(main)/search/page.tsx
```

### 테스트 결과
- [x] `npm run lint` 전체 실행 — 0 errors, 15 warnings(모두 기존 파일, 변경 파일과 무관)
- [x] `npm run typecheck` 통과
- [x] 오케스트레이터가 review 브랜치를 로컬에 체크아웃해 `next dev`로 실렌더 확인 —
      TopNav-검색폼 간격 정상 확보, 버튼 실측 높이 34.8px(기존 44px에서 약 21% 축소),
      입력 필드 32px, 폼 전체 42.8px로 측정. 가로폭(398px, 페이지 컨테이너 기준)은 그대로 유지됨

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
- [x] 해당 없음 — 레이아웃·크기 조정만, 텍스트 변경 없음

### 배포 정보
- 배포일: 2026-09-15
- 환경: staging
- 커밋: (머지 커밋, staging push 예정)

### 주요 의사결정 / 핵심 메모
- 현재 `UserSearchBar`는 `search/page.tsx`에서만 렌더링된다(홈 화면은 검색 아이콘 링크로
  대체됨, 티켓 20260830_2030). 그래도 향후 재사용 대비 size prop 기본값을 `'default'`로 두어
  하위 호환을 유지했다.
- **접근성 트레이드오프 (사용자 명시 승인)**: 인터페이스 리뷰에서 `size="xs"` 버튼의 실제
  높이(34.8px)가 `Button.tsx` 자체 주석에 명시된 "44px 터치 타겟 규칙의 예외 — 본문 액션에는
  쓰지 않는다" 조건과 어긋난다는 지적이 있었다. 오케스트레이터가 이를 사용자에게 보고하고
  ①버튼은 44px 유지 + 패딩만 축소, ②요청대로 xs 유지(터치 영역 축소 감수) 두 옵션을 제시한
  결과, 사용자가 ②를 명시적으로 선택했다. 검색 버튼의 터치 영역이 표준보다 작아지는 점을
  인지한 상태의 의도적 선택이며, 향후 재검토가 필요하면 이 메모를 참고할 것.

### 잔여 이슈
-
