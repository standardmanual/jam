---
id: 20260915_2315
category: Content
priority: P2
status: OPEN
created: 2026-09-15
closed:
---

# [Content] SEO 문구 및 공용 컴포넌트 라벨 톤 정비

> 상위 스펙: [`20260915_2227_Service_사용자노출텍스트-톤앤매너통합정비-스펙.md`](../P1-중요/20260915_2227_Service_사용자노출텍스트-톤앤매너통합정비-스펙.md)
> 출처: 티켓 20260915_2237(공용 컴포넌트+SEO 텍스트 정비) 한국어 리뷰에서 나온 제안 — 게이트는 PASS했으나 병합 전 반영하지 않고 이 후속 티켓으로 분리하기로 사용자가 결정함(2026-09-15)

## 배경 / 문제 정의

티켓 20260915_2237에서 하드코딩 텍스트를 딕셔너리로 이관하는 작업(게이트 PASS) 자체는
끝났지만, 한국어 리뷰(`humanize-korean`)가 옮겨진 문구 표현 자체에서 다음을 지적했다.
가장 심각한 항목은 SEO description이 `UX_WRITING_GUIDELINE.md`의 핵심 철학·§1.1 공급자
언어 금지 원칙을 정면으로 위반한다는 점이다.

## 상세 요구사항

### 서비스/코드베이스 관점 — 딕셔너리 값만 수정 (구조 변경 없음)

1. **`d.common.seoDescription`** (`jam-web/src/app/layout.tsx` 경유) — 현재 "피지털
   게이미피케이션 플랫폼"은 공급자 언어·번역투 나열이라 사용자가 읽어도 서비스가 뭘 하는지
   알 수 없다. 검색 결과·SNS 공유 미리보기라는 노출 지점 특성상 영향이 크다.
   → 제안 대안: "운동 기록이 배지가 되는 곳. Strava 동기화로 오늘의 배지와 아이템을
   획득합니다." (§2.5 명사형/`-습니다`체 예외 유지, §7.3 엠대시·이모지 금지 준수)
2. **`d.common.loading`** (`LoadingSpinner` aria-label) — "로딩 중"은 §5.3이 명시적으로
   금지한 Bad 목록("Loading...")의 한글 표기다. → "불러오는 중"
3. **`d.map.*`** (`MapView` 마커 title·버튼 aria-label) — "현재 위치"/"현재 위치로 이동"은
   §1.2 사용자 시점 원칙상 1인칭이 더 맞는다. → "내 위치" / "내 위치로 돌아가기" (두 문구의
   앞부분 표기를 반드시 통일)
4. **`d.drops.poiCarouselListAriaLabel`** (`PoiCarouselModal`) — "주변 지점 목록"은 §1.1의
   기존 어휘("근처에 아이템이 떨어졌어요")와 톤이 어긋난다. → "근처 지점 목록"
5. **`d.itembooks.completed`** ("완성", `CollectionGridCard`) — 이 문구 자체는 기존 키
   재사용이라 위반이 아니지만, `UX_WRITING_GUIDELINE.md` §1.3 용어표에 아직 등재되지 않았다.
   **먼저 확인할 것**: 이 배지 라벨이 컬렉션 "전부 획득"을 뜻하는지 "전부 장착"을 뜻하는지
   코드·기획 의도를 확인한 뒤 — §1.3 「장착」 행이 "획득과 장착은 다른 단계이므로 섞어 쓰지
   않는다"고 명시하므로, 전자면 그대로 용어표에 「완성(COMPLETE)」 행을 신설하고, 후자면
   문구 자체를 다른 표현으로 바꿔야 한다.

### UI/UX 관점
해당 없음 — 딕셔너리 값(문자열)만 교체, 컴포넌트 구조·레이아웃 변경 없음.

## 구현 계획
- `jam-web/src/lib/i18n/ko.ts`의 위 5개 키 값만 수정한다. 구조(딕셔너리 편입 자체)는
  20260915_2237에서 이미 끝났으므로 이 티켓은 순수 문구 교체다.
- 5번(완성/용어표 등재)은 먼저 의미 확인이 필요하므로 다른 4개와 순서를 분리해도 된다.
- `/im-not-ai`로 교체된 문구를 다시 검사해 새 표현에 다른 anti-slop 이슈가 없는지 확인한다.

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약

### 변경된 파일
```
-
```

### 테스트 결과
- [ ] `/im-not-ai` 전수검사 통과
- [ ] 딕셔너리 값 교체 후 실렌더 대조(SEO 메타데이터·로딩 스피너·지도 마커·POI 캐러셀·컬렉션 배지)

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
**가이드:** `Service Plan/Specs/UX_WRITING_GUIDELINE.md` 참조

- [ ] 용어 일관성: 고정 용어만 사용 (획득·드랍·픽업·체크인·포인트 등)
- [ ] 톤앤매너: 상황에 맞는 톤 (배지=신남, 거래=단호, 오류=전문)
- [ ] 에러 메시지: [현상] → [원인] → [해결책] 3단계 구조
- [ ] 문장 규칙: 해요체, 간결함, 마침표 위치 정확
- [ ] 표기 규칙: 날짜/시간/금액/기간 직관적 형식

### 배포 정보
- 배포일:
- 환경: production
- 커밋:

### 주요 의사결정 / 핵심 메모
> 개발 과정에서 검토·결정된 사항, 선택하지 않은 대안과 그 이유.

### 잔여 이슈
-
