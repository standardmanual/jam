---
id: 20260908_0055
category: UI
priority: P3
status: CLOSED
created: 2026-09-08
closed: 2026-09-14
---

# [UI] 장착 개체 선택 시트 — 요청 진행 중 상태의 잔여 엣지케이스 4건

## 배경 / 문제 정의

티켓 [20260907_2221](20260907_2221_UI_장착개체-선택시트-ListRowCard-재구성.md) 3차 보완의
게이트 재리뷰가 남긴 사이드 파인딩이다. 전부 경미하고 지시 범위 밖이라 그 티켓에서는 손대지
않았다. 발생 빈도가 낮거나 사용자 영향이 작아 P3로 둔다.

## 상세 요구사항

### 1. 요청 중단 판정이 배지 단위가 아니라 전역이다

`sheetBusy`가 `pendingBadgeId != null`이라, 배지 A의 장착 요청이 도는 동안(그리드에서는 A의
버튼만 비활성) 배지 B의 「추가」를 눌러 시트를 열면 **B의 시트도 모든 행이 억제되고 닫기도
막힌다.** A의 요청이 끝나면 자동으로 풀리는 1초 미만의 과도 상태이고, 1차 구현부터 있던 같은
전역 판정(`disabled={pendingBadgeId != null}`)이라 이번 리팩터가 만든 회귀는 아니다.

### 2. 네트워크가 끊기면 시트에 갇힌다

요청 중 닫기를 무시하도록 만들었는데(20260907_2221 3차, 의도된 설계), 요청에 타임아웃이나
`AbortController`가 없어 응답이 영영 안 오면 그동안 시트를 닫을 수 없다.

### 3. 억제된 행이 키보드로는 여전히 접근된다

`opacity-40 pointer-events-none`만 주고 `disabled`를 쓰지 않아, 마우스 클릭은 막히지만
키보드 Tab으로는 여전히 포커스·Enter 입력이 된다(동작 자체는 `handleSelectCandidate`의 가드가
막아 무동작으로 끝난다). 완전한 키보드 억제는 `ListRowCard`(13개 화면 공용)에 `disabled` prop이
필요해 범위 밖으로 남겨졌다.

### 4. 장착 실패 후 후보 목록이 스스로 갱신되지 않는다

`requestSlot`은 성공 시에만 `router.refresh()`를 부른다. 409(이미 다른 개체가 장착됨) 실패 후
후보 목록은 능동으로 다시 조회되지 않는다 — 3차의 `sheetView` 이중 폴백은 "상위에서 갱신이
도착하면 시트가 따라간다"만 보장할 뿐, 실패 자체가 갱신을 유발하지는 않는다.

## 구현 계획
> 착수 시 방향을 정한다. 넷 다 굳이 함께 고칠 필요는 없다 — 개별 우선순위로 봐도 된다.

- 1번: 판정을 배지별로 좁히거나(맵 형태), 과도 상태 자체를 없앨 필요는 없다고 판단해 현행 유지
- 2번: fetch에 타임아웃 추가, 또는 진행 중에도 닫기를 허용하되 완료 후 실패를 별도로 알리는
  방식으로 전환(20260907_2221이 배제한 배너 방식이 아닌 토스트 등)
- 3번: `ListRowCard`에 `disabled`/`aria-disabled` 추가 여부 판단 — 13개 화면 회귀 표면 고려
- 4번: 실패 시에도 `router.refresh()`를 부르거나, 시트 안에서 후보를 능동 재조회

## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
4건 중 2·3·4번을 고쳤다. 1번은 구현 계획이 제시한 대로 "과도 상태 자체를 없앨 필요는
없다"고 판단해 현행(전역 판정)을 유지한다 — 1초 미만의 짧은 과도 상태이고, 배지별로
좁히려면 `pendingBadgeId`를 맵으로 바꿔야 해 이번 P3 범위에서는 이득 대비 변경 폭이 크다.

- **2번(네트워크 끊김 시 시트에 갇힘)**: `SlotGrid.tsx`의 세 요청 함수(`requestSlot`·
  `requestSwap`·`requestUnslot`)가 공유하는 `fetchWithTimeout` 헬퍼를 추가해 15초
  타임아웃을 걸었다. 시간 초과 시 `AbortController`로 요청을 끊고 `slotTimeout` 문구를
  반환한다 — `finally`에서 `pendingBadgeId`가 풀리므로 `sheetBusy`도 함께 풀려 시트를
  닫을 수 있다. 구현 계획의 두 안 중 "fetch에 타임아웃 추가"를 택했다(토스트 인프라
  신규 도입 없이 기존 `sheetError` 표시 경로를 그대로 재사용할 수 있어서).
- **3번(억제된 행의 키보드 접근)**: `ListRowCard`에 `disabled` prop을 추가했다(button
  변형에서만 의미, 기본값 false라 기존 13개 화면에는 영향 없음). `ItemCandidateRow`가
  `muted`를 그대로 `disabled`로 전달해, 요청 진행 중 억제된 행은 이제 Tab 포커스도
  받지 않는다. `className`의 `pointer-events-none`은 마우스 커서 모양 억제용으로
  남겨뒀다(disabled 버튼은 클릭 자체는 막지만 커서 스타일은 className이 정한다).
- **4번(장착 실패 후 후보 목록 미갱신)**: `requestSlot`·`requestSwap`의 실패 분기에도
  `router.refresh()`를 추가했다. 409(경합) 등으로 실패하면 서버의 최신 후보 구성을
  다시 가져와 `sheetView`가 갱신되므로, 이미 사라진 후보를 계속 탭할 수 있던 문제가
  없어진다. `requestUnslot`은 후보 구성 변화와 직접 관련이 없어 범위에서 제외했다.

### 변경된 파일
```
jam-web/src/app/(main)/collections/[id]/SlotGrid.tsx
jam-web/src/components/ui/ListRowCard.tsx
jam-web/src/components/inventory/ItemCandidateRow.tsx
jam-web/src/lib/i18n/ko.ts
```

### 테스트 결과
- [x] `npm run lint` — 0 errors, 14 warnings(모두 이번 변경과 무관한 기존 경고)
- [x] `npx tsc --noEmit` — 오류 없음
- [ ] 실제 네트워크 끊김·409 경합 시나리오는 수동 재현이 어려워 코드 리뷰로 대체

### UX Writing 검증 *(사용자 노출 텍스트가 있을 경우 필수)*
- [x] 신규 문구 `slotTimeout`("요청이 너무 오래 걸려서 중단했어요. 잠시 후 다시
  시도해 주세요.") — 기존 `slotFailed`/`networkError`와 같은 해요체·경어체를 따르고,
  [현상] 요청이 오래 걸림 → [해결책] 잠시 후 재시도 구조로 작성했다.

### 배포 정보
- 배포일: (미배포 — review 브랜치 push까지만 수행)
- 환경: 
- 커밋: 

### 주요 의사결정 / 핵심 메모
- 1번은 의도적으로 미착수 — 위 요약 참고.
- `requestUnslot`에도 타임아웃은 적용했지만 실패 시 `router.refresh()`는 추가하지
  않았다(4번의 지적 대상이 아니라서 범위를 넘지 않으려 했다).

### 잔여 이슈
- 1번(전역 요청 중단 판정)은 여전히 남아 있다 — 재발 시 `pendingBadgeId`를
  `Set<string>` 또는 맵으로 바꾸는 별도 티켓으로 다룰 수 있다.
