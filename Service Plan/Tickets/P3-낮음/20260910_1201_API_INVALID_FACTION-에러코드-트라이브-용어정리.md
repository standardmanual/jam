---
id: 20260910_1201
category: API
priority: P3
status: OPEN
created: 2026-09-10
closed:
---

# [API] INVALID_FACTION 에러 코드를 트라이브 용어로 정리

## 배경 / 문제 정의

티켓 20260910_1129에서 서비스 용어를 트라이브로 통일했으나, 온보딩 API의 에러 코드
문자열 `INVALID_FACTION` 2건만 옛 용어로 남았다.

- 서버: `jam-web/src/app/api/onboarding/complete/route.ts`
- 클라이언트: `jam-web/src/app/(main)/onboarding/page.tsx`

**의도적으로 남긴 것이다.** 이 코드 문자열은 서버가 내려주고 클라이언트가 비교하는
계약이라, 한쪽만 배포되는 순간 온보딩에서 트라이브 검증 에러가 잡히지 않는다. 배포
시차 동안 유저가 실제로 겪을 수 있는 문제라 용어 통일보다 안전을 택했다.

다만 지금 상태로 두면 나중에 "왜 이것만 안 바뀌었지"라는 의문이 반복되고, 정리할 수
있는 시점을 놓치기 쉽다.

## 상세 요구사항

- 서버와 클라이언트를 **같은 배포에 함께** 올릴 수 있는 시점에 `INVALID_FACTION` →
  `INVALID_TRIBE`로 바꾼다. 두 파일을 한 커밋·한 배포로 처리하는 것이 조건이다.
- 이 에러 코드는 유저에게 그대로 노출되지 않는다. 화면 문구는 이미 "이 트라이브는
  지금 선택할 수 없어요"로 트라이브 용어를 쓰고 있으므로 UX 영향은 없다.
- 함께 확인할 것: 같은 파일의 `ALREADY_SET` 등 다른 에러 코드에는 faction 어휘가
  없는지 점검한다.

## 구현 계획

1. `grep -rn "INVALID_FACTION" jam-web/src`로 잔여 지점을 전수 확인한다
2. 서버·클라이언트를 한 커밋으로 함께 수정한다
3. 온보딩에서 비활성 트라이브를 제출하는 경로를 재현해 400 응답과 화면 문구를 확인한다

---
## 완료 기록

### 구현 내용 요약

에러 코드 문자열 `INVALID_FACTION`을 `INVALID_TRIBE`로 바꿨다. 서버와 클라이언트가
비교하는 계약이라 **한 커밋에 함께** 넣었다.

- `api/onboarding/complete/route.ts:108` — 비활성·미존재 트라이브 제출을 400으로 거부할 때
  돌려주는 코드
- `(main)/onboarding/page.tsx:283` — 그 코드를 받아 트라이브 선택을 초기화하고 목록을
  다시 불러오는 분기

`ALREADY_SET` 등 같은 경로의 다른 에러 코드에는 faction 어휘가 없어 손댈 곳이 없었다.

### 변경된 파일
```
jam-web/src/app/api/onboarding/complete/route.ts
jam-web/src/app/(main)/onboarding/page.tsx
```

### 테스트 결과
- [x] `grep -rn "INVALID_FACTION" jam-web/src` 잔여 0건
- [x] 서버가 반환하는 문자열과 클라이언트가 비교하는 문자열이 `INVALID_TRIBE`로 일치
- [x] `npx tsc --noEmit` 오류 0건
- [x] `npx eslint` 대상 파일 오류·경고 0건
- [x] `npm run build` 통과
- [x] `npx vitest run` 73개 파일 1227개 테스트 전부 통과

### UX Writing 검증

- [x] 유저 노출 문구는 바뀌지 않았다. 이 코드는 내부 계약 문자열이고, 화면에는 이미
      `d.onboarding.invalidTribeError`("이 트라이브는 지금 선택할 수 없어요. 다른 트라이브를
      선택해 주세요.")가 표시된다.

### 배포 정보
- 배포일:
- 환경: production
- 커밋:

### 주요 의사결정 / 핵심 메모

- **하위 호환 분기를 두지 않았다.** 원 티켓이 우려한 것은 서버와 클라이언트가 다른 배포에
  올라 코드가 어긋나는 상황이다. Next.js 단일 앱이라 두 파일은 항상 같은 배포에 함께 나가고,
  남는 위험은 **배포 순간에 이미 온보딩 화면을 열어둔 유저**가 구 번들로 제출하는 경우뿐이다.
  그때도 실제 영향은 "비활성 트라이브를 골랐을 때 안내 문구가 뜨지 않고 일반 오류로 처리됨"
  정도이고, 애초에 활성 트라이브만 목록에 보이므로 도달 확률이 매우 낮다. 이 정도를 막자고
  두 코드를 모두 받아들이는 분기를 남기면 나중에 걷어낼 부채만 생긴다고 판단했다.

### 잔여 이슈
- 없음
