---
id: 20260911_1019
category: Admin
priority: P3
status: OPEN
created: 2026-09-11
---

# [Admin] 조건이 비어 있는 배지 폼에도 "진행률이 표시되지 않아요" 경고가 뜸

## 배경 / 문제 정의

티켓 [20260911_0901](../P2-일반/20260911_0901_Admin_배지생성조회수정-아티팩트기준리뉴얼.md) 게이트
리뷰에서 발견된 범위 밖 이슈.

새 액티비티 배지를 등록하려고 폼을 열면, 조건을 하나도 넣지 않았는데도 "이 조건은 배지 트리
화면에 진행률이 표시되지 않아요" 경고가 뜬다. `BadgeForm.tsx`의 `progressIssue`가
`explainUnsupportedProgress(condPreview ?? {})`를 호출하는데, 이 함수가 빈 조건에도 사유를
돌려주기 때문이다(`src/lib/badge-engine/badgeProgress.ts`). 리뉴얼 전에도 같은 식이었으므로
기존 동작이다. 빈 조건에 경고를 띄우는 것은 오탐이라 어드민이 경고를 무시하게 만든다.

## 상세 요구사항

### 서비스/코드베이스 관점

- 조건이 비어 있으면(`condPreview`가 null이거나 키가 없으면) 진행률 경고를 계산하지 않는다.
- `explainUnsupportedProgress` 자체를 고칠지, 폼 쪽에서만 막을지는 이 함수의 다른 호출부를
  확인한 뒤 정한다.

## 구현 계획

폼의 `progressIssue` 계산에 빈 조건 가드를 추가하는 정도로 해결될 가능성이 높다.

---
## 완료 기록 *(작업 완료 후 작성)*

### 구현 내용 요약
`BadgeForm.tsx`의 `progressIssue` 계산부에 빈 조건 가드를 추가했다. `condPreview`가 null이거나
키가 하나도 없으면 `explainUnsupportedProgress`를 호출하지 않고 `null`을 반환한다.
`explainUnsupportedProgress`의 다른 호출부는 없어 함수 자체는 수정하지 않았다(폼 쪽에서만 막음).

### 변경된 파일
```
jam-web/src/app/admin/badges/BadgeForm.tsx
```

### 테스트 결과
- [ ] `npm run lint` 실행 (결과는 오케스트레이터가 최종 요약에 반영)

### 배포 정보
- 배포일:
- 환경: staging
- 커밋:

### 주요 의사결정 / 핵심 메모
- `explainUnsupportedProgress`의 유일한 호출부가 `BadgeForm.tsx`임을 확인 후, 함수 자체를
  고치는 대신 호출부에 가드를 추가하는 쪽으로 결정했다.

### 잔여 이슈
-
