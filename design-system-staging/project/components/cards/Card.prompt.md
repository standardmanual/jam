General-purpose surface container for grouping content (badge tiles, shortcut tiles, drop cards).

```jsx
<Card tone="tint" padding={24}>
  <h3>배지를 받았어요</h3>
</Card>
```

`tone="inverse"` for the rare full-bleed black section; default `white` for most content.
