Small uppercase pill showing a badge's rarity tier. `rarity="common"` renders nothing (common is the default tier, not called out). Fixed 3-color mapping for rare/legend/mythic (rare=green, legend=purple, mythic=amber) — never re-map these, users have learned the color language.

```jsx
<RarityBadge rarity="legend" />
```
