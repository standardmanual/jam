diff --git a/node_modules/@basementstudio/shader-lab/dist/src/renderer/blend-modes.js b/node_modules/@basementstudio/shader-lab/dist/src/renderer/blend-modes.js
index 3edd440..f6c7b9d 100644
--- a/node_modules/@basementstudio/shader-lab/dist/src/renderer/blend-modes.js
+++ b/node_modules/@basementstudio/shader-lab/dist/src/renderer/blend-modes.js
@@ -150,7 +150,18 @@ export function buildBlendNode(mode, base, blend, opacity, compositeMode = "filt
             break;
     }
     if (compositeMode === "filter") {
-        return vec4(mix(baseRgb, composited, normalizedOpacity.mul(blendAlpha)), float(1));
+        // 티켓 20260914_1045 — 이전에는 이 알파를 항상 float(1)로 고정해, 레이어(blend)가
+        // 자기 알파를 아무리 정확히 계산해도(예: 텍스트 textColor, 리퀴드 메탈 colorBack의
+        // 알파 슬라이더) 합성 결과의 알파는 늘 완전 불투명으로 되돌아갔다. base(이전
+        // 패스까지의 누적 알파)와 blend(이 레이어 자신의 알파)를 표준 소스-오버 알파 합성
+        // 공식으로 계산해, 레이어 알파가 낮을수록 최종 알파도 그에 비례해 낮아지도록 한다.
+        // RGB 합성식(mix)은 회귀 방지를 위해 그대로 둔다 — base 알파가 항상 1이던 기존
+        // 동작에서는 이 결과가 이전과 동일하다(0 + t·(1-0) = t와, base.a=1일 때 1 + t·0 = 1
+        // 두 경우 모두 기존 출력과 일치).
+        const baseAlpha = float(clamp(base.a, float(0), float(1)));
+        const blendFactor = normalizedOpacity.mul(blendAlpha);
+        const outAlpha = baseAlpha.add(blendFactor.mul(float(1).sub(baseAlpha)));
+        return vec4(mix(baseRgb, composited, blendFactor), outAlpha);
     }
     const source = maskConfig?.source ?? "luminance";
     let maskValue;
diff --git a/node_modules/@basementstudio/shader-lab/dist/src/renderer/create-webgpu-renderer.js b/node_modules/@basementstudio/shader-lab/dist/src/renderer/create-webgpu-renderer.js
index fa00441..aa836e7 100644
--- a/node_modules/@basementstudio/shader-lab/dist/src/renderer/create-webgpu-renderer.js
+++ b/node_modules/@basementstudio/shader-lab/dist/src/renderer/create-webgpu-renderer.js
@@ -4,8 +4,11 @@ export function browserSupportsWebGPU() {
     return typeof navigator !== "undefined" && "gpu" in navigator;
 }
 export async function createWebGPURenderer(canvas, onRuntimeError) {
+    // 티켓 20260914_1045 — alpha:false로 생성하면 WebGPU 캔버스 컨텍스트 자체가 알파 채널을
+    // 지원하지 않는 opaque 프레젠테이션으로 만들어져, 아래 파이프라인에서 알파를 아무리
+    // 정확히 계산해도 최종 캔버스(및 여기서 읽어오는 PNG export)는 항상 alpha=255로 고정된다.
     const renderer = new THREE.WebGPURenderer({
-        alpha: false,
+        alpha: true,
         antialias: false,
         canvas,
     });
@@ -22,7 +25,11 @@ export async function createWebGPURenderer(canvas, onRuntimeError) {
             await renderer.init();
             renderer.outputColorSpace = THREE.SRGBColorSpace;
             renderer.toneMapping = THREE.NoToneMapping;
-            renderer.setClearColor("#0a0d10", 1);
+            // 클리어 알파도 0으로 맞춘다 — 실제 투명도는 pipeline-manager.js의 baseMesh가
+            // 매 프레임 전체 화면을 덮으며 결정하지만, 클리어 컬러의 알파가 1로 남아있으면
+            // 레이어가 하나도 없는(activePasses.length === 0) 특수 경로 등에서 혼동을 줄 수
+            // 있어 일관되게 0으로 둔다.
+            renderer.setClearColor("#0a0d10", 0);
         },
         resize(size, pixelRatio) {
             currentPixelRatio = pixelRatio;
diff --git a/node_modules/@basementstudio/shader-lab/dist/src/renderer/edge-detect-pass.js b/node_modules/@basementstudio/shader-lab/dist/src/renderer/edge-detect-pass.js
index 6598b9b..1bb3c88 100644
--- a/node_modules/@basementstudio/shader-lab/dist/src/renderer/edge-detect-pass.js
+++ b/node_modules/@basementstudio/shader-lab/dist/src/renderer/edge-detect-pass.js
@@ -9,6 +9,15 @@ function hexToVec3(hex) {
     const color = new THREE.Color(`#${normalized}`);
     return [color.r, color.g, color.b];
 }
+// 티켓 20260915_1103 후속 — bgColor의 8자리 hex(#RRGGBBAA) 알파를 추출한다.
+function hexToAlpha(hex) {
+    const normalized = hex.trim().replace("#", "");
+    if (normalized.length !== 8) {
+        return 1;
+    }
+    const alphaByte = Number.parseInt(normalized.slice(6, 8), 16);
+    return Number.isNaN(alphaByte) ? 1 : alphaByte / 255;
+}
 function toColorModeValue(value) {
     switch (value) {
         case "mono":
@@ -30,6 +39,7 @@ export class EdgeDetectPass extends PassNode {
     bgColorRUniform;
     bgColorGUniform;
     bgColorBUniform;
+    bgAlphaUniform;
     logicalWidthUniform;
     logicalHeightUniform;
     sourceTextureNodes = [];
@@ -46,6 +56,7 @@ export class EdgeDetectPass extends PassNode {
         this.bgColorRUniform = uniform(0);
         this.bgColorGUniform = uniform(0);
         this.bgColorBUniform = uniform(0);
+        this.bgAlphaUniform = uniform(1);
         this.logicalWidthUniform = uniform(1);
         this.logicalHeightUniform = uniform(1);
         this.rebuildEffectNode();
@@ -86,6 +97,7 @@ export class EdgeDetectPass extends PassNode {
             this.bgColorRUniform.value = r;
             this.bgColorGUniform.value = g;
             this.bgColorBUniform.value = b;
+            this.bgAlphaUniform.value = hexToAlpha(params.bgColor);
         }
     }
     trackSourceTextureNode(uvNode) {
@@ -138,7 +150,9 @@ export class EdgeDetectPass extends PassNode {
         if (this.colorMode === COLOR_MODE_MONO) {
             const lineCol = vec3(this.lineColorRUniform, this.lineColorGUniform, this.lineColorBUniform);
             const bgCol = vec3(this.bgColorRUniform, this.bgColorGUniform, this.bgColorBUniform);
-            return vec4(mix(bgCol, lineCol, finalEdge), float(1));
+            // 티켓 20260915_1103 후속 — 엣지 라인이 없는(배경만 보이는) 영역은 bgColor
+            // 자신의 알파를, 라인이 그려진 영역은 항상 완전 불투명(1)을 쓴다.
+            return vec4(mix(bgCol, lineCol, finalEdge), mix(this.bgAlphaUniform, float(1), finalEdge));
         }
         if (this.colorMode === COLOR_MODE_SOURCE) {
             return vec4(centerColor.rgb.mul(finalEdge), float(1));
diff --git a/node_modules/@basementstudio/shader-lab/dist/src/renderer/ink-pass.js b/node_modules/@basementstudio/shader-lab/dist/src/renderer/ink-pass.js
index b02e6da..db801f2 100644
--- a/node_modules/@basementstudio/shader-lab/dist/src/renderer/ink-pass.js
+++ b/node_modules/@basementstudio/shader-lab/dist/src/renderer/ink-pass.js
@@ -29,6 +29,17 @@ function hexToRgb(hex) {
     const color = new THREE.Color(`#${value}`);
     return [color.r, color.g, color.b];
 }
+// 티켓 20260915_1103 후속 — backgroundColor의 8자리 hex(#RRGGBBAA) 알파를 추출한다.
+// hexToRgb()는 항상 RGB 6자리만 보므로 별도 함수로 뒀다. 6자리 이하 입력은 완전 불투명(1)으로
+// 취급해 기존 저장된 값(알파 없이 저장된 6자리 hex)과 호환된다.
+function hexToAlpha(hex) {
+    const normalized = hex.trim().replace("#", "");
+    if (normalized.length !== 8) {
+        return 1;
+    }
+    const alphaByte = Number.parseInt(normalized.slice(6, 8), 16);
+    return Number.isNaN(alphaByte) ? 1 : alphaByte / 255;
+}
 function resolveInkColorMode(value) {
     return value === "source" ? "source" : "gradient";
 }
@@ -109,7 +120,7 @@ export class InkPass extends PassNode {
         this.bloomRadiusUniform = uniform(6);
         this.bloomSoftnessUniform = uniform(0.35);
         this.bloomThresholdUniform = uniform(0.6);
-        this.backgroundColorUniform = uniform(new THREE.Vector3(0.039, 0.043, 0.051));
+        this.backgroundColorUniform = uniform(new THREE.Vector4(0.039, 0.043, 0.051, 1));
         this.coreColorUniform = uniform(new THREE.Vector3(1.0, 0.992, 0.91));
         this.midColorUniform = uniform(new THREE.Vector3(0.784, 0.961, 0.259));
         this.edgeColorUniform = uniform(new THREE.Vector3(0.0, 0.788, 0.655));
@@ -147,6 +158,11 @@ export class InkPass extends PassNode {
         copyMesh.frustumCulled = false;
         this.copyScene.add(copyMesh);
         this.compositeMaterial = new THREE.MeshBasicNodeMaterial();
+        // 티켓 20260915_1103 후속 — 이 pass 전용 머티리얼은 PassNode 기본 생성자가 patch하는
+        // this.material과 별개라 알파 강제 클로버링(three.js NodeMaterial.js isOpaque())을
+        // 그대로 맞는다. compositeTarget의 알파(backgroundColor 알파)가 buildEffectNode까지
+        // 살아남으려면 여기서도 켜야 한다.
+        this.compositeMaterial.alphaToCoverage = true;
         this.compositeMaterial.colorNode = this.buildCompositeNode();
         const compositeMesh = new THREE.Mesh(new THREE.PlaneGeometry(2, 2), this.compositeMaterial);
         compositeMesh.frustumCulled = false;
@@ -242,7 +258,7 @@ export class InkPass extends PassNode {
         this.grainScaleUniform.value =
             typeof params.grainScale === "number" ? Math.max(0.5, params.grainScale) : 1.5;
         this.colorModeUniform.value = toInkColorModeValue(resolveInkColorMode(params.colorMode));
-        this.setColorUniform(this.backgroundColorUniform, typeof params.backgroundColor === "string" ? params.backgroundColor : "#0a0b0d");
+        this.setBackgroundColorUniform(typeof params.backgroundColor === "string" ? params.backgroundColor : "#0a0b0dff");
         this.setColorUniform(this.coreColorUniform, typeof params.coreColor === "string" ? params.coreColor : "#fffde8");
         this.setColorUniform(this.midColorUniform, typeof params.midColor === "string" ? params.midColor : "#c8f542");
         this.setColorUniform(this.edgeColorUniform, typeof params.edgeColor === "string" ? params.edgeColor : "#00c9a7");
@@ -298,16 +314,20 @@ export class InkPass extends PassNode {
     }
     buildEffectNode() {
         if (!this.finalInputNode) {
-            return vec4(float(0), float(0), float(0), float(1));
+            return vec4(float(0), float(0), float(0), float(0));
         }
         const baseColor = vec3(this.finalInputNode.r, this.finalInputNode.g, this.finalInputNode.b);
         if (!this.bloomEnabled) {
-            return vec4(baseColor, float(1));
+            // compositeTarget이 이미 backgroundColor의 알파를 반영해 기록했으므로
+            // (buildCompositeNode 참고) 여기서는 그 알파를 그대로 읽어 전달한다.
+            return vec4(baseColor, this.finalInputNode.a);
         }
         if (!this.bloomCompositor) {
             this.bloomCompositor = new BloomCompositor();
         }
         this.applyBloomSettings();
+        // 알려진 한계(티켓 20260915_1103): BloomCompositor는 공용 컴포지터라 알파를
+        // 항상 1로 반환한다 — 블룸 활성화 시에는 배경 투명도가 아직 반영되지 않는다.
         return this.bloomCompositor.build(baseColor);
     }
     buildBlurNode() {
@@ -396,6 +416,7 @@ export class InkPass extends PassNode {
         const blurR = this.trackCompositeBlurNode(texUv.add(vec2(texelSize.x.mul(2), texelSize.y))).r;
         const blurB = this.trackCompositeBlurNode(texUv.sub(vec2(texelSize.x.mul(2), texelSize.y))).b;
         const backgroundColor = vec3(float(this.backgroundColorUniform.x), float(this.backgroundColorUniform.y), float(this.backgroundColorUniform.z));
+        const backgroundAlpha = float(this.backgroundColorUniform.w);
         const edgeColor = vec3(float(this.edgeColorUniform.x), float(this.edgeColorUniform.y), float(this.edgeColorUniform.z));
         const midColor = vec3(float(this.midColorUniform.x), float(this.midColorUniform.y), float(this.midColorUniform.z));
         const coreColor = vec3(float(this.coreColorUniform.x), float(this.coreColorUniform.y), float(this.coreColorUniform.z));
@@ -415,11 +436,16 @@ export class InkPass extends PassNode {
             .sub(0.5)
             .mul(this.grainIntensityUniform);
         combined = select(this.grainEnabledUniform.greaterThan(float(0.5)), combined.add(vec3(grain, grain, grain)), combined);
-        const alpha = max(fluidMask, crispMask);
-        let finalColor = mix(backgroundColor, combined, smoothstep(float(0.01), float(0.85), alpha));
+        const mask = max(fluidMask, crispMask);
+        const maskT = smoothstep(float(0.01), float(0.85), mask);
+        let finalColor = mix(backgroundColor, combined, maskT);
         const vignetteUv = texUv.mul(2).sub(vec2(1, 1));
         finalColor = finalColor.mul(float(1).sub(vignetteUv.dot(vignetteUv).mul(0.15)));
-        return vec4(clamp(finalColor, vec3(0, 0, 0), vec3(1, 1, 1)), float(1));
+        // 티켓 20260915_1103 후속 — 잉크 패턴이 없는(배경만 보이는) 영역은 backgroundColor
+        // 자신의 알파를, 패턴이 그려진 영역은 항상 완전 불투명(1)을 쓴다. RGB와 동일한
+        // maskT 보간을 알파에도 적용해 경계가 자연스럽게 이어지도록 한다.
+        const finalAlpha = mix(backgroundAlpha, float(1), maskT);
+        return vec4(clamp(finalColor, vec3(0, 0, 0), vec3(1, 1, 1)), finalAlpha);
     }
     applyColorGradient(intensity, bgColor, edgeColor, midColor, coreColor) {
         const t1 = smoothstep(float(0.6), float(0.95), intensity);
@@ -464,4 +490,8 @@ export class InkPass extends PassNode {
         const [r, g, b] = hexToRgb(value);
         target.value.set(r, g, b);
     }
+    setBackgroundColorUniform(value) {
+        const [r, g, b] = hexToRgb(value);
+        this.backgroundColorUniform.value.set(r, g, b, hexToAlpha(value));
+    }
 }
diff --git a/node_modules/@basementstudio/shader-lab/dist/src/renderer/liquid-metal-pass.js b/node_modules/@basementstudio/shader-lab/dist/src/renderer/liquid-metal-pass.js
new file mode 100644
index 0000000..5b5d585
--- /dev/null
+++ b/node_modules/@basementstudio/shader-lab/dist/src/renderer/liquid-metal-pass.js
@@ -0,0 +1,308 @@
+import { float, texture as tslTexture, uv, vec2, vec4 } from "three/tsl";
+import * as THREE from "three/webgpu";
+import {
+    ShaderMount,
+    defaultObjectSizing,
+    getShaderColorFromString,
+    liquidMetalFragmentShader,
+    LiquidMetalShapes,
+    ShaderFitOptions,
+    toProcessedLiquidMetal,
+} from "@paper-design/shaders";
+import { createPipelinePlaceholder, PassNode } from "./pass-node.js";
+const TRANSPARENT_PIXEL = "data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==";
+export class LiquidMetalPass extends PassNode {
+    placeholder;
+    textureNode;
+    liquidTexture = null;
+    mirrorCanvas = null;
+    mirrorContext = null;
+    container;
+    shaderMount = null;
+    params = {};
+    width = 1;
+    height = 1;
+    transparentPixelImage = null;
+    disposed = false;
+    // 티켓 20260913_2110 — 이미지(SVG 포함) 업로드 시 그 형태를 마스크로 리퀴드 메탈을
+    // 입힌다. 업로드가 없으면(uploadedImage=null) 배경 전체 모드로 동작한다.
+    uploadedImageSrc = null;
+    uploadedImage = null;
+    uploadedObjectUrl = null;
+    uploadedImageAspectRatio = 1;
+    imageLoadNonce = 0;
+    constructor(layerId) {
+        super(layerId);
+        this.placeholder = createPipelinePlaceholder();
+        this.textureNode = tslTexture(this.placeholder, vec2(uv().x, float(1).sub(uv().y)));
+        // JAM! 확장(원본 basement.studio에는 없음, 티켓 20260913_1948): @paper-design/shaders
+        // (순수 WebGL2 GLSL)를 이 WebGPU 파이프라인에 연결하려고, 뷰포트 좌상단에 겹치되
+        // 시각적으로 숨긴 <div>에 ShaderMount를 마운트한다. display:none이나 화면 밖 좌표
+        // (left:-9999px)는 쓰지 않는다 — ShaderMount의 IntersectionObserver가 "화면 밖"으로
+        // 판정해 렌더링을 자동 정지시키기 때문(실측 확인, 완료 기록 참고). opacity:0 +
+        // pointer-events:none으로 화면에는 보이지 않으면서 뷰포트 교차 상태는 유지한다.
+        this.container = document.createElement("div");
+        Object.assign(this.container.style, {
+            position: "fixed",
+            top: "0",
+            left: "0",
+            width: "1px",
+            height: "1px",
+            opacity: "0",
+            pointerEvents: "none",
+            overflow: "hidden",
+            zIndex: "-1",
+        });
+        document.body.appendChild(this.container);
+        // 핵심 버그 수정: PassNode 생성자(super(layerId))가 실행되는 도중 서브클래스의
+        // buildEffectNode()가 이미 한 번 호출된다 — 그 시점엔 this.textureNode가 아직
+        // undefined라 buildEffectNode()의 "!this.textureNode" 분기(완전 투명한
+        // vec4(0,0,0,0))로 effectNode/material.colorNode가 영구 고정돼버린다. text-pass.js/
+        // fluid-pass.js가 하듯 필드 설정 후 rebuildEffectNode()를 명시적으로 다시 불러야
+        // 실제 텍스처 노드로 갱신된다(이 한 줄이 빠져서 렌더 결과가 항상 완전 검정이었다 —
+        // WebGL 셰이더 자체는 readPixels로 정상 렌더링을 확인했었다. 완료 기록 참고).
+        this.rebuildEffectNode();
+        // 코어 ShaderMount(react 래퍼가 아닌 순수 클래스)는 u_image 유니폼으로 문자열(URL)을
+        // 받지 못하고 HTMLImageElement만 받는다(react 패키지의 ShaderMount 컴포넌트가 문자열
+        // ->이미지 변환을 대신 해줄 뿐, 코어 클래스 자체엔 그 로직이 없다 — 실측으로 확인:
+        // 문자열을 넘기면 "Unsupported uniform type for u_image: string" 경고만 내고 무시한다).
+        // 이미지 미업로드 상태(배경 전체 모드)에서는 1x1 투명 픽셀을 u_image에 채운다.
+        // media-pass.js의 mediaLoadNonce 패턴과 동일한 목적의 가드: dispose() 이후에
+        // 이 콜백이 실행되면 이미 정리된 인스턴스에 새 ShaderMount(WebGL 컨텍스트+canvas)를
+        // 만들어버릴 수 있다(게이트 리뷰 20260913_1948 지적, 실사용 경로에서 재현하려면
+        // 마이크로태스크 단위 타이밍 안에 레이어를 곧바로 삭제해야 하는 좁은 레이스지만
+        // 방어적으로 막아둔다).
+        void getTransparentPixelImage().then((img) => {
+            if (this.disposed) {
+                return;
+            }
+            this.transparentPixelImage = img;
+            this.mountShaderIfReady();
+        });
+    }
+    /**
+     * 티켓 20260913_2110 — image 레이어와 같은 공용 `layer.asset` 필드를 받는다.
+     * `toProcessedLiquidMetal()`(paper-design/shaders, 순수 브라우저 API·SVG 지원)로
+     * 이미지를 전처리해 PNG blob을 얻고, Object URL을 거쳐 `HTMLImageElement`로 로드한다
+     * (코어 ShaderMount는 문자열 URL을 u_image로 받지 못해서 — 위 생성자 주석 참고).
+     * `pipeline-manager.js`의 patch(`applyLayerState`)가 이 메서드를 호출한다.
+     */
+    setImageAsset(src) {
+        if (this.uploadedImageSrc === src) {
+            return;
+        }
+        this.uploadedImageSrc = src;
+        this.imageLoadNonce += 1;
+        const requestId = this.imageLoadNonce;
+        if (!src) {
+            this.clearUploadedImage();
+            this.refreshImageState();
+            return;
+        }
+        void toProcessedLiquidMetal(src)
+            .then((result) => {
+            if (this.disposed || requestId !== this.imageLoadNonce) {
+                return;
+            }
+            const objectUrl = URL.createObjectURL(result.pngBlob);
+            const img = new Image();
+            img.onload = () => {
+                if (this.disposed || requestId !== this.imageLoadNonce) {
+                    URL.revokeObjectURL(objectUrl);
+                    return;
+                }
+                this.clearUploadedImage();
+                this.uploadedImage = img;
+                this.uploadedObjectUrl = objectUrl;
+                this.uploadedImageAspectRatio = result.imageData.width / Math.max(1, result.imageData.height);
+                this.refreshImageState();
+            };
+            img.onerror = () => {
+                URL.revokeObjectURL(objectUrl);
+            };
+            img.src = objectUrl;
+        })
+            .catch(() => {
+            // 이미지 전처리 실패(지원하지 않는 형식 등) 시 조용히 무시하고 배경 전체
+            // 모드로 남긴다 — 이 pass엔 CustomShaderPass 같은 onRuntimeError 콜백이 없다.
+        });
+    }
+    clearUploadedImage() {
+        if (this.uploadedObjectUrl) {
+            URL.revokeObjectURL(this.uploadedObjectUrl);
+        }
+        this.uploadedImage = null;
+        this.uploadedObjectUrl = null;
+        this.uploadedImageAspectRatio = 1;
+    }
+    refreshImageState() {
+        if (this.shaderMount) {
+            this.shaderMount.setUniforms(buildLiquidMetalUniforms(this.params, this.resolveImageState()));
+        }
+    }
+    resolveImageState() {
+        if (this.uploadedImage) {
+            return { aspectRatio: this.uploadedImageAspectRatio, image: this.uploadedImage, isImage: true };
+        }
+        return { aspectRatio: 1, image: this.transparentPixelImage, isImage: false };
+    }
+    mountShaderIfReady() {
+        if (this.shaderMount || !this.transparentPixelImage) {
+            return;
+        }
+        this.container.style.width = `${this.width}px`;
+        this.container.style.height = `${this.height}px`;
+        const uniforms = buildLiquidMetalUniforms(this.params, this.resolveImageState());
+        // preserveDrawingBuffer:true가 필수다 — 기본값(false)이면 브라우저가 그리기 직후
+        // WebGL 백버퍼를 지울 수 있어, 나중에 THREE.CanvasTexture가 이 canvas를 읽을 때
+        // 빈 화면만 보인다(실측: readPixels 직후엔 정상 픽셀이 나오지만 WebGPU 쪽 최종
+        // 합성 결과는 항상 검정이었다 — 이 옵션으로 해결 확인).
+        this.shaderMount = new ShaderMount(this.container, liquidMetalFragmentShader, uniforms, { preserveDrawingBuffer: true }, 1, 0, 1, undefined, ["u_image"]);
+    }
+    render(renderer, inputTexture, outputTarget, time, delta) {
+        if (this.shaderMount) {
+            const sourceCanvas = this.shaderMount.canvasElement;
+            // WebGPURenderer가 WebGL 컨텍스트를 가진 canvas를 THREE.CanvasTexture
+            // 소스로 직접 받았을 때 텍스처가 갱신되지 않는 현상을 실측으로 확인했다
+            // (readPixels로는 WebGL 쪽 렌더링이 정상임을 확인했지만 WebGPU 합성
+            // 결과는 항상 빈 화면이었다). text-pass.js가 이미 검증한 2D 캔버스
+            // 브릿지 패턴을 그대로 재사용해 우회한다 — 매 프레임 WebGL 캔버스를
+            // 일반 2D 캔버스로 복사한 뒤 그 2D 캔버스를 텍스처 소스로 쓴다.
+            const sizeChanged = !this.mirrorCanvas ||
+                this.mirrorCanvas.width !== sourceCanvas.width ||
+                this.mirrorCanvas.height !== sourceCanvas.height;
+            if (sizeChanged) {
+                // 티켓 20260914_0953 — 실측 근본 원인: mountShaderIfReady()가 ShaderMount를
+                // 생성한 직후(ResizeObserver 첫 콜백이 아직 실행되기 전) sourceCanvas는
+                // 브라우저 <canvas> 기본 크기(300x150)다. 그런데 이 pass의 render()가 같은
+                // 프레임(파이프라인의 syncLayers() 직후 render() 호출)에서 바로 실행되므로,
+                // 이 시점에 THREE.CanvasTexture를 300x150 크기로 만들어버리는 경우가 있다.
+                // 이후 sourceCanvas가 ResizeObserver에 의해 실제 크기(예: 1280x1280)로
+                // 정정돼도, 기존 텍스처 객체에 needsUpdate=true만 주는 것으로는 WebGPU
+                // 백엔드가 GPU 텍스처 자체를 새 크기로 재할당하지 않는다(실측 확인 — 이
+                // 상태에서는 패턴의 한쪽 귀퉁이만 전체 프레임에 확대되어 나타난다). 크기가
+                // 바뀔 때마다 텍스처 객체 자체를 새로 만들어 GPU 텍스처도 항상 최신 소스
+                // 크기로 재할당되도록 한다.
+                if (!this.mirrorCanvas) {
+                    this.mirrorCanvas = document.createElement("canvas");
+                    this.mirrorContext = this.mirrorCanvas.getContext("2d");
+                }
+                this.mirrorCanvas.width = sourceCanvas.width;
+                this.mirrorCanvas.height = sourceCanvas.height;
+                this.liquidTexture?.dispose();
+                this.liquidTexture = new THREE.CanvasTexture(this.mirrorCanvas);
+                this.liquidTexture.flipY = false;
+                this.liquidTexture.generateMipmaps = false;
+                this.liquidTexture.magFilter = THREE.LinearFilter;
+                this.liquidTexture.minFilter = THREE.LinearFilter;
+                this.liquidTexture.wrapS = THREE.ClampToEdgeWrapping;
+                this.liquidTexture.wrapT = THREE.ClampToEdgeWrapping;
+                this.liquidTexture.colorSpace = THREE.SRGBColorSpace;
+            }
+            // 티켓 20260915_1147 후속 — clearRect 없이 매 프레임 drawImage만 하면 2D 캔버스의
+            // 기본 합성 방식(source-over)상 새 프레임의 알파가 낮은 픽셀은 이전 프레임의
+            // 불투명한 내용을 지우지 못하고 그 아래 그대로 남는다. "이미지 업로드 후에도
+            // 배경 채우기 효과가 안 사라짐", "배경색 알파 슬라이더가 안 먹힘" 두 증상 모두
+            // 이 누적 때문이었다(다른 설정을 바꾸다 캔버스 리사이즈가 걸리면 mirrorCanvas가
+            // 통째로 새로 만들어져 누적이 초기화되는 것으로 오인됨). 매 프레임 지우고 새로
+            // 그린다.
+            this.mirrorContext.clearRect(0, 0, this.mirrorCanvas.width, this.mirrorCanvas.height);
+            this.mirrorContext.drawImage(sourceCanvas, 0, 0);
+            this.liquidTexture.needsUpdate = true;
+            this.textureNode.value = this.liquidTexture;
+        }
+        super.render(renderer, inputTexture, outputTarget, time, delta);
+    }
+    updateParams(params) {
+        this.params = params;
+        if (!this.shaderMount) {
+            this.mountShaderIfReady();
+            return;
+        }
+        this.shaderMount.setUniforms(buildLiquidMetalUniforms(params, this.resolveImageState()));
+    }
+    resize(width, height) {
+        this.width = Math.max(1, width);
+        this.height = Math.max(1, height);
+        this.container.style.width = `${this.width}px`;
+        this.container.style.height = `${this.height}px`;
+    }
+    needsContinuousRender() {
+        // 줄무늬 패턴이 항상 흐르는 것이 이 레이어의 정체성이다(플루이드·픽셀 트레일과
+        // 같은 카테고리) — 티켓 20260913_1819에서 제거한 "정적 이미지의 불필요한 자동
+        // 애니메이션"과는 성격이 다르다.
+        return true;
+    }
+    dispose() {
+        this.disposed = true;
+        this.shaderMount?.dispose();
+        this.container.remove();
+        this.liquidTexture?.dispose();
+        this.mirrorCanvas = null;
+        this.mirrorContext = null;
+        this.clearUploadedImage();
+        this.placeholder.dispose();
+        super.dispose();
+    }
+    buildEffectNode() {
+        if (!this.textureNode) {
+            return vec4(float(0), float(0), float(0), float(0));
+        }
+        return vec4(this.textureNode.rgb, this.textureNode.a);
+    }
+}
+let transparentPixelImagePromise = null;
+function getTransparentPixelImage() {
+    if (!transparentPixelImagePromise) {
+        transparentPixelImagePromise = new Promise((resolve, reject) => {
+            const img = new Image();
+            img.onload = () => resolve(img);
+            img.onerror = () => reject(new Error("Failed to load transparent pixel placeholder image."));
+            img.src = TRANSPARENT_PIXEL;
+        });
+    }
+    return transparentPixelImagePromise;
+}
+function buildLiquidMetalUniforms(params, imageState) {
+    // 게이트 리뷰(20260913_1948) 지적: 이 fallback들은 effectRegistry.ts의 liquidMetalFields
+    // 기본값(라이브러리 내장 "Backdrop" 프리셋 기준)과 정확히 일치시킨다 — 정상 경로에서는
+    // layerFactory가 registry 기본값으로 params를 채운 뒤 곧바로 updateParams()가 호출되므로
+    // 이 fallback 자체가 실행될 일은 없지만(코드 추적으로 확인), 실사용 기본값과 다른 값을
+    // fallback으로 남겨두는 건 잠재적 함정이라 명시적으로 맞춰둔다.
+    // colorBack은 8자리 hex(#RRGGBBAA)로 투명도를 지원한다(티켓 20260913_2110) —
+    // getShaderColorFromString이 이미 완전히 지원함을 확인했다.
+    const colorBack = typeof params.colorBack === "string" ? params.colorBack : "#AAAAAC00";
+    const colorTint = typeof params.colorTint === "string" ? params.colorTint : "#ffffff";
+    const repetition = typeof params.repetition === "number" ? params.repetition : 1.5;
+    const softness = typeof params.softness === "number" ? params.softness : 0.05;
+    const shiftRed = typeof params.shiftRed === "number" ? params.shiftRed : 0.3;
+    const shiftBlue = typeof params.shiftBlue === "number" ? params.shiftBlue : 0.3;
+    const distortion = typeof params.distortion === "number" ? params.distortion : 0.1;
+    const contour = typeof params.contour === "number" ? params.contour : 0.4;
+    const angle = typeof params.angle === "number" ? params.angle : 90;
+    return {
+        u_colorBack: getShaderColorFromString(colorBack),
+        u_colorTint: getShaderColorFromString(colorTint),
+        u_image: imageState.image,
+        u_imageAspectRatio: imageState.aspectRatio,
+        u_isImage: imageState.isImage,
+        u_repetition: repetition,
+        u_softness: softness,
+        u_shiftRed: shiftRed,
+        u_shiftBlue: shiftBlue,
+        u_distortion: distortion,
+        u_contour: contour,
+        u_angle: angle,
+        // 티켓 20260913_2110 — shape 파라미터를 UI에서 제거했다(이미지 유무로만 분기).
+        u_shape: LiquidMetalShapes.none,
+        u_fit: ShaderFitOptions[defaultObjectSizing.fit],
+        u_scale: 1,
+        u_rotation: defaultObjectSizing.rotation,
+        u_offsetX: defaultObjectSizing.offsetX,
+        u_offsetY: defaultObjectSizing.offsetY,
+        u_originX: defaultObjectSizing.originX,
+        u_originY: defaultObjectSizing.originY,
+        u_worldWidth: 0,
+        u_worldHeight: 0,
+    };
+}
diff --git a/node_modules/@basementstudio/shader-lab/dist/src/renderer/media-pass.js b/node_modules/@basementstudio/shader-lab/dist/src/renderer/media-pass.js
index dfa5260..f727dca 100644
--- a/node_modules/@basementstudio/shader-lab/dist/src/renderer/media-pass.js
+++ b/node_modules/@basementstudio/shader-lab/dist/src/renderer/media-pass.js
@@ -20,7 +20,7 @@ export class MediaPass extends PassNode {
         super(layerId);
         this.placeholder = new THREE.Texture();
         this.canvasAspectUniform = uniform(1);
-        this.fitModeUniform = uniform(0);
+        this.fitModeUniform = uniform(1);
         this.offsetXUniform = uniform(0);
         this.offsetYUniform = uniform(0);
         this.scaleUniform = uniform(1);
@@ -70,7 +70,10 @@ export class MediaPass extends PassNode {
         this.releaseCurrentMedia();
     }
     updateParams(params) {
-        this.fitModeUniform.value = params.fitMode === "contain" ? 1 : 0;
+        // JAM! 정책 — "채우기" 옵션을 없애고 항상 비율 유지(contain)로 고정한다(티켓
+        // 20260915_1042 후속). params.fitMode는 더 이상 UI에서 설정되지 않지만, 기존
+        // 저장된 컴포지션에 남아있는 "cover" 값도 무시하도록 여기서 강제한다.
+        this.fitModeUniform.value = 1;
         this.scaleUniform.value =
             typeof params.scale === "number" ? 1 / Math.max(params.scale, 0.01) : 1;
         if (Array.isArray(params.offset) && params.offset.length === 2) {
@@ -128,7 +131,7 @@ export class MediaPass extends PassNode {
             .and(sampledUv.x.lessThanEqual(1))
             .and(sampledUv.y.greaterThanEqual(0))
             .and(sampledUv.y.lessThanEqual(1));
-        const contained = select(inBounds, this.mediaTextureNode, vec4(0, 0, 0, 1));
+        const contained = select(inBounds, this.mediaTextureNode, vec4(0, 0, 0, 0));
         return mix(this.mediaTextureNode, contained, useContain);
     }
     releaseCurrentMedia() {
diff --git a/node_modules/@basementstudio/shader-lab/dist/src/renderer/particle-grid-pass.js b/node_modules/@basementstudio/shader-lab/dist/src/renderer/particle-grid-pass.js
index 22e2098..14594e9 100644
--- a/node_modules/@basementstudio/shader-lab/dist/src/renderer/particle-grid-pass.js
+++ b/node_modules/@basementstudio/shader-lab/dist/src/renderer/particle-grid-pass.js
@@ -32,6 +32,15 @@ function resolveGridResolution(value) {
 function clamp01(value) {
     return Math.max(0, Math.min(1, value));
 }
+// 티켓 20260915_1103 후속 — backgroundColor의 8자리 hex(#RRGGBBAA) 알파를 추출한다.
+function hexToAlpha(hex) {
+    const normalized = hex.trim().replace("#", "");
+    if (normalized.length !== 8) {
+        return 1;
+    }
+    const alphaByte = Number.parseInt(normalized.slice(6, 8), 16);
+    return Number.isNaN(alphaByte) ? 1 : alphaByte / 255;
+}
 export class ParticleGridPass extends PassNode {
     perspScene;
     perspCamera;
@@ -60,6 +69,7 @@ export class ParticleGridPass extends PassNode {
     mesh = null;
     meshMaterial = null;
     bgColor = new THREE.Color(0x000000);
+    bgAlpha = 1;
     gridResolution = 64;
     isAnimated = false;
     needsRebuild = true;
@@ -115,7 +125,7 @@ export class ParticleGridPass extends PassNode {
         if (this.isAnimated) {
             renderer.compute(this.noiseComputeNode);
         }
-        renderer.setClearColor(this.bgColor, 1);
+        renderer.setClearColor(this.bgColor, this.bgAlpha);
         renderer.setRenderTarget(this.internalRT);
         renderer.render(this.perspScene, this.perspCamera);
         this.blitInputNode.value = this.internalRT.texture;
@@ -142,9 +152,11 @@ export class ParticleGridPass extends PassNode {
         }
         this.displacementUniform.value =
             typeof params.displacement === "number" ? params.displacement : 0.5;
-        this.bgColor.set(typeof params.backgroundColor === "string"
-            ? params.backgroundColor
-            : "#000000");
+        const nextBackgroundColor = typeof params.backgroundColor === "string" ? params.backgroundColor : "#00000000";
+        // THREE.Color.set()은 #RRGGBB(6자리)만 인식한다 — 8자리 알파 접미사를 떼고 넘긴다.
+        const rgbHex = nextBackgroundColor.trim().replace("#", "").padEnd(6, "0").slice(0, 6);
+        this.bgColor.set(`#${rgbHex}`);
+        this.bgAlpha = hexToAlpha(nextBackgroundColor);
         const noiseAmount = typeof params.noiseAmount === "number" ? params.noiseAmount : 0;
         this.noiseAmountUniform.value = noiseAmount;
         this.noiseScaleUniform.value =
@@ -207,7 +219,10 @@ export class ParticleGridPass extends PassNode {
         }
         const baseColor = vec3(this.blitInputNode.r, this.blitInputNode.g, this.blitInputNode.b);
         if (!this.bloomEnabled) {
-            return vec4(baseColor, float(1));
+            // internalRT는 배경(bgAlpha로 클리어)과 파티클(circleMask) 모두 실제 알파를
+            // 갖고 블렌딩됐으므로(위 render(), rebuildGrid()의 material.transparent=true
+            // 참고) 그 알파를 그대로 읽어 전달한다.
+            return vec4(baseColor, this.blitInputNode.a);
         }
         if (!this.bloomCompositor) {
             this.bloomCompositor = new BloomCompositor();
diff --git a/node_modules/@basementstudio/shader-lab/dist/src/renderer/pass-node.js b/node_modules/@basementstudio/shader-lab/dist/src/renderer/pass-node.js
index 406d6f2..d14ff90 100644
--- a/node_modules/@basementstudio/shader-lab/dist/src/renderer/pass-node.js
+++ b/node_modules/@basementstudio/shader-lab/dist/src/renderer/pass-node.js
@@ -44,6 +44,7 @@ export class PassNode {
         this.scene = new THREE.Scene();
         this.camera = new THREE.OrthographicCamera(-1, 1, 1, -1, 0, 1);
         this.material = new THREE.MeshBasicNodeMaterial();
+        this.material.alphaToCoverage = true;
         this.opacityUniform = uniform(1);
         this.hueUniform = uniform(0);
         this.saturationUniform = uniform(1);
@@ -165,6 +166,7 @@ export class PassNode {
         }
         const generation = ++this.effectSwapGeneration;
         const nextMaterial = new THREE.MeshBasicNodeMaterial();
+        nextMaterial.alphaToCoverage = true;
         nextMaterial.colorNode = this.composeColorNode(nextEffectNode);
         const standbyScene = new THREE.Scene();
         const standbyMesh = new THREE.Mesh(this.compositeGeometry, nextMaterial);
diff --git a/node_modules/@basementstudio/shader-lab/dist/src/renderer/pattern-pass.js b/node_modules/@basementstudio/shader-lab/dist/src/renderer/pattern-pass.js
index 3f53698..ebc93c7 100644
--- a/node_modules/@basementstudio/shader-lab/dist/src/renderer/pattern-pass.js
+++ b/node_modules/@basementstudio/shader-lab/dist/src/renderer/pattern-pass.js
@@ -6,6 +6,15 @@ import { createPipelinePlaceholder, PassNode } from "./pass-node.js";
 function clamp01(value) {
     return Math.max(0, Math.min(1, value));
 }
+// 티켓 20260915_1103 후속 — customBgColor의 8자리 hex(#RRGGBBAA) 알파를 추출한다.
+function hexToAlpha(hex) {
+    const normalized = hex.trim().replace("#", "");
+    if (normalized.length !== 8) {
+        return 1;
+    }
+    const alphaByte = Number.parseInt(normalized.slice(6, 8), 16);
+    return Number.isNaN(alphaByte) ? 1 : alphaByte / 255;
+}
 function parseCssColorRgb(value) {
     const rgba = value.match(/rgba?\s*\(\s*([\d.]+)\s*,\s*([\d.]+)\s*,\s*([\d.]+)(?:\s*,\s*[\d.]+)?\s*\)/i);
     if (rgba) {
@@ -67,7 +76,7 @@ export class PatternPass extends PassNode {
         this.colorModeUniform = uniform(0);
         this.customColorCountUniform = uniform(4);
         this.customLuminanceBiasUniform = uniform(0);
-        this.customBgColorUniform = uniform(new THREE.Vector3(0.96, 0.96, 0.94));
+        this.customBgColorUniform = uniform(new THREE.Vector4(0.96, 0.96, 0.94, 1));
         this.customColor1Uniform = uniform(new THREE.Vector3(0.05, 0.063, 0.078));
         this.customColor2Uniform = uniform(new THREE.Vector3(0.302, 0.314, 0.341));
         this.customColor3Uniform = uniform(new THREE.Vector3(0.588, 0.604, 0.635));
@@ -124,7 +133,7 @@ export class PatternPass extends PassNode {
         this.monoRedUniform.value = red;
         this.monoGreenUniform.value = green;
         this.monoBlueUniform.value = blue;
-        this.setCustomColorUniform(this.customBgColorUniform, typeof params.customBgColor === "string" ? params.customBgColor : "#F5F5F0");
+        this.setCustomBgColorUniform(typeof params.customBgColor === "string" ? params.customBgColor : "#F5F5F000");
         this.setCustomColorUniform(this.customColor1Uniform, typeof params.customColor1 === "string" ? params.customColor1 : "#0d1014");
         this.setCustomColorUniform(this.customColor2Uniform, typeof params.customColor2 === "string" ? params.customColor2 : "#4d5057");
         this.setCustomColorUniform(this.customColor3Uniform, typeof params.customColor3 === "string" ? params.customColor3 : "#969aa2");
@@ -173,6 +182,7 @@ export class PatternPass extends PassNode {
         const quantizeLevels = vec3(float(7), float(7), float(3));
         const inverseQuantizeLevels = vec3(float(1 / 7), float(1 / 7), float(1 / 3));
         const customBgVec = vec3(float(this.customBgColorUniform.x), float(this.customBgColorUniform.y), float(this.customBgColorUniform.z));
+        const customBgAlpha = float(this.customBgColorUniform.w);
         const customColor1Vec = vec3(float(this.customColor1Uniform.x), float(this.customColor1Uniform.y), float(this.customColor1Uniform.z));
         const customColor2Vec = vec3(float(this.customColor2Uniform.x), float(this.customColor2Uniform.y), float(this.customColor2Uniform.z));
         const customColor3Vec = vec3(float(this.customColor3Uniform.x), float(this.customColor3Uniform.y), float(this.customColor3Uniform.z));
@@ -211,7 +221,11 @@ export class PatternPass extends PassNode {
             const patternColor = select(this.colorModeUniform.lessThan(float(0.5)), sourceColor, select(this.colorModeUniform.lessThan(float(1.5)), quantizedColor, select(this.colorModeUniform.lessThan(float(2.5)), monochromeColor, customColor)));
             const sourceBackground = sourceColor.mul(this.bgOpacityUniform);
             const backgroundColor = select(this.colorModeUniform.lessThan(float(0.5)), sourceBackground, select(this.colorModeUniform.lessThan(float(2.5)), vec3(float(0), float(0), float(0)), customBgVec));
-            return vec4(mix(backgroundColor, patternColor, patternMask), float(1));
+            // 티켓 20260915_1103 후속 — "원본"·"단색" 배경은 이번 범위 밖이라 그대로 불투명
+            // 1을 쓰고, "커스텀" 배경(customBgColor)만 자신의 알파를 패턴 없는 영역(gap)에
+            // 반영한다. 패턴이 그려진 영역(patternMask≈1)은 항상 불투명이다.
+            const backgroundAlpha = select(this.colorModeUniform.lessThan(float(2.5)), float(1), customBgAlpha);
+            return vec4(mix(backgroundColor, patternColor, patternMask), mix(backgroundAlpha, float(1), patternMask));
         };
         const baseSample = samplePattern(renderTargetUv);
         if (!this.bloomEnabled) {
@@ -281,4 +295,8 @@ export class PatternPass extends PassNode {
         const [r, g, b] = parseCssColorRgb(value);
         target.value.set(r, g, b);
     }
+    setCustomBgColorUniform(value) {
+        const [r, g, b] = parseCssColorRgb(value);
+        this.customBgColorUniform.value.set(r, g, b, hexToAlpha(value));
+    }
 }
diff --git a/node_modules/@basementstudio/shader-lab/dist/src/renderer/pipeline-manager.js b/node_modules/@basementstudio/shader-lab/dist/src/renderer/pipeline-manager.js
index 1c1439f..261585a 100644
--- a/node_modules/@basementstudio/shader-lab/dist/src/renderer/pipeline-manager.js
+++ b/node_modules/@basementstudio/shader-lab/dist/src/renderer/pipeline-manager.js
@@ -1,4 +1,4 @@
-import { float, texture as tslTexture, uv, vec2 } from "three/tsl";
+import { float, texture as tslTexture, uv, vec2, vec3, vec4 } from "three/tsl";
 import * as THREE from "three/webgpu";
 import { DEFAULT_MASK_CONFIG } from "../types/editor.js";
 import { AsciiPass } from "./ascii-pass.js";
@@ -17,6 +17,7 @@ import { FluidPass } from "./fluid-pass.js";
 import { GradientPass } from "./gradient-pass.js";
 import { HalftonePass } from "./halftone-pass.js";
 import { InkPass } from "./ink-pass.js";
+import { LiquidMetalPass } from "./liquid-metal-pass.js";
 import { LivePass } from "./live-pass.js";
 import { MagnifyLensPass } from "./magnify-lens-pass.js";
 import { MediaPass } from "./media-pass.js";
@@ -129,7 +130,17 @@ export class PipelineManager {
         this.logicalHeight = this.height;
         this.baseScene = new THREE.Scene();
         this.baseCamera = new THREE.OrthographicCamera(-1, 1, 1, -1, 0, 1);
-        const baseMaterial = new THREE.MeshBasicMaterial({ color: "#080808" });
+        // 티켓 20260914_1045 — 레이어가 없거나 activePasses가 비었을 때 렌더링되는 베이스 판을
+        // RGB는 기존과 동일한 #080808로 유지하되, 알파는 0(완전 투명)으로 직접 기록한다.
+        // MeshBasicMaterial(color) 대신 MeshBasicNodeMaterial + colorNode를 쓰는 이유는,
+        // material.opacity/transparent 조합은 블렌딩 여부만 바꿀 뿐 프래그먼트가 실제로 쓰는
+        // 알파 값 자체를 지정할 수 없기 때문이다. transparent:false(기본값, 블렌딩 비활성)
+        // 상태에서 colorNode가 alpha=0을 반환하면, 블렌딩 없이 RGBA를 그대로 렌더타겟에
+        // 기록(replace)하므로 다른 레이어 타입(29종)이 기대하는 "완전 불투명 덮어쓰기" 동작은
+        // 그대로 유지되고, 알파 채널만 0으로 바뀐다.
+        const baseMaterial = new THREE.MeshBasicNodeMaterial();
+        baseMaterial.alphaToCoverage = true;
+        baseMaterial.colorNode = vec4(vec3(float(8 / 255), float(8 / 255), float(8 / 255)), float(0));
         const baseMesh = new THREE.Mesh(new THREE.PlaneGeometry(2, 2), baseMaterial);
         baseMesh.frustumCulled = false;
         this.baseScene.add(baseMesh);
@@ -140,6 +151,7 @@ export class PipelineManager {
         const blitUv = vec2(uv().x, float(1).sub(uv().y));
         this.blitInputNode = tslTexture(createPipelinePlaceholder(), blitUv);
         this.blitMaterial = new THREE.MeshBasicNodeMaterial();
+        this.blitMaterial.alphaToCoverage = true;
         this.blitMaterial.colorNode = this.blitInputNode;
         const blitMesh = new THREE.Mesh(new THREE.PlaneGeometry(2, 2), this.blitMaterial);
         blitMesh.frustumCulled = false;
@@ -341,6 +353,13 @@ export class PipelineManager {
                 pass.clearMedia();
             }
         }
+        if (pass instanceof LiquidMetalPass) {
+            // 티켓 20260913_2110 — image 레이어(MediaPass)와 같은 공용 layer.asset 필드를
+            // 리퀴드 메탈에도 연결한다. 업로드된 이미지가 있으면 그 형태를 마스크로 쓰고,
+            // 없으면(asset이 없거나 kind가 image가 아니면) 배경 전체 모드로 되돌린다.
+            const asset = layer.asset;
+            pass.setImageAsset(asset?.kind === "image" ? asset.src : null);
+        }
         if (pass instanceof LivePass) {
             const facingMode = typeof layer.params.facingMode === "string"
                 ? layer.params.facingMode
@@ -431,6 +450,9 @@ export class PipelineManager {
         if (layer.kind === "source" && layer.type === "text") {
             return new TextPass(layer.id);
         }
+        if (layer.kind === "source" && layer.type === "liquid-metal") {
+            return new LiquidMetalPass(layer.id);
+        }
         if (layer.kind === "source" && layer.type === "custom-shader") {
             return new CustomShaderPass(layer.id, this.onRuntimeError);
         }
diff --git a/node_modules/@basementstudio/shader-lab/dist/src/renderer/plotter-pass.js b/node_modules/@basementstudio/shader-lab/dist/src/renderer/plotter-pass.js
index 10d8f9a..f48f29f 100644
--- a/node_modules/@basementstudio/shader-lab/dist/src/renderer/plotter-pass.js
+++ b/node_modules/@basementstudio/shader-lab/dist/src/renderer/plotter-pass.js
@@ -17,6 +17,14 @@ function parseHexColor(hex, fallback) {
     }
     return [r, g, b];
 }
+// 티켓 20260915_1103 후속 — paperColor의 8자리 hex(#RRGGBBAA) 알파를 추출한다.
+function parseHexAlpha(hex, fallback) {
+    if (typeof hex !== "string" || hex.replace("#", "").length !== 8) {
+        return fallback;
+    }
+    const alphaByte = Number.parseInt(hex.slice(7, 9), 16);
+    return Number.isNaN(alphaByte) ? fallback : alphaByte / 255;
+}
 export class PlotterPass extends PassNode {
     colorModeUniform;
     gapUniform;
@@ -29,6 +37,7 @@ export class PlotterPass extends PassNode {
     paperColorRUniform;
     paperColorGUniform;
     paperColorBUniform;
+    paperAlphaUniform;
     inkColorRUniform;
     inkColorGUniform;
     inkColorBUniform;
@@ -47,6 +56,7 @@ export class PlotterPass extends PassNode {
         this.paperColorRUniform = uniform(0.96);
         this.paperColorGUniform = uniform(0.94);
         this.paperColorBUniform = uniform(0.91);
+        this.paperAlphaUniform = uniform(1);
         this.inkColorRUniform = uniform(0.1);
         this.inkColorGUniform = uniform(0.1);
         this.inkColorBUniform = uniform(0.1);
@@ -92,6 +102,7 @@ export class PlotterPass extends PassNode {
         this.paperColorRUniform.value = pr;
         this.paperColorGUniform.value = pg;
         this.paperColorBUniform.value = pb;
+        this.paperAlphaUniform.value = parseHexAlpha(params.paperColor, 1);
         const [ir, ig, ib] = parseHexColor(params.inkColor, [0.1, 0.1, 0.1]);
         this.inkColorRUniform.value = ir;
         this.inkColorGUniform.value = ig;
@@ -133,6 +144,9 @@ export class PlotterPass extends PassNode {
         const isSourceMode = this.colorModeUniform.greaterThan(float(0.5));
         const lineColor = vec3(select(isSourceMode, sourceColor.x, inkColor.x), select(isSourceMode, sourceColor.y, inkColor.y), select(isSourceMode, sourceColor.z, inkColor.z));
         const result = mix(paperColor, lineColor, combinedHatch);
-        return vec4(result, float(1));
+        // 티켓 20260915_1103 후속 — 선이 없는(종이만 보이는) 영역은 paperColor 자신의
+        // 알파를, 선이 그려진 영역은 항상 완전 불투명(1)을 쓴다.
+        const resultAlpha = mix(this.paperAlphaUniform, float(1), combinedHatch);
+        return vec4(result, resultAlpha);
     }
 }
diff --git a/node_modules/@basementstudio/shader-lab/dist/src/renderer/text-pass.js b/node_modules/@basementstudio/shader-lab/dist/src/renderer/text-pass.js
index cf82971..a370d8a 100644
--- a/node_modules/@basementstudio/shader-lab/dist/src/renderer/text-pass.js
+++ b/node_modules/@basementstudio/shader-lab/dist/src/renderer/text-pass.js
@@ -156,34 +156,51 @@ export class TextPass extends PassNode {
         context.textAlign = "left";
         context.textBaseline = "alphabetic";
         context.font = `${normalizedFontWeight} ${fontSize}px ${fontFamily}`;
-        const characters = [...text];
         const spacing = fontSize * letterSpacing;
-        const glyphLayout = [];
-        let visualLeft = Number.POSITIVE_INFINITY;
-        let visualRight = Number.NEGATIVE_INFINITY;
-        let cursorX = 0;
-        for (const [index, char] of characters.entries()) {
-            const metrics = context.measureText(char);
-            const advance = metrics.width;
-            const glyphLeft = cursorX - Math.max(0, metrics.actualBoundingBoxLeft);
-            const glyphRight = cursorX + Math.max(metrics.actualBoundingBoxRight, advance);
-            glyphLayout.push({
-                advance,
-                char,
-                x: cursorX,
-            });
-            visualLeft = Math.min(visualLeft, glyphLeft);
-            visualRight = Math.max(visualRight, glyphRight);
-            cursorX += advance;
-            if (index < characters.length - 1) {
-                cursorX += spacing;
+        // JAM! 확장(원본 basement.studio에는 없음, 티켓 20260913_1901): "\n" 기준으로 줄을
+        // 나누고 줄마다 독립적으로 글리프 레이아웃(가로 정렬 기준폭)을 계산한다. 줄이
+        // 하나뿐이면(개행 없는 입력) 아래 각 분기의 lines.length===1 케이스가 정확히 원본과
+        // 같은 좌표를 내도록 설계했다(회귀 방지, esbuild+playwright 픽셀 대조로 실측 확인).
+        const lines = text.split("\n");
+        const lineHeight = fontSize * 1.2;
+        const lineLayouts = lines.map((lineText) => {
+            const characters = [...lineText];
+            const glyphLayout = [];
+            let visualLeft = Number.POSITIVE_INFINITY;
+            let visualRight = Number.NEGATIVE_INFINITY;
+            let cursorX = 0;
+            for (const [index, char] of characters.entries()) {
+                const metrics = context.measureText(char);
+                const advance = metrics.width;
+                const glyphLeft = cursorX - Math.max(0, metrics.actualBoundingBoxLeft);
+                const glyphRight = cursorX + Math.max(metrics.actualBoundingBoxRight, advance);
+                glyphLayout.push({
+                    advance,
+                    char,
+                    x: cursorX,
+                });
+                visualLeft = Math.min(visualLeft, glyphLeft);
+                visualRight = Math.max(visualRight, glyphRight);
+                cursorX += advance;
+                if (index < characters.length - 1) {
+                    cursorX += spacing;
+                }
             }
-        }
-        if (!Number.isFinite(visualLeft)) {
-            visualLeft = 0;
-            visualRight = 0;
-        }
-        const metrics = getTextMetrics(context, text, fontSize);
+            if (!Number.isFinite(visualLeft)) {
+                visualLeft = 0;
+                visualRight = 0;
+            }
+            return { glyphLayout, visualLeft, visualRight };
+        });
+        // 세로 정렬은 "첫 줄의 ascent + 마지막 줄의 descent"로 블록 전체 높이를 잡는다 —
+        // 게이트 리뷰(20260913_1901)가 지적한 대로, 첫 줄만으로 ascent/descent를 재사용하면
+        // 첫/마지막 줄의 글리프 하강폭(디센더 유무)이 다를 때 center/bottom 정렬이 어긋난다.
+        // lines.length===1이면 firstLineMetrics와 lastLineMetrics가 같은 텍스트를 측정하므로
+        // 원본과 동일한 결과가 나온다(회귀 없음, 재검증 완료).
+        const firstLineMetrics = getTextMetrics(context, lines[0] ?? text, fontSize);
+        const lastLineMetrics = lines.length > 1
+            ? getTextMetrics(context, lines[lines.length - 1], fontSize)
+            : firstLineMetrics;
         let anchorX = this.width * 0.5;
         if (horizontal === "left") {
             anchorX = 0;
@@ -200,23 +217,28 @@ export class TextPass extends PassNode {
         }
         const offsetX = offset[0] * this.width;
         const offsetY = offset[1] * this.height;
-        let startX = anchorX + offsetX - (visualLeft + visualRight) * 0.5;
-        if (horizontal === "left") {
-            startX = anchorX + offsetX - visualLeft;
-        }
-        else if (horizontal === "right") {
-            startX = anchorX + offsetX - visualRight;
-        }
-        let baselineY = anchorY - offsetY + (metrics.ascent - metrics.descent) * 0.5;
+        const blockExtraHeight = (lines.length - 1) * lineHeight;
+        let firstBaselineY = anchorY - offsetY + (firstLineMetrics.ascent - lastLineMetrics.descent - blockExtraHeight) * 0.5;
         if (vertical === "top") {
-            baselineY = anchorY - offsetY + metrics.ascent;
+            firstBaselineY = anchorY - offsetY + firstLineMetrics.ascent;
         }
         else if (vertical === "bottom") {
-            baselineY = anchorY - offsetY - metrics.descent;
-        }
-        for (const glyph of glyphLayout) {
-            context.fillText(glyph.char, startX + glyph.x, baselineY);
+            firstBaselineY = anchorY - offsetY - lastLineMetrics.descent - blockExtraHeight;
         }
+        lines.forEach((lineText, lineIndex) => {
+            const { glyphLayout, visualLeft, visualRight } = lineLayouts[lineIndex];
+            let startX = anchorX + offsetX - (visualLeft + visualRight) * 0.5;
+            if (horizontal === "left") {
+                startX = anchorX + offsetX - visualLeft;
+            }
+            else if (horizontal === "right") {
+                startX = anchorX + offsetX - visualRight;
+            }
+            const baselineY = firstBaselineY + lineIndex * lineHeight;
+            for (const glyph of glyphLayout) {
+                context.fillText(glyph.char, startX + glyph.x, baselineY);
+            }
+        });
         if (!this.textTexture) {
             this.textTexture = new THREE.CanvasTexture(canvas);
             this.textTexture.flipY = false;
diff --git a/node_modules/@basementstudio/shader-lab/dist/src/types.d.ts b/node_modules/@basementstudio/shader-lab/dist/src/types.d.ts
index c82a982..b6a4376 100644
--- a/node_modules/@basementstudio/shader-lab/dist/src/types.d.ts
+++ b/node_modules/@basementstudio/shader-lab/dist/src/types.d.ts
@@ -1,6 +1,6 @@
 export type ShaderLabParameterValue = number | string | boolean | [number, number] | [number, number, number];
 export type ShaderLabLayerKind = "effect" | "source";
-export type ShaderLabSourceLayerType = "custom-shader" | "fluid" | "gradient" | "image" | "live" | "magnify-lens" | "pixel-trail" | "text" | "video";
+export type ShaderLabSourceLayerType = "custom-shader" | "fluid" | "gradient" | "image" | "liquid-metal" | "live" | "magnify-lens" | "pixel-trail" | "text" | "video";
 export type ShaderLabEffectLayerType = "ascii" | "blob-tracking" | "bloom" | "circuit-bent" | "directional-blur" | "chromatic-aberration" | "crt" | "displacement-map" | "dithering" | "edge-detect" | "fluted-glass" | "halftone" | "ink" | "particle-grid" | "pattern" | "pixelation" | "pixel-sorting" | "plotter" | "posterize" | "slice" | "smear" | "threshold" | "voxel";
 export type ShaderLabLayerType = ShaderLabEffectLayerType | ShaderLabSourceLayerType;
 export type ShaderLabBlendMode = "normal" | "multiply" | "screen" | "overlay" | "darken" | "lighten" | "color-dodge" | "color-burn" | "hard-light" | "soft-light" | "difference" | "exclusion" | "hue" | "saturation" | "color" | "luminosity";
